export const englishModule = Object.freeze({
  slug: "llm-training",
  title: "LLM Training",
  subtitle: "Turn data, optimization, distributed systems, and evaluation into a releasable model",
  definition: "LLM Training covers the path from governed data preparation through pretraining, supervised fine-tuning, preference optimization, evaluation, and release. It connects each learning stage to the compute, data, recovery, and governance capabilities required to deliver it.",
  position: "This module links model foundations to the compute layer. It explains where model capabilities come from and what a training program must prove across data, infrastructure, evaluation, and release.",
  relatedSlugs: Object.freeze(["llm", "fine-tuning", "evaluation", "ai-infra-compute", "ai-infra-platform"]),
  sections: Object.freeze([
    Object.freeze({ id: "study-guide", eyebrow: "LEARN, PRACTICE, PROVE", title: "Treat training as a long-running, evidence-producing system", lead: "Training is not a GPU-count exercise. It is a sequence of learning objectives whose data, optimization target, infrastructure, recovery state, and exit gate must remain traceable.", blocks: Object.freeze([
      Object.freeze({ type: "cards", title: "Learning outcomes", items: Object.freeze([
        Object.freeze({ id: "outcome-training-stages", title: "Separate the learning stages", body: "Explain the relationships among data preparation, pretraining, supervised fine-tuning, preference optimization, reasoning-oriented training, and evaluation." }),
        Object.freeze({ id: "outcome-scaling-boundary", title: "Use scaling evidence within scope", body: "Explain empirical scaling relationships without turning them into an unconditional purchasing formula." }),
        Object.freeze({ id: "outcome-effective-training-time", title: "Measure effective training time", body: "Connect parallelism, communication, data supply, checkpoints, and recovery to useful model progress." }),
        Object.freeze({ id: "outcome-training-investment", title: "Choose the lightest valid route", body: "Distinguish training from scratch, continued pretraining, fine-tuning, retrieval, and prompt or workflow changes." }),
      ]) }),
      Object.freeze({ type: "steps", title: "Recommended learning route", items: Object.freeze([
        Object.freeze({ id: "route-learning-objectives", title: "Start with learning objectives", body: "Separate general-pattern learning, instruction demonstrations, preference signals, and verifiable outcomes.", decision: "Checkpoint: state the data, objective, and evaluation required by each stage." }),
        Object.freeze({ id: "route-data-compute", title: "Connect data and compute", body: "Analyze quality, rights, deduplication, token budget, parallelism, and communication together.", decision: "Checkpoint: never treat parameter count or GPU count as an outcome guarantee." }),
        Object.freeze({ id: "route-long-running-experiment", title: "Control a long-running experiment", body: "Use checkpoints, reproducible environments, failure recovery, and stage gates.", decision: "Checkpoint: report effective training time, data version, and tested recovery evidence." }),
        Object.freeze({ id: "route-experiment-lineage", title: "Build experiment lineage", body: "Bind code, data, tokenizer, hyperparameters, parallel topology, environment, checkpoints, and evaluation.", decision: "Checkpoint: trace every candidate model to its complete training conditions." }),
        Object.freeze({ id: "route-goodput-acceptance", title: "Accept the cluster on useful progress", body: "Observe compute, communication, I/O, failures, recovery, and qualifying model output together.", decision: "Checkpoint: GPU-hours and instantaneous utilization are no longer proxies for training success." }),
      ]) }),
      Object.freeze({ type: "cards", title: "Practice labs", items: Object.freeze([
        Object.freeze({ id: "lab-training-path", title: "Choose the training path", subtitle: "The customer has domain text and a small number of expert demonstrations", body: "Compare pretraining from scratch, continued pretraining, SFT, and RAG; estimate data, compute, evaluation, and operations; design the smallest falsifiable experiment.", decision: "Deliverable: a route decision with stage gates.", boundary: "Acceptance: a high-cost route is supported by evidence that lighter approaches cannot close the gap.", sourceIds: Object.freeze(["chinchilla-2022", "instructgpt-2022", "nist-genai-profile"]) }),
        Object.freeze({ id: "lab-training-stall", title: "Diagnose a stalled run", subtitle: "Loss is unstable, throughput falls, and GPU utilization varies", body: "Check data batches, numerical stability, learning rate, and recovery points; correlate compute, network, storage, and scheduler events; separate model and infrastructure faults.", decision: "Deliverable: a layered diagnostic tree and reproducible experiment.", boundary: "Acceptance: evidence locates the bottleneck before capacity is added, and post-recovery results remain comparable.", sourceIds: Object.freeze(["opentelemetry-semconv", "nist-genai-profile"]) }),
        Object.freeze({ id: "lab-training-resume", title: "Review an interrupted multi-node run", subtitle: "The final result changed after checkpoint recovery", body: "Verify data cursor, random state, optimizer, scheduler, and topology; compare loss, throughput, and unseen-task results; establish checkpoint cadence, integrity checks, and recovery rehearsal.", decision: "Deliverable: a training-state continuity report.", boundary: "Acceptance: prove continuity or identify the earlier trusted checkpoint to which the run must return.", sourceIds: Object.freeze(["opentelemetry-semconv", "nist-genai-profile"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "curriculum", eyebrow: "CURRICULUM ATLAS", title: "The complete training evidence chain", lead: "Training is a long-running experiment jointly driven by data, objectives, distributed systems, and evaluation—not merely by parameters and accelerators.", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "curriculum-training-pipeline", title: "Training stages and their objectives", subtitle: "Training Pipeline", body: "Pretraining learns broad patterns; SFT learns demonstrated behavior; preference optimization learns relative choices; reasoning-oriented reinforcement can use verifiable rewards to shape search and problem solving. Each stage uses different data, loss functions, and evaluation.", decision: "State the new capability, data source, and exit gate for every stage.", boundary: "Shared stage names do not imply identical data, algorithms, or outcomes across model builders.", sourceIds: Object.freeze(["instructgpt-2022", "dpo-2023"]) }),
        Object.freeze({ id: "curriculum-data-foundation", title: "Data, deduplication, and tokenization", subtitle: "Data Foundation", body: "Source, rights, quality, mixture, deduplication, contamination controls, filtering, and the tokenizer determine what the model sees and how it is represented. Repetition wastes compute and can increase memorization; evaluation contamination creates false capability.", decision: "Version data and its governance evidence as part of the model artifact.", boundary: "More raw data is not necessarily more effective signal; aggressive filtering can erase minority languages and specialist material.", sourceIds: Object.freeze(["chinchilla-2022", "nist-genai-profile"]) }),
        Object.freeze({ id: "curriculum-pretraining-scaling", title: "Pretraining, scaling, and sparse experts", subtitle: "Pretraining", body: "Next-token prediction learns statistical structure from large corpora. Empirical scaling laws relate model size, data, and compute under specific setups. Sparse experts increase total capacity while activating a subset per token, adding communication and load-balancing constraints.", decision: "Constrain scale by compute-optimal evidence and available high-quality data, not a parameter target alone.", boundary: "Empirical scaling relationships do not transfer unchanged across architectures, languages, data quality, and budgets.", sourceIds: Object.freeze(["chinchilla-2022", "nist-genai-profile"]) }),
        Object.freeze({ id: "curriculum-alignment", title: "SFT, RLHF, and DPO", subtitle: "Alignment", body: "SFT establishes behavior from demonstrations; RLHF learns a reward signal and optimizes against it; DPO learns directly from preference pairs. The choice depends on whether the target can be demonstrated, compared, or must be explored.", decision: "Start with the simplest method that can validate the target, and retain regression tests for base capability.", boundary: "Alignment changes the probability of desired behavior; it does not establish absolute safety or eliminate factual error.", sourceIds: Object.freeze(["instructgpt-2022", "dpo-2023"]) }),
        Object.freeze({ id: "curriculum-reasoning-training", title: "Reasoning training and verifiable rewards", subtitle: "Reasoning Training", body: "A reasoning-training pipeline may combine cold-start demonstrations, reinforcement learning, further supervised training, and distillation. Verifiable rewards fit tasks such as mathematics and code with objective checks; open-ended work still needs calibrated rubrics and human judgment.", decision: "Use reasoning reinforcement only when the verifier is reliable and the task value covers both training and inference cost.", boundary: "Reinforcement learning is not a universal stage, longer hidden reasoning does not guarantee correctness, and hidden chain of thought is not external audit evidence.", sourceIds: Object.freeze(["deepseek-r1-2025", "openai-model-spec-hidden-cot", "nist-genai-profile"]) }),
        Object.freeze({ id: "curriculum-distributed-systems", title: "Parallelism, communication, and checkpoints", subtitle: "Distributed Systems", body: "Data, tensor, and pipeline parallelism trade memory against collective communication, waiting, and pipeline bubbles. Long runs also need an explicit checkpoint state contract and a cadence that balances write overhead against lost progress.", decision: "Profile compute, communication, and I/O before changing the parallel plan or cluster.", boundary: "Megatron and CheckFreq report specific systems and experiments, not universal scaling or checkpoint intervals; a short throughput test does not prove long-run stability.", sourceIds: Object.freeze(["megatron-3d-parallelism-2021", "nccl-collectives", "torchsnapshot-checkpoint", "checkfreq-2021"]) }),
        Object.freeze({ id: "curriculum-training-evidence", title: "Evaluation and release gates", subtitle: "Training Evidence", body: "Every stage must compare unseen tasks, critical slices, safety, capability retention, resources, and uncertainty. Completing optimization only creates a candidate artifact; serving, shadow traffic, and continuing monitoring remain separate gates.", decision: "Use predeclared evidence to decide whether to continue, change data, revert, or enter release.", boundary: "A public benchmark improvement cannot replace customer-task, serving-performance, and risk validation.", sourceIds: Object.freeze(["nist-genai-profile"]) }),
        Object.freeze({ id: "curriculum-experiment-lineage", title: "Experiment lineage and reproducible artifacts", subtitle: "Experiment Lineage", body: "A result must link code, data, tokenizer, initialization, hyperparameters, random state, parallel configuration, checkpoints, evaluation, and environment. A resumed run must also prove state continuity.", decision: "Treat reproducibility evidence as part of the model artifact rather than operator memory.", boundary: "Complete records cannot remove all differences caused by hardware, kernels, and nondeterministic execution.", sourceIds: Object.freeze(["opentelemetry-semconv", "nist-genai-profile"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "principles", eyebrow: "CORE PIPELINE", title: "From governed data to a release candidate", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "principle-data-preparation", title: "Data preparation defines what can be learned", body: "Provenance, rights, cleaning, deduplication, mixture, tokenization, and data splits jointly define the corpus; volume cannot compensate for systematic contamination.", decision: "Begin with a Data Card, rights inventory, cleaning rules, and frozen evaluation split." }),
        Object.freeze({ id: "principle-pretraining", title: "Pretraining learns general patterns", body: "Large-scale next-token prediction builds broad language and capability foundations, with model size, effective data, and compute budget coordinated.", decision: "Reuse an existing foundation unless differentiated data and sustained training capability justify pretraining." }),
        Object.freeze({ id: "principle-sft", title: "SFT teaches demonstrated behavior", body: "Supervised Fine-tuning uses instructions and ideal responses to shape task behavior, format, and assistant conventions.", decision: "Confirm that high-quality demonstrations can express the target before starting SFT." }),
        Object.freeze({ id: "principle-preference", title: "Preference optimization learns relative quality", body: "RLHF and DPO use preference signals to trade off usefulness, honesty, safety, and style among plausible outputs.", decision: "Use preference training for a relative-choice problem, not as a fact-update mechanism." }),
        Object.freeze({ id: "principle-evaluation", title: "Evaluation runs throughout", body: "Data, training, alignment, and release each require a frozen baseline, unseen tasks, critical slices, safety checks, and target-endpoint evidence.", decision: "Define the exam and stop rules before training; training data cannot prove training success." }),
      ]) }),
      Object.freeze({ type: "boundary", title: "Critical boundary", items: Object.freeze([Object.freeze({ id: "training-product-boundary", title: "A trained model is not a complete product", body: "Training can change model weights and behavior.", boundary: "It does not automatically establish correctness, legal rights, operational readiness, safe serving, or release control. Those require independent acceptance." })]) }),
    ]) }),
    Object.freeze({ id: "decisions", eyebrow: "PRESALES DECISIONS", title: "Choose the route, data, capacity, and release gate", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "decision-train-from-scratch", title: "Does the customer need pretraining from scratch?", body: "Signal: large, lawful, differentiated data; sustained compute; a model-research team; and systematic foundation-model failure on critical capability.", decision: "Evaluate pretraining or continued pretraining only when those conditions coexist; otherwise begin with prompting, RAG, or fine-tuning.", boundary: "Producing weights does not remove the post-training, evaluation, serving, and governance investment." }),
        Object.freeze({ id: "decision-data-stage", title: "Which stage matches the available data?", body: "Unlabeled domain text, ideal demonstrations, preference pairs, and objectively verifiable outcomes represent different training signals.", decision: "Map them respectively to continued pretraining, SFT, preference methods, and reinforcement routes with reliable verifiers.", boundary: "A compatible format does not establish rights, coverage, quality, or absence of leakage." }),
        Object.freeze({ id: "decision-after-sft", title: "Is preference optimization needed after SFT?", body: "Signal: the model performs the task but remains verbose, poorly calibrated at refusal, inconsistent in tone, or weak in relative choice.", decision: "Measure the remaining SFT errors on a frozen set before adding DPO, RLHF, or another preference stage.", boundary: "Preference optimization can create over-refusal, formulaic output, or capability regressions." }),
        Object.freeze({ id: "decision-build-or-rent", title: "Build or rent the training cluster?", body: "Signal: the load is a bursty project, continuous high-utilization training, or constrained by data sovereignty and existing assets.", decision: "Favor elastic cloud evaluation for bursty work; compare owned or hybrid capacity for sustained use and strong boundaries.", boundary: "Accelerator price is only part of TCO; network, storage, fault tolerance, and people determine effective training time." }),
        Object.freeze({ id: "decision-training-release", title: "Is the candidate ready for release?", body: "Signal: training loss improved, but business samples, safety slices, stability, and target-endpoint cost remain unverified.", decision: "Require unseen-task results, hard task criteria, calibrated review, safety regression, and target-serving evidence together.", boundary: "Training-set results and public benchmarks cannot replace the customer's Go/No-Go gate." }),
      ]) }),
    ]) }),
    Object.freeze({ id: "deep-dive", eyebrow: "EFFECTIVE TRAINING TIME", title: "Treat training as compute, data supply, communication, and recovery", lead: "Billed accelerator time becomes useful learning only while every critical path progresses and failures can be recovered without losing the experiment state.", blocks: Object.freeze([
      Object.freeze({ type: "table", title: "Paths that determine useful progress", columns: Object.freeze(["Path", "Mechanism", "Verification", "Boundary"]), items: Object.freeze([
        Object.freeze({ id: "deep-model-compute", title: "Model compute", body: "Matrix operations, precision, sequence length, batch size, and parallel partitioning determine step cost; model scale and training tokens must fit the same compute budget.", decision: "Measure step time, memory margin, and convergence at small scale before selecting accelerator, precision, and parallel strategy.", boundary: "Peak FLOPS do not directly predict completion time.", sourceIds: Object.freeze(["transformer-2017", "chinchilla-2022"]) }),
        Object.freeze({ id: "deep-collective-communication", title: "Collective communication", body: "Data, tensor, pipeline, and expert parallelism communicate gradients or activations; topology, slow nodes, and cross-domain links make peers wait.", decision: "Test all-reduce, inter-node bandwidth, and scaling efficiency on the target cluster and topology.", boundary: "More accelerators can reduce parallel efficiency; twice the devices does not guarantee half the duration.", sourceIds: Object.freeze(["opentelemetry-semconv"]) }),
        Object.freeze({ id: "deep-input-pipeline", title: "Input pipeline", body: "Object storage, sharding, decompression, tokenization, and loading can starve accelerators and create periodic idle time.", decision: "Measure data wait and read throughput; compare preprocessing, caches, sharding, and parallel file systems.", boundary: "Faster reads do not repair rights, duplication, contamination, or mixture problems.", sourceIds: Object.freeze(["nist-genai-profile"]) }),
        Object.freeze({ id: "deep-failure-recovery", title: "Failure and recovery", body: "Long, large-scale runs expose node, network, and storage faults; checkpoint interval and restore time determine how much work must be repeated.", decision: "Rehearse fault injection, checkpoint write, and cross-node restore; accept effective training time, RPO, and RTO.", boundary: "A checkpoint file does not prove that optimizer, scheduler, random state, and data cursor can be restored.", sourceIds: Object.freeze(["opentelemetry-semconv", "nist-genai-profile"]) }),
      ]) }),
      Object.freeze({ type: "cards", title: "Diagnose before adding compute", items: Object.freeze([
        Object.freeze({ id: "triage-loss-spike", title: "Loss spike or NaN", body: "An anomalous batch, numerical overflow, excessive learning rate, or inconsistent restored state can corrupt gradients.", decision: "Trace step, data shard, gradient norm, learning rate, and node event together; replay the smallest interval around the fault.", boundary: "Silently skipping a batch can conceal a systematic data defect.", sourceIds: Object.freeze(["nist-genai-profile"]) }),
        Object.freeze({ id: "triage-validation-divergence", title: "Training loss falls; validation worsens", body: "Overfitting, test contamination, business-distribution mismatch, or an objective that rewards surface form can create false progress.", decision: "Compare source and task slices against the baseline, detect near-duplicates and temporal leakage, and retain a final untouched release set.", boundary: "Repeated experimentation can indirectly overfit one validation set.", sourceIds: Object.freeze(["chinchilla-2022", "instructgpt-2022"]) }),
        Object.freeze({ id: "triage-throughput-sawtooth", title: "Periodic throughput decline", body: "Data loading, collective communication, checkpoint writes, or preemption can create recurring stalls hidden by average utilization.", decision: "Break step time into compute, communication, I/O, and checkpoint phases and align them with scheduler and storage events.", boundary: "Faster checkpointing cannot sacrifice artifact integrity or recoverability.", sourceIds: Object.freeze(["checkfreq-2021", "nccl-collectives"]) }),
        Object.freeze({ id: "triage-resume-drift", title: "Drift after resume", body: "Incomplete optimizer, scheduler, RNG, or application-state restoration turns a continued run into a different experiment; data cursor, precision, and topology must be covered by the training application's own contract.", decision: "Compare uninterrupted and interrupted curves at small scale; record a complete run manifest and checkpoint compatibility.", boundary: "TorchSnapshot documents one implementation, and distributed execution may remain nondeterministic; acceptance is explainable continuity, not assumed bitwise identity.", sourceIds: Object.freeze(["torchsnapshot-checkpoint", "checkfreq-2021"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "cloud", eyebrow: "CLOUD DELIVERY", title: "Map training stages to platform responsibilities", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "cloud-training-data", title: "Data preparation", subtitle: "Object storage, lakehouse, ETL, deduplication, de-identification, labeling, and data versioning", body: "Build a lawful, traceable, reproducible training-data supply chain.", decision: "Discover source, rights, deletion, near-duplicates, and test leakage." }),
        Object.freeze({ id: "cloud-distributed-training", title: "Distributed training", subtitle: "Accelerator clusters, high-speed interconnect, parallel storage, scheduling, and fault tolerance", body: "Operate multiple devices as one recoverable training system and maximize useful progress.", decision: "Discover model and data scale, run duration, parallel plan, recovery objective, and budget." }),
        Object.freeze({ id: "cloud-experiment-management", title: "Experiment and model management", subtitle: "Experiment tracking, checkpoints, registry, lineage, approval, and artifact storage", body: "Make runs reproducible and connect every model to its data, code, configuration, and environment.", decision: "Discover whether the team can reconstruct how any candidate was produced." }),
        Object.freeze({ id: "cloud-evaluation-release", title: "Evaluation and release", subtitle: "Automated evaluation, red teaming, managed endpoints, shadow traffic, canary, and monitoring", body: "Convert training metrics into quality, safety, performance, and cost evidence in the target environment.", decision: "Discover release thresholds, protected slices, rollback path, and accountable approver." }),
      ]) }),
    ]) }),
  ]),
  qa: Object.freeze([
    Object.freeze({ id: "training-pipeline-overview", q: "How is a useful large language model trained?", a: "A typical path is governed data preparation, pretraining, supervised fine-tuning, preference optimization, evaluation, and release; some reasoning models add stages based on verifiable rewards.", depth: "Pretraining builds broad capability, SFT shapes instruction behavior, preference methods choose among plausible responses, and evaluation spans all stages. Model builders combine data and methods differently, so this is a conceptual map rather than one recipe.", ask: "Ask the customer: is the goal a new foundation model, domain continued pretraining, or enterprise customization of an existing model?", tag: "Training overview", basis: "Representative pretraining and post-training research", evidence: Object.freeze([Object.freeze({ sourceId: "chinchilla-2022", supports: "Supports coordinating model scale and training tokens under a compute budget." }), Object.freeze({ sourceId: "instructgpt-2022", supports: "Supports shaping a pretrained model with supervised demonstrations and human feedback." }), Object.freeze({ sourceId: "dpo-2023", supports: "Supports direct preference optimization as an alternative route within preference training." })]) }),
    Object.freeze({ id: "more-scale-not-always-better", q: "Do more parameters and more data always produce a better model?", a: "No. Model scale, effective data, data quality, training compute, architecture, and post-training must be coordinated; increasing one variable can simply increase cost.", depth: "Scaling laws are empirical relationships under specific setups, not an unlimited purchasing formula. Most enterprises need better data, evaluation, retrieval, or fine-tuning rather than pretraining a larger foundation.", ask: "Ask the customer: on which frozen samples does the current model fail systematically, and which measurable outcome is more scale expected to change?", tag: "Scaling boundary", basis: "Compute-optimal training research", evidence: Object.freeze([Object.freeze({ sourceId: "chinchilla-2022", supports: "Supports jointly scaling parameters and training data under a fixed compute budget rather than increasing parameters alone." })]) }),
    Object.freeze({ id: "sft-rlhf-dpo-roles", q: "What do SFT, RLHF, and DPO each solve?", a: "SFT teaches from ideal demonstrations; RLHF learns a reward model and optimizes against it; DPO learns relative choices directly from preference pairs.", depth: "A program need not use all three. Establish task behavior with SFT, then collect preferences only when the remaining gap is the relative quality of plausible answers. Choose by data form, evaluation maturity, team capability, and risk.", ask: "Ask the customer: can experts write the ideal answer, or can they only choose which of two answers is better?", tag: "Post-training", basis: "Representative alignment research", evidence: Object.freeze([Object.freeze({ sourceId: "instructgpt-2022", supports: "Supports a pipeline of supervised demonstrations, reward modeling, and reinforcement learning." }), Object.freeze({ sourceId: "dpo-2023", supports: "Supports optimizing a policy directly from preference pairs." })]) }),
    Object.freeze({ id: "prove-model-improved", q: "How do we prove that training actually improved the model?", a: "Compare against the baseline on frozen, unseen samples and jointly examine task outcomes, safety, critical slices, stability, and target-endpoint cost.", depth: "Training loss only shows movement on the optimization objective. Leakage, selection of the best run, and judge bias can create false gains. Retain split reports, repeated runs, calibrated human review, and shadow or A/B evidence.", ask: "Ask the customer: who supplies the exam, which slices may not regress, and who adjudicates disputed results?", tag: "Evaluation and release", basis: "Risk management plus training evaluation discipline", evidence: Object.freeze([Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports contextual risk measurement before and after deployment." }), Object.freeze({ sourceId: "instructgpt-2022", supports: "Uses human preference evaluation beyond the training data; this is not the same as enterprise acceptance." })]) }),
    Object.freeze({ id: "gpu-scaling-efficiency", q: "Why does doubling the GPU count not necessarily halve training time?", a: "Data, tensor, and pipeline parallelism introduce collective communication, waiting, and bubbles, so scaling depends on the model, parallel plan, topology, and implementation.", depth: "Once work is partitioned across devices, measure collective time, pipeline idle time, token throughput, model-FLOPs utilization, input wait, and checkpoint time. Megatron reports strong but non-linear results on a particular large-scale configuration; the target cluster needs its own scaling curve.", ask: "Ask the customer: does the profile show compute, communication, input, or checkpoint time, and how much did useful tokens per second improve after scaling?", tag: "Scaling efficiency", basis: "Compute-optimal training plus distributed systems", evidence: Object.freeze([Object.freeze({ sourceId: "megatron-3d-parallelism-2021", supports: "Directly studies composed data, tensor, and pipeline parallelism, including cross-node communication, waiting, and pipeline bubbles; results are configuration-specific." }), Object.freeze({ sourceId: "nccl-collectives", supports: "Defines the collective communication operations and diagnostics used by distributed GPU work, but does not promise linear scaling." })]) }),
    Object.freeze({ id: "checkpoint-resume-different-result", q: "Why can a checkpoint resume produce a different result after a training interruption?", a: "Weights alone are not a complete training state. A resume contract may also require optimizer, learning-rate scheduler, RNG, application state, and workload-specific data-position and topology metadata.", depth: "Explicitly enumerate, save, restore, and validate the required state. Compare uninterrupted and interrupted runs at small scale and measure recovery time and lost progress. TorchSnapshot covers common PyTorch objects and RNG when registered; it does not automatically prove that a customer's data cursor or every topology-dependent state was captured.", ask: "Ask the customer: must the run continue convergence, reproduce exactly, or merely preserve weights, and how many steps may be lost?", tag: "Reliability", basis: "Training state plus lifecycle governance", evidence: Object.freeze([Object.freeze({ sourceId: "torchsnapshot-checkpoint", supports: "Documents explicit snapshot and restore of model, optimizer, LR scheduler, custom application state, and RNG; data-cursor and topology coverage remain application-defined." }), Object.freeze({ sourceId: "checkfreq-2021", supports: "Studies resumable data-loader state and checkpoint-frequency trade-offs; it does not prove another framework's checkpoint completeness." })]) }),
    Object.freeze({ id: "loss-continuity-insufficient", q: "Why does a continuous loss curve after recovery not prove that the state is correct?", a: "Loss continuity is one signal. Optimizer, scheduler, RNG, application state, data position, topology, precision, or sample order may still have drifted.", depth: "Compare the checkpoint manifest, next data batch, restored optimizer and scheduler, throughput, and unseen-task performance. Check for repeated or skipped data. If the application's state contract cannot demonstrate continuity, return to an earlier trusted checkpoint.", ask: "Ask the customer: which states are saved, and how would the system detect repeated samples, omissions, or optimizer reset?", tag: "Training recovery", basis: "Distributed state plus evaluation evidence", evidence: Object.freeze([Object.freeze({ sourceId: "torchsnapshot-checkpoint", supports: "Documents explicit state registration and restore for model, optimizer, scheduler, custom state, and RNG; it does not automatically cover every data or topology state." }), Object.freeze({ sourceId: "checkfreq-2021", supports: "Shows why data-loader state and checkpoint cadence matter to continuous training; target-stack recovery still requires rehearsal." })]) }),
    Object.freeze({ id: "scaling-law-task-boundary", q: "Do more parameters and training tokens necessarily yield a better model?", a: "No. Gains depend on data quality, model-data balance, architecture, optimization, and target task, while scale increases communication and failure exposure.", depth: "A scaling law can form a budget hypothesis within the studied setup, but it cannot be extrapolated independently of task and data. Compare better data, continued pretraining, fine-tuning, or a model change before committing to scale.", ask: "Ask the customer: what capability gap is being targeted, and what evidence justifies adding parameters, data, or another training stage?", tag: "Scaling-law boundary", basis: "Compute optimality plus task validation", evidence: Object.freeze([Object.freeze({ sourceId: "chinchilla-2022", supports: "Supports joint scaling of model size and training data within the paper's experimental setup." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports measuring capability and risk in the intended context of use." })]) }),
    Object.freeze({ id: "deduplication-capability", q: "Will a smaller corpus after deduplication reduce model capability?", a: "Not necessarily. Removing near-duplicates can reduce memorization and evaluation contamination, while excessive deduplication can erase useful linguistic and domain variation.", depth: "Measure repetition at document, passage, and semantic levels and retain source, rights, and deletion lineage. Compare thresholds on unseen tasks, memorization risk, and training efficiency instead of maximizing token count or deduplication rate.", ask: "Ask the customer: is repetition caused by templates, redistribution, or meaningful domain frequency, and how is train-test contamination detected?", tag: "Data deduplication", basis: "Data quality plus memorization risk", evidence: Object.freeze([Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports managing training-data provenance, quality, privacy, and evaluation-contamination risks." }), Object.freeze({ sourceId: "chinchilla-2022", supports: "Supports considering model scale with effective training data." })]) }),
    Object.freeze({ id: "tokenizer-change-contract", q: "Can an existing model and data pipeline simply be adapted to a new tokenizer?", a: "Usually not as a minor compatibility change. Vocabulary and segmentation affect input representations, embeddings, sequence lengths, and the training distribution.", depth: "Define which weights can be reused, map special tokens, and migrate chat templates and encoded data. Use a small recovery run and critical-task evaluation before full continued pretraining; metrics from the old tokenizer version are not directly comparable.", ask: "Ask the customer: why is the tokenizer changing, and who versions the vocabulary, special tokens, and downstream templates together?", tag: "Tokenizer change", basis: "Input contract plus weight compatibility", evidence: Object.freeze([Object.freeze({ sourceId: "transformer-2017", supports: "Establishes token representations as part of the Transformer's input contract." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports remeasurement of performance and risk after a component change." })]) }),
    Object.freeze({ id: "parallelism-strategy", q: "Should data, tensor, or pipeline parallelism be selected first?", a: "There is no universal order. The choice depends on model fit, compute-to-communication ratio, topology, batch size, and the target scale.", depth: "Profile one device and one node before composing dimensions. Data parallelism replicates the model, tensor parallelism partitions operations, and pipeline parallelism partitions layers. The Megatron study quantifies communication and bubble trade-offs for one family of large-scale configurations; use the same reasoning, not its numeric result, on the customer stack.", ask: "Ask the customer: is the current constraint memory, compute, or communication, and how much did Goodput improve after the change?", tag: "Parallelism strategy", basis: "Model placement plus topology efficiency", evidence: Object.freeze([Object.freeze({ sourceId: "megatron-3d-parallelism-2021", supports: "Directly compares and composes data, tensor, and pipeline parallelism and documents communication, waiting, and pipeline-bubble trade-offs in its tested setup." }), Object.freeze({ sourceId: "nccl-collectives", supports: "Defines collective operations used to implement distributed GPU communication; target performance remains workload- and topology-specific." })]) }),
    Object.freeze({ id: "checkpoint-frequency", q: "Do more frequent checkpoints always make training more reliable?", a: "No. Saving too often consumes I/O and training time; saving too rarely increases work lost to a failure. The interval must reflect measured save cost, failure exposure, and recovery objective.", depth: "Profile save duration, acceptable progress loss, and storage pressure, then verify that checkpoints are complete, loadable, and recoverable. Consistent asynchronous snapshots can reduce blocking, but completion and retained-version checks still matter.", ask: "Ask the customer: how much effective training time may one incident lose, and did the latest recovery rehearsal prove the artifact usable?", tag: "Checkpoint strategy", basis: "Recovery point objective plus I/O cost", evidence: Object.freeze([Object.freeze({ sourceId: "checkfreq-2021", supports: "Directly studies adaptive checkpoint frequency, write overhead, and resumable data-loader state; its chosen cadence is experiment-specific." }), Object.freeze({ sourceId: "torchsnapshot-checkpoint", supports: "Documents consistent asynchronous snapshots and explicit application-state restore; completeness must still be verified for the target workload." })]) }),
  ]),
  evidenceCards: Object.freeze([
    Object.freeze({ id: "pretraining-balance", metric: "Parameters × data × compute", title: "Pretraining investment must be balanced", finding: "Chinchilla studies a compute-optimal relationship in which parameter count and training data are coordinated under a fixed compute budget.", boundary: "The result is specific to its model family, data, and settings; it is not a fixed recipe for every architecture or objective.", sourceId: "chinchilla-2022", accent: true }),
    Object.freeze({ id: "post-training-demonstrations-preferences", metric: "Demonstrations + preferences", title: "Post-training shapes assistant behavior", finding: "InstructGPT demonstrates that supervised examples and human preference feedback can materially change instruction-following behavior.", boundary: "The result does not show that any enterprise dataset will reproduce the gain or make model statements factual.", sourceId: "instructgpt-2022" }),
    Object.freeze({ id: "dpo-preference-pairs", metric: "Preference pairs", title: "DPO simplifies one preference-optimization route", finding: "DPO optimizes from chosen and rejected responses without the explicit reward-model and policy-sampling loop used in traditional RLHF pipelines.", boundary: "Comparative performance depends on the data, base model, task, and evaluation; it is not a universal default.", sourceId: "dpo-2023" }),
    Object.freeze({ id: "training-data-format", metric: "Format defines trainer input", title: "Conversation data must match the base template", finding: "Text, conversational, prompt-completion, and preference datasets use different structures, roles, and special-token conventions.", boundary: "A readable format does not establish sample quality, rights, or correct target behavior.", sourceId: "hf-trl-data-formats" }),
    Object.freeze({ id: "fine-tuning-four-layer-gate", metric: "Data / training / task / serving", title: "A fine-tuned release needs four layers of evidence", finding: "Training curves must be assessed with unseen tasks, retained capability, safety, memory, latency, and cost.", boundary: "Lower loss alone cannot authorize release or prove superiority to prompting, retrieval, or model replacement.", sourceId: "hf-trl-sft-trainer" }),
  ]),
  terms: Object.freeze({
    transformer: Object.freeze({ name: "Transformer Architecture", definition: "A sequence architecture built around attention, feed-forward transformations, residual paths, and normalization." }),
    "llm-training": Object.freeze({ name: "LLM Training", definition: "The governed process that updates model parameters through data, optimization, distributed computation, checkpoints, evaluation, and release gates." }),
    pretraining: Object.freeze({ name: "Pretraining", definition: "Large-scale learning of general language or multimodal patterns before task- and behavior-oriented post-training." }),
    "distributed-training": Object.freeze({ name: "Distributed Training", definition: "Training that partitions data, model state, or work across devices while managing communication, topology, and recovery." }),
    "fine-tuning": Object.freeze({ name: "Fine-tuning", definition: "Additional training on a foundation model to shape task behavior, format, or domain performance." }),
    sft: Object.freeze({ name: "Supervised Fine-tuning", abbr: "SFT", definition: "Training on input-and-ideal-output demonstrations to establish target behavior." }),
    rlhf: Object.freeze({ name: "Reinforcement Learning from Human Feedback", abbr: "RLHF", definition: "A post-training approach that learns preference signals from human feedback and uses reinforcement learning to optimize behavior." }),
    evaluation: Object.freeze({ name: "Evaluation", definition: "A controlled comparison using fixed tasks, samples, metrics, failure analysis, and acceptance thresholds to determine whether a model or system is releasable." }),
    "gang-scheduling": Object.freeze({ name: "Gang Scheduling", definition: "Starting a distributed workload only when its complete required resource group can be allocated together." }),
    "scale-up": Object.freeze({ name: "Scale-up", definition: "Expanding tightly coupled accelerator capacity within a node or local high-bandwidth fabric." }),
    "scale-out": Object.freeze({ name: "Scale-out", definition: "Expanding a workload across nodes through network, collective communication, congestion control, and distributed recovery." }),
  }),
  sources: Object.freeze({
    "transformer-2017": Object.freeze({ kind: "Peer-reviewed paper", shortTitle: "Transformer", note: "Introduces the attention-centered Transformer and its parallel training structure. The original experiments concern machine translation and do not describe every later LLM architecture, training pipeline, or production serving implementation." }),
    "chinchilla-2022": Object.freeze({ kind: "Primary research paper", shortTitle: "Chinchilla Scaling", note: "Studies compute-optimal relationships among model size, training tokens, and compute within the paper's model family, data, and settings. It is not a universal training-budget or capacity formula." }),
    "nist-genai-profile": Object.freeze({ kind: "Official risk-management framework", shortTitle: "NIST AI 600-1", note: "Organizes generative-AI risk management around Govern, Map, Measure, and Manage. It is a voluntary resource, not a product certification, universal compliance checklist, model ranking, retrieval benchmark, or hardware-performance standard." }),
    "instructgpt-2022": Object.freeze({ kind: "Peer-reviewed paper", shortTitle: "InstructGPT", note: "Reports a post-training pipeline using supervised demonstrations, preference data, a reward model, and reinforcement learning. Results remain specific to the studied models and evaluations." }),
    "dpo-2023": Object.freeze({ kind: "Peer-reviewed paper", shortTitle: "DPO", note: "Introduces Direct Preference Optimization using preference pairs. Applicability depends on the model, preference data, objective, and evaluation." }),
    "flashattention-2022": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "FlashAttention", note: "Presents an IO-aware exact-attention algorithm that reduces memory traffic. Realized benefit depends on sequence shape, hardware, kernels, and the surrounding training or inference stack." }),
    "opentelemetry-semconv": Object.freeze({ kind: "Open observability specification", shortTitle: "OpenTelemetry SemConv", note: "Defines common semantics for traces, metrics, logs, events, and resources. AI-specific progress, data lineage, business outcomes, approvals, and risk evidence still require project-defined fields." }),
    "hf-trl-data-formats": Object.freeze({ kind: "Official engineering documentation", shortTitle: "TRL Data Formats", note: "Documents supported text, conversational, prompt-completion, and preference dataset structures. Format compatibility does not establish dataset rights or quality." }),
    "hf-trl-sft-trainer": Object.freeze({ kind: "Official engineering documentation", shortTitle: "TRL SFTTrainer", note: "Documents SFTTrainer data, training, evaluation, and template handling. Tool support does not prove that the resulting model is effective or releasable." }),
    "deepseek-r1-2025": Object.freeze({ kind: "Peer-reviewed journal paper", shortTitle: "DeepSeek-R1", note: "Reports one model family's combination of verifiable rewards, reinforcement learning, cold-start data, staged training, and distillation. It does not establish universal benefit for open-ended tasks or make internal reasoning an audit explanation." }),
    "openai-model-spec-hidden-cot": Object.freeze({ kind: "Official model-behavior specification", shortTitle: "OpenAI Model Spec · Hidden CoT", note: "Documents a provider-specific interface boundary for hidden chain of thought. It is not a universal audit standard or evidence that internal reasoning is correct." }),
    "megatron-3d-parallelism-2021": Object.freeze({ kind: "Primary systems paper", shortTitle: "Megatron 3D Parallelism", note: "Studies composed data, tensor, and pipeline parallelism and the effects of cross-node communication, waiting, and bubbles. Results are limited to the paper's models, hardware, scale, and configuration." }),
    "nccl-collectives": Object.freeze({ kind: "Official technical documentation", shortTitle: "NCCL Collectives", note: "Documents GPU collective operations, communicators, topology, network, and performance diagnostics. It defines communication mechanisms but does not promise linear scaling or one best network." }),
    "torchsnapshot-checkpoint": Object.freeze({ kind: "Official framework documentation", shortTitle: "TorchSnapshot Checkpoint", note: "Documents capture and restore of model, optimizer, learning-rate scheduler, custom application state, and RNG, plus consistent asynchronous snapshots. Data-cursor and topology coverage remain application-specific." }),
    "checkfreq-2021": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "CheckFreq", note: "Studies adaptive checkpoint frequency, pipelined writes, and resumable data-loader state. Its measured trade-offs do not determine the best interval for another training stack." }),
  }),
});
