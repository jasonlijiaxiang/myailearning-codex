export const englishModule = Object.freeze({
  slug: "ai-infra-platform",
  title: "AI Infrastructure Platform",
  subtitle: "Operate accelerators as schedulable, isolated, recoverable AI services",
  definition: "An AI Infrastructure Platform is a self-service product for developers and operators. It organizes heterogeneous compute, network, storage, runtimes, and policy into training and inference services that can be requested, admitted, scheduled, isolated, recovered, observed, and costed.",
  position: "It sits between physical compute and training or inference workloads. The platform control layer provides a capability catalog, APIs, templates, policy, quota, versions, and audit; the workload execution layer runs notebooks, training jobs, batch work, and model-service replicas. The platform owns resource, job, and service-runtime lifecycles—not hardware procurement, training-state correctness, inference-engine internals, request routing, model quality, or project ROI.",
  relatedSlugs: Object.freeze(["ai-infra-compute", "llm-training", "llm-inference", "ai-ops", "security", "solution-patterns"]),
  sections: Object.freeze([
    Object.freeze({ id: "study-guide", eyebrow: "LEARN, PRACTICE, PROVE", title: "Build a platform around workload contracts and useful output", lead: "A platform is complete only when devices, queues, tenancy, environments, recovery, releases, observability, and economics work together for the workload.", blocks: Object.freeze([
      Object.freeze({ type: "cards", title: "Learning outcomes", items: Object.freeze([
        Object.freeze({ id: "outcome-platform-duty", title: "Define the platform as a product", body: "Start from internal users, supported journeys, a self-service contract, service levels, and explicit ownership." }),
        Object.freeze({ id: "outcome-scheduler-choice", title: "Separate control from execution", body: "Trace how admission, scheduling, workload execution, domain correctness, and recovery divide responsibility." }),
        Object.freeze({ id: "outcome-device-sharing", title: "Validate tenant boundaries and portability", body: "Test four multi-tenant acceptance boundaries and distinguish image, orchestration, device, and end-to-end workload portability." }),
        Object.freeze({ id: "outcome-platform-goodput", title: "Use resource economics correctly", body: "Relate queueing, utilization, Goodput, waste, SLOs, and resource cost without claiming application ROI." }),
      ]) }),
      Object.freeze({ type: "steps", title: "Recommended learning route", items: Object.freeze([
        Object.freeze({ id: "route-resource-contract", title: "Start with the user and paved path", body: "Choose a representative developer, training, or serving journey and define its catalog, interface, support boundary, and service level.", decision: "Checkpoint: platform demand comes from user work, not a component inventory." }),
        Object.freeze({ id: "route-queue-sharing", title: "Write the workload-control contract", body: "Describe identity, devices, topology, network, storage, runtime, deadlines, recovery, and rejection semantics.", decision: "Checkpoint: control-layer and execution-layer objects, state, and owners are traceable." }),
        Object.freeze({ id: "route-recoverable-operations", title: "Design admission, placement, and tenant boundaries", body: "Combine quota, priority, gangs, device claims, topology, security and interference controls, and accountable cost allocation.", decision: "Checkpoint: explain queueing, fragmentation, noisy neighbors, security boundaries, and cost ownership separately." }),
        Object.freeze({ id: "route-useful-output", title: "Connect development, operation, recovery, and exit", body: "Version environments, jobs, artifacts, and services; rehearse upgrades, rollback, and target-environment migration.", decision: "Checkpoint: containerization is never mistaken for complete portability." }),
        Object.freeze({ id: "route-training-serving-boundary", title: "Close with useful output", body: "Relate queueing, utilization, Goodput, idle, reserve, retries, and accountable resource cost.", decision: "Checkpoint: platform economics and application business ROI have different owners." }),
      ]) }),
      Object.freeze({ type: "cards", title: "Practice labs", items: Object.freeze([
        Object.freeze({ id: "lab-idle-gpu-queue", title: "Diagnose idle GPUs with queued jobs", subtitle: "The cluster reports 20% idle GPU capacity while training teams wait for hours", body: "Inspect request shapes, topology, quota, priority, and gang constraints; identify whole-device fragmentation and unsatisfied placement; compare waiting, smaller requests, and preemption.", decision: "Deliverable: a scheduling diagnosis and capacity-improvement plan.", boundary: "Acceptance: the queue cause is reproducible and critical completion time is not sacrificed for aggregate utilization.", sourceIds: Object.freeze(["kubernetes-dra", "opentelemetry-semconv"]) }),
        Object.freeze({ id: "lab-runtime-upgrade", title: "Design a GPU-stack upgrade", subtitle: "Driver, firmware, Operator, and inference runtime must change together", body: "Build the compatibility matrix and rollback points; choose canary nodes and representative loads; validate training recovery, inference quality, and telemetry continuity.", decision: "Deliverable: a five-stage upgrade and recovery runbook.", boundary: "Acceptance: every failed stage can return to a known-good combination without mislabeling artifacts or job state as recovered.", sourceIds: Object.freeze(["nvidia-gpu-operator", "kubernetes-dra"]) }),
        Object.freeze({ id: "lab-training-serving-colocation", title: "Design training and online inference colocation", subtitle: "The customer wants higher utilization without losing online tail latency", body: "Define resource, topology, deadline, and recovery contracts for both loads; compare separate pools, quota sharing, preemption, and partitioning; load-test interference, scale, failure, and cost attribution.", decision: "Deliverable: a colocation policy and stop conditions.", boundary: "Acceptance: state when to share, when to isolate, and how to recover after an SLO breach.", sourceIds: Object.freeze(["kubernetes-dra", "nvidia-gpu-operator", "opentelemetry-semconv"]) }),
        Object.freeze({ id: "lab-platform-exit", title: "Rehearse platform exit and migration", subtitle: "The team assumes the same OCI image and Kubernetes YAML can move unchanged to another cloud and accelerator", body: "Inventory image, driver, kernel, resource-claim, data, IAM, network, storage, telemetry, and managed-service dependencies; replay build, data access, execution, recovery, SLO, and rollback in the target environment.", decision: "Deliverable: a layered portability inventory, migration evidence, and exit runbook.", boundary: "Acceptance: claim portability only for the tested scope; every failed dependency has a replace, retain, or stop decision.", sourceIds: Object.freeze(["oci-image-spec-v1-1-1", "kubernetes-dra", "nist-zero-trust"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "curriculum", eyebrow: "CURRICULUM ATLAS", title: "From platform product to verifiable operation", lead: "A supported path connects self-service, control and execution, admission, isolation, versioned environments, recovery, portability, and resource economics.", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "curriculum-platform-duties", title: "Platform as a product and self-service contract", subtitle: "Platform as a Product", body: "The platform starts with users and recurring workloads, delivering devices, environments, jobs, and services through a capability catalog, APIs, templates, documentation, and supported paths. The workload contract declares resources, topology, data, runtime, deadlines, recovery, and service level.", decision: "Make one frequent development-to-operation path self-service, rejectable, and supportable before expanding capability.", boundary: "Deploying Kubernetes or listing components does not prove a coherent platform experience.", sourceIds: Object.freeze(["cncf-platforms-whitepaper", "kubernetes-dra"]) }),
        Object.freeze({ id: "curriculum-control-execution", legacyIds: Object.freeze(["curriculum-serving-platform"]), title: "Platform control layer and workload execution layer", subtitle: "Control & Execution", body: "The control layer maintains capability, desired state, identity, policy, quota, versions, and audit. The execution layer runs notebooks, training jobs, batch work, and model-service replicas with their compute, network, and storage.", decision: "For every object, name the reconciling controller, the executing workload, and the recovery owner.", boundary: "Healthy controllers do not prove task correctness; healthy execution does not prove policy, quality, or business outcome.", sourceIds: Object.freeze(["cncf-platforms-whitepaper", "opentelemetry-semconv"]) }),
        Object.freeze({ id: "curriculum-kubernetes-gpu-stack", title: "DRA, Device Plugin, and the device runtime stack", subtitle: "Device Resource Stack", body: "Device Plugins expose traditional extended resources. DRA core APIs became stable in 1.34, while optional capabilities in 1.36 remain split across Stable, Beta, and Alpha. GPU Operator manages a particular vendor's drivers, plugins, and monitoring.", decision: "Adopt DRA only for a confirmed resource-expression or lifecycle gap and migrate by cluster, driver, optional feature, and workload.", boundary: "Core API GA does not make every feature or driver GA, and DRA does not replace queues, training orchestration, model serving, or business authorization.", sourceIds: Object.freeze(["kubernetes-dra-1-34-ga", "kubernetes-dra-1-36", "kubernetes-dra", "nvidia-gpu-operator"]) }),
        Object.freeze({ id: "curriculum-scheduling", title: "Admission, queues, gangs, and topology-aware placement", subtitle: "Admission & Placement", body: "Admission first checks identity, policy, quota, request shape, and optional external conditions. Queues, priority, gangs, borrowing, preemption, device attributes, and topology then determine when and where work runs.", decision: "Split pending reasons into quota, policy, shape, topology, health, and preparation before choosing wait, backfill, preemption, overflow, or rejection.", boundary: "Kueue mechanisms are not universal Kubernetes guarantees; preemption loses progress, and time sharing does not eliminate every fragmentation pattern.", sourceIds: Object.freeze(["kueue-all-or-nothing", "kueue-fair-sharing", "kubernetes-dra"]) }),
        Object.freeze({ id: "curriculum-device-sharing", title: "Four multi-tenant acceptance boundaries", subtitle: "Tenant Acceptance Boundaries", body: "Management and control, identity/data/network, performance/resources, and cost allocation/accountability need separate acceptance. The first three cover isolation and interference; the fourth makes shared cost ownership explicit. Whole devices, partitions, time sharing, namespaces, and quota each cover only part of the problem.", decision: "Combine control-plane boundaries, RBAC, network and storage, keys, devices, caches, logs, and cost allocation by trust level and SLO, then stress-test noisy neighbors.", boundary: "Shared-cluster primitives do not automatically create hard isolation, and cost allocation is not security evidence; privileged platform operations also require audit.", sourceIds: Object.freeze(["kubernetes-multi-tenancy", "nist-zero-trust", "nvidia-gpu-operator", "finops-unit-economics"]) }),
        Object.freeze({ id: "curriculum-resilience", title: "Versioned paved path, recovery, and upgrade", subtitle: "Paved Path & Recovery", body: "Development environments, jobs, pipelines, artifacts, runtime configuration, and service deployment connect through versioned handoffs. The platform supplies draining, rescheduling, health, canary, and rollback primitives.", decision: "Rehearse failure, upgrade, and rollback with representative training and inference chains while leaving state correctness and release semantics to their owning modules.", boundary: "A restarted Pod, replaced node, or restored weight file alone does not prove correct training or service state.", sourceIds: Object.freeze(["cncf-platforms-whitepaper", "torchsnapshot-checkpoint", "checkfreq-2021"]) }),
        Object.freeze({ id: "curriculum-portability-exit", title: "Portability and exit rehearsal", subtitle: "Portability & Exit", body: "Image format, orchestration API, device-resource contract, and end-to-end workload are different portability layers. Drivers, kernels, data, IAM, network, storage, telemetry, and managed services remain target-environment dependencies.", decision: "Inventory platform-specific dependencies and replay build, data access, execution, recovery, SLO, cost, and rollback before claiming a precise portable scope.", boundary: "An OCI image or Kubernetes YAML is not evidence of cross-cloud or cross-accelerator migration.", sourceIds: Object.freeze(["oci-image-spec-v1-1-1", "kubernetes-dra", "nist-zero-trust"]) }),
        Object.freeze({ id: "curriculum-capacity-economics", legacyIds: Object.freeze(["curriculum-platform-observability"]), title: "Goodput, resource cost, and business ROI", subtitle: "Useful Output & Economics", body: "The platform correlates allocated, used, idle, shared, reserved, queued, and retry or replay cost with utilization, MFU, Goodput, and service SLOs.", decision: "Use resource facts for capacity, showback, or chargeback; send quality gates, cost per successful outcome, and ROI to the application and Solution Patterns layers.", boundary: "High utilization is not high Goodput, and high Goodput does not automatically prove business value; normalize every denominator.", sourceIds: Object.freeze(["opentelemetry-semconv", "opentelemetry-genai-semconv", "finops-unit-economics"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "principles", eyebrow: "PLATFORM PRODUCT", title: "Build a supported path from self-service contract to recovery", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "principle-device-claim", title: "Platform product and workload contract", body: "A catalog, self-service APIs, templates, and a support path serve named users, while workloads declare resources, topology, runtime, deadlines, and recovery needs.", decision: "Define users, common workloads, and service levels before choosing components." }),
        Object.freeze({ id: "principle-gang-queue", title: "Admission, queueing, and fairness", body: "Identity, policy, quota, request shape, priority, gangs, borrowing, and preemption govern when scarce capacity is delivered.", decision: "Define fairness, business priority, interruptibility, and rejection reasons before choosing a scheduler." }),
        Object.freeze({ id: "principle-sharing-isolation", title: "Placement, sharing, and four tenant boundaries", body: "Device attributes, topology, health, and failure domains govern placement; control, identity/data/network, performance/resources, and cost allocation/accountability require separate acceptance evidence. The first three concern isolation or interference; the fourth concerns ownership.", decision: "Never treat a namespace, quota, sharing mode, or cost report as proof that every boundary is satisfied." }),
        Object.freeze({ id: "principle-environment-reproducibility", title: "Versioned paved path", body: "Development environments, reproducible jobs, pipelines, artifacts, runtimes, and services connect through versioned handoffs.", decision: "Preserve identity, versions, inputs, outputs, and owners at every handoff." }),
        Object.freeze({ id: "principle-failure-recovery", title: "Recovery, upgrade, and exit", body: "The platform supplies health, rescheduling, draining, canary, rollback, and target-environment migration primitives.", decision: "Use different recovery gates for training and serving, and prove portability in the target environment." }),
        Object.freeze({ id: "principle-serving-observability", title: "Useful output and resource economics", body: "Allocated, used, idle, shared, and reserved resources—plus retry and replay costs—connect to queues, Goodput, SLOs, and cost ownership.", decision: "Let the platform prove resource economics while the application proves quality, unit success, and ROI." }),
      ]) }),
      Object.freeze({ type: "boundary", title: "Critical boundary", items: Object.freeze([Object.freeze({ id: "platform-component-boundary", title: "A component stack is not a platform product", body: "The platform owns resource, job, and service-runtime lifecycles and an operable user contract.", boundary: "It does not own hardware procurement, training-state correctness, inference-engine internals, gateway policy, model quality, or project ROI. Kubernetes, GPU Operator, schedulers, notebooks, and serving frameworks are components, not proof of a complete platform." })]) }),
    ]) }),
    Object.freeze({ id: "decisions", eyebrow: "PRESALES DECISIONS", title: "Choose control planes, sharing, service model, and objective", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "decision-kubernetes-slurm", title: "Kubernetes, Slurm, or both?", body: "Research training values mature HPC queues, while online services value containers, APIs, and cloud-native integrations.", decision: "Layer by workload and team; if both remain, unify identity, data, artifacts, quotas, and cost views.", boundary: "Dual stacks increase operations and capacity fragmentation and need a measurable workload benefit." }),
        Object.freeze({ id: "decision-dra-adoption", title: "When should Kubernetes DRA be adopted?", body: "Static device resources cannot express needed attributes, parameters, preparation, sharing, or vendor-neutral claims.", decision: "Separate the stable 1.34 core APIs from the Stable, Beta, and Alpha optional capabilities documented for 1.36; validate cluster, driver, claim lifecycle, health, upgrade, and recovery in a noncritical pool.", boundary: "DRA improves resource expression and allocation; it does not prove driver production readiness or provide queue fairness, job orchestration, or model serving." }),
        Object.freeze({ id: "decision-gpu-sharing", title: "Whole device, hardware partition, or time sharing?", body: "Small models and development can leave whole devices idle, while production requires predictability and isolation.", decision: "Choose by workload pool, then verify control-plane, identity/data/network, performance/resource, and cost-ownership boundaries separately.", boundary: "Higher sharing is not always better; validate memory, bandwidth, failure isolation, and side-channel risk, and do not treat cost allocation as security evidence." }),
        Object.freeze({ id: "decision-managed-platform", title: "Build the platform or use a managed service?", body: "The organization trades custom scheduling, mixed hardware, and compliance control against delivery speed and managed upgrades.", decision: "Compare user experience, responsibility, and exit cost across managed Kubernetes, managed training or inference, and a self-built control plane; rehearse migration in the target environment.", boundary: "Managed does not mean no operations, and open interfaces do not prove workload portability; model, data, quota, SLO, and business outcomes remain customer responsibilities." }),
        Object.freeze({ id: "decision-utilization-queue", title: "Raise utilization or shorten queues?", body: "Aggregate utilization can be high while critical work waits, or low queues can rely on expensive reserve.", decision: "Use queues, quota, reserve, and elastic overflow by business tier; measure useful job utilization and end-to-end delivery time.", boundary: "Aggregate utilization can hide low-priority saturation or wasteful preemption and replay of critical jobs." }),
      ]) }),
    ]) }),
    Object.freeze({ id: "deep-dive", eyebrow: "QUEUE & STACK RELEASE", title: "Explain idle capacity and upgrade the runtime safely", lead: "An idle GPU does not prove that current requests can use it, and a supported driver does not prove that a target workload survives the complete stack change.", blocks: Object.freeze([
      Object.freeze({ type: "table", title: "Five reasons jobs queue despite idle GPUs", columns: Object.freeze(["Cause", "Mechanism", "Action", "Boundary"]), items: Object.freeze([
        Object.freeze({ id: "queue-gang-fragmentation", title: "Gang fragmentation", body: "Free devices are split across nodes, models, or failure domains and cannot form the homogeneous group a distributed job needs.", decision: "Compare free inventory with pending request shape and test topology placement, backfill, or workload decomposition.", boundary: "Forcing cross-node placement may reduce communication performance and widen the failure domain.", sourceIds: Object.freeze(["kubernetes-dra"]) }),
        Object.freeze({ id: "queue-unsatisfied-claim", title: "Unsatisfied device claim", body: "The request requires memory, model, driver, sharing mode, or topology that idle devices do not satisfy.", decision: "Expose the unsatisfied attributes and test real claims and drivers in a noncritical pool.", boundary: "Relaxing a constraint can break model compatibility, performance, or isolation.", sourceIds: Object.freeze(["kubernetes-dra"]) }),
        Object.freeze({ id: "queue-unhealthy-device", title: "Visible but unhealthy device", body: "Registered capacity can still have driver, plugin, firmware, monitoring, or hardware faults.", decision: "Include health and driver-stack evidence in scheduling and quarantine failed devices until recovery is verified.", boundary: "A node restart can mask rather than repair hardware degradation.", sourceIds: Object.freeze(["nvidia-gpu-operator"]) }),
        Object.freeze({ id: "queue-quota-priority", title: "Quota and priority block", body: "Physical capacity is idle while project quota, reserve, priority, or preemption policy prevents allocation.", decision: "Display physical idle, allocatable capacity, and organizational quota separately and review business intent.", boundary: "Utilization cannot override isolation, budgets, and critical reserves.", sourceIds: Object.freeze(["opentelemetry-semconv"]) }),
        Object.freeze({ id: "queue-startup-path", title: "Startup-path delay", body: "A device is allocated while image pull, model load, data mount, and network preparation still consume delivery time.", decision: "Trace scheduler wait, device prepare, image, data, and model load before choosing warm pools or caches.", boundary: "Warm capacity reduces cold start but consumes storage and compute.", sourceIds: Object.freeze(["opentelemetry-semconv", "nvidia-gpu-operator"]) }),
      ]) }),
      Object.freeze({ type: "steps", title: "Five-stage stack upgrade", items: Object.freeze([
        Object.freeze({ id: "upgrade-compatibility-manifest", title: "Freeze the compatibility manifest", body: "Bind firmware, driver, container toolkit, device plugin, framework, engine, model, and extensions.", decision: "Deliver the validated complete combination, not a generic GPU-support statement.", boundary: "A vendor matrix is a shortlist, not customer-workload evidence.", sourceIds: Object.freeze(["nvidia-gpu-operator"]) }),
        Object.freeze({ id: "upgrade-canary-pool", title: "Create an isolated canary pool", body: "Upgrade a small independent node set while keeping the old pool and blocking unvalidated migration.", decision: "Validate discovery, ResourceClaim, telemetry, network, and storage before model jobs.", boundary: "One-node success does not prove multi-node communication, sharing, or recovery.", sourceIds: Object.freeze(["kubernetes-dra", "nvidia-gpu-operator"]) }),
        Object.freeze({ id: "upgrade-representative-replay", title: "Replay representative work", body: "Run training, online serving, long context, quantization, and distributed cases against fixed artifacts and inputs.", decision: "Compare quality, throughput, P95, memory, and errors and block promotion on material regression.", boundary: "Synthetic kernel tests do not form the complete release gate.", sourceIds: Object.freeze(["vllm-2023", "opentelemetry-semconv"]) }),
        Object.freeze({ id: "upgrade-drain-promote", title: "Drain and promote by failure domain", body: "Move recoverable jobs, upgrade, return capacity, and watch queues and serving SLOs continuously.", decision: "Use checkpoint-aware draining for training and redundant replicas for online service.", boundary: "Forced interruption cannot assume correct framework recovery semantics.", sourceIds: Object.freeze(["opentelemetry-semconv"]) }),
        Object.freeze({ id: "upgrade-executable-rollback", title: "Retain executable rollback", body: "Keep old images, drivers, and configuration and define triggers, ordering, and handling of new-format artifacts.", decision: "Rehearse rollback on a node batch before full promotion and prevent scheduling back into the failed pool.", boundary: "Firmware and data formats may require forward repair rather than simple rollback.", sourceIds: Object.freeze(["nvidia-gpu-operator"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "cloud", eyebrow: "CLOUD DELIVERY", title: "Map platform responsibilities to managed capabilities", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "cloud-managed-cluster", title: "Managed cluster", subtitle: "Managed Kubernetes, GPU pools, repair, images, and drivers", body: "Reduce control-plane and node-lifecycle work while focusing the team on resource and model-service governance.", decision: "Discover who owns Kubernetes version, driver, node repair, and security patching." }),
        Object.freeze({ id: "cloud-training-batch", title: "Training and batch", subtitle: "Managed training, batch, queues, Spot, checkpoints, and experiments", body: "Combine scarce capacity by priority and interruptibility while retaining data, model, experiment, and cost lineage.", decision: "Discover job scale, duration, interruptibility, checkpoint maturity, and team quotas." }),
        Object.freeze({ id: "cloud-online-serving", title: "Online model serving", subtitle: "Managed inference, Kubernetes serving, load balancing, autoscaling, registry, and canary", body: "Unify model version, traffic, elasticity, health, and rollback into a production loop.", decision: "Discover P95, availability, cold start, peak concurrency, and release cadence." }),
        Object.freeze({ id: "cloud-governance-observability", title: "Governance and observability", subtitle: "IAM, policy, GPU metrics, logs, traces, cost allocation, and capacity forecast", body: "Explain who used which device, why work queued, whether jobs were useful, and where cost belongs.", decision: "Discover whether the customer can attribute occupancy, wait, failure, and cost by team, project, and model." }),
      ]) }),
    ]) }),
  ]),
  qa: Object.freeze([
    Object.freeze({ id: "small-gpu-platform-need", q: "Does a team with only a few GPUs need an AI infrastructure platform?", a: "Not necessarily a complex platform, but it does need a minimal, repeatable self-service path: device and driver inventory, identity, queueing, standard environments, run records, failure handling, and cost. Productize further as scale and shared responsibility grow.", depth: "With few devices, the usual problem is not a weak scheduling algorithm but shared accounts, hand-built environments, overwritten work, and unclear ownership. Start with supported templates, a simple queue, standard images, run manifests, monitoring, and a recovery path. Add multi-team fairness, distributed-job admission, partitioning, or serving SLO controls only when the workload creates that need.", ask: "Ask the customer: who uses the platform, is the dominant pain access, environment, queueing, recovery, or production release, and which self-service path should become stable first?", tag: "Starting point", basis: "Progressive platform build", evidence: Object.freeze([Object.freeze({ sourceId: "cncf-platforms-whitepaper", supports: "Supports treating a platform as an internal product delivered through self-service interfaces, templates, documentation, and supported paths." }), Object.freeze({ sourceId: "nvidia-gpu-operator", supports: "Documents the driver, device-plugin, container-toolkit, and monitoring stack needed for Kubernetes GPU operation." }), Object.freeze({ sourceId: "kubernetes-dra", supports: "Documents structured device requests and allocation whose adoption depends on cluster and driver support." })]) }),
    Object.freeze({ id: "kubernetes-versus-slurm", q: "Is Kubernetes or Slurm better for AI?", a: "It depends on workload and organization. Slurm is a cluster workload manager for queued parallel work; Kubernetes can combine containerized services with add-ons such as Kueue for batch admission and fairness.", depth: "Compare all-or-nothing admission, fairness, topology, preemption, checkpoint integration, online serving, identity, network, telemetry, and team skills. A research cluster may retain Slurm while product services use Kubernetes, linked by shared data, artifacts, and cost. Dual stacks need a measured workload benefit.", ask: "Ask the customer: is the primary load long distributed training, interactive development, or 24×7 online inference, and what does the team already operate well?", tag: "Scheduler selection", basis: "Workload and responsibility matrix", evidence: Object.freeze([Object.freeze({ sourceId: "slurm-overview", supports: "Defines Slurm resource allocation, parallel-job execution, and queue arbitration and lists topology, backfill, gang-scheduling, preemption, and priority plugins; it does not prove a site enabled them." }), Object.freeze({ sourceId: "kueue-all-or-nothing", supports: "Documents one Kubernetes-native all-or-nothing admission and requeue mechanism for synchronized jobs; it is not a guarantee from the default scheduler." }), Object.freeze({ sourceId: "kueue-fair-sharing", supports: "Documents Kueue quota, priority, borrowing, preemption, and weighted fair sharing; the organization still defines fairness and disruption cost." }), Object.freeze({ sourceId: "kubernetes-dra", supports: "Documents structured device selection and preparation; DRA is not a queue or job-lifecycle system." })]) }),
    Object.freeze({ id: "dra-migration", q: "Will DRA replace Device Plugins, and should every cluster migrate immediately?", a: "DRA core APIs reached stable v1 in Kubernetes 1.34, but that does not require immediate replacement of every Device Plugin. Migration depends on the driver, desired capabilities, and existing workloads.", depth: "First identify the current gap: attribute selection, sharing, parameterization, cross-node resources, or node preparation. Then separate the stable core APIs from the Stable, Beta, and Alpha optional capabilities documented for 1.36 and validate the target cluster and driver across claim lifecycle, health, failure, upgrade, quota, monitoring, and rollback.", ask: "Ask the customer: which allocation problem is unsolved today, and does the target version and hardware driver support the needed behavior?", tag: "DRA", basis: "Official Kubernetes device-resource model", evidence: Object.freeze([Object.freeze({ sourceId: "kubernetes-dra-1-34-ga", supports: "Records the DRA core API graduating to stable v1 and default enablement in Kubernetes 1.34." }), Object.freeze({ sourceId: "kubernetes-dra-1-36", supports: "Records the different Stable, Beta, and Alpha maturity states of optional DRA capabilities in Kubernetes 1.36; it does not establish driver or workload production readiness." }), Object.freeze({ sourceId: "kubernetes-dra", supports: "Documents ResourceClaim and driver-based device expression, preparation, and allocation." })]) }),
    Object.freeze({ id: "vllm-operator-not-platform", q: "Do vLLM and GPU Operator together constitute a production AI platform?", a: "No. One optimizes model execution and the other manages the Kubernetes GPU software stack. A production platform also needs a stable self-service contract, admission, isolation, a development-to-operation path, recovery, observability, and cost governance.", depth: "Accept through responsibilities: can users request capability and receive an explicit rejection reason; can devices be discovered and isolated; can jobs be admitted fairly; can environments, artifacts, jobs, and services hand off through versions; can incidents be diagnosed and recovered; and can resource cost be allocated? Components may be minimal, but no responsibility may be missing.", ask: "Ask the customer: is the missing capability efficient generation, GPU-stack operation, or the lifecycle for multi-team production services?", tag: "Platform boundary", basis: "Layered component responsibilities", evidence: Object.freeze([Object.freeze({ sourceId: "cncf-platforms-whitepaper", supports: "Supports treating the platform as a product for internal users with self-service interfaces, templates, documentation, and supported paths; it does not prescribe an AI component list." }), Object.freeze({ sourceId: "vllm-2023", supports: "Focuses vLLM on LLM serving and KV-cache management." }), Object.freeze({ sourceId: "nvidia-gpu-operator", supports: "Focuses GPU Operator on deploying and operating Kubernetes GPU software components." }), Object.freeze({ sourceId: "opentelemetry-genai-semconv", supports: "Provides evolving cross-component telemetry semantics for generative-AI systems." })]) }),
    Object.freeze({ id: "low-utilization-sharing", q: "Does low GPU utilization always mean that more workloads should share the device?", a: "No. Low utilization can reflect input wait, communication, CPU limits, fragmentation, or measurement semantics. Oversubscription can damage performance and isolation.", depth: "Profile device idle, memory, communication, and I/O by job before choosing queues, elasticity, hardware partitions, or time sharing. Training and latency-sensitive inference tolerate interference differently. Optimize useful completed work and fairness, not a 100% dashboard.", ask: "Ask the customer: which job and phase is underutilized, and what happens to P95, completion time, and failures after colocation?", tag: "Resource efficiency", basis: "Device management plus workload isolation", evidence: Object.freeze([Object.freeze({ sourceId: "nvidia-gpu-operator", supports: "Documents the integrated GPU runtime and monitoring stack." }), Object.freeze({ sourceId: "kubernetes-dra", supports: "Documents structured resource claims for specific device requirements." })]) }),
    Object.freeze({ id: "platform-upgrade-impact", q: "Why do AI platform upgrades often disrupt running workloads?", a: "Drivers, CUDA, frameworks, containers, device plugins, and schedulers form a compatibility chain; changing one layer can alter visible devices or execution behavior.", depth: "Maintain tested combinations and immutable images, replay representative work in a canary pool, then drain and upgrade in batches. Long training must prove checkpoint recovery. Link version, node, and job so regressions can be rolled back rather than patched on shared nodes.", ask: "Ask the customer: what is the compatibility matrix and rollback unit, which representative jobs must pass, and how are long jobs moved safely?", tag: "Upgrade governance", basis: "GPU software stack plus declarative platform", evidence: Object.freeze([Object.freeze({ sourceId: "nvidia-gpu-operator", supports: "Documents the interdependent driver, container toolkit, device plugin, and monitoring components." }), Object.freeze({ sourceId: "kubernetes-dra", supports: "Documents declarative matching of workloads to device capabilities." })]) }),
    Object.freeze({ id: "high-utilization-low-output", q: "Why can training and inference output remain poor when GPU utilization is high?", a: "Utilization only means the device is active. It does not prove useful model computation, on-time job completion, conforming service, or accepted output.", depth: "Training needs MFU, Goodput, communication, input wait, checkpoints, retries, and qualifying candidates. Inference needs task success, tail latency, rejection, and cost per success. Start platform optimization from queue causes and outcomes.", ask: "Ask the customer: how much conforming work completed while devices were busy, and how much time was communication, wait, replay, or rejected output?", tag: "Useful output", basis: "Resource telemetry plus business outcome", evidence: Object.freeze([Object.freeze({ sourceId: "opentelemetry-semconv", supports: "Provides common resource and runtime telemetry semantics." }), Object.freeze({ sourceId: "opentelemetry-genai-semconv", supports: "Provides generative-AI call attributes; business Goodput still requires application outcomes." }), Object.freeze({ sourceId: "nvidia-gpu-operator", supports: "Provides GPU-stack operation and monitoring, not a definition of business Goodput." })]) }),
    Object.freeze({ id: "training-serving-shared-pool", q: "Can training and online inference share one GPU pool permanently?", a: "They can be evaluated for sharing, but interference, preemption, fragmentation, and failure must not break online tail latency or training recovery.", depth: "Training favors long-run throughput and checkpoints; serving favors priority, elasticity, and tail latency. Compare separate pools, reserve, quota, low-priority training, and device partitioning under peak, failure, and restore tests—not average utilization alone.", ask: "Ask the customer: what is the hard online SLO, how much progress may a preempted training job lose, and who pays the replay cost?", tag: "Colocation boundary", basis: "Scheduling contract plus isolation validation", evidence: Object.freeze([Object.freeze({ sourceId: "kubernetes-dra", supports: "Supports device attributes and claims in scheduling; each workload still needs a resource contract." }), Object.freeze({ sourceId: "nvidia-gpu-operator", supports: "Documents GPU sharing and runtime-management capabilities whose isolation and performance require target validation." })]) }),
    Object.freeze({ id: "fifo-gpu-fairness", q: "Is first-come, first-served the fairest GPU queue?", a: "Not necessarily. FIFO is one possible policy; multi-tenant schedulers can also incorporate historical share, priority, job size, quota, QOS, or site policy.", depth: "Define policies for online, training, development, and emergencies, then observe wait distributions, preemption, lost work, and completed value. The organization must state what fairness means—such as weighted share or deadline protection—because no scheduler can infer business commitments.", ask: "Ask the customer: which jobs have deadlines, who may preempt whom, and who owns lost progress and cost?", tag: "Queue fairness", basis: "Quota governance plus business priority", evidence: Object.freeze([Object.freeze({ sourceId: "slurm-multifactor-priority", supports: "Documents FIFO as one Slurm option and multifactor priority using age, fair-share, job size, partition, QOS, and site factors." }), Object.freeze({ sourceId: "kueue-fair-sharing", supports: "Documents quota, priority, borrowing, preemption, and weighted-share mechanisms; it does not define the customer's fairness objective." })]) }),
    Object.freeze({ id: "same-gpu-topology-variance", q: "Why can a distributed job perform differently on GPUs of the same model?", a: "Topology, links, NUMA placement, health, and neighboring workloads differ. Identical device names do not imply equivalent placement.", depth: "Include device model, local and network interconnect, node topology, health, driver, and occupancy in scheduling. Validate intra- and inter-node bandwidth and failure domains, quarantine degraded nodes, and retain diagnostic evidence.", ask: "Ask the customer: does scheduling understand high-speed links, network, and failure domains, and can poor performance be traced to its placement?", tag: "Topology-aware scheduling", basis: "Device attributes plus topology awareness", evidence: Object.freeze([Object.freeze({ sourceId: "kubernetes-dra", supports: "Documents allocation based on device attributes and claims." }), Object.freeze({ sourceId: "nvidia-gpu-operator", supports: "Documents cluster operation of GPU device, driver, and monitoring components." })]) }),
    Object.freeze({ id: "quota-not-isolation", q: "Does tenant quota complete platform isolation?", a: "No. Quota handles only part of resource fairness. Validate control-plane, identity/data/network, performance/resource, and cost-ownership boundaries separately. The first three address isolation or interference; the fourth addresses allocation and accountability, not security isolation.", depth: "Namespaces and ResourceQuota cannot prevent every cross-tenant read, malicious image, debug exposure, shared-cache leak, or noisy neighbor. Combine workload identity, RBAC, network and storage policy, artifact and key boundaries, device-sharing stress tests, and administrator audit; mutually untrusted tenants may need virtual or separate control planes. For the cost boundary, map shared resources, idle capacity, retries, and business outcomes to an accountable owner instead of treating quota or security controls as allocation evidence.", ask: "Ask the customer: which APIs, storage, networks, logs, caches, and device metrics can tenants control or see; how are authorization and non-interference proven; how are administrators audited; and who owns the resulting cost?", tag: "Multi-tenant boundaries", basis: "Isolation and interference validation plus cost ownership", evidence: Object.freeze([Object.freeze({ sourceId: "kubernetes-multi-tenancy", supports: "Treats multi-tenancy as security, fairness, and noisy-neighbor concerns and combines namespaces, RBAC, quota, network controls, and different control-plane models; it does not guarantee hard isolation for a GPU-sharing implementation." }), Object.freeze({ sourceId: "nist-zero-trust", supports: "Supports continuous least-privilege enforcement across identities, workloads, and resources." }), Object.freeze({ sourceId: "finops-unit-economics", supports: "Supports connecting cloud cost to business outcomes for allocation and accountability; it does not define cost allocation as security isolation." })]) }),
    Object.freeze({ id: "containerized-not-fully-portable", q: "Does containerizing an AI workload and deploying it on Kubernetes make it freely portable across clouds and accelerator platforms?", a: "No. Containers and orchestration APIs solve only part of packaging and control; real migration still depends on accelerators, drivers, kernels, data, identity, network, storage, telemetry, and managed services.", depth: "Separate portability into image format, orchestration objects, device-resource contract, and end-to-end workload. Inventory platform-specific dependencies, then replay build, data access, training or inference, recovery, SLO, cost, and rollback in the target environment. Claim portability only for the representative scope that passes, and rehearse actual exit instead of retaining a second YAML file.", ask: "Ask the customer: must the image, job definition, data, or complete service move, and which driver, kernel, IAM, network, and telemetry dependencies remain unverified in the target environment?", tag: "Portability", basis: "Standard artifact plus target-environment exit rehearsal", evidence: Object.freeze([Object.freeze({ sourceId: "oci-image-spec-v1-1-1", supports: "Supports interoperability for building, transferring, and preparing images to run; it does not cover GPU drivers, data, IAM, network, storage, or complete workload behavior." }), Object.freeze({ sourceId: "kubernetes-dra", supports: "Supports expressing and allocating device requirements through claims; the concrete driver, hardware, and target environment still require validation." })]), addedAt: "2026-08-01" }),
  ]),
  evidenceCards: Object.freeze([
    Object.freeze({ id: "dra-resource-claim", metric: "ResourceClaim", title: "Devices enter declarative resource management", finding: "Kubernetes DRA uses structured claims and drivers to bring device selection, allocation, and preparation into scheduling.", boundary: "DRA is not a queue, training orchestrator, or model service, and support depends on Kubernetes, driver, and feature version.", sourceId: "kubernetes-dra", accent: true }),
    Object.freeze({ id: "gpu-stack-lifecycle", metric: "Lifecycle", title: "A usable GPU requires a coordinated software stack", finding: "NVIDIA GPU Operator manages drivers, container toolkit, device plugins, discovery, and monitoring components in Kubernetes.", boundary: "It is a vendor-specific component, not a complete AI platform or universal hardware abstraction.", sourceId: "nvidia-gpu-operator" }),
    Object.freeze({ id: "platform-cross-layer-trace", metric: "Cross-layer trace", title: "Resource metrics must connect to model and business tasks", finding: "OpenTelemetry GenAI conventions provide evolving shared telemetry structures for model and agent calls that can correlate with infrastructure traces and metrics.", boundary: "GPU utilization and call attributes cannot establish business success without an application outcome.", sourceId: "opentelemetry-genai-semconv" }),
    Object.freeze({ id: "platform-product", metric: "Platform as product", title: "Self-service interfaces and supported paths matter more than component count", finding: "The CNCF platforms white paper describes a platform as a product for internal users, emphasizing consistent experience, self-service APIs, templates, documentation, and golden paths.", boundary: "The method does not prescribe which AI products to buy or prove that a platform improved business outcomes.", sourceId: "cncf-platforms-whitepaper" }),
    Object.freeze({ id: "four-dimensional-isolation", metric: "Multi-tenant primitives", title: "Quota is only one part of multi-tenancy", finding: "Kubernetes multi-tenancy guidance treats security, fairness, and noisy neighbors together and combines namespaces, RBAC, quota, network controls, and different control-plane boundaries.", boundary: "Those primitives do not automatically guarantee hard isolation for a particular CNI, GPU-sharing mechanism, cache, or storage implementation; cost ownership requires separate evidence.", sourceId: "kubernetes-multi-tenancy" }),
    Object.freeze({ id: "image-not-migration", metric: "Image ≠ migration", title: "The OCI specification covers only the image-interoperability boundary", finding: "The OCI Image Specification standardizes manifests, indexes, filesystem layers, and runtime configuration so tools can build, transfer, and prepare images to run.", boundary: "It does not standardize accelerator drivers, kernels, data, IAM, network, storage, telemetry, or complete workload behavior.", sourceId: "oci-image-spec-v1-1-1" }),
  ]),
  terms: Object.freeze({
    observability: Object.freeze({ name: "Observability", definition: "The use of metrics, logs, traces, events, and version context to explain system behavior and outcomes." }),
    "distributed-training": Object.freeze({ name: "Distributed Training", definition: "Training that partitions data, model state, or work across devices while managing communication, topology, and recovery." }),
    batching: Object.freeze({ name: "Continuous Batching", definition: "Admitting ready requests and retiring completed requests at generation-step boundaries to improve throughput while controlling queueing, fairness, and tail latency." }),
    "ai-infra-platform": Object.freeze({ name: "AI Infrastructure Platform", definition: "A self-service platform product that connects a governed control layer and workload execution layer into a supported, recoverable path across heterogeneous compute." }),
    "resource-scheduling": Object.freeze({ name: "Resource Scheduling", definition: "Allocating scarce devices according to request shape, attributes, topology, quota, priority, and policy." }),
    "gang-scheduling": Object.freeze({ name: "Gang Scheduling", definition: "Starting a distributed workload only when its complete required resource group can be allocated together." }),
    goodput: Object.freeze({ name: "Goodput", definition: "Completed work that satisfies its stated quality or service objective, rather than raw device activity or attempted throughput." }),
    "heterogeneous-compute": Object.freeze({ name: "Heterogeneous Compute", definition: "Operating workloads across different CPU, GPU, or accelerator types with explicit software, placement, observability, and portability boundaries." }),
    finops: Object.freeze({ name: "FinOps", definition: "The joint engineering, finance, and business practice of connecting technology use, unit cost, budget, and value." }),
  }),
  sources: Object.freeze({
    "kubernetes-dra": Object.freeze({ kind: "Official technical documentation", shortTitle: "Kubernetes DRA", note: "Documents device requests, selection, preparation, and allocation through structured claims and drivers. Feature state, API fields, driver maturity, and vendor support depend on Kubernetes and implementation versions." }),
    "nvidia-gpu-operator": Object.freeze({ kind: "Official product documentation", shortTitle: "NVIDIA GPU Operator", note: "Documents lifecycle management of NVIDIA drivers, container toolkit, device plugins, discovery, monitoring, and related Kubernetes components. Platform support, component versions, and upgrade compatibility remain dynamic." }),
    "opentelemetry-semconv": Object.freeze({ kind: "Open observability specification", shortTitle: "OpenTelemetry SemConv", note: "Defines common semantics for traces, metrics, logs, events, and resources. AI-specific progress, data lineage, business outcomes, approvals, and risk evidence still require project-defined fields." }),
    "vllm-2023": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "vLLM / PagedAttention", note: "Introduces PagedAttention and KV-cache memory management for LLM serving. Reported throughput remains specific to the tested models, hardware, engine version, and workloads." }),
    "kubernetes-dra-1-34-ga": Object.freeze({ kind: "Official release documentation", shortTitle: "Kubernetes DRA GA", note: "Records the DRA core API graduating to stable v1 and default enablement in Kubernetes 1.34. Optional features and individual drivers retain their own maturity and compatibility boundaries." }),
    "opentelemetry-genai-semconv": Object.freeze({ kind: "Open observability specification", shortTitle: "OpenTelemetry GenAI Conventions", note: "Defines evolving telemetry conventions for generative-AI clients, retrieval, agents, MCP, and tool calls. Pin the adopted version and add business outcomes, authorization, approvals, evidence coverage, and risk fields." }),
    "nist-zero-trust": Object.freeze({ kind: "Official architecture guidance", shortTitle: "NIST SP 800-207", note: "Defines resource access around explicit, current identity and policy evaluation rather than implicit trust from network location." }),
    "kueue-all-or-nothing": Object.freeze({ kind: "Official technical documentation", shortTitle: "Kueue All-or-Nothing", note: "Documents waitForPodsReady as a simple all-or-nothing mechanism for synchronized jobs, including timeout, eviction, and requeue. It is not a universal Kubernetes scheduler guarantee." }),
    "kueue-fair-sharing": Object.freeze({ kind: "Official technical documentation", shortTitle: "Kueue Fair Sharing", note: "Documents Kueue quota, priority, borrowing, preemption, and weighted fair sharing. The customer still owns the fairness objective, service tiers, and acceptable disruption." }),
    "slurm-overview": Object.freeze({ kind: "Official technical documentation", shortTitle: "Slurm Overview", note: "Defines Slurm resource allocation, parallel-job execution, and queue arbitration and lists optional topology, backfill, gang-scheduling, preemption, and priority plugins. A site's enabled configuration must be verified." }),
    "slurm-multifactor-priority": Object.freeze({ kind: "Official technical documentation", shortTitle: "Slurm Multifactor Priority", note: "Documents FIFO and multifactor prioritization using age, fair-share, job size, partition, QOS, and site factors. It does not define organizational fairness or guarantee deadlines." }),
    "torchsnapshot-checkpoint": Object.freeze({ kind: "Official framework documentation", shortTitle: "TorchSnapshot Checkpoint", note: "Documents capture and restore of model, optimizer, learning-rate scheduler, custom application state, and RNG, plus consistent asynchronous snapshots. Data-cursor and topology coverage remain application-specific." }),
    "checkfreq-2021": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "CheckFreq", note: "Studies adaptive checkpoint frequency, pipelined writes, and resumable data-loader state. Its measured trade-offs do not determine the best interval for another training stack." }),
    "cncf-platforms-whitepaper": Object.freeze({ kind: "Industry foundation guidance", shortTitle: "CNCF Platforms White Paper", note: "Frames a platform as a product for internal users and emphasizes consistent experience, self-service APIs, templates, documentation, and golden paths. It does not prescribe an AI-platform component list or prove business impact." }),
    "kubernetes-multi-tenancy": Object.freeze({ kind: "Official technical documentation", shortTitle: "Kubernetes Multi-tenancy", note: "Treats shared-cluster security, fairness, and noisy neighbors together and discusses namespaces, RBAC, ResourceQuota, NetworkPolicy, and virtual control planes. These primitives do not automatically guarantee hard isolation for a particular CNI, runtime, storage layer, or GPU-sharing mode." }),
    "kubernetes-dra-1-36": Object.freeze({ kind: "Official technical article", shortTitle: "Kubernetes 1.36 DRA", note: "Records the different Stable, Beta, and Alpha maturity states of DRA capabilities in Kubernetes 1.36. It does not prove that a specific driver, vendor integration, or customer workload is production-ready." }),
    "oci-image-spec-v1-1-1": Object.freeze({ kind: "Open standard", shortTitle: "OCI Image Spec 1.1.1", note: "Defines image manifests, indexes, filesystem layers, and runtime configuration for tool interoperability in image build, transfer, and runtime preparation. It does not standardize accelerator drivers, kernels, data, IAM, network, storage, telemetry, or complete workload behavior." }),
    "finops-unit-economics": Object.freeze({ kind: "Industry foundation framework", shortTitle: "FinOps Unit Economics", note: "Relates technology spend to product, service, or activity value and distinguishes technical-efficiency units from business-outcome units. It does not prove ROI, causality, or a customer's value baseline." }),
  }),
});
