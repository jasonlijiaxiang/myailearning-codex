import {
  promptDeepDives as canonicalDeepDives,
  promptEvidenceCards as canonicalEvidenceCards,
  promptQa as canonicalQa,
} from "../../../prompt-content.mjs";

const freeze = Object.freeze;
const cards = (title, intro, items) => freeze({ type: "cards", title, ...(intro ? { intro } : {}), items: freeze(items.map(freeze)) });
const steps = (title, intro, items) => freeze({ type: "steps", title, ...(intro ? { intro } : {}), items: freeze(items.map(freeze)) });
const table = (title, intro, columns, items) => freeze({ type: "table", title, ...(intro ? { intro } : {}), columns: freeze(columns), items: freeze(items.map((item) => freeze({ ...item, cells: freeze(item.cells) }))) });
const boundary = (title, items) => freeze({ type: "boundary", title, items: freeze(items.map(freeze)) });

const sections = [
  {
    id: "prompt-context-boundary",
    eyebrow: "FOUNDATION AND SCOPE",
    title: "Engineer the complete model input, not a magic sentence",
    lead: "Prompt Engineering expresses a defined task, its constraints, examples, and output contract. Context Engineering decides what instructions, identity, evidence, history, tools, and runtime state the model should see for this invocation. Neither discipline replaces application authorization or deterministic business controls.",
    blocks: [
      cards("Three distinct responsibilities", "Separating them makes failures diagnosable and ownership explicit.", [
        { id: "responsibility-prompt", title: "Prompt Engineering", subtitle: "Task expression", body: "Design and version the task, instructions, examples, refusal conditions, and expected response so a selected model can perform a defined job consistently.", decision: "Use it when the model has the needed facts and capability but the task or output expectation is unclear." },
        { id: "responsibility-context", title: "Context Engineering", subtitle: "Runtime assembly", body: "Select, order, budget, and govern the identity, session state, retrieved evidence, tool definitions, tool results, and other data supplied to one invocation.", decision: "Use it when the model sees the wrong, stale, unauthorized, noisy, or poorly positioned information." },
        { id: "responsibility-application", title: "Application control", subtitle: "Authority and truth", body: "Authenticate the subject, authorize resources and actions, validate business invariants, execute tools, confirm external state, and preserve audit evidence outside the model.", decision: "Use deterministic controls for rules that must always hold." },
      ]),
      boundary("Critical boundary", [
        { id: "boundary-instructions-authorization", title: "Instruction priority is not access control", body: "System, developer, and user roles are model-interface mechanisms whose exact semantics vary by provider. A higher-priority instruction can guide model behavior, but it cannot grant IAM rights, validate a transaction, protect a secret that the model can see, or prove that an external action succeeded.", boundary: "Keep secrets and mandatory authorization decisions outside the model; reauthorize every proposed action at execution time.", sourceIds: freeze(["openai-model-spec-command", "google-system-instructions", "nist-zero-trust"]) },
      ]),
    ],
  },
  {
    id: "controlled-context-assembly",
    eyebrow: "CONTEXT ASSEMBLY",
    title: "Treat context as an auditable assembly plan",
    lead: "A production request is a collection of blocks with different owners, trust levels, access rules, freshness, and token costs. Preserve that metadata instead of flattening everything into an untraceable string.",
    blocks: [
      steps("The context manifest", "Record why each block was present and how the final input was produced.", [
        { id: "assembly-register", title: "Register blocks", body: "Give each block a stable ID, source, owner, access scope, validity period, trust class, priority, and token estimate." },
        { id: "assembly-budget", title: "Allocate a budget", body: "Protect the task, hard constraints, and output schema first; allocate separate budgets to evidence, history, examples, and tool descriptions." },
        { id: "assembly-order", title: "Order by policy", body: "Keep reusable instructions in a stable order and place decisive evidence where the chosen model can use it reliably. Do not optimize cacheability by preserving stale or unauthorized material." },
        { id: "assembly-evict", title: "Evict or compress", body: "Deduplicate and remove low-value blocks under explicit rules. If content is summarized, preserve scope, negation, version, and source coordinates." },
        { id: "assembly-persist", title: "Persist the manifest", body: "Record block order, omissions, token use, model snapshot, prompt version, tool set, and schema so failures can be reproduced without logging unnecessary sensitive content." },
      ]),
      table("Context budget zones", "A large context window is capacity, not proof that every token will be used equally well.", ["Contains", "Primary control", "Failure if neglected"], [
        { id: "budget-stable-instructions", title: "Stable instructions", cells: ["Task, role, policy intent, refusal rules, and output contract", "Version control and regression testing", "Behavior changes without an attributable release"] },
        { id: "budget-dynamic-state", title: "Dynamic state", cells: ["Identity, request, current business state, and session facts", "Type, access, freshness, and length checks", "Stale or unauthorized state enters the model"] },
        { id: "budget-evidence", title: "Evidence", cells: ["Retrieved documents, records, and citations", "Authority, ACL, version, and claim coverage", "Fluent output outruns its support"] },
        { id: "budget-tools", title: "Tools and results", cells: ["Capability schemas and returned observations", "Least visibility, result validation, and postcondition checks", "A proposed or partial action is mistaken for success"] },
        { id: "budget-examples-history", title: "Examples and history", cells: ["Demonstrations and relevant prior turns", "Representativeness, privacy, expiry, and compression", "Bias, leakage, or old rules are reproduced"] },
      ]),
      boundary("Long-context boundary", [
        { id: "boundary-context-capacity", title: "Capacity is not reliable utilization", body: "Long-context research reports position-sensitive performance in specific tasks and models. Use the result to justify position and ablation tests, not to claim that every model fails in the middle or that retrieval always outperforms long context.", sourceIds: freeze(["lost-middle", "nist-genai-profile"]) },
      ]),
    ],
  },
  {
    id: "prompt-pattern-diagnostics",
    eyebrow: "TECHNIQUE SELECTION",
    title: "Select techniques from the failure mode",
    lead: "Begin with the smallest prompt that establishes a measurable baseline. Add examples, decomposition, retrieval, tools, or model reasoning only when evidence identifies the corresponding failure.",
    blocks: [
      table("Technique diagnostic", "A technique is an intervention with cost and failure modes, not a ritual.", ["Use when", "What changes", "Boundary"], [
        { id: "technique-zero-shot", title: "Zero-shot instruction", cells: ["The task and success condition are unambiguous", "States the objective, constraints, and response contract", "It does not supply missing knowledge or application authority"] },
        { id: "technique-few-shot", title: "Few-shot examples", cells: ["Labels, style, or edge cases are easily misread", "Demonstrates representative inputs, outputs, refusals, and boundaries", "More examples consume context and can encode bias or obsolete policy"] },
        { id: "technique-structured-output", title: "Structured output", cells: ["Downstream software requires a typed payload", "Constrains fields, types, required properties, and supported schema behavior", "Schema conformance does not establish factual or business validity"] },
        { id: "technique-chaining", title: "Prompt chaining", cells: ["Steps and checkpoints are known in advance", "Makes intermediate artifacts testable and routes them through code", "Dynamic path selection and stateful recovery belong to workflow or agent orchestration"] },
        { id: "technique-reasoning", title: "Reasoning model", cells: ["The task requires global trade-offs or difficult inference", "Allocates more model-side reasoning to a bounded decision", "Hidden chain of thought is not an audit artifact"] },
      ]),
      cards("Choose the right layer", "A prompt should not be used to disguise a data, model, or application defect.", [
        { id: "layer-use-prompt", title: "Use a prompt", body: "The facts are present, but the task, constraints, style, refusal behavior, or response format are unclear." },
        { id: "layer-use-rag", title: "Use retrieval", body: "The answer depends on current, attributable, withdrawable, or access-controlled external knowledge." },
        { id: "layer-use-finetuning", title: "Evaluate fine-tuning", body: "A stable behavior or specialized pattern must be reproduced at scale after prompting and examples have been tested." },
        { id: "layer-use-code", title: "Use code or policy", body: "A calculation, authorization, state transition, threshold, or transaction invariant must be deterministic." },
      ]),
    ],
  },
  {
    id: "output-tool-contracts",
    eyebrow: "OUTPUT AND ACTION",
    title: "Validate structure, meaning, authority, and outcome separately",
    lead: "Structured output and tool calling improve the interface between a model and an application. They do not transfer execution authority or establish that a business action is valid or complete.",
    blocks: [
      steps("The action path", "Keep the model's proposal distinct from the application's decision and the external system's result.", [
        { id: "action-offer-tools", title: "Offer a minimal tool set", body: "Expose only tools relevant to the authenticated subject, task, tenant, and current risk policy." },
        { id: "action-model-proposes", title: "Model proposes", body: "The model returns a tool name and arguments that conform to the supported interface; this is intent, not execution." },
        { id: "action-validate-authorize", title: "Validate and authorize", body: "The application validates types and business constraints, reauthorizes the subject and resource, binds allowed parameters, and obtains any required confirmation." },
        { id: "action-execute-observe", title: "Execute and observe", body: "The application invokes the external system with idempotency and retry controls, then records status, errors, and an operation identifier." },
        { id: "action-confirm-postcondition", title: "Confirm the postcondition", body: "Read authoritative state to determine whether the intended business outcome occurred, is pending, failed, or requires recovery." },
      ]),
      table("Four validation layers", "Passing one layer does not imply that the next layer passes.", ["Question", "Owner", "Typical failure"], [
        { id: "validation-syntax", title: "Syntactic validity", cells: ["Can the payload be parsed?", "Parser or API client", "Malformed or truncated representation"] },
        { id: "validation-schema", title: "Schema validity", cells: ["Do fields, types, required values, and supported constraints match?", "Schema validator and model interface", "Missing field, invalid enum, or unsupported schema feature"] },
        { id: "validation-business", title: "Business validity", cells: ["Are values true, authorized, current, and consistent with business rules?", "Application, policy engine, and authoritative systems", "Invalid amount, stale inventory, or forbidden state transition"] },
        { id: "validation-transaction", title: "Transaction acceptance", cells: ["Did the external system reach a confirmed acceptable state?", "System of record and workflow", "Timeout, partial success, unknown result, or uncompensated side effect"] },
      ]),
      boundary("Contract boundary", [
        { id: "boundary-schema-authority", title: "A valid schema is not a valid decision", body: "JSON Schema and API descriptions constrain data shape. They do not establish truth, consent, entitlement, idempotency, compensation, or a successful postcondition.", sourceIds: freeze(["openai-structured-outputs", "openapi-3-1-1", "openai-function-calling"]) },
      ]),
    ],
  },
  {
    id: "prompt-injection-controls",
    eyebrow: "SECURITY ENGINEERING",
    title: "Control the path from untrusted sources to dangerous sinks",
    lead: "Direct injection arrives in the user's request; indirect injection arrives through content the system reads, such as a webpage, email, PDF, retrieved passage, image, or tool result. Detection helps, but the system must remain bounded when detection fails.",
    blocks: [
      cards("Layered controls", "The purpose is to reduce both attack probability and maximum impact.", [
        { id: "security-label-sources", title: "Label untrusted sources", body: "Preserve provenance, trust, ACL, and intended use. Treat external natural language as data rather than newly authorized instructions." },
        { id: "security-limit-sinks", title: "Limit dangerous sinks", body: "Constrain messaging, payments, data writes, deletion, code execution, link access, and sensitive-data transfer by identity, task, target, and value." },
        { id: "security-confirm-actions", title: "Confirm high-impact actions", body: "Require an independent approval or explicit user confirmation with the exact target and consequence before irreversible operations." },
        { id: "security-validate-output", title: "Validate outputs and parameters", body: "Apply allowlists, typed parameters, DLP, business rules, and postcondition checks outside the model." },
        { id: "security-test-chains", title: "Test attack chains", body: "Evaluate multilingual, encoded, hidden, cross-document, and tool-result attacks and measure whether a compromised model can reach a dangerous capability." },
      ]),
      boundary("Security claim boundary", [
        { id: "boundary-prompt-hardening", title: "Prompt hardening is defense in depth, not a security boundary", body: "A system prompt, retrieval pipeline, fine-tuned model, detector, or keyword rule cannot by itself eliminate prompt injection. Authorization, least privilege, isolation, validation, confirmation, and audit must remain effective if model behavior is manipulated.", sourceIds: freeze(["owasp-prompt-injection", "openai-source-sink-injection", "nist-zero-trust"]) },
      ]),
    ],
  },
  {
    id: "evaluation-release-governance",
    eyebrow: "EVALUATION AND RELEASE",
    title: "Release a configuration bundle, not an isolated prompt",
    lead: "Observed behavior depends on the model snapshot, prompt, context assembler, retriever, tools, schemas, safety policy, and task distribution. Version and evaluate them as one release unit while retaining component-level attribution.",
    blocks: [
      steps("The release loop", "Make every change comparable, attributable, and reversible.", [
        { id: "release-define-success", title: "Define success and unacceptable error", body: "Freeze task-specific criteria, production-distribution samples, high-risk slices, latency, token use, and cost per successful task before optimization." },
        { id: "release-change-one-factor", title: "Make a controlled change", body: "Change one main factor when diagnosing causality, or explicitly version the complete bundle when coordinated changes are necessary." },
        { id: "release-offline-evaluate", title: "Run offline regression", body: "Use development, regression, adversarial, and hidden holdout sets. Calibrate automated judges against blind human review and deterministic truth where available." },
        { id: "release-canary", title: "Canary under stop conditions", body: "Compare the candidate with a frozen baseline using segmented quality, safety, schema, tool, P95, and cost signals." },
        { id: "release-rollback-learn", title: "Roll back and learn", body: "Restore the last validated bundle, inspect traces and external transactions, and admit confirmed production failures into governed evaluation data." },
      ]),
      table("Minimum release manifest", "A prompt ID alone cannot reproduce an invocation.", ["Record", "Why", "Rollback implication"], [
        { id: "manifest-prompt-model", title: "Prompt and model", cells: ["prompt_version, provider, model_id, snapshot, and reasoning settings", "Model behavior can change without prompt text changing", "Restore both prompt and model route"] },
        { id: "manifest-context-retrieval", title: "Context and retrieval", cells: ["assembler, block policy, retriever, ordering, and truncation versions", "Evidence coverage and position affect the answer", "Restore the validated assembly policy"] },
        { id: "manifest-tools-schema", title: "Tools and schemas", cells: ["visible tool set, tool descriptions, input schema, output schema, and policy versions", "Selection, compatibility, and action risk can change", "Restore compatible contracts together"] },
        { id: "manifest-evaluation", title: "Evaluation evidence", cells: ["eval-set version, rubric, judge, human calibration, results, and approval", "A score has meaning only within a stated method and population", "Preserve the baseline and release decision"] },
      ]),
      boundary("Evaluation boundary", [
        { id: "boundary-judge-truth", title: "An LLM judge is a calibrated instrument, not ground truth", body: "Prefer specific classifications or pairwise comparisons, randomize candidate order, monitor position and verbosity effects, and revalidate agreement with human reviewers. Use authoritative records or deterministic checks for factual fields and business postconditions whenever possible.", sourceIds: freeze(["openai-eval-best-practices", "nist-genai-profile"]) },
      ]),
    ],
  },
  {
    id: "cloud-poc-operating-model",
    eyebrow: "PRESALES AND POC",
    title: "Turn prompting into an operable cloud capability",
    lead: "The saleable unit is not prompt wording. It is a controlled path for model access, configuration, context, tools, evaluation, security, observability, release, rollback, and cost accountability.",
    blocks: [
      cards("Minimum platform capabilities", "Verify current regional availability, quotas, networking, retention, keys, caching, telemetry, and pricing against the target provider before making a commitment.", [
        { id: "platform-model-gateway", title: "Model access and gateway", body: "Pin model versions, route by task and policy, protect credentials, enforce quotas, and preserve provider-specific boundaries." },
        { id: "platform-configuration", title: "Configuration control", body: "Version prompts, context assembly, tools, schemas, safety policies, and their compatible release bundle." },
        { id: "platform-evaluation", title: "Evaluation and deployment", body: "Run representative regression, adversarial tests, approval, canary, monitoring, and rollback." },
        { id: "platform-security", title: "Identity and action security", body: "Propagate subject identity, authorize tools and data, protect sensitive information, and record approvals and transactions." },
        { id: "platform-observability", title: "Trace and economics", body: "Correlate inputs, versions, retrieval, tools, outcomes, P95, token use, cache behavior, and cost per successful task without indiscriminate sensitive logging." },
      ]),
      table("A decision-ready PoC", "A stable demo is evidence of possibility, not production readiness.", ["Acceptance evidence", "Required slices", "Decision use"], [
        { id: "poc-task-quality", title: "Task quality", cells: ["Correctness, completeness, grounding, refusal, schema, and tool parameters", "Main paths, long tail, boundary cases, languages, and input lengths", "Compare with the current human or system baseline"] },
        { id: "poc-risk", title: "Risk and control", cells: ["Injection, unauthorized data, unsafe actions, and recovery", "Direct and indirect attacks plus unacceptable business errors", "Establish absolute stop conditions by risk class"] },
        { id: "poc-operations", title: "Operations", cells: ["P95, quotas, failure handling, traceability, retention, and rollback", "Expected and peak traffic, dependency failures, and version change", "Prove the service can be operated under the required environment"] },
        { id: "poc-economics", title: "Economics", cells: ["Input/output tokens, retrieval, tools, retries, human review, and cost per success", "Simple, complex, successful, failed, and escalated tasks", "Prevent a lower unit price from hiding a lower success rate"] },
      ]),
      boundary("Go / No-Go boundary", [
        { id: "boundary-universal-threshold", title: "There is no universal 90% or 95% release threshold", body: "Set thresholds from the existing baseline, task risk, error cost, reversibility, and available human fallback. Low-risk writing may tolerate correction; unauthorized access, sensitive-data exposure, and irreversible actions can require deterministic prevention and zero-tolerance stop conditions.", sourceIds: freeze(["openai-eval-best-practices", "nist-genai-profile"]) },
      ]),
    ],
  },
];

const qaCopy = [
  { id: "prompt-alone-and-accuracy", q: "Can a sufficiently good prompt solve an accuracy problem?", a: "No. Prompting can improve task interpretation, context use, and response organization, but it cannot by itself repair missing knowledge, bad source data, inadequate model capability, incorrect tool results, or absent business rules.", depth: "Diagnose the failure layer first. Add grounding or retrieval for current external facts, use structured output plus application validation for interface drift, choose or compose a more capable model for tasks beyond the current model, and enforce deterministic rules in code or a policy engine. Prompt work should begin only after success criteria and a measurable baseline exist.", ask: "Ask the customer: is the dominant error missing knowledge, misunderstood instructions, unstable format, or an incorrectly executed business action?", tag: "Capability boundary", basis: "Provider workflow plus system decomposition", supports: ["Supports defining measurable success and deciding whether the target metric is suitable for prompt engineering.", "Supports separating goals, instructions, context, examples, and response format during iterative testing."] },
  { id: "system-prompt-is-not-security", q: "Does the higher priority of a system prompt make it a security boundary?", a: "No. System instructions can guide stable behavior, but the model still processes them as input. They must not contain secrets or carry real authorization responsibility.", depth: "Message roles and priority semantics differ across APIs. Even a model that usually follows higher-level instructions can be affected by direct or indirect prompt injection. Keep sensitive material outside model-visible context, and enforce data filtering, access control, tool allowlists, and high-risk approvals in the application.", ask: "Ask the customer: which rules must hold without exception, and which of them are already enforced outside the model by IAM, policy, or code?", tag: "Security boundary", basis: "Official provider guidance plus OWASP", supports: ["Supports the intended use of system instructions and states that they do not completely prevent jailbreaks or leakage.", "Supports layered prompt-injection mitigations such as least privilege and human approval rather than reliance on a system prompt."] },
  { id: "zero-shot-or-few-shot", q: "How should we choose between zero-shot and few-shot prompting, and are more examples always better?", a: "Start with a zero-shot baseline. Add a small, representative set of examples when labels, style, boundaries, or refusal behavior are consistently misunderstood; more examples are not automatically better.", depth: "Each example consumes context and can reproduce bias, obsolete rules, or incidental formatting. Cover common cases, easily confused boundaries, counterexamples, and refusals with a consistent input-output structure. Accept another example only if a fixed evaluation set shows material improvement in the relevant slice.", ask: "Ask the customer: which inputs are most often misclassified, and do we have representative counterexamples, edge cases, and refusal cases?", tag: "Core pattern", basis: "Official provider prompting guidance", supports: ["Identifies few-shot examples as an optional prompt component that demonstrates expected output.", "Supports organizing concise examples and running tests for every production release."] },
  { id: "json-request-vs-structured-output", q: "Is asking the model to return JSON reliable enough for production?", a: "No. A natural-language request for JSON states an intention. Production integration should use a supported structured-output mechanism or strict schema and still handle refusals, truncation, and semantic errors.", depth: "Separate parseable JSON, schema-valid data, and business-correct values. Structured output strengthens representation and supported schema conformance, but it does not prove that an amount, date, product code, citation, or decision is true. Validate types, ranges, enums, cross-field rules, permissions, and high-risk values before downstream use.", ask: "Ask the customer: which fields, enums, and cross-field invariants does the downstream system require, and what happens if a value is wrong?", tag: "Structured output", basis: "Official API documentation plus application boundary", supports: ["Supports JSON Schema conformance while documenting refusal, truncation, and feature-support boundaries."] },
  { id: "tool-definition-and-safe-execution", q: "Does putting an API in a tool definition make it safe for the model to call?", a: "No. A tool definition describes available capability and argument shape. The model proposes a call; the application decides whether to authorize and execute it.", depth: "Revalidate the tool name, typed parameters, user identity, resource scope, and idempotency key for every execution. Separate read and write privileges, require explicit confirmation for high-impact actions, and record approvals and results. A strict schema reduces shape errors but neither grants permission nor decides whether the action should occur.", ask: "Ask the customer: which tools only read, which change state, and who can invoke, approve, or revoke each one?", tag: "Tool security", basis: "Official tool loop plus security guidance", supports: ["Supports the application-model execution loop and the application's responsibility to run the tool.", "Supports least privilege, human confirmation for high-risk actions, and isolation of untrusted external content."] },
  { id: "prompt-rag-context-engineering", q: "How do prompts, RAG, and Context Engineering relate?", a: "A prompt expresses the task and constraints; RAG selects external evidence; Context Engineering selects and assembles everything the model sees for the current invocation.", depth: "Runtime context can include identity, session state, retrieved evidence, examples, tool definitions, tool results, time, and token budgets. Prompt Engineering is one part of that larger assembly discipline. Version prompt templates, retrieval policy, tool schemas, and context assembly independently so a failure can be attributed to the correct layer.", ask: "Ask the customer: where does each context block come from, who owns it, when does it expire, and was it filtered for the current user's access?", tag: "Architecture boundary", basis: "Official prompt composition plus production engineering", supports: ["Supports managing context separately from instructions, examples, and response format.", "Supports separating stable guidance, task details, and examples and managing them as production artifacts."] },
  { id: "model-upgrade-regression", q: "Can the same prompt go live unchanged after a model migration or upgrade?", a: "Do not assume equivalence. Model family, snapshot, reasoning behavior, message roles, tool protocol, and structured-output support can all change results.", depth: "Record prompt version, provider, model ID or snapshot, reasoning settings, tool and schema versions, and evaluation-set version. Compare critical tasks, refusals, safety, schema conformance, P95, and cost per successful task through offline replay or shadow traffic, then expand only after passing customer-defined gates.", ask: "Ask the customer: are calls pinned to a snapshot or a floating alias, who approves an upgrade, and can the complete configuration be rolled back quickly?", tag: "Version governance", basis: "Official prompt lifecycle guidance", supports: ["Supports versioning production prompts and running representative evaluations at release.", "Supports defining success and a measurable method before optimization."] },
  { id: "typed-template-variables", q: "Should variables in a prompt template be inserted through direct string concatenation?", a: "Avoid it. Keep stable instructions separate from dynamic values; validate each value's type, length, source, authorization, and sensitivity before insertion, and mark untrusted data with clear structural boundaries.", depth: "Direct concatenation makes instruction-data confusion, delimiter breakage, log leakage, and isolated testing more likely. Put user input, retrieved evidence, configuration, and tool results into distinct fields, with source and trust metadata. Version and review the template itself, and make traces distinguish a template change from a variable change.", ask: "Ask the customer: do the variables come from users, databases, or external content, and which are untrusted or sensitive?", tag: "Template engineering", basis: "Official engineering guidance plus threat boundary", supports: ["Supports typed parameters or validated input objects for dynamic prompt values and code-based production prompt management.", "Supports identifying and isolating untrusted external content."] },
  { id: "evaluate-prompt-systematically", q: "How do we evaluate a prompt without relying on subjective impressions?", a: "Freeze representative tasks and success criteria, then compare versions as complete configurations: model, prompt, context, tools, and schemas.", depth: "Cover common paths, edge cases, refusals, sensitive information, injection, and high-cost failures. Measure task correctness, completeness, grounding, schema validity, tool selection and arguments, refusal quality, P95, token use, and cost per success. Calibrate automated evaluation against sampled human review and retain confirmed failures for governed regression data.", ask: "Ask the customer: what is success, which errors are unacceptable, and who supplies authoritative answers and signs the release gate?", tag: "PoC evaluation", basis: "Provider evaluation prerequisites plus continuous risk management", supports: ["Supports defining success criteria and a testable method before prompt optimization.", "Supports continuous Govern, Map, Measure, and Manage activities in context."] },
  { id: "cloud-opportunity-from-prompting", q: "Which cloud-service opportunities can Prompt Engineering create?", a: "The opportunity is not a standalone block of text. It connects model access, configuration management, an AI gateway, evaluation, security, tracing, secrets, and elastic runtime capabilities.", depth: "Translate the need into purchasable capabilities: model catalog and version pinning, isolated templates and credentials, routing and quotas, structured output and tools, offline evaluation and online monitoring, sensitive-data protection, audit, and cost allocation. Verify product names, regions, retention, limits, and pricing from current provider documentation before committing.", ask: "Ask the customer: who owns templates, is multi-model routing required, and are private networking, audit, residency, or departmental chargeback mandatory?", tag: "Cloud services", basis: "Capability mapping with current-product verification", supports: ["Supports treating production prompts as versioned, tested deployment artifacts.", "Supports ongoing governance, measurement, and risk management around generative-AI systems."] },
  { id: "reasoning-model-and-chain-of-thought", q: "Do reasoning models still need long chain-of-thought prompts?", a: "Usually, do not prescribe a complete reasoning transcript, and never treat hidden chain of thought as audit evidence. Provide a clear task, necessary evidence, constraints, an output contract, and an appropriate reasoning budget.", depth: "General models can benefit from decomposition or observable checkpoints, while reasoning models may perform extended internal reasoning. Evaluate the final answer, citations, tool arguments, decision summary, and authoritative business outcome. Demanding a verbatim internal rationale is provider-specific and can produce a plausible but unfaithful explanation.", ask: "Ask the customer: is the need higher task success, adjustable reasoning effort, or a specific auditable record?", tag: "Reasoning models", basis: "Official behavior boundary plus task evaluation", supports: ["Supports the provider-specific interface boundary that hidden chain of thought is not exposed as an application artifact.", "Supports adapting instructions to the model and validating them with representative evaluations rather than a universal trick."] },
  { id: "prompt-chaining-or-agent", q: "Where is the boundary between prompt chaining and an agent?", a: "Use prompt chaining or a workflow when steps, order, and exception paths are known. Use an agent only when the next step must be selected dynamically from environmental feedback.", depth: "A chain creates testable intermediate artifacts while code controls routing. An agent delegates some path selection to a model and therefore needs tool contracts, state, stop conditions, budgets, identity, and trajectory evaluation. A workflow can contain a bounded agent decision point; the labels are not mutually exclusive.", ask: "Ask the customer: which steps can be drawn in advance, and which exceptions truly require runtime selection from tool results?", tag: "Orchestration boundary", basis: "Provider engineering guidance plus research mechanism", supports: ["Supports distinguishing predefined workflows from model-directed agent processes.", "Supports an interleaved reasoning, action, and observation pattern for dynamic interaction."] },
  { id: "prompt-caching-economics", q: "Does prompt caching always reduce cost substantially?", a: "No. Value appears only when requests repeatedly share a sufficiently long stable prefix and the selected product's cache behavior matches the workload.", depth: "Place stable instructions and reusable examples before dynamic user or evidence content, but verify cache keys, retention, model version, tenant isolation, billing, and hit conditions for the provider. Measure hit rate, input cost, P95, time to first token, and isolation behavior; do not convert a provider's cached-input discount directly into an end-to-end savings claim.", ask: "Ask the customer: how much of each request is truly identical, what is the tenant boundary, and what are the current cache and billing rules for the selected model?", tag: "Cost optimization", basis: "Official product documentation plus workload measurement", supports: ["Supports the current OpenAI prefix-cache mechanism and observable usage fields within that product boundary.", "Supports separating stable and dynamic content and managing the resulting prompt as a production artifact."] },
  { id: "indirect-injection-difficulty", q: "Why is indirect prompt injection harder to defend against than an openly malicious user request?", a: "Because the attack can be hidden inside content the application itself retrieves or reads, such as a webpage, email, PDF, RAG document, or tool result, rather than being typed directly by the user.", depth: "The model receives both the legitimate task and external natural language and may not reliably preserve an instruction-versus-data distinction. Label trust and provenance, restrict tool permissions, validate outputs and arguments, add independent approval for write actions, and include complete attack chains in regression and red-team testing.", ask: "Ask the customer: which external sources can the model read, which tools become reachable afterward, and which actions could cause irreversible impact?", tag: "Security", basis: "Community threat guidance plus zero trust", supports: ["Supports indirect injection through external content and layered mitigations including least privilege and human approval.", "Supports explicit verification of identity and resource access instead of implicit trust."] },
  { id: "conflicting-instruction-sources", q: "When system instructions, user requests, retrieved content, and tool results conflict, which should the model follow?", a: "Apply the selected model API's instruction hierarchy to platform, application, and user instructions. Treat retrieved documents, webpages, and tool results as data by default, not as newly authorized instructions.", depth: "Give each context block provenance, trust, ACL, freshness, and purpose metadata. Prevent lower-trust content from redefining higher-level task boundaries, but do not treat one provider's role names as a universal protocol. Mandatory authorization and business invariants remain outside the model.", ask: "Ask the customer: which rules guide model behavior, and which are enforced by the gateway, IAM, code, or policy engine?", tag: "Instruction governance", basis: "Official model behavior specifications", supports: ["Supports source-based instruction authority and the treatment of untrusted content as data rather than privileged command.", "Supports system-level behavioral configuration while stating that it cannot fully prevent jailbreaks or leakage."] },
  { id: "structured-output-needs-validation", q: "Why does Structured Outputs still require application validation when it enforces a JSON Schema?", a: "A supported schema constrains field shape; it does not prove that values are true, that the subject is authorized, that current business state permits the action, or that an external transaction succeeded.", depth: "Validate parseability, schema conformance, business rules, and transaction postconditions as distinct layers. The application checks amounts, inventory, dates, cross-field constraints, permissions, and state transitions. Refusal, truncation, unsupported schema features, and unknown outcomes need explicit failure paths.", ask: "Ask the customer: which downstream invariants are mandatory, and how should the system stop or recover when a value is wrong or a transaction partly succeeds?", tag: "Output contract", basis: "Structured-output documentation plus API contract boundary", supports: ["Supports schema-constrained output and documents refusal, truncation, and supported-feature boundaries.", "Supports machine-readable API structure but not authorization or business postconditions."] },
  { id: "large-tool-catalog", q: "How can a model reliably select the correct tool from a large catalog?", a: "Do not show every tool at once. Reduce visibility by task, identity, tenant, and risk, and use controlled discovery only when the remaining catalog is still too large.", depth: "Give tools distinct responsibilities and describe when to use and not use them, plus risk, idempotency, error categories, and postconditions. A scalable flow is discover, select, authorize, then execute. Evaluate discovery recall@k, correct selection, unauthorized exposure, added tokens, P95, and version mismatch.", ask: "Ask the customer: which tools overlap, how many definitions are visible per request, and is dynamic loading controlled by task, identity, or tenant policy?", tag: "Tool context", basis: "On-demand discovery plus tool-contract guidance", supports: ["Supports on-demand tool search and deferred loading while leaving authorization and execution to the application.", "Supports the effect of tool names, descriptions, and input schemas on model selection.", "Supports the boundary between model-proposed calls and application execution."] },
  { id: "attribute-prompt-improvement", q: "How can we prove that a prompt change caused improvement rather than a coincidental model or context change?", a: "Freeze the baseline configuration and task population, change one main variable where possible, and compare candidates on the same inputs with attributable version records.", depth: "Record the model snapshot, prompt, context assembler, retrieval, tools, schemas, and evaluation method for both versions. Use paired or blind comparisons and segment by scenario, risk, language, and input length. If several components changed together, call it a bundle improvement rather than attributing the result solely to the prompt.", ask: "Ask the customer: can every evaluated output be tied to an identical input set and a complete configuration version?", tag: "Release governance", basis: "Continuous evaluation plus versioned prompt practice", supports: ["Supports task-specific continuous evaluation, production-distribution samples, and explicit criteria.", "Supports versioning production prompts and running release-time tests."] },
  { id: "system-developer-user-roles", q: "How should system, developer, and user instructions be divided, and can all business rules go into the system prompt?", a: "Use higher-level instructions for stable application intent and behavior, and user instructions for the current request. Do not place every business rule there: mandatory invariants require deterministic enforcement.", depth: "Provider role names and priority rules differ, so define a provider-neutral policy intent and map it through a thin adapter. System or developer instructions may describe allowed behavior, but IAM, resource scoping, monetary limits, state transitions, and secrets must remain in code, policy, or protected systems.", ask: "Ask the customer: which rules are guidance for model behavior and which must be provably enforced for every request?", tag: "Instruction hierarchy", basis: "Provider-specific command hierarchy plus system-instruction limits", supports: ["Supports a source-based chain of command within the relevant model interface.", "Supports system instructions for role and behavior while documenting their jailbreak and leakage limits."] },
  { id: "large-context-load-everything", q: "If the model has a large context window, can we load every document, rule, and conversation turn at once?", a: "No. Window size is a capacity ceiling, not evidence that every item is relevant, authorized, fresh, affordable, or used uniformly.", depth: "Create explicit budgets for task rules, evidence, history, examples, and tools. Apply access and freshness filters before ranking, remove duplicates, protect decisive constraints, and test position sensitivity and ablations on the chosen model. Preserve a context manifest so omissions and ordering can be reproduced.", ask: "Ask the customer: which content must never be truncated, which can be summarized, and can the system explain why every block entered the request?", tag: "Context assembly", basis: "Long-context research plus risk management", supports: ["Supports testing position-sensitive behavior within the paper's specific long-context experiments.", "Supports context-specific mapping and measurement of generative-AI risks."] },
  { id: "conversation-history-lifecycle", q: "Should every turn of a conversation be retained, and can a summary be treated as the original fact?", a: "Retain only task-relevant history under explicit lifecycle and access rules. A generated summary is a new derived artifact, not the original authoritative record.", depth: "Classify turns by operational need, privacy, expiry, and evidentiary value. Preserve original source coordinates and critical scope, negation, approvals, and operation IDs when compressing. Revalidate facts against authoritative systems before sensitive decisions, and separately govern deletion of raw history, summaries, embeddings, and caches.", ask: "Ask the customer: which history is needed for continuity, which is authoritative evidence, and what retention or deletion obligations apply to derived summaries?", tag: "Context lifecycle", basis: "Long-context limits plus lifecycle governance", supports: ["Supports the need to test rather than assume uniform use of long histories.", "Supports governance and measurement of lifecycle and data-related generative-AI risks."] },
  { id: "few-shot-customer-conversations", q: "Can real customer conversations be copied directly into few-shot examples?", a: "Not by default. Examples require purpose, permission, minimization, de-identification where appropriate, quality review, and lifecycle controls before they enter a reusable prompt.", depth: "A conversation can contain personal data, confidential facts, malicious instructions, outdated policy, or an atypical outcome. Create curated examples that preserve the required decision boundary without unnecessary identifiers. Keep them separate from the evaluation holdout and re-review them when policies, products, or data-use permissions change.", ask: "Ask the customer: who authorized reuse, what sensitive content can be removed, and do the examples represent typical and boundary cases rather than memorable anecdotes?", tag: "Few-shot governance", basis: "Prompt component guidance plus injection risk", supports: ["Supports few-shot examples as an optional demonstration component, not an unrestricted data-reuse license.", "Supports treating externally influenced content as a potential injection source requiring layered controls."] },
  { id: "structured-output-recovery", q: "When Structured Outputs fails, should the system repair the JSON automatically or call the model again?", a: "Classify the failure first, then reject, retry, repair, or degrade accordingly. A generic JSON repair step can hide refusal, truncation, or a business error inside a valid-looking payload.", depth: "A narrowly syntactic repair may be safe when meaning is unchanged. Schema errors may justify a bounded retry with validation feedback; truncation requires budget or task changes; refusal needs its own path; and business-invalid values must return to an authoritative system or human. Bound retry count, cost, and idempotency, especially for transactions.", ask: "Ask the customer: can current telemetry distinguish parsing, schema, truncation, refusal, business-validation, and transaction failures?", tag: "Structured output", basis: "Official schema behavior plus failure decomposition", supports: ["Supports schema-constrained output and the distinct handling of refusal, truncation, and feature boundaries."] },
  { id: "tool-result-trust", q: "Can a tool result be passed to the model as trusted fact simply because it came from a tool?", a: "No. Tool results can be stale, incorrectly permission-filtered, partial, failed, or contaminated by indirect prompt injection.", depth: "Attach tool identity, parameters, timestamp, status, data version, and trust class. Validate critical fields with typed and business checks, and treat partial or unknown results separately. The model may explain a result, but confirm a material action by operation ID or an authoritative read-back; natural language from webpages, email, or files remains data.", ask: "Ask the customer: which tools read authoritative systems, which read the open internet, and can the application confirm actual state after an action?", tag: "Tool results", basis: "Official tool loop plus injection boundary", supports: ["Supports application execution and return of tool results to the model as separate steps.", "Supports the risk of indirect injection in external content and the need for layered control."] },
  { id: "reasoning-model-or-chaining", q: "For a complex task, should we use a reasoning model or a standard model with prompt chaining?", a: "Choose from task uncertainty, tool needs, observability, latency, and error cost—not from the word 'complex.' Compare success, P95, and cost per successful task on representative work.", depth: "A reasoning model can suit tasks requiring global trade-offs where intermediate state need not be exposed. A chain is easier to inspect, retry, and approve when steps and checks are known. They can be combined: a bounded model decision inside a workflow whose code owns state, authority, and postcondition verification.", ask: "Ask the customer: is success rate, observable checkpoints, latency, or cost the dominant objective, and which steps require deterministic or human approval?", tag: "Reasoning models", basis: "Model-interface boundary plus workflow selection", supports: ["Supports that hidden chain of thought is not an application-facing audit interface.", "Supports using the simplest suitable orchestration and distinguishing workflows from agents."] },
  { id: "prompt-rag-or-finetuning", q: "For a knowledge task, should we start with prompting, RAG, or fine-tuning?", a: "Use prompting for task expression and behavior constraints, RAG for current and attributable external knowledge, and fine-tuning primarily for stable behavior or specialized patterns. They can be combined, but they solve different problems.", depth: "Prefer RAG when knowledge changes, requires citations, or must be filtered by identity. Use prompting when the necessary facts are already present but the task or format is misunderstood. Evaluate fine-tuning when a well-tested behavior still needs to be reproduced across many similar tasks. Fine-tuning is not a live knowledge base and does not provide citation, revocation, or deletion controls automatically.", ask: "Ask the customer: is the failure missing current knowledge, unclear task expression, or inability to reproduce a stable behavior at scale?", tag: "Solution selection", basis: "Prompt workflow plus architecture boundary", supports: ["Supports determining whether a target metric is appropriate for prompt optimization.", "Supports organizing task input through instructions, context, examples, and response format."] },
  { id: "direct-vs-indirect-injection", q: "What is the difference between direct and indirect prompt injection?", a: "Direct injection is supplied explicitly in the user's request. Indirect injection is hidden in content the system later reads—such as a webpage, email, document, image, retrieved passage, or tool result.", depth: "Test more than a fixed phrase such as 'ignore previous instructions.' Cover multilingual and encoded attacks, hidden long-document content, cross-document composition, contaminated tool results, and attempts to trigger data exfiltration or state-changing actions. Measure whether an untrusted source can reach a dangerous sink.", ask: "Ask the customer: which external content can affect model behavior, and which writes, outbound messages, or sensitive-data operations are reachable afterward?", tag: "Prompt injection", basis: "OWASP classification plus source-sink modeling", supports: ["Supports the distinction between direct and indirect injection and layered mitigations.", "Supports analyzing real impact as a path from untrusted source to dangerous sink."] },
  { id: "detector-does-not-authorize-tools", q: "Can high-risk tools be enabled once a prompt-injection detector or keyword filter is deployed?", a: "No. Detectors have false negatives, false positives, and distribution drift. High-risk capabilities must remain deterministically bounded when detection fails.", depth: "Limit impact at the sink: expose a minimal tool set, bind allowed targets and parameters, separate read from write, cap amounts or resource scope, confirm messaging, payment, deletion, or code execution, and record operation IDs. Treat detection as one defense layer, never as the authorization decision.", ask: "Ask the customer: if one attack bypasses detection, what data and tools are reachable and what irreversible impact is possible?", tag: "Security controls", basis: "Source-sink engineering plus zero trust", supports: ["Supports limiting dangerous sinks so detection failure does not imply unlimited impact.", "Supports explicit identity- and resource-based authorization for every access."] },
  { id: "calibrate-llm-judge", q: "How can we tune prompts with an LLM judge without optimizing for the judge's preferences?", a: "Treat the judge as a calibrated measurement instrument, not truth. Require blind holdout evaluation and human gold labels to confirm that a wording change improves the actual task.", depth: "Prefer specific classification or pairwise comparison over an unconstrained overall score. Randomize candidate order, monitor position, verbosity, self-preference, and style effects, and segment agreement with human reviewers. Recalibrate when the judge, rubric, or task distribution changes; use authoritative data for factual fields and business outcomes.", ask: "Ask the customer: does the improvement persist in blind human review and on the holdout set, and does the conclusion reverse with another judge?", tag: "Evaluation method", basis: "Official evaluation practices plus human calibration", supports: ["Supports task-specific criteria, human calibration, and awareness that model-based grading can be biased."] },
  { id: "build-prompt-evaluation-set", q: "How should a prompt evaluation set be built without overfitting the prompt to the test?", a: "Sample the real task distribution, critical boundaries, and unacceptable errors, and retain a hidden holdout that developers do not repeatedly optimize against.", depth: "Stratify by business scenario, risk, language, input length, authorization, refusal, and attack class. Separate development, regression, holdout, and production-monitoring sets. Record source, authoritative answer, scope, annotator, and version. Admit confirmed production failures through review so the suite grows without becoming a homogeneous list of the current prompt's mistakes.", ask: "Ask the customer: does the set cover real long-tail and high-risk tasks, who owns the gold answer, and how are samples retired when business rules change?", tag: "Evaluation data", basis: "Continuous evaluation plus risk segmentation", supports: ["Supports task-specific, production-distribution, edge-case, and continuing evaluation.", "Supports recurring mapping, measurement, and management rather than one-time validation."] },
  { id: "multi-tenant-cache-isolation", q: "Can prompt caching leak one enterprise tenant's content to another?", a: "The feature name alone cannot establish safety. Verify the selected product's cache keys, isolation boundary, retention, observable fields, and data terms, and keep tenant-specific dynamic content out of any intentionally shared prefix.", depth: "Separate public, non-sensitive, version-stable instructions from tenant data. Isolate cache metrics, logs, and debugging views as well as model requests. If documentation does not meet the customer's isolation and retention requirements, disable the optimization or use a stronger resource boundary. Caching does not alter IAM, residency, or deletion duties.", ask: "Ask the customer: what content may be reused across users or tenants, and has security approved the target service's current isolation, retention, deletion, and billing behavior?", tag: "Cache security", basis: "Official cache mechanism plus zero-trust boundary", supports: ["Supports product-specific prefix caching and observability whose current isolation and billing rules require verification.", "Supports continuous subject- and resource-focused access decisions."] },
  { id: "cost-beyond-caching", q: "Besides prompt caching, how can model-call cost be reduced?", a: "Optimize total cost per successful task, not token price alone. Remove irrelevant context, route tasks to fit-for-purpose models, reduce retries, narrow tools, reuse stable prefixes, and stop unproductive workflows early.", depth: "Include input and output tokens, caching, retrieval, reranking, tool APIs, retries, human escalation, and rework. Smaller models may handle simple tasks while difficult or high-risk work escalates. Retrieval, summarization, or chunking can shorten input only after regression tests show that decisive facts remain available. Compare every candidate with the baseline on success rate, P95 end-to-end latency, and cost per successful task; a lower token bill is not an improvement if success falls or tail latency breaches the SLO.", ask: "Ask the customer: is cost driven mainly by long input, long output, model tier, low cache hit rate, tools, or failed retries?", tag: "Cost optimization", basis: "Cache mechanism plus evaluation-driven optimization", supports: ["Supports reuse of stable prefixes within the provider's documented cache behavior, not an end-to-end savings guarantee.", "Supports measuring changes on representative tasks rather than optimizing from anecdotes."] },
  { id: "version-complete-release-bundle", q: "Should a prompt version identify only the template text?", a: "No. Production behavior also depends on the model snapshot, context assembler, retriever, tools, output schema, and safety policy. The release unit must be the compatible configuration bundle.", depth: "Record prompt_version, model_snapshot, context_assembler, retriever, tool_schema, output_schema, safety_policy, and evaluation-set version for every release. A floating model alias, reordered context, or edited tool description can cause regression even when prompt text is unchanged. Capture purpose, owner, approval, result, and rollback target.", ask: "Ask the customer: can the system reconstruct the model, context, tools, schemas, and policies for a production response, or only its prompt ID?", tag: "Version governance", basis: "Versioned prompts plus complete-system evaluation", supports: ["Supports version control and release-time evaluation for production prompts.", "Supports continuous evaluation and recording failures for attribution."] },
  { id: "canary-rollback-unit", q: "What exactly should be rolled back when a prompt canary degrades?", a: "Roll back the complete release bundle, not merely the template text. The regression may come from the model, context assembly, retrieval, tool schema, output schema, or safety policy.", depth: "Freeze the baseline, traffic allocation, and stop conditions before canarying. Monitor segmented quality, safety, schema, tool errors, P95, and cost per successful task. On a trigger, restore the last validated bundle and retain traces and failed requests for attribution; inspect or compensate external transactions created during the canary.", ask: "Ask the customer: can deployment restore the full configuration in one operation, and which signals stop traffic automatically versus requiring human judgment?", tag: "Canary rollback", basis: "Continuous evaluation plus release engineering", supports: ["Supports continuing evaluation using production samples and failure feedback.", "Supports production prompt versioning, representative tests, and release-time evaluation."] },
  { id: "minimum-operable-prompt-platform", q: "What platform capabilities are minimally required to operate Prompt Engineering as a cloud service?", a: "At minimum: version-pinned model access, prompt and configuration management, context and secret isolation, an AI gateway, evaluation, safety policy, tracing and logs, and cost allocation. Tool use also requires identity propagation, approval, and transaction audit.", depth: "Separate a control plane for templates, models, evaluation sets, releases, and permissions from a data plane for request routing, context assembly, model invocation, validation, and tool execution. Verify current region, quota, private networking, retention, caching, keys, log fields, and billing for the target cloud, and document shared responsibility.", ask: "Ask the customer: is the target single-cloud or multi-cloud, and are private access, residency, customer-managed keys, disaster recovery, or departmental chargeback required?", tag: "Cloud operating model", basis: "Engineering decomposition plus continuous governance", supports: ["Supports managing production prompts through version, test, and deployment workflows.", "Supports governance, measurement, monitoring, and management of generative-AI risk."] },
  { id: "cross-model-prompt-portability", q: "Can a prompt move between models and cloud providers without modification?", a: "Do not assume byte-for-byte portability. Message roles, model behavior, tool interfaces, structured output, reasoning controls, and safety policies differ across providers.", depth: "Keep the task definition, business invariants, terminology, semantic intent of examples, domain output model, and evaluation set provider-neutral. Put provider-specific role mapping, tool-schema restrictions, phrasing, and reasoning settings in a thin adapter. Revalidate quality, safety, P95, and cost with the same acceptance suite after migration.", ask: "Ask the customer: is multi-cloud needed for resilience, negotiating leverage, residency, or task routing, and is the team prepared to maintain adapters and regression suites?", tag: "Portability", basis: "Provider-role differences plus versioned evaluation", supports: ["Supports one provider's specific system-instruction semantics and their documented limits.", "Supports model-aware prompt organization and evaluation of production changes."] },
  { id: "demo-vs-poc-acceptance", q: "If a prompt PoC demonstrates several cases reliably, is that enough to declare success?", a: "No. A demonstration establishes possibility, not reliable operation across the real distribution, boundary inputs, attacks, and production constraints.", depth: "Acceptance should cover task correctness and completeness, schema and tool arguments, refusal and authorization, adversarial attacks, P95, tokens, cost per success, and version regression. Include representative main paths, long-tail cases, and unacceptable errors, segmented by business risk. Also verify quotas, regions, networking, logging, retention, and degraded operation.", ask: "Ask the customer: who defines success and unacceptable error, does PoC data represent real traffic, and are security and operations part of acceptance?", tag: "PoC acceptance", basis: "Official optimization prerequisite plus representative evaluation", supports: ["Supports defining success criteria and a measurable method before prompt optimization.", "Supports task-specific, production-distribution, edge-case, and continuous evaluation rather than subjective demonstrations."] },
  { id: "risk-based-go-no-go", q: "Should every prompt PoC use a universal 90% or 95% Go / No-Go threshold?", a: "No. Thresholds should reflect the existing baseline, task risk, cost of each error class, reversibility, and human fallback. Different tasks in one system may require different gates.", depth: "Separate ordinary style problems, factual errors, incorrect refusal, unauthorized action, sensitive-data exposure, and irreversible side effects. Low-risk drafting can allow human correction; a high-risk transaction may require deterministic validation and zero-tolerance policy violations. Use both improvement over baseline and absolute risk stop conditions.", ask: "Ask the customer: which errors can be corrected safely, which must stop release, and what are the quality, latency, and cost baselines of the current process?", tag: "PoC gates", basis: "Task-specific evaluation plus contextual risk management", supports: ["Supports explicit, task-specific criteria and production-distribution evaluation.", "Supports mapping, measuring, and managing generative-AI risk in its actual context."] },
];

if (qaCopy.length !== canonicalQa.length) throw new Error(`Prompt Engineering English question count mismatch: ${qaCopy.length} / ${canonicalQa.length}`);
const qa = freeze(qaCopy.map((copy, index) => {
  const canonical = canonicalQa[index];
  if (copy.supports.length !== canonical.evidence.length) throw new Error(`Prompt Engineering English evidence mismatch at ${copy.id}`);
  return freeze({
    id: copy.id,
    q: copy.q,
    a: copy.a,
    depth: copy.depth,
    ask: copy.ask,
    tag: copy.tag,
    basis: copy.basis,
    evidence: freeze(canonical.evidence.map((entry, evidenceIndex) => freeze({ sourceId: entry.sourceId, supports: copy.supports[evidenceIndex] }))),
    ...(canonical.addedAt ? { addedAt: canonical.addedAt } : {}),
  });
}));

const evidenceCopy = [
  { id: "four-prompt-components", metric: "4", title: "Common prompt components", finding: "Google's official guide lists task, system instructions, few-shot examples, and contextual information as common components; only the task is central to every request.", boundary: "This is a provider's practical taxonomy, not a cross-provider protocol. Field names, roles, and priority semantics depend on the selected model API." },
  { id: "three-optimization-prerequisites", metric: "3", title: "Prerequisites for prompt optimization", finding: "Anthropic recommends a clear success criterion, an empirical test method, and a first prompt to improve before optimization begins.", boundary: "This is an engineering workflow, not an outcome guarantee. Model choice, data, latency, cost, or architecture may be the more appropriate intervention." },
  { id: "schema-constrained-output", metric: "Schema", title: "Constrain the output structure", finding: "Structured Outputs can make supported models follow a supplied JSON Schema and reduce missing fields or invalid enums.", boundary: "Correct structure does not establish factual truth. Applications must still handle refusals, truncation, unsupported schema behavior, and business validation.", accent: true },
  { id: "five-step-tool-loop", metric: "5", title: "Tool-calling loop", finding: "The documented flow separates providing tools, receiving the model's proposed call, executing application code, returning the result, and producing a final response.", boundary: "A generated tool name and arguments are neither execution nor authorization. The application owns permissions, validation, idempotency, approval, and outcome verification." },
  { id: "evaluate-every-release", metric: "Every release", title: "Run prompt evaluations", finding: "OpenAI's prompting guidance recommends versioning production prompts with application code and running representative tests and evaluations for releases.", boundary: "The guidance does not define a universal pass rate. Customer risk, baseline performance, and unacceptable error classes determine the gate." },
  { id: "system-prompt-residual-risk", metric: "Nonzero risk", title: "A system prompt is not a security boundary", finding: "OWASP guidance states that prompt injection cannot be eliminated by model-internal constraints alone and calls for layered controls such as least privilege and high-risk approval.", boundary: "Prompt hardening can reduce risk; it cannot replace authentication, authorization, output validation, isolation, and approval." },
  { id: "source-sink-impact-control", metric: "Source + Sink", title: "Control prompt-injection impact", finding: "OpenAI's security engineering guidance models attacks as paths from untrusted sources to dangerous sinks and emphasizes limiting impact when detection fails.", boundary: "This is an analysis method, not a standalone product feature. Each organization must define permissions, confirmations, and acceptable actions for its own system." },
  { id: "continuous-release-evaluation", metric: "Continuous", title: "Release is not a one-time score", finding: "Official evaluation guidance recommends task-specific tests, production-distribution samples, evaluation on every change, and human calibration of automated grading.", boundary: "Example tasks and thresholds are not cross-business standards. Release criteria must reflect the customer's error costs and operating controls." },
];

if (evidenceCopy.length !== canonicalEvidenceCards.length) throw new Error("Prompt Engineering English evidence-card count mismatch");
const evidenceCards = freeze(evidenceCopy.map((copy, index) => freeze({
  ...copy,
  sourceId: canonicalEvidenceCards[index].sourceId,
  ...(canonicalEvidenceCards[index].accent ? { accent: true } : {}),
})));

const terms = freeze({
  "prompt-engineering": freeze({ name: "Prompt Engineering", definition: "Designing task instructions, examples, constraints, and response requirements so a model can perform a defined task consistently and measurably." }),
  "context-engineering": freeze({ name: "Context Engineering", definition: "Selecting, assembling, budgeting, and governing the instructions, data, tools, and state supplied to each model invocation." }),
  instructions: freeze({ name: "Instructions", definition: "High-priority statements of role, objective, policy intent, and behavior boundaries whose exact hierarchy depends on the chosen model interface." }),
  context: freeze({ name: "Context", definition: "The dynamic facts, user and session state, retrieved evidence, examples, tool definitions, and results made available for the current invocation." }),
  "tools-schema": freeze({ name: "Tools & Schema", definition: "Machine-readable capability names, parameters, types, and result contracts through which a model can propose external operations while the application retains authority." }),
  "structured-outputs": freeze({ name: "Structured Outputs", definition: "A model-interface capability that constrains output to a supported schema for reliable parsing and validation; it does not establish semantic or business correctness." }),
  "prompt-injection": freeze({ name: "Prompt Injection", definition: "An attack in which untrusted input attempts to override instructions, expose sensitive information, or cause unauthorized action, directly or through retrieved or tool-supplied content." }),
});

const sources = freeze({
  "anthropic-prompt-overview": freeze({ kind: "Official provider documentation", shortTitle: "Anthropic Prompt Engineering Overview", note: "Requires success criteria, an empirical test method, and an initial prompt before optimization, and notes that some latency or cost problems may be better addressed through model choice. It is provider guidance, not an outcome guarantee." }),
  "google-prompt-strategies": freeze({ kind: "Official provider documentation", shortTitle: "Google Prompt Strategies", note: "Presents prompt design as test-driven iteration over goals, instructions, constraints, context, examples, and response format. It is provider-specific guidance, not a cross-model protocol." }),
  "google-system-instructions": freeze({ kind: "Official provider documentation", shortTitle: "Google System Instructions", note: "Documents system instructions for role, format, style, goals, and context, while warning that they cannot fully prevent jailbreaks or leakage and should not contain sensitive data. Exact semantics are API-specific." }),
  "owasp-prompt-injection": freeze({ kind: "Community security guidance", shortTitle: "OWASP LLM01:2025 Prompt Injection", note: "Defines direct and indirect prompt-injection risk and states that retrieval, fine-tuning, or a system prompt cannot eliminate it alone. It is a risk guide, not proof that a deployment is secure." }),
  "google-prompt-introduction": freeze({ kind: "Official provider documentation", shortTitle: "Google Prompt Introduction", note: "Lists task, system instructions, few-shot examples, and contextual information as common prompt components. The classification applies to the documented provider platform and is not a universal message standard." }),
  "openai-prompting-guide": freeze({ kind: "Official provider documentation", shortTitle: "OpenAI Prompting Guide", note: "Recommends versioning production prompts, constructing dynamic content from typed or validated inputs, and running representative evaluations at release. The advice is provider-specific rather than a cross-model message protocol." }),
  "openai-structured-outputs": freeze({ kind: "Official API documentation", shortTitle: "OpenAI Structured Outputs", note: "Documents schema-constrained output for supported models and interfaces, including refusal, truncation, and feature boundaries. Schema conformance does not prove factual truth, authorization, or business validity." }),
  "openai-function-calling": freeze({ kind: "Official API documentation", shortTitle: "Function Calling", note: "Documents the application-model loop for offering tools, proposing calls, executing code, returning results, and forming a response. Tool definitions and strict schemas do not grant authorization or prove business completion." }),
  "nist-genai-profile": freeze({ kind: "Official risk-management framework", shortTitle: "NIST AI 600-1", note: "Organizes generative-AI risk management around Govern, Map, Measure, and Manage in context. It is a voluntary resource, not product certification, a universal compliance checklist, or a performance threshold." }),
  "openai-model-spec-hidden-cot": freeze({ kind: "Official model behavior specification", shortTitle: "OpenAI Model Spec — Hidden CoT", note: "States that hidden chain of thought for some OpenAI models is not exposed to users or developers and may be summarized. This is a provider-specific interface boundary, not a universal audit standard." }),
  "anthropic-effective-agents": freeze({ kind: "Provider engineering guide", shortTitle: "Building Effective Agents", note: "Distinguishes predefined workflows from model-directed agents and recommends matching orchestration complexity to the task. Suitability, quality, cost, and risk thresholds require customer-specific evaluation." }),
  "react-2023": freeze({ kind: "Peer-reviewed research paper", shortTitle: "ReAct", note: "Presents interleaved reasoning, action, and environmental observation. Reported task results apply only to the paper's models, prompts, benchmarks, and baselines and do not establish enterprise-agent reliability." }),
  "openai-prompt-caching": freeze({ kind: "Official product documentation", shortTitle: "OpenAI Prompt Caching", note: "Documents current prefix-caching behavior, hit conditions, and usage visibility for the OpenAI API. It cannot be generalized to other providers, tenant-isolation designs, or total application cost." }),
  "nist-zero-trust": freeze({ kind: "Official architecture guidance", shortTitle: "NIST SP 800-207", note: "Supports explicit verification of subjects and authorization before enterprise-resource access rather than implicit trust. It does not prescribe a complete model, tool, or prompt-security implementation." }),
  "openai-model-spec-command": freeze({ kind: "Official model behavior specification", shortTitle: "OpenAI Model Spec — Chain of Command", note: "Describes instruction authority and untrusted-data boundaries for the relevant model behavior. It is not enterprise IAM, an authorization policy, or a cross-provider protocol." }),
  "openapi-3-1-1": freeze({ kind: "Open technical standard", shortTitle: "OpenAPI 3.1.1", note: "Defines a machine-readable description for HTTP APIs. Interface structure does not supply authorization, idempotency, compensation, or business postcondition semantics." }),
  "anthropic-tool-search": freeze({ kind: "Official product documentation", shortTitle: "Claude Tool Search", note: "Documents on-demand search and deferred loading for large tool catalogs. Discovery returns candidate capabilities; authorization, version validation, execution, and outcome verification remain application responsibilities." }),
  "anthropic-tool-definitions": freeze({ kind: "Official product documentation", shortTitle: "Claude Tool Definitions", note: "Documents how tool names, descriptions, and input schemas enter model context and affect selection. It does not transfer business authorization, idempotency, or post-execution validation to the model." }),
  "openai-eval-best-practices": freeze({ kind: "Official engineering guidance", shortTitle: "OpenAI Evaluation Best Practices", note: "Recommends task-specific continuous evaluation, production-distribution samples, explicit criteria, and human calibration of automated grading. Example tasks and thresholds are not universal customer release gates." }),
  "lost-middle": freeze({ kind: "Peer-reviewed journal paper", shortTitle: "Lost in the Middle", note: "Reports position-sensitive performance in specific multi-document question-answering and key-value retrieval experiments. It does not prove that every model behaves identically or that retrieval always beats long context." }),
  "openai-source-sink-injection": freeze({ kind: "Official security engineering guidance", shortTitle: "Designing Agents to Resist Prompt Injection", note: "Models prompt-injection impact as a path from untrusted sources to dangerous sinks and emphasizes deterministic limits when detection fails. Organizations must define their own authority and acceptable-action boundaries." }),
});

if (canonicalDeepDives.length !== 4) throw new Error("Prompt Engineering canonical deep-dive contract changed");

export const englishModule = freeze({
  slug: "prompt-engineering",
  title: "Prompt Engineering",
  subtitle: "Prompt, Context & Output Contracts",
  definition: "Translate a business objective into a controlled model invocation by designing task instructions, runtime context, examples, tool interfaces, and output contracts, then validate the resulting behavior through versioning, evaluation, security controls, and release governance.",
  position: "Prompt Engineering governs how a defined task is expressed to a model. Context Engineering governs what the model sees at runtime. Retrieval, agent orchestration, model capability, application authorization, deterministic business rules, and external transaction truth retain separate owners and cannot be replaced by stronger wording.",
  relatedSlugs: freeze(["llm", "rag", "ai-agent", "evaluation", "security", "ai-gateway", "ai-ops", "fine-tuning"]),
  sections: freeze(sections.map((section) => freeze({ ...section, blocks: freeze(section.blocks) }))),
  qa,
  evidenceCards,
  terms,
  sources,
});
