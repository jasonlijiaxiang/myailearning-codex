export const englishModule = Object.freeze({
  slug: "model-landscape",
  title: "Model Landscape",
  subtitle: "Turn business loss and delivery constraints into a testable model decision",
  definition: "Model landscape and selection translates a business task, error loss, delivery constraints, and lifecycle requirements into a feasible set, fully identified candidates, and an exit strategy. It does not produce a universal winner; it chooses the simplest portfolio that survives like-for-like customer testing and can be released, observed, and replaced safely.",
  position: "This solution-layer module owns model eligibility and candidate selection. Evaluation owns the measurement contract, the AI gateway executes runtime routes, and AI FinOps owns the complete investment decision.",
  relatedSlugs: Object.freeze([
    "solution-patterns",
    "evaluation",
    "ai-gateway",
    "prompt-engineering",
    "multimodal",
    "fine-tuning",
    "ai-ops",
  ]),
  sections: Object.freeze([
    Object.freeze({
      id: "selection-coordinate",
      eyebrow: "SELECTION COORDINATES",
      title: "Start model selection from business loss, not a leaderboard",
      lead: "Use a cross-region insurance-claim intake assistant as the working case: it reads multilingual forms, scans, and images, then drafts evidence-linked intake findings. Final eligibility, payment, and approval remain outside the model.",
      blocks: Object.freeze([
        Object.freeze({
          type: "steps",
          title: "Four coordinates for a defensible shortlist",
          items: Object.freeze([
            Object.freeze({
              id: "selection-task-capability",
              title: "Task and loss contract",
              subtitle: "Intake fields, evidence locations, severe omissions, and human baseline",
              body: "Separate document classification, fact extraction, missing-item detection, evidence localization, and explanation drafting. State who adjudicates each slice and which failures cannot be compensated by an average score.",
              decision: "Do not compare models until representative cases, the current manual baseline, and unacceptable losses are explicit.",
            }),
            Object.freeze({
              id: "selection-quality-risk",
              title: "Feasibility and candidate identity",
              subtitle: "Region, sensitive data, modality, license, endpoint, and exact version",
              body: "Apply hard gates before quality ranking and bind every candidate to its provider, endpoint, region, exact version, delivery form, and operating configuration.",
              decision: "Record pass, fail, or unknown with evidence; a hard-gate failure cannot be repaired by a higher aggregate score.",
            }),
            Object.freeze({
              id: "selection-serving-constraints",
              title: "Controlled pilot",
              subtitle: "Same prompt, context, tools, schema, budget, and test population",
              body: "Compare severe errors, critical slices, P95 latency, human-review demand, and cost per accepted intake result under one frozen invocation contract.",
              decision: "Keep one model when it passes. Introduce routing only when stable, observable task segmentation justifies the added control surface.",
            }),
            Object.freeze({
              id: "selection-lifecycle",
              title: "Portfolio, release, and exit",
              subtitle: "Protected routes, shadow, canary, rollback, and qualified substitutes",
              body: "Bind the selected model and invocation configuration to evidence, then exercise upgrades, regional failure, retirement, and provider exit.",
              decision: "A reserve candidate is real only after it passes the same hard gates; otherwise block the request or hand it to a person.",
            }),
          ]),
        }),
        Object.freeze({
          type: "cards",
          title: "Choose the simplest portfolio that still meets the boundary",
          items: Object.freeze([
            Object.freeze({
              id: "portfolio-single-model",
              title: "One model",
              body: "When tasks are similar and governance capacity is limited, a common version reduces the evaluation, integration, and operating surface.",
              decision: "Start here unless sustained evidence shows that task segments need materially different treatment.",
            }),
            Object.freeze({
              id: "portfolio-multi-model",
              title: "Model portfolio",
              body: "Add routing and fallback only when task segments show persistent, explainable differences in quality, latency, modality, data boundary, or cost.",
              decision: "Each route needs its own evidence, failure semantics, and rollback path.",
            }),
            Object.freeze({
              id: "portfolio-replace-foundation",
              title: "Route the failure to its accountable layer",
              body: "When a candidate fails a hard capability or delivery gate, return to model selection instead of patching around the failure. Once a feasible base passes, Multimodal owns capture, parsing, cross-modal alignment, and evidence coordinates; Fine-tuning owns stable residual behavior adaptation; AI Gateway owns runtime route execution.",
              decision: "Replace and requalify the base only for a failed hard gate. Do not absorb adjacent-module work into model selection.",
            }),
          ]),
        }),
        Object.freeze({
          type: "boundary",
          title: "Presales application",
          items: Object.freeze([
            Object.freeze({
              id: "selection-coordinate-deliverable",
              title: "Deliver an intake-task contract, candidate identity matrix, and controlled pilot",
              body: "Use those artifacts to decide whether one model is sufficient, whether a protected route is justified, and which version can serve as a tested replacement.",
              boundary: "The assistant drafts intake findings only. A model or route change never transfers claim eligibility, payment, or approval authority.",
            }),
          ]),
        }),
      ]),
    }),
    Object.freeze({
      id: "study-guide",
      eyebrow: "LEARN, PRACTICE, PROVE",
      title: "Build a selection method that survives catalog and price changes",
      lead: "Follow one claim-intake decision from task and loss through hard gates, exact candidate identity, like-for-like testing, portfolio choice, release, and exit.",
      blocks: Object.freeze([
        Object.freeze({
          type: "cards",
          title: "Learning outcomes",
          items: Object.freeze([
            Object.freeze({
              id: "outcome-capability-product-deployment",
              title: "Separate capability, product, and deployment",
              body: "Explain why a model family, a hosted product, an API endpoint, and an open-weight artifact are not interchangeable procurement objects.",
            }),
            Object.freeze({
              id: "outcome-customer-task-set",
              title: "Build a customer task set",
              body: "Replace generic leaderboard reasoning with representative tasks, critical slices, unacceptable failures, and explicit acceptance criteria.",
            }),
            Object.freeze({
              id: "outcome-routing-fallback",
              title: "Design routing and fallback",
              body: "Define which requests take each path, how misrouting is detected, and what may or may not change during fallback.",
            }),
            Object.freeze({
              id: "outcome-version-evaluation",
              title: "Govern version change continuously",
              body: "Bind the model version to the prompt, tools, data, evaluation result, rollout, and rollback evidence.",
            }),
          ]),
        }),
        Object.freeze({
          type: "steps",
          title: "Recommended learning route",
          items: Object.freeze([
            Object.freeze({
              id: "route-define-task-loss",
              title: "Define the task and loss contract",
              body: "Separate intake tasks, record the human baseline, identify critical slices, and state which errors or authority transfers are unacceptable.",
              decision: "Checkpoint: every model metric resolves to a customer task, loss, or hard business boundary.",
            }),
            Object.freeze({
              id: "route-establish-feasible-set",
              title: "Apply hard gates and identify each candidate",
              body: "Use data handling, region, latency, context, modality, license, tool capability, and procurement constraints to remove ineligible candidates. Bind every survivor to provider, endpoint, region, exact version, delivery form, and operating configuration.",
              decision: "Checkpoint: explain an exclusion without saying only that the model ranked lower, and reconstruct every candidate without a floating alias.",
            }),
            Object.freeze({
              id: "route-compare-task-performance",
              title: "Run a controlled pilot and choose one model or a portfolio",
              body: "Use customer samples to compare task success, severe errors, critical slices, stability, latency, human review, and cost per accepted outcome under the same invocation contract. Test one model first.",
              decision: "Checkpoint: introduce routing only when stable, observable task segmentation proves net benefit after misrouting, retries, gateway cost, and added operations are included.",
            }),
            Object.freeze({
              id: "route-convert-choice-to-release-evidence",
              title: "Release, fall back, and exercise exit",
              body: "Bind the task set, exact candidate tuple, prompt, tools, data, policy, and results; then define shadow, canary, monitoring, rollback, qualified substitutes, and approval.",
              decision: "Checkpoint: reconstruct every go-live conclusion and preserve the business boundary during an upgrade, regional outage, retirement, or provider exit.",
            }),
          ]),
        }),
        Object.freeze({
          type: "cards",
          title: "Practice labs",
          intro: "Each lab ends with an artifact that can be used in a design review, Proof of Concept, or release decision.",
          items: Object.freeze([
            Object.freeze({
              id: "lab-model-feasible-set",
              title: "Build a model feasible set",
              subtitle: "Scenario: multilingual claim forms, scans, accident images, sensitive data, and human approval",
              body: "Split the intake tasks and unacceptable losses, separate hard gates from preferences, and record provider, region, endpoint, version, delivery form, and operating configuration for every candidate.",
              decision: "Deliverable: task contract, feasibility matrix, and candidate identity ledger.",
              boundary: "Acceptance: an unknown is never recorded as a pass, and no aggregate score compensates for a failed hard gate.",
              sourceIds: Object.freeze(["nist-genai-profile", "osi-open-source-ai-definition-1-0", "openai-models", "google-models", "anthropic-models"]),
            }),
            Object.freeze({
              id: "lab-tiered-model-routing",
              title: "Run a like-for-like claim-intake pilot",
              subtitle: "Scenario: a leaderboard winner and a cheaper routing design have both been proposed without customer evidence",
              body: "Freeze the prompt, context, tools, schema, budget, and test population. Report severe errors and uncertainty by language, layout, risk, and long-tail slice, then compare one-model, routed, and human-escalation options.",
              decision: "Deliverable: per-slice evidence and a Go, Hold, or No-Go recommendation.",
              boundary: "Acceptance: every conclusion resolves to a complete candidate tuple, and a high-risk omission is never hidden by an average.",
              sourceIds: Object.freeze(["nist-ai-800-3", "finops-unit-economics", "nist-genai-profile"]),
            }),
            Object.freeze({
              id: "lab-provider-automatic-upgrade",
              title: "Exercise an upgrade and provider exit",
              subtitle: "Scenario: the primary alias will change and the reserve endpoint has never passed the scan or residency gates",
              body: "Freeze the current release tuple, replay old and new candidates, verify the reserve against all hard gates and prohibited degradations, then design shadow, canary, block, human handoff, and rollback paths.",
              decision: "Deliverable: upgrade review, exit exercise, and recovery evidence.",
              boundary: "Acceptance: an unsafe fallback is blocked or handed to a person rather than silently degraded.",
              sourceIds: Object.freeze(["nist-genai-profile", "opentelemetry-genai-semconv"]),
            }),
          ]),
        }),
      ]),
    }),
    Object.freeze({
      id: "curriculum",
      eyebrow: "CURRICULUM ATLAS",
      title: "The complete knowledge map for model selection",
      lead: "Follow the full decision chain from the procurement object and access model to capability evidence, economics, routing, release, and replacement.",
      blocks: Object.freeze([
        Object.freeze({
          type: "cards",
          items: Object.freeze([
            Object.freeze({
              id: "curriculum-model-product-service",
              title: "Model, product, and service",
              body: "The same model name may refer to an API, a managed platform offer, a consumer product, or an open-weight artifact. Context limits, tools, data processing, region, quotas, and support are part of delivered capability; comparing only the foundation model is incomplete.",
              decision: "Identify the procurement object and responsibility boundary before comparing capability.",
              boundary: "A capability stated on a provider page cannot automatically be extended to every region, account tier, or integration form.",
              sourceIds: Object.freeze(["openai-models", "google-models", "anthropic-models"]),
            }),
            Object.freeze({
              id: "curriculum-access-spectrum",
              title: "Proprietary service, open weights, and open source",
              subtitle: "Model Access Spectrum",
              body: "A managed API supplies a service boundary. Open weights provide parameters under a particular license. Open Source AI additionally requires the freedoms to use, study, modify, and share, together with sufficient data information, code, and parameters.",
              decision: "Read the license and assess data boundaries, operating capability, and audit requirements together.",
              boundary: "Downloadable weights do not establish unrestricted commercial rights, Open Source AI status, transparency, compliance, or safety.",
              sourceIds: Object.freeze(["osi-open-source-ai-definition-1-0", "nist-genai-profile"]),
            }),
            Object.freeze({
              id: "curriculum-capability-matrix",
              title: "Capability matrix and task slices",
              body: "Text, code, vision, speech, tool use, long context, and reasoning budget must be validated against customer tasks. Where a product exposes controls such as reasoning effort, the same model under different configurations is a different operating candidate: measure task success, reasoning tokens, time to first token, total latency, and cost per accepted outcome together.",
              decision: "Use the customer task set to determine the model, supported reasoning configuration, and division of work; do not search for an unconditional winner.",
              boundary: "Not every model exposes an explicit reasoning budget. More test-time compute does not guarantee correctness, and public benchmarks can only form hypotheses for customer validation.",
              sourceIds: Object.freeze(["nist-genai-profile", "deepseek-r1-2025", "openai-reasoning-guide", "openai-models", "google-models"]),
            }),
            Object.freeze({
              id: "curriculum-cost-structure",
              title: "Operating economics and outcome units",
              subtitle: "Operating Economics",
              body: "Input, output, caching, batch processing, tool loops, retries, human review, and failed-outcome rework jointly determine cost. Pricing and performance must be measured against the current candidate and target load.",
              decision: "At the same quality gate, report P95 latency, success, and cost per accepted business outcome.",
              boundary: "Token price, call cost, and customer ROI are different layers of analysis. Current prices and context limits remain time-sensitive.",
              sourceIds: Object.freeze(["finops-unit-economics", "openai-models", "anthropic-models", "google-models"]),
            }),
            Object.freeze({
              id: "curriculum-routing-fallback",
              title: "Routing, fallback, and portfolio design",
              body: "Rules can handle explicit constraints; a classifier or model router can estimate task complexity; people or a higher-capability model can handle high-risk and low-confidence cases. Fallback is not merely a provider swap: it must state which quality, function, or data boundary may be traded away.",
              decision: "Predefine the primary route, protected groups, fallback, and stop condition for each request class.",
              boundary: "Dynamic routing can itself misclassify and must be evaluated, observed, and rapidly disabled independently.",
              sourceIds: Object.freeze(["nist-genai-profile", "opentelemetry-genai-semconv"]),
            }),
            Object.freeze({
              id: "curriculum-release-discipline",
              title: "Version release and replaceability",
              subtitle: "Release Discipline",
              body: "A model version can change output, refusal, tool schemas, token use, and latency. Replaceability comes from a common contract, representative regression tests, shadow validation, and portable data—not from keeping a second model name in a configuration file.",
              decision: "Treat a model upgrade as a joint software and risk-policy change.",
              boundary: "A newer version in the same family can still drift in behavior; a provider compatibility statement does not remove the need for customer regression testing.",
              sourceIds: Object.freeze(["nist-genai-profile", "opentelemetry-genai-semconv"]),
            }),
            Object.freeze({
              id: "curriculum-exit-exercise",
              title: "Exit exercise and supply continuity",
              subtitle: "Exit Exercise",
              body: "A reserve candidate is real only after it passes the same region, data, modality, interface, and critical-slice hard gates. Exercise regional failure, version retirement, and provider exit, recording block, human-handoff, cutover, and recovery evidence.",
              decision: "Turn exit capability into an executable exercise; a second model name in configuration is not continuity evidence.",
              boundary: "Any latency or function sacrificed during fallback must be approved in advance. Never silently relax data boundaries, authority boundaries, or unacceptable-error gates.",
              sourceIds: Object.freeze(["nist-genai-profile", "opentelemetry-genai-semconv"]),
            }),
          ]),
        }),
      ]),
    }),
    Object.freeze({
      id: "principle",
      eyebrow: "MECHANISM, FAILURE, AND CONTROL",
      title: "Move from a model catalog to constraint-driven selection",
      blocks: Object.freeze([
        Object.freeze({
          type: "cards",
          items: Object.freeze([
            Object.freeze({
              id: "principle-no-context-free-best",
              title: "There is no context-free best model",
              body: "Capability has to be judged within a task, language, modality, latency target, data boundary, and cost of error. Consumer popularity, a general benchmark, and an enterprise workload answer different questions.",
              decision: "When a customer asks who is strongest, complete the task and constraint definition before testing candidates.",
            }),
            Object.freeze({
              id: "principle-delivery-model-is-capability",
              title: "The delivery model is part of capability",
              body: "One model family may be delivered through a public API, cloud marketplace, dedicated endpoint, or open weights. Region, data processing, quotas, tool interfaces, contractual support, and exit options can change the final solution.",
              decision: "Compare the delivery channel, contract, and operating control plane alongside the model name.",
            }),
            Object.freeze({
              id: "principle-open-weights-not-open-source",
              title: "Open weights do not necessarily mean open source",
              body: "The ability to download weights does not establish that training information, license rights, redistribution conditions, and responsibility for derivatives satisfy an open-source definition.",
              decision: "Read the license itself and obtain legal confirmation for commercial use, modification, redistribution, and downstream use.",
            }),
            Object.freeze({
              id: "principle-unit-price-not-total-cost",
              title: "Unit price is not total cost",
              body: "Model cost also depends on input and output shape, caching, retries, routing, managed operations, GPU utilization, human review, and rework after failure. A cheaper model with a lower task-success rate can cost more per business outcome.",
              decision: "Anchor commercial comparison on cost per accepted outcome, not token price alone.",
            }),
            Object.freeze({
              id: "principle-selection-is-release-lifecycle",
              title: "Selection is a release lifecycle",
              body: "Models are upgraded, retired, and exposed through changing interfaces. A candidate version should progress through a frozen evaluation, shadow traffic, canary, observation, and reversible promotion rather than replacing a model ID directly.",
              decision: "Treat the evaluation set, gateway abstraction, primary and reserve models, and exit terms as selection deliverables.",
            }),
          ]),
        }),
        Object.freeze({
          type: "boundary",
          title: "Critical boundary",
          items: Object.freeze([
            Object.freeze({
              id: "boundary-dynamic-market-facts",
              title: "Catalogs, prices, rankings, and platform features are time-sensitive",
              body: "Any customer-facing comparison must state the verification date, region, billing basis, and official source.",
              boundary: "A model leaderboard is not a conclusion about the customer's use case.",
            }),
          ]),
        }),
      ]),
    }),
    Object.freeze({
      id: "decisions",
      eyebrow: "SELECTION DECISIONS",
      title: "Turn customer constraints into a model and delivery decision",
      blocks: Object.freeze([
        Object.freeze({
          type: "cards",
          items: Object.freeze([
            Object.freeze({
              id: "decision-external-data-processing",
              title: "Can the customer send data to an external model service?",
              body: "Signal: the workload includes regulated data, trade secrets, data-residency requirements, or an unacceptable third-party processing boundary.",
              decision: "First narrow the set to dedicated services that meet the regional and contractual conditions, or evaluate private deployment of an open-weight model.",
              boundary: "Private deployment is not automatically compliant; licensing, supply chain, operating access, and audit still require validation.",
            }),
            Object.freeze({
              id: "decision-capability-threshold",
              title: "Does the task have a material capability threshold?",
              body: "Signal: complex reasoning, code, long documents, multimodal input, or a language-specific slice has a high cost of error.",
              decision: "Use real customer samples to set the gate. Eliminate models that fail before comparing cost and operating conditions among those that pass.",
              boundary: "A general benchmark can help form a shortlist, but it cannot conduct the final use-case assessment.",
            }),
            Object.freeze({
              id: "decision-tiered-request-volume",
              title: "Is request volume strongly tiered?",
              body: "Signal: most tasks are simple and frequent, while a small number are difficult or high risk.",
              decision: "First test whether one model passes every hard gate. Add model routing only when stable, observable task slices prove enough net benefit to justify separate paths and human escalation.",
              boundary: "The router must be evaluated independently. Misrouting, retries, gateway cost, and the added operating surface belong in total cost.",
            }),
            Object.freeze({
              id: "decision-rapid-substitution",
              title: "Must the customer change provider or version quickly?",
              body: "Signal: concerns include lock-in, price change, regional unavailability, model retirement, or business continuity.",
              decision: "Use a common gateway and invocation contract, retain a second qualified candidate, and decouple datasets, evaluation assets, and prompts from the model endpoint.",
              boundary: "Tool protocols, structured output, and prompt behavior are not fully equivalent across models; switching still requires regression testing.",
            }),
            Object.freeze({
              id: "decision-sustainable-business-outcome",
              title: "Is the customer buying a model or a sustainable business outcome?",
              body: "Signal: discussion is limited to parameters, rankings, and token prices without a definition of business success.",
              decision: "Define quality, safety, latency, and cost acceptance first, then work backwards to the model portfolio and cloud services.",
              boundary: "The model is one layer of the solution; data, workflow, permissions, and operations may be the binding constraint.",
            }),
          ]),
        }),
      ]),
    }),
    Object.freeze({
      id: "deep-dive",
      eyebrow: "PORTFOLIO ENGINEERING",
      title: "Use one claim-intake decision to replace leaderboard reasoning",
      lead: "The assistant may extract and draft evidence-linked intake findings. It may not decide eligibility, payment, or approval. That boundary lets the model decision proceed from loss and feasibility to like-for-like evidence, portfolio choice, release, and exit.",
      blocks: Object.freeze([
        Object.freeze({
          type: "table",
          title: "Define the feasible set before comparing the Pareto frontier",
          intro: "A candidate that violates data-residency, licensing, or interface constraints should not recover through a higher average score. Only feasible candidates should be compared on trade-offs that cannot all be optimized simultaneously.",
          columns: Object.freeze(["Decision dimension", "How to build evidence", "Presales action", "Boundary"]),
          items: Object.freeze([
            Object.freeze({
              id: "deep-dive-hard-constraint-gate",
              title: "Hard-constraint gate",
              body: "Check region, data processing, license, modality, context, tool interfaces, and procurement channel first. Exclude candidates that fail mandatory conditions and retain the verification snapshot in the model registry.",
              decision: "Use cloud catalogs, regional availability, contractual terms, and IAM or private-network design to create the qualified list before spending evaluation budget.",
              boundary: "A provider catalog establishes a current product claim, not quality on the customer workload or a contractual Service Level Agreement.",
              sourceIds: Object.freeze(["nist-genai-profile", "osi-open-source-ai-definition-1-0", "openai-models", "google-models", "anthropic-models"]),
            }),
            Object.freeze({
              id: "deep-dive-slice-level-quality",
              title: "Slice-level quality",
              body: "Freeze customer samples and measure pass rates and severe errors separately by language, task difficulty, risk level, and long-tail type rather than relying on one average score.",
              decision: "Version the dataset, adjudication rules, and model in the evaluation service. Set elimination gates before discussing small score differences among candidates.",
              boundary: "Automated judges need calibration against human-reviewed samples, and an average improvement cannot hide degradation in a high-risk slice.",
              sourceIds: Object.freeze(["nist-genai-profile", "openai-models", "google-models", "anthropic-models"]),
            }),
            Object.freeze({
              id: "deep-dive-operating-envelope",
              title: "Operating envelope",
              body: "Under target request lengths, concurrency, structured output, and tool-use conditions, measure time to first token, output rate, quotas, failure recovery, and degradation behavior.",
              decision: "Run the same load profile against candidate cloud endpoints and the AI gateway, and record sustainable capacity rather than one-off demonstration latency.",
              boundary: "Peak results in a test environment are not a contractual SLA and cannot be extrapolated to a different region or quota tier.",
              sourceIds: Object.freeze(["nist-genai-profile", "openai-models", "google-models", "anthropic-models"]),
            }),
            Object.freeze({
              id: "deep-dive-robust-economics",
              title: "Robust economics",
              body: "Include cache hits, routing, retries, human review, and failure rework in cost per accepted outcome. Test sensitivity to traffic, input length, and price changes.",
              decision: "Show business success volume and cloud consumption on the same FinOps view. Prefer a portfolio that remains acceptable over a reasonable range, not the cheapest model at one point estimate.",
              boundary: "The result belongs to a workload snapshot, not a permanent price ranking; recalculate after model or billing changes.",
              sourceIds: Object.freeze(["nist-genai-profile", "finops-unit-economics", "openai-models", "google-models", "anthropic-models"]),
            }),
          ]),
        }),
        Object.freeze({
          type: "cards",
          title: "How one claim intake passes from task contract to exit plan",
          intro: "This teaching case does not recommend an insurer, product, provider, or model. It shows how evidence removes unsupported options before a release decision.",
          items: Object.freeze([
            Object.freeze({
              id: "claim-intake-task-loss-contract",
              title: "Freeze the task and loss contract",
              body: "Separate document classification, field extraction, evidence localization, and explanation drafting. Capture the manual baseline, severe omissions, inappropriate escalation, and cases that require adjudication.",
              decision: "Obtain authoritative samples, accountable adjudicators, and unacceptable-error definitions before model comparison.",
              boundary: "A demonstration without a baseline and dispute rule cannot support procurement.",
              sourceIds: Object.freeze(["nist-genai-profile", "nist-ai-800-3"]),
            }),
            Object.freeze({
              id: "claim-intake-feasibility-identity",
              title: "Apply hard gates and identify the candidate",
              body: "Test residency, sensitive-data treatment, multilingual forms, scans and images, version pinning, license, and latency. Record the exact endpoint and operating configuration.",
              decision: "Each hard gate is pass, fail, or unknown; a fail cannot be compensated by a higher average score.",
              boundary: "A provider catalog establishes a current product claim, not customer quality, contractual responsibility, or an SLA.",
              sourceIds: Object.freeze(["nist-genai-profile", "osi-open-source-ai-definition-1-0", "openai-models", "google-models", "anthropic-models"]),
            }),
            Object.freeze({
              id: "claim-intake-controlled-pilot",
              title: "Run the controlled pilot",
              body: "Freeze prompt, context, tools, schema, budget, and cases. Compare severe errors, P95, human-review demand, and cost per accepted intake result by language, layout, risk, and long-tail slice.",
              decision: "Keep one model when it passes. Add routing only when stable task segmentation is observable and each route can be tested.",
              boundary: "An aggregate cannot hide a severe omission, and self-reported confidence is not an automatic routing truth.",
              sourceIds: Object.freeze(["nist-ai-800-3", "finops-unit-economics", "nist-genai-profile"]),
            }),
            Object.freeze({
              id: "claim-intake-release-exit",
              title: "Release, fall back, and exit",
              body: "Bind candidate identity, invocation configuration, evaluation set, and policy. Exercise shadow, canary, rollback, regional outage, retirement, and provider exit.",
              decision: "Only a candidate that passes the same hard gates and critical slices can be a reserve; otherwise block or hand off.",
              boundary: "Eligibility, payment, signature, and risk acceptance remain with deterministic controls and authorized people.",
              sourceIds: Object.freeze(["nist-genai-profile", "opentelemetry-genai-semconv"]),
            }),
          ]),
        }),
      ]),
    }),
    Object.freeze({
      id: "selection-evidence-boundaries",
      eyebrow: "EVIDENCE AND LIMITS",
      title: "Use catalogs to establish current product claims, not a universal winner",
      lead: "The evidence cards below separate what official catalogs and a risk framework can support from conclusions that still require customer-specific evaluation.",
      blocks: Object.freeze([
        Object.freeze({
          type: "boundary",
          items: Object.freeze([
            Object.freeze({
              id: "evidence-use-boundary",
              title: "Evidence interpretation",
              body: "Provider catalogs are primary sources for the provider's current model families, interfaces, and stated product positioning. NIST AI 600-1 supports continuous, context-specific risk management.",
              boundary: "Neither source type supplies a fair cross-provider ranking or the customer's production acceptance result.",
              sourceIds: Object.freeze(["openai-models", "google-models", "anthropic-models", "nist-genai-profile"]),
            }),
          ]),
        }),
      ]),
    }),
    Object.freeze({
      id: "cloud",
      eyebrow: "CLOUD SERVICE CONNECTIONS",
      title: "Map selection evidence to cloud capabilities",
      lead: "Describe the required capability and responsibility first; only then map it to current products, regions, quotas, Service Level Agreements, and billing units.",
      blocks: Object.freeze([
        Object.freeze({
          type: "cards",
          items: Object.freeze([
            Object.freeze({
              id: "cloud-model-access-catalog",
              title: "Model access and catalog",
              subtitle: "Cloud model marketplaces, Model as a Service, dedicated inference endpoints, and open-weight hosting",
              body: "Bring multiple providers under a common procurement and identity boundary, then narrow the feasible set by region and compliance condition.",
              decision: "Discovery question: where does the customer already operate, which regions are allowed, and is the required delivery model a public API, dedicated capacity, or private deployment?",
            }),
            Object.freeze({
              id: "cloud-evaluation-registry",
              title: "Model evaluation and registry",
              subtitle: "Evaluation services, model registry, experiment tracking, and dataset versioning",
              body: "Make selection and upgrade decisions reproducible rather than dependent on a leaderboard or personal preference.",
              decision: "Discovery question: who provides representative samples and adjudication, and which slices and red lines must pass independently?",
            }),
            Object.freeze({
              id: "cloud-routing-continuity",
              title: "Routing and continuity",
              subtitle: "AI gateway, model routing, rate limiting, circuit breaking, primary and reserve paths, and canary release",
              body: "Match quality and cost to the task while retaining a tested switch path for model retirement, provider failure, or regional unavailability.",
              decision: "Discovery question: are multiple providers allowed, and what are the SLA, degraded-service, failover, and exit requirements?",
            }),
            Object.freeze({
              id: "cloud-cost-governance",
              title: "Cost and governance",
              subtitle: "Usage metering, caching, budget alerts, audit, keys, and private connectivity",
              body: "Move from unit-price comparison to observable end-to-end Total Cost of Ownership and explicit risk controls.",
              decision: "Discovery question: what are the average input and output lengths, peak traffic, cache reuse, failure rework, and data boundary?",
            }),
          ]),
        }),
      ]),
    }),
  ]),
  qa: Object.freeze([
    Object.freeze({
      id: "contextual-model-choice",
      q: "Which provider has the strongest model right now?",
      a: "There is no strongest model independent of task and constraint. Define the customer's tasks, languages, modalities, cost of error, latency, data boundary, and budget first; then select candidates using a representative evaluation set.",
      depth: "Provider catalogs can show which models and interfaces each provider offers, but they cannot produce a fair cross-provider verdict for the customer's workload. Reframe the discussion around four questions: which candidates pass the critical task slices, whether their delivery conditions are acceptable, what each accepted outcome costs, and whether upgrade and exit remain controllable.",
      ask: "What are the three most important real tasks? Which errors are unacceptable? What hard constraints apply to data handling and response time?",
      tag: "Selection method",
      basis: "Official model catalogs plus use-case evaluation principles",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "openai-models", supports: "Supports that OpenAI model families have different capabilities and intended uses; it does not support a cross-provider winner." }),
        Object.freeze({ sourceId: "anthropic-models", supports: "Supports Anthropic model-family capability and delivery information; it does not support a task-independent cross-provider ranking." }),
        Object.freeze({ sourceId: "google-models", supports: "Supports differences in Gemini model capabilities and interfaces; it does not support replacing customer evaluation with a general leaderboard." }),
        Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports continuing measurement and management of generative-AI risk in the specific context of use." }),
      ]),
    }),
    Object.freeze({
      id: "supplier-continuity",
      q: "Why not procure from a single model provider?",
      a: "A single provider can be appropriate, but the choice should be explicit. Task segmentation, model retirement, regional failure, and price change can make a primary-and-reserve or multi-model portfolio more resilient.",
      depth: "Multiple models are not valuable simply because they add options. They can route routine, difficult, and sensitive-data requests differently and preserve a path through upgrades or outages. They also add gateway, adaptation, evaluation, and operating complexity, so begin with one primary and one qualified reserve or a clearly justified task split.",
      ask: "How long can the business stop if the sole provider is unavailable, and which calls must continue on premises?",
      tag: "Supplier continuity",
      basis: "Model portfolio plus lifecycle governance",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports identifying, measuring, and managing supply and operating risks throughout the lifecycle." }),
        Object.freeze({ sourceId: "openai-models", supports: "Supports that capabilities and product forms differ across model families, so a switch requires renewed matching." }),
        Object.freeze({ sourceId: "google-models", supports: "Supports product differences in capability, modality, and context characteristics." }),
      ]),
    }),
    Object.freeze({
      id: "open-weights-total-cost",
      q: "Is an open-weight model always cheaper than a commercial API?",
      a: "No. Open weights change the procurement and control model but add GPU capacity, inference optimization, security updates, reliability engineering, and operations. The cheaper path can be established only against the customer's load and the same quality gate.",
      depth: "Include the model and license, hardware amortization, peak headroom, platform and people, upgrade regression, availability, failed-outcome rework, and exit cost. Compare sensitivity using cost per accepted outcome; neither managed nor self-hosted deployment wins before traffic, service objectives, data boundaries, and operating capability are known.",
      ask: "What are the expected steady utilization and peak concurrency, which GPUs are already available, and what can the platform team operate reliably?",
      tag: "TCO",
      basis: "Delivery-model comparison plus risk governance",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports considering technical, operational, security, and supply-chain risks in deployment rather than a single cost line." }),
        Object.freeze({ sourceId: "finops-unit-economics", supports: "Supports connecting technology spend to an outcome unit; it does not determine whether managed or self-hosted delivery is cheaper for this customer." }),
      ]),
    }),
    Object.freeze({
      id: "model-upgrade-release",
      q: "Can we upgrade a model by simply changing the version identifier?",
      a: "That is not advisable. An upgrade can change output, tool calls, refusal, latency, and cost, so it should pass regression, progressive rollout, and rollback controls like a software release.",
      depth: "Freeze the current version and evaluation set, then compare the candidate offline and with shadow traffic. Promote it to a canary only after critical slices, safety, structured output, tool use, and cost all pass. Version the prompt, schema, and tool-adaptation layer with the model.",
      ask: "Who approves a model upgrade, can current traffic be shadowed, and how quickly can the service roll back after a regression?",
      tag: "Lifecycle",
      basis: "Continuous measurement plus product-version boundary",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports evaluation, monitoring, and risk treatment before and after deployment." }),
        Object.freeze({ sourceId: "openai-models", supports: "Supports distinguishing model families and capabilities by version; it does not establish behavioral compatibility between versions." }),
      ]),
    }),
    Object.freeze({
      id: "single-versus-multi-model",
      q: "Should we standardize on one model or build a multi-model portfolio?",
      a: "Standardize first when task differences are small and governance capacity is limited. Introduce explainable routing only when quality, latency, modality, data boundary, or cost shows stable segmentation.",
      depth: "A portfolio can send simple work to a low-latency model and complex work to a higher-capability model, while satisfying different regions and deployment boundaries. It also expands the evaluation matrix and creates routing errors, fallback differences, and version drift. Routing rules should come from observable task features, and every path should preserve the same definition of success and a tested failure route.",
      ask: "Which task slices consistently require different quality or latency, and can a wrong route be detected and reversed?",
      tag: "Portfolio strategy",
      basis: "Model catalogs plus use-case evaluation",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "openai-models", supports: "Supports that even one provider offers model families with different capabilities and intended uses." }),
        Object.freeze({ sourceId: "google-models", supports: "Supports differences in capability, modality, and delivery form; final selection still requires customer-task evaluation." }),
      ]),
    }),
    Object.freeze({
      id: "automatic-upgrade-governance",
      q: "Is automatic model upgrading always better than pinning a version?",
      a: "No. Automatic upgrades reduce maintenance, but critical workloads need reproducible behavioral versions, regression evidence, and an explicit rollback path.",
      depth: "The production release unit should record the model identifier, prompt, tool schema, knowledge version, and evaluation result. When a provider changes an alias or default version, regress it on the fixed task set and safety slices before progressive rollout. If no snapshot can be pinned, strengthen online monitoring and fallback, and make behavioral-change responsibility explicit in the shared-responsibility model.",
      ask: "Who approves a behavioral change, can affected requests be identified, and within what time can the system be rolled back?",
      tag: "Version governance",
      basis: "Model lifecycle plus continuous evaluation",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "anthropic-models", supports: "Supports that model families and versions have distinct capabilities and lifecycle information." }),
        Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports continuing measurement of risk and performance after a model or system change." }),
      ]),
    }),
    Object.freeze({
      id: "benchmark-shortlisting",
      q: "How should public benchmarks be used to shortlist models rather than declare a winner?",
      a: "First define the feasible set using hard constraints such as region, version, tools, data handling, and budget. Then use benchmarks related to the customer task as ranking signals and send only a small shortlist into like-for-like validation.",
      depth: "Record the model version, hardware, prompt, scoring method, and target task behind each published score, and map it to a customer capability. Exclude irrelevant or irreproducible scores from the ordering. The shortlisting output should be a candidate set, exclusion reasons, and hypotheses to test. Production acceptance still compares task success, severe errors, P95 latency, and cost per successful outcome under the customer's data, tools, and constraints.",
      ask: "Which benchmark dimensions map to the real workload, and which candidates already meet the hard constraints well enough to justify like-for-like testing?",
      tag: "Candidate shortlisting",
      basis: "Model feasible set plus evidence mapping",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports measuring generative AI against the context of use, affected parties, and risk tolerance rather than one general score." }),
        Object.freeze({ sourceId: "openai-models", supports: "Supports that model families have different capabilities and intended uses, so selection must remain task-specific." }),
      ]),
    }),
    Object.freeze({
      id: "model-exit-plan",
      q: "How should we prepare a replacement for model retirement or unavailability?",
      a: "Maintain candidate models that have passed the same hard gates, together with an adaptation layer and regression set. Predefine which tasks may degrade, be blocked, or move to a human queue.",
      depth: "Replacement is more than changing an endpoint. Context limits, tool use, schemas, language behavior, safety policy, and output quality may differ. The release package should record primary and reserve models, adaptation templates, tasks that must never degrade, switch conditions, and rollback evidence, and the team should exercise provider and regional failure periodically.",
      ask: "Which capabilities and data boundaries must remain unchanged during a switch, and may the fallback trade latency, quality, or function?",
      tag: "Exit strategy",
      basis: "Supply continuity plus version governance",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports identifying third-party dependencies, preparing responses, and managing deployment risk continuously." }),
        Object.freeze({ sourceId: "opentelemetry-genai-semconv", supports: "Supports common telemetry for model and provider attributes, enabling comparison before and after a switch." }),
      ]),
    }),
    Object.freeze({
      id: "frontier-model-task-tiering",
      q: "Is it safest to use the most capable frontier model at the highest reasoning setting for every task?",
      a: "Not as a default. Treat a smaller model or lower reasoning effort as a pilot hypothesis for lower-risk, structured, or high-throughput slices—not as a conclusion from model size—and remember that not every model exposes a reasoning control.",
      depth: "Separate reasoning difficulty, tool requirements, data boundary, latency, and cost of error before combining the model, supported reasoning configuration, and tool set into a regression candidate. Compare the hypothesis under one contract and protect critical slices. More reasoning can improve some difficult tasks, but it also increases tokens, tail latency, and the impact of version change. Choose on accepted outcomes, not assumed capability tiers.",
      ask: "Does the target service expose a reasoning control, which requests truly benefit from more test-time compute, and does the gain justify the latency and cost?",
      tag: "Task tiering",
      basis: "Task threshold plus available reasoning controls plus model portfolio",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "openai-reasoning-guide", supports: "Supports that some OpenAI models trade quality, speed, and token use through reasoning effort, with supported settings depending on the model." }),
        Object.freeze({ sourceId: "nist-ai-800-3", supports: "Supports defining the task population, assumptions, comparison conditions, and uncertainty before generalizing a performance claim." }),
        Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports selecting and measuring generative AI according to context of use and impact." }),
      ]),
    }),
    Object.freeze({
      id: "model-identity-across-hosting",
      q: "Can the same model name in different regions, cloud platforms, or hosting forms be treated as identical?",
      a: "Do not assume so. The model snapshot, service parameters, content policy, tool interface, quotas, and data-processing boundary may differ.",
      depth: "At minimum, model identity should bind provider, region, endpoint, exact version, default parameters, and capability contract. During migration, use the same evaluation set and real invocation chain to retest output, structured results, tool calls, tail latency, and error semantics; comparing the product name is not enough.",
      ask: "Can the team reconstruct the endpoint, model snapshot, and default parameters used for a specific request today?",
      tag: "Model identity",
      basis: "Version identity plus environment differences",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "openai-models", supports: "Supports understanding capability through a specific model and version identifier." }),
        Object.freeze({ sourceId: "google-models", supports: "Supports that a model service contains distinct models and capability boundaries that cannot be compared by a generic name alone." }),
      ]),
    }),
    Object.freeze({
      id: "open-weights-control-responsibility",
      q: "If an open-weight model runs in our own environment, does that automatically make it more controllable?",
      a: "No. It increases freedom to deploy and inspect, but it also transfers responsibility for hardening, the inference stack, upgrades, capacity, and incident response to the adopter.",
      depth: "Break control into weight access, licensing, data path, runtime, version pinning, patching, evaluation, and exit capability. Self-hosting can satisfy some regional or customization needs, but without artifact governance, service reliability, and a skilled operating team, actual risk can exceed that of a managed API.",
      ask: "Is the open-weight option being chosen for data boundaries, cost, customization, or supplier continuity, and who will own each resulting responsibility?",
      tag: "Deployment responsibility",
      basis: "Control rights plus operating responsibility",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports identifying model supply-chain, deployment-context, and organizational responsibilities." }),
      ]),
    }),
    Object.freeze({
      id: "effective-context-capacity",
      q: "If a model supports a very long context window, can we treat the maximum as usable capacity?",
      a: "No. The maximum window is a protocol limit, not evidence that every position is equally useful or that the full length is sustainable at the target latency, concurrency, and cost.",
      depth: "Test task success, evidence position, time to first token, output rate, and unit cost across the real input-length distribution. Include the space consumed by tool definitions, history, and retrieved content. Record the normal operating range and degradation point in the selection matrix rather than copying the maximum token count.",
      ask: "What is the real request-length distribution, where does the relevant evidence appear, what concurrency is expected, and at what length does quality or the Service Level Objective begin to fail?",
      tag: "Context boundary",
      basis: "Effective context plus capacity constraints",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "openai-models", supports: "Supports that models have specific context and capability specifications, which still need task-level validation." }),
        Object.freeze({ sourceId: "lost-middle", supports: "Directly reports position-sensitive performance in the paper's multi-document question-answering and key-value retrieval experiments; it does not establish a universal degradation pattern." }),
        Object.freeze({ sourceId: "opentelemetry-genai-semconv", supports: "Supports recording model and usage attributes for generative-AI requests so actual workload behavior can be measured." }),
      ]),
    }),
  ]),
  evidenceCards: Object.freeze([
    Object.freeze({
      id: "contextual-model-selection",
      metric: "Task × constraint",
      title: "Model selection requires context",
      finding: "OpenAI's official catalog organizes products by model family, capability, and intended use. A model name alone is therefore insufficient for enterprise selection.",
      boundary: "Provider catalogs are provider-authored primary sources, not a fair comparative evaluation. The final conclusion must come from the customer's evaluation set.",
      sourceId: "openai-models",
      accent: true,
    }),
    Object.freeze({
      id: "continuous-model-assurance",
      metric: "Continuous measurement",
      title: "Selection is not a one-off purchase",
      finding: "The NIST Generative AI Profile calls for risks to be identified, measured, and managed across design, deployment, and operation.",
      boundary: "It is a voluntary risk-management profile, not a model ranking or procurement recommendation.",
      sourceId: "nist-genai-profile",
    }),
    Object.freeze({
      id: "accepted-outcome-unit-economics",
      metric: "Cost / accepted outcome",
      title: "Unit economics must attach cost to a qualified result",
      finding: "FinOps Unit Economics distinguishes resource-efficiency units from business-outcome units. Model spend should be observed with accepted intake results, human review, and failure rework.",
      boundary: "An outcome unit creates a comparison basis; without a customer baseline, actual outcomes, and complete costs, it does not prove ROI.",
      sourceId: "finops-unit-economics",
    }),
  ]),
  terms: Object.freeze({
    "model-landscape": Object.freeze({
      name: "Model Landscape",
      definition: "A decision discipline that selects a model portfolio from task evidence, risk, serving constraints, delivery form, economics, and lifecycle requirements rather than a single leaderboard.",
    }),
    "model-routing": Object.freeze({
      name: "Model Routing",
      definition: "Selecting a model endpoint and fallback path according to task, quality, latency, cost, region, policy, or failure state.",
    }),
    "access-spectrum": Object.freeze({
      name: "Model Access Spectrum",
      definition: "The range from managed API services through open-weight artifacts to fully open-source systems, used to reason about control, licensing, operating responsibility, and replacement cost.",
    }),
    "capability-matrix": Object.freeze({
      name: "Capability Matrix",
      definition: "A task-sliced comparison of quality, latency, cost, risk, modality, tool behavior, and delivery constraints that prevents one aggregate score from standing in for use-case selection.",
    }),
    "model-lifecycle": Object.freeze({
      name: "Model Lifecycle Governance",
      definition: "Continuous control of model identity, version, regression evidence, rollout, rollback, retirement, and replaceability.",
    }),
    llm: Object.freeze({
      name: "Large Language Model",
      abbr: "LLM",
      definition: "A model that learns statistical language patterns from large corpora and generates tokens in response to the current context.",
    }),
    poc: Object.freeze({
      name: "Proof of Concept",
      abbr: "PoC",
      definition: "A controlled exercise that tests critical assumptions, risks, and acceptance criteria without implying that the complete production solution has already been delivered.",
    }),
    sla: Object.freeze({
      name: "Service Level Agreement",
      abbr: "SLA",
      definition: "A measurable agreement covering service levels such as availability, performance, support, or remedies.",
    }),
    tco: Object.freeze({
      name: "Total Cost of Ownership",
      abbr: "TCO",
      definition: "The complete cost of acquisition, integration, operation, people, maintenance, reliability, and handling failed outcomes—not merely the provider's unit price.",
    }),
  }),
  sources: Object.freeze({
    "openai-models": Object.freeze({
      kind: "Official model catalog",
      shortTitle: "OpenAI Models",
      note: "The official OpenAI model catalog. Model identifiers, capabilities, context limits, supported interfaces, retirement, and pricing are time-sensitive and must be rechecked at the point of use.",
    }),
    "nist-genai-profile": Object.freeze({
      kind: "Official risk-management framework",
      shortTitle: "NIST AI 600-1",
      note: "Organizes generative-AI risk management around Govern, Map, Measure, and Manage. It is a voluntary risk-management profile, not a product certification, model ranking, or performance standard.",
    }),
    "nist-ai-800-3": Object.freeze({
      kind: "Official technical report",
      shortTitle: "NIST AI 800-3",
      note: "Distinguishes accuracy on a fixed benchmark from generalization to a similar task population and requires assumptions, repeated trials, and uncertainty to be explicit. It does not set a customer's sample size, threshold, or release authority.",
    }),
    "finops-unit-economics": Object.freeze({
      kind: "Industry foundation framework",
      shortTitle: "FinOps Unit Economics",
      note: "Connects technology spend to product, service, or activity outcomes and distinguishes resource-efficiency units from business-outcome units. It supports baselines and trends but does not prove causality or ROI.",
    }),
    "osi-open-source-ai-definition-1-0": Object.freeze({
      kind: "Open definition",
      shortTitle: "Open Source AI Definition 1.0",
      note: "Defines the freedoms to use, study, modify, and share Open Source AI and the data information, code, and parameters needed to exercise them. Weight availability alone does not establish that status.",
    }),
    "google-models": Object.freeze({
      kind: "Official model catalog",
      shortTitle: "Gemini Models",
      note: "The official Gemini model catalog. Models, capabilities, limits, and release stages change, so this fieldbook does not freeze a current ranking into the body content.",
    }),
    "anthropic-models": Object.freeze({
      kind: "Official model catalog",
      shortTitle: "Claude Models",
      note: "The official Claude model catalog. Recheck capability, lifecycle, regional availability, and pricing when making a selection decision.",
    }),
    "opentelemetry-genai-semconv": Object.freeze({
      kind: "Open observability specification",
      shortTitle: "OTel GenAI SemConv",
      note: "Provides evolving semantic conventions for generative-AI clients, agents, MCP, retrieval, and tool calls. Pin the adopted version and add business outcomes, approvals, and risk fields that the specification does not supply.",
    }),
    "openai-reasoning-guide": Object.freeze({
      kind: "Official technical documentation",
      shortTitle: "OpenAI Reasoning Guide",
      note: "Documents provider-specific reasoning controls for supported OpenAI models and their quality, speed, and token-use trade-offs. Supported values and defaults vary by model and can change.",
    }),
    "deepseek-r1-2025": Object.freeze({
      kind: "Peer-reviewed journal paper",
      shortTitle: "DeepSeek-R1",
      note: "Reports a model-family approach combining verifiable rewards, reinforcement learning, cold-start data, staged training, and distillation. The result does not show that reinforcement learning improves every open-ended task, and internal reasoning is not an audit explanation.",
    }),
    "lost-middle": Object.freeze({
      kind: "Peer-reviewed journal paper",
      shortTitle: "Lost in the Middle",
      note: "Reports position-sensitive performance in specific multi-document question-answering and key-value retrieval experiments. It does not prove that every model behaves identically or that retrieval always beats long context.",
    }),
  }),
});
