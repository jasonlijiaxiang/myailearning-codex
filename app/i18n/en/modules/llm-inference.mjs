export const englishModule = Object.freeze({
  slug: "llm-inference",
  title: "LLM Inference",
  subtitle: "Deliver accepted model outputs with controlled latency, capacity, quality, and cost",
  definition: "LLM Inference turns trained weights into online or batch output. An inference engine loads the model, schedules requests, performs attention, manages the KV cache, batches and partitions work, and streams generated tokens.",
  position: "This module sits between model artifacts and AI applications. It connects gateways, retrieval, agents, and batch jobs to accelerators, networks, and storage, with emphasis on useful throughput, latency, reliability, and retained quality per unit resource.",
  relatedSlugs: Object.freeze(["llm", "ai-gateway", "ai-infra-compute", "ai-infra-platform", "evaluation", "prompt-engineering"]),
  sections: Object.freeze([
    Object.freeze({ id: "study-guide", eyebrow: "LEARN, PRACTICE, PROVE", title: "Read the request path before optimizing it", lead: "Inference has distinct time and memory accounts. A defensible capacity claim separates request phases, workload shapes, quality, recovery, and the point at which an output is accepted by the business.", blocks: Object.freeze([
      Object.freeze({ type: "cards", title: "Learning outcomes", items: Object.freeze([
        Object.freeze({ id: "outcome-request-phases", title: "Separate request phases", body: "Distinguish prefill, decode, TTFT, TPOT or ITL, end-to-end latency, and successful throughput." }),
        Object.freeze({ id: "outcome-runtime-memory", title: "Account for runtime memory", body: "Explain KV-cache growth, continuous batching, PagedAttention, workspaces, and safety margin." }),
        Object.freeze({ id: "outcome-inference-optimization", title: "Evaluate optimization as a release", body: "Assess quantization, prefix caching, speculative decoding, compilation, and distributed serving against quality and recovery." }),
        Object.freeze({ id: "outcome-workload-slo", title: "Build workload-specific SLOs", body: "Establish separate capacity baselines for interactive, long-context, agentic, and offline work." }),
      ]) }),
      Object.freeze({ type: "steps", title: "Recommended learning route", items: Object.freeze([
        Object.freeze({ id: "route-single-request", title: "Start with one request", body: "Trace tokenization, queueing, prefill, decode, sampling, and streaming.", decision: "Checkpoint: distinguish time to first token from time between output tokens." }),
        Object.freeze({ id: "route-request-contention", title: "Study competing requests", body: "Analyze batching, KV cache, scheduling, fairness, admission, and tail latency.", decision: "Checkpoint: explain why concurrency per accelerator is not a constant." }),
        Object.freeze({ id: "route-capacity-validation", title: "Validate optimization and capacity", body: "Benchmark interactive, long-context, agentic, and batch workloads separately.", decision: "Checkpoint: every optimization conclusion covers quality, cost, stability, and recovery." }),
        Object.freeze({ id: "route-time-memory-accounts", title: "Separate time and memory accounts", body: "Measure queue, prefill, decode, and transfer time; account for weights, KV cache, workspaces, fragmentation, and reserve.", decision: "Checkpoint: identify exactly where time and memory are consumed." }),
        Object.freeze({ id: "route-inference-release", title: "Release the serving stack as one unit", body: "Regress model, tokenizer, template, engine, quantization, cache, batching, and routing changes together.", decision: "Checkpoint: performance gain never conceals quality or recovery regression." }),
      ]) }),
      Object.freeze({ type: "cards", title: "Practice labs", items: Object.freeze([
        Object.freeze({ id: "lab-four-load-shapes", title: "Build four workload baselines", subtitle: "High aggregate tokens per second but unstable chat and agent experience", body: "Define short interactive, long-context, tool-loop, and offline batch loads; measure TTFT, TPOT, throughput, P95, memory, and quality while varying concurrency, sequence lengths, and batch policy.", decision: "Deliverable: capacity curves and workload-specific SLOs.", boundary: "Acceptance: no single average-throughput number stands in for all workloads; each capacity point is sustained and successful.", sourceIds: Object.freeze(["vllm-2023", "opentelemetry-genai-semconv"]) }),
        Object.freeze({ id: "lab-quantization", title: "Validate a 4-bit quantization decision", subtitle: "The team wants the model to fit on a smaller GPU", body: "Separate weights, KV cache, and runtime memory; evaluate customer tasks and tail failures; measure latency, throughput, and stability on the target hardware.", decision: "Deliverable: a quality-performance-cost comparison.", boundary: "Acceptance: the result is scoped to one model, method, hardware stack, and workload; GPTQ's reported results are not a universal guarantee.", sourceIds: Object.freeze(["gptq-2023", "nist-genai-profile"]) }),
        Object.freeze({ id: "lab-ttft-regression", title: "Diagnose a TTFT regression", subtitle: "The model version is unchanged but users wait longer for the first token", body: "Measure client, gateway, queue, model-load, and prefill time; slice by input length, concurrency, cache hit, and retry; validate that the minimum fix does not regress TPOT, throughput, or quality.", decision: "Deliverable: a layered TTFT diagnosis.", boundary: "Acceptance: the regression is located to a specific stage and condition rather than attributed vaguely to the model.", sourceIds: Object.freeze(["vllm-2023", "opentelemetry-genai-semconv"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "curriculum", eyebrow: "CURRICULUM ATLAS", title: "From request lifecycle to production release", lead: "Inference optimization must manage a single request, multi-request scheduling, model quality, accelerator memory, and production recovery at the same time.", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "curriculum-request-lifecycle", title: "Prefill, decode, and service metrics", subtitle: "Request Lifecycle", body: "Prefill processes the input in parallel and dominates work before the first token; decode generates sequentially and is often sensitive to memory bandwidth. TTFT, TPOT or ITL, end-to-end latency, and throughput describe different experiences.", decision: "Choose metrics by interaction intent rather than reducing the request to average response time.", boundary: "Client first-byte time also includes gateways, networks, buffering, and rendering.", sourceIds: Object.freeze(["vllm-2023", "opentelemetry-genai-semconv"]) }),
        Object.freeze({ id: "curriculum-runtime-memory", title: "KV cache and the memory account", subtitle: "Runtime Memory", body: "Weights are relatively fixed; KV cache grows with architecture, sequence length, precision, and active requests, while activations, workspaces, and fragmentation also consume memory. A model that loads is not necessarily serviceable at target concurrency.", decision: "Account separately for weights, KV cache, runtime workspaces, and safety reserve.", boundary: "Cache formulas depend on architecture and precision; maximum context and maximum concurrency cannot generally be delivered simultaneously.", sourceIds: Object.freeze(["vllm-2023"]) }),
        Object.freeze({ id: "curriculum-scheduling-caching", title: "Continuous batching, paging, and prefix caching", subtitle: "Scheduling, Paging & Prefix Cache", body: "Continuous batching admits and retires requests dynamically, PagedAttention manages KV memory in blocks, and prefix caching reuses computed KV for identical prefixes. Acceptance must include cache hit rate, locality, isolation, P95, and successful Goodput.", decision: "Choose cache and batch policy from real prefix repetition and request distributions.", boundary: "Prefix caching reduces prefill for the matched prefix; it does not shorten decode of new tokens, and cross-tenant reuse requires an explicit public-content boundary.", sourceIds: Object.freeze(["vllm-2023", "vllm-prefix-caching"]) }),
        Object.freeze({ id: "curriculum-inference-engines", title: "Engine, serving layer, and platform", subtitle: "Inference Engines", body: "The engine owns kernels, memory, scheduling, and model execution. A serving layer adds APIs, routing, identity, releases, telemetry, and tenancy. The platform owns devices, placement, capacity, and recovery.", decision: "State which responsibility is supplied by the engine, service, and platform.", boundary: "Installing vLLM or another engine does not create a production multi-tenant security and operations platform.", sourceIds: Object.freeze(["vllm-2023", "kubernetes-dra"]) }),
        Object.freeze({ id: "curriculum-quantization", title: "Quantization and the quality account", subtitle: "Quantization", body: "Lower-precision weights can reduce model memory; end-to-end speed and retained quality depend on method, kernels, hardware, and workload. GPTQ reports specific compression, accuracy, and speed results for its tested models and devices.", decision: "Measure quality, latency, throughput, memory, and stability on the target hardware and customer task set.", boundary: "The paper's averages and speedups cannot be projected onto another model, bit width, kernel, hardware, or critical customer slice.", sourceIds: Object.freeze(["gptq-2023", "nist-genai-profile"]) }),
        Object.freeze({ id: "curriculum-speculative-decoding", title: "Speculative decoding and algorithmic acceleration", subtitle: "Speculative Decoding", body: "An approximation model proposes multiple tokens and the target model verifies them in parallel with a sampling procedure that preserves the target distribution. Benefit depends on how well the draft approximates the target and on implementation cost.", decision: "Measure acceptance and end-to-end tail latency before adding the complexity.", boundary: "The ICML result is model- and implementation-specific; speculative decoding does not solve prefill or every capacity problem.", sourceIds: Object.freeze(["speculative-decoding-2023"]) }),
        Object.freeze({ id: "curriculum-disaggregated-serving", title: "Prefill-decode disaggregation and distributed inference", subtitle: "Disaggregated Serving", body: "Prefill and decode have different resource profiles and can be scaled independently, but separation adds KV transfer, routing, scheduling, network, and failure dependencies. Compare Goodput under both TTFT and TPOT objectives.", decision: "Add distributed complexity only when a measured resource conflict or model size requires it.", boundary: "Network and transfer overhead can erase the benefit; paper results do not transfer unchanged across hardware and workloads.", sourceIds: Object.freeze(["distserve-2024", "vllm-2023"]) }),
        Object.freeze({ id: "curriculum-production-serving", title: "Capacity, SLOs, and failure recovery", subtitle: "Production Serving", body: "Interactive, long-context, agentic, and batch loads require separate baselines. Production acceptance must also cover cold starts, model loading, node failure, rate limits, degradation, version rollout, and correct recovery.", decision: "Use workload-sliced curves and tail SLOs to define pools and reserve.", boundary: "An automatic restart restores a process or capacity, not necessarily request state, cache correctness, or external business effects.", sourceIds: Object.freeze(["opentelemetry-genai-semconv", "nist-genai-profile"]) }),
        Object.freeze({ id: "curriculum-serving-release", title: "Version release and request continuity", subtitle: "Serving Release", body: "Model, engine, quantization, tokenizer, chat template, and routing policy jointly determine behavior. Canary evaluation must track quality, TTFT, TPOT, refusal, cost, and in-flight requests.", decision: "Release the inference stack as a unit with drain, rollback, and cache policy defined in advance.", boundary: "A healthy process and loaded weights do not prove protocol compatibility, retained quality, or request continuity.", sourceIds: Object.freeze(["vllm-2023", "opentelemetry-genai-semconv"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "principles", eyebrow: "CORE MECHANISM", title: "The execution path from context to streamed tokens", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "principle-autoregressive", title: "Autoregressive generation", body: "The model generates each token conditioned on the existing context, so TTFT and subsequent token rate affect different parts of interaction quality.", decision: "Observe TTFT, ITL or TPOT, end-to-end latency, and successful throughput separately." }),
        Object.freeze({ id: "principle-prefill-decode", title: "Prefill and decode", body: "Prefill processes input context and is often compute-heavy; decode repeatedly generates tokens and is often memory- and scheduling-sensitive.", decision: "Test long input, long output, and high concurrency as separate load shapes." }),
        Object.freeze({ id: "principle-kv-cache", title: "Key-Value cache", body: "The KV cache stores historical attention state and avoids full-prefix recomputation for each new token, but consumes substantial device memory.", decision: "Size capacity from weights, context, concurrency, precision, and cache management together." }),
        Object.freeze({ id: "principle-continuous-batching", title: "Continuous batching and PagedAttention", body: "Requests enter and leave batches at different times, while paged cache management reduces fragmentation and improves sharing of device memory.", decision: "Tune scheduling to the business SLO and request distribution, not aggregate throughput alone." }),
        Object.freeze({ id: "principle-inference-optimization", title: "Inference optimization", body: "Quantization, compilation, IO-aware attention, prefix caching, and speculative decoding attack different compute, memory, and sequential-work costs.", decision: "Validate every optimization on the target model, hardware, workload, quality set, cold start, and tail latency." }),
        Object.freeze({ id: "principle-distributed-inference", title: "Distributed inference", body: "Model, pipeline, expert, and disaggregated prefill-decode serving can exceed single-device limits or scale throughput, while adding network and scheduler costs.", decision: "Optimize one instance first; add distribution only after the target load proves the need." }),
      ]) }),
      Object.freeze({ type: "boundary", title: "Critical boundary", items: Object.freeze([Object.freeze({ id: "inference-engine-boundary", title: "Tokens per second is not business success", body: "The engine optimizes model execution.", boundary: "It does not own cross-model business routing, user authorization, knowledge retrieval, agent tool workflows, or cluster governance. Accept quality, SLO, and cost per successful task together." })]) }),
    ]) }),
    Object.freeze({ id: "decisions", eyebrow: "PRESALES DECISIONS", title: "Choose the service model and optimization path", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "decision-managed-or-self-hosted", title: "Managed API or self-hosted inference?", body: "Trade time to value, model control, data boundary, sustained utilization, custom optimization, and operating capability.", decision: "Favor managed service for exploratory or variable load; validate self-hosting for stable high utilization and a real control requirement.", boundary: "Compare total cost, including idle reserve, engineering, upgrades, disaster recovery, network, and failed retries—not token price alone." }),
        Object.freeze({ id: "decision-inference-engine", title: "Which inference engine?", body: "Candidates differ in model and hardware support, scheduler, quantization, structured output, adapters, observability, and maturity.", decision: "Replay the same model, precision, hardware, and request distribution and compare quality, TTFT, ITL, P95, throughput, stability, and operations.", boundary: "Public benchmarks commonly use incomparable hardware, batches, and sequence lengths." }),
        Object.freeze({ id: "decision-long-context-capacity", title: "Larger GPU or context engineering?", body: "Long inputs create KV-cache pressure, slower TTFT, lower concurrency, or rejected requests.", decision: "First prove that the context is useful; then test retrieval, summarization, prefix reuse, paged cache, and length-aware pools before hardware expansion.", boundary: "Maximum context is not effective context at an acceptable quality, concurrency, and cost." }),
        Object.freeze({ id: "decision-quantization", title: "Should the model be quantized?", body: "Weights or cache limit capacity and the objective is to fit a model or more replicas on the same hardware.", decision: "Begin with a rollback-safe weight candidate and regress representative tasks, languages, long context, tools, and safety.", boundary: "Low precision is not always faster; kernel support, calibration, model structure, and quality determine value." }),
        Object.freeze({ id: "decision-prefill-decode-disaggregation", title: "When should prefill and decode be disaggregated?", body: "At large scale, one pool cannot meet both first-token and sustained-generation SLOs because the phases compete for different resources.", decision: "Prove the single-pool constraint first, then measure KV transfer, routing, network, and fault behavior.", boundary: "This is a workload- and scale-specific design, not a universal default." }),
      ]) }),
    ]) }),
    Object.freeze({ id: "deep-dive", eyebrow: "BOTTLENECK DIAGNOSIS", title: "Turn online symptoms into reproducible workload evidence", lead: "First-token delay, slow continuation, queueing, rejection, memory growth, and quality regression come from different paths and require separate experiments.", blocks: Object.freeze([
      Object.freeze({ type: "table", title: "Locate five common production symptoms", columns: Object.freeze(["Symptom", "Mechanism", "Verification", "Boundary"]), items: Object.freeze([
        Object.freeze({ id: "diagnostic-slow-ttft", title: "TTFT rises; generation remains normal", body: "Queueing, cold load, long-input prefill, or an oversized batch often delays the first token.", decision: "Separate queue, model load, and prefill; slice by input length; evaluate faster attention or a separate pool only when prefill is the measured limit.", boundary: "Client first byte also includes gateway buffering and network time.", sourceIds: Object.freeze(["vllm-2023", "flashattention-2022", "opentelemetry-genai-semconv"]) }),
        Object.freeze({ id: "diagnostic-slow-itl", title: "ITL slows while GPU utilization remains high", body: "Decode can be limited by memory bandwidth, KV-cache access, excessive batching, or cross-device synchronization.", decision: "Hold concurrency and vary batch and context; observe the knee in ITL, bandwidth, and cache occupancy.", boundary: "Lower ITL may reduce total throughput; the interaction SLO decides the tradeoff.", sourceIds: Object.freeze(["vllm-2023"]) }),
        Object.freeze({ id: "diagnostic-throughput-collapse", title: "Throughput falls as concurrency rises", body: "Length variation, cache fragmentation, preemption, recomputation, and very long outputs can reduce batch efficiency and trigger retries.", decision: "Use length-aware pools and inspect cache use, preemption, and recomputation; report sustained successful throughput.", boundary: "More pools reduce sharing and may increase idle capacity at low traffic.", sourceIds: Object.freeze(["vllm-2023"]) }),
        Object.freeze({ id: "diagnostic-progressive-oom", title: "Intermittent OOM after hours", body: "Growing caches, fragmentation, leaked request state, or admission based only on weights can accumulate pressure.", decision: "Trace input and output tokens, cache blocks, reclaim, and rejection per request; use a long soak test.", boundary: "Restarting restores capacity but does not prove the memory-lifecycle defect is fixed.", sourceIds: Object.freeze(["vllm-2023", "opentelemetry-genai-semconv"]) }),
        Object.freeze({ id: "diagnostic-quality-regression", title: "Performance improves; business quality falls", body: "Quantization, compilation, speculation, or model replacement can change numerics, output distribution, tool arguments, or safety behavior.", decision: "Treat the optimized artifact as a new release and regress facts, Chinese, long context, structured output, tools, and high-risk samples.", boundary: "Public quality benchmarks cannot replace customer-task evidence.", sourceIds: Object.freeze(["nist-genai-profile"]) }),
      ]) }),
      Object.freeze({ type: "cards", title: "Four load shapes require separate baselines", items: Object.freeze([
        Object.freeze({ id: "load-interactive-short", title: "Interactive short-form", body: "Short inputs and streamed output make queueing, TTFT, ITL, connection stability, and tail latency decisive.", decision: "Load-test bursty arrivals rather than fixed concurrency and accept successful throughput under timeout.", boundary: "Maximum tokens per second is not the best interaction experience.", sourceIds: Object.freeze(["opentelemetry-genai-semconv"]) }),
        Object.freeze({ id: "load-long-context", title: "Long-context analysis", body: "Prefill and KV cache dominate, and a small number of long requests can displace short ones.", decision: "Test stepped input lengths, cache use, and mixed short-long traffic; isolate queues or pools when justified.", boundary: "Maximum context is an allowed input, not a quality, concurrency, or cost commitment.", sourceIds: Object.freeze(["vllm-2023"]) }),
        Object.freeze({ id: "load-agentic-bursts", title: "Agentic bursts", body: "One user task creates clustered model and tool calls, external waits, and retry amplification.", decision: "Simulate the complete loop, stopping conditions, and failures; accept calls, P95, and cost per successful task.", boundary: "A single-model-call test cannot predict agent capacity.", sourceIds: Object.freeze(["opentelemetry-genai-semconv"]) }),
        Object.freeze({ id: "load-offline-batch", title: "Offline batch", body: "Single-request latency can be traded for larger batches and utilization, subject to deadline, fairness, and replay after failure.", decision: "Accept valid outputs completed by the deadline and cost per result; use separate parameters from online serving.", boundary: "A batch job that fills the cluster can destroy reserved online capacity.", sourceIds: Object.freeze(["nist-genai-profile"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "cloud", eyebrow: "CLOUD DELIVERY", title: "Connect the inference stack to cloud responsibilities", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "cloud-managed-serving", title: "Managed model serving", subtitle: "Model APIs, serverless inference, dedicated throughput, catalogs, and version management", body: "Reduce model loading, scaling, and engine-upgrade work while the request distribution and business value are proven.", decision: "Discover traffic variability, fixed-version needs, dedicated capacity, private networking, and region." }),
        Object.freeze({ id: "cloud-accelerated-compute", title: "Accelerated compute", subtitle: "GPU instances, bare metal, accelerators, images, and drivers", body: "Match compute and memory to model, precision, context, concurrency, and SLO for self-hosted inference.", decision: "Discover whether model, precision, sequence distributions, concurrency, and SLO are frozen." }),
        Object.freeze({ id: "cloud-elastic-orchestration", title: "Elastic orchestration", subtitle: "Managed Kubernetes, serving platforms, autoscaling, queues, and reserved capacity", body: "Unify replicas, canary release, recovery, and elasticity rather than building one script per model.", decision: "Discover cold-start tolerance, queue tolerance, burst shape, and whether the customer needs rapid scale or a warm pool." }),
        Object.freeze({ id: "cloud-data-path-observability", title: "Data path and observability", subtitle: "Storage, high-speed network, caches, tracing, GPU metrics, and cost control", body: "Accelerate loading and distribution while connecting application SLOs to engine and infrastructure behavior.", decision: "Discover whether the bottleneck is load, compute, memory, network, queue, or downstream application and whether end-to-end traces exist." }),
      ]) }),
    ]) }),
  ]),
  qa: Object.freeze([
    Object.freeze({ id: "same-model-different-speed", q: "Why can the same model run at very different speeds on different platforms?", a: "A model name does not specify the execution stack. Hardware, precision, kernels, KV-cache management, batching, parallelism, request shape, and scheduler policy all change TTFT, generation rate, and throughput.", depth: "Lock the weights or snapshot, quantization, hardware, concurrency, and input-output distribution. Report TTFT, ITL, end-to-end P95, successful throughput, and retained quality. PagedAttention demonstrates the impact of cache management, while FlashAttention demonstrates the impact of memory traffic.", ask: "Ask the customer: is the problem first-token delay, continuation speed, or queueing, and did the test use realistic contexts and concurrency?", tag: "Performance mechanism", basis: "Inference-systems research plus workload modeling", evidence: Object.freeze([Object.freeze({ sourceId: "vllm-2023", supports: "Supports KV-cache management and continuous batching as material factors in LLM-serving throughput." }), Object.freeze({ sourceId: "flashattention-2022", supports: "Supports reducing memory traffic between high-bandwidth memory and on-chip storage for exact attention." })]) }),
    Object.freeze({ id: "gpu-concurrency-capacity", q: "How many concurrent requests can one GPU serve?", a: "There is no fixed answer without a model, precision, context and output distribution, engine policy, and SLO. Capacity is constrained by weights, KV cache, workspaces, batching, and tail-latency limits.", depth: "Replay the actual length distribution and raise load in steps, recording successful concurrency within the SLO rather than the engine's configured maximum sequences. Separate short chat, long summaries, agent loops, and batch work. Record model, hardware, engine, parameters, and test date.", ask: "Ask the customer: what are P95 and timeout targets, P50 and P95 input-output lengths, peak concurrency, and burst pattern?", tag: "Capacity planning", basis: "KV-cache mechanics plus PoC load test", evidence: Object.freeze([Object.freeze({ sourceId: "vllm-2023", supports: "Supports the effect of KV-cache capacity, fragmentation, and sharing on concurrent serving." }), Object.freeze({ sourceId: "opentelemetry-genai-semconv", supports: "Provides evolving semantics for correlating model, token, latency, and error data in capacity tests." })]) }),
    Object.freeze({ id: "quantization-speed-quality", q: "If quantization makes a model smaller, is it always faster with almost no quality loss?", a: "No. A quantization method can reduce weight memory, but end-to-end speed and quality remain specific to the model, bit width, kernels, hardware, and evaluated tasks.", depth: "Treat the quantized artifact as a release candidate. Compare target-task quality, long context, structured output, tools, and safety, then measure cold start, TTFT, ITL, throughput, memory, and power on the same stack. GPTQ reports strong results for specific GPT/OPT models and A100/A6000 tests; those numbers do not transfer automatically.", ask: "Ask the customer: is the objective to fit the model, increase concurrency, or reduce accepted-outcome cost, and which quality losses are unacceptable?", tag: "Optimization boundary", basis: "Systems engineering plus task evaluation", evidence: Object.freeze([Object.freeze({ sourceId: "gptq-2023", supports: "Reports memory, accuracy, and end-to-end speed results for one weight-quantization method on specific models and hardware; it does not support universal gains." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports remeasuring quality and risk after a model or system change; it does not prove a quantization mechanism or speedup." })]) }),
    Object.freeze({ id: "vllm-platform-boundary", q: "Is vLLM already a complete inference platform, or is Kubernetes or an AI platform still needed?", a: "vLLM is an inference engine and serving runtime component, not the complete production platform. Capacity placement, identity, network, release, elasticity, recovery, observability, and cost governance remain separate responsibilities.", depth: "The engine answers how to execute model requests efficiently. The platform decides placement, GPU allocation, rollout, scaling, isolation, and recovery. The AI gateway decides which application may call which model and how a request is routed and audited.", ask: "Ask the customer: is the missing capability single-instance performance or multi-model placement, upgrades, and high availability?", tag: "Component boundary", basis: "Inference engine plus platform responsibility", evidence: Object.freeze([Object.freeze({ sourceId: "vllm-2023", supports: "Focuses vLLM on serving throughput and KV-cache management." }), Object.freeze({ sourceId: "kubernetes-dra", supports: "Documents Kubernetes structured device requests and allocation as a platform resource responsibility." })]) }),
    Object.freeze({ id: "ttft-or-throughput", q: "Should an inference service optimize TTFT or total throughput first?", a: "It depends on interaction. Chat is sensitive to the first token; batch work prioritizes completion and throughput. One metric cannot represent both.", depth: "Prefill establishes the cache from input, while decode emits tokens sequentially. Test TTFT, ITL, P95 end-to-end latency, successful throughput, and cost by input-output length, concurrency, and priority. Continuous batching must not silently sacrifice interactive tails.", ask: "Ask the customer: does the user wait before the first token, during generation, or in a queue, and what are P50 and P95 lengths?", tag: "SLO tradeoff", basis: "PagedAttention plus workload modeling", evidence: Object.freeze([Object.freeze({ sourceId: "vllm-2023", supports: "Supports the serving impact of KV-cache management and continuous batching." }), Object.freeze({ sourceId: "flashattention-2022", supports: "Supports the performance impact of attention memory traffic." })]) }),
    Object.freeze({ id: "quantization-cost", q: "Does quantization always reduce inference cost?", a: "No. Lower-bit weights can reduce memory and may improve speed on a supported stack, but cost also depends on kernels, replicas, batch, retained quality, operations, and accepted-task rate.", depth: "A smaller artifact may fit fewer devices or more sessions, yet weak kernel support or quality regression can erase end-to-end value. Compare quality, TTFT, ITL, throughput, memory, power, operations, and cost per accepted result on the same workload; do not reuse GPTQ's paper speedups as a pricing assumption.", ask: "Ask the customer: is the objective fit, speed, or unit successful cost, and does the target hardware have mature kernels?", tag: "Precision choice", basis: "Inference systems plus contextual evaluation", evidence: Object.freeze([Object.freeze({ sourceId: "gptq-2023", supports: "Reports compression and speed for specific models, bit widths, kernels, and GPUs; it does not establish end-to-end cost for another service." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports remeasurement after optimization changes; project cost still requires workload and outcome data." })]) }),
    Object.freeze({ id: "weights-fit-but-oom", q: "Why can a service run out of memory under concurrency even when the weights fit?", a: "Weights are only the fixed memory account. KV cache, activations, workspaces, fragmentation, and active sequences continue to consume memory.", depth: "Model the cache from layers, KV heads, head dimension, precision, input-output lengths, and concurrency, then add runtime workspace and safety margin. Maximum context and maximum concurrency generally cannot be delivered together; batching and cache policy change the peak.", ask: "Ask the customer: what are the real input, output, and concurrency distributions, the long-request share, and the allowed queue time?", tag: "Memory capacity", basis: "KV cache plus runtime memory", evidence: Object.freeze([Object.freeze({ sourceId: "vllm-2023", supports: "Supports the capacity impact of cache blocks, continuous batching, and fragmentation." })]) }),
    Object.freeze({ id: "quantization-throughput-not-release", q: "Why might a throughput gain after quantization still be unfit for release?", a: "Release also requires target-task quality, long-context behavior, tail latency, kernel maturity, stability, and cost per successful task.", depth: "Formats reduce memory or bandwidth differently across hardware. Quality loss may concentrate in rare, high-value slices. Compare TTFT, TPOT, throughput, rejection, energy, and critical tasks under the same load and prove the full model-tokenizer-template-engine combination can roll back.", ask: "Ask the customer: is quantization solving capacity, latency, or cost, and which task regressions are prohibited?", tag: "Quantized release", basis: "Joint performance and quality acceptance", evidence: Object.freeze([Object.freeze({ sourceId: "gptq-2023", supports: "Supports weight compression and scoped accuracy and speed results for the paper's tested models and hardware; it does not establish release fitness on another workload." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports continuing performance and risk evaluation after model change." })]) }),
    Object.freeze({ id: "batching-fairness", q: "Why can some users become slower after continuous batching increases total throughput?", a: "Long requests can consume more decode steps while short requests wait; aggregate throughput does not guarantee tail latency for every request class.", depth: "Slice queue, TTFT, and TPOT by input length, output length, priority, and tenant. Use admission, batch caps, and fairness policy, and give high-priority interactive work an independent SLO.", ask: "Ask the customer: what are request-length and priority distributions, and whose P95 regressed after the throughput change?", tag: "Scheduler fairness", basis: "Continuous batching plus tail latency", evidence: Object.freeze([Object.freeze({ sourceId: "vllm-2023", supports: "Supports continuous batching and cache management as throughput mechanisms while request latency remains scheduler-dependent." }), Object.freeze({ sourceId: "opentelemetry-genai-semconv", supports: "Supports request-level observation of generative-AI call attributes and latency." })]) }),
    Object.freeze({ id: "speculative-decoding-default", q: "Should speculative decoding be enabled by default for every model and request?", a: "No. It uses an approximation model to propose tokens for parallel verification by the target model; benefit depends on draft quality and the relative cost of proposing and verifying.", depth: "Under the same sampling policy, compare TTFT, TPOT, throughput, memory, and power across real request slices. The original paper reports 2×–3× acceleration for its T5-XXL experiments with identical outputs; treat that as scoped evidence, not a default expectation. Version and roll back the draft-target pair together.", ask: "Ask the customer: is the objective single-request latency or cluster throughput, and what is the acceptance rate on real tasks?", tag: "Speculative decoding", basis: "Acceptance rate plus extra computation", evidence: Object.freeze([Object.freeze({ sourceId: "speculative-decoding-2023", supports: "Defines draft-and-verify speculative decoding that preserves the target distribution and reports scoped acceleration on tested T5-XXL configurations." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports remeasurement after system optimization changes; it does not establish speculative-decoding mechanics or gains." })]) }),
    Object.freeze({ id: "autoscaling-timeouts", q: "Why can an autoscaled inference service still time out during a traffic peak?", a: "Model loading and cache warmup may lag traffic growth, and a reactive metric may trigger only after the queue is congested.", depth: "Distinguish ready, loading, and unavailable capacity. Use arrival rate, queue depth, service time, and load time to predict scale. Reserve warm capacity for critical models and define queue, reject, and degrade behavior instead of unlimited backlog.", ask: "Ask the customer: how long from zero to ready, can capacity be warmed before a peak, and which tasks are protected beyond capacity?", tag: "Elastic cold start", basis: "Scale latency plus admission control", evidence: Object.freeze([Object.freeze({ sourceId: "kubernetes-dra", supports: "Documents that devices must be requested and allocated; allocation alone does not mean a model is ready to serve." }), Object.freeze({ sourceId: "opentelemetry-semconv", supports: "Provides semantics used to correlate queue, call, and service stages." })]) }),
    Object.freeze({ id: "multi-tenant-kv-cache", q: "Can a shared multi-tenant inference instance leak another customer's content through the KV cache?", a: "A correct implementation should not, but isolation must be tested rather than assumed across cache addressing, reuse, cleanup, diagnostics, and failure paths.", depth: "Distinguish prefix cache, session cache, and ordinary KV cache. Include tenant and complete context identity in cache keys. Permit cross-tenant reuse only for explicitly public content, and control logs, debug interfaces, and memory dumps. Regress isolation on upgrades.", ask: "Ask the customer: which caches persist across requests, and where does tenant identity enter cache keys and cleanup?", tag: "Cache security", basis: "Cache isolation plus multi-tenant validation", evidence: Object.freeze([Object.freeze({ sourceId: "vllm-2023", supports: "Supports block-level management of shared inference memory." }), Object.freeze({ sourceId: "nist-zero-trust", supports: "Supports continuous authorization and isolation for shared resources." })]) }),
    Object.freeze({ id: "average-latency-insufficient", q: "Why is average response time insufficient to locate an inference slowdown?", a: "It combines queueing, first-token delay, and token-by-token generation and hides long requests and tail congestion.", depth: "Record queue time, TTFT, TPOT, output length, rejection, and completion, sliced by model, version, tenant, and length. Slow first token points to queue or prefill; slow continuation points to decode or scheduling; the fixes differ.", ask: "Ask the customer: does slowness occur before the first token or during generation, and at which lengths and concurrency does P95 fail first?", tag: "Performance attribution", basis: "Phase metrics plus workload slices", evidence: Object.freeze([Object.freeze({ sourceId: "opentelemetry-genai-semconv", supports: "Provides evolving generative-AI attributes for correlating model, usage, and latency." }), Object.freeze({ sourceId: "vllm-2023", supports: "Supports the joint impact of prefill, decode, batching, and cache on serving." })]) }),
    Object.freeze({ id: "maximum-context-admission", q: "If the service supports a maximum context length, should every request be allowed to use it?", a: "Not by default. One extremely long request can consume substantial compute and KV cache and displace other users.", depth: "Set input, output, concurrency, and total-token budgets by tenant and task, with length-aware admission and queues. Validate quality, cost, and deadlines separately for genuine long-context work; otherwise use retrieval, summarization, asynchronous processing, or rejection.", ask: "Ask the customer: is the maximum window an occasional exception or normal load, and how much shared capacity may one long request consume?", tag: "Admission control", basis: "Context budget plus shared capacity", evidence: Object.freeze([Object.freeze({ sourceId: "vllm-2023", supports: "Supports the relationship among KV-cache consumption, sequence length, and concurrency." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports configuring controls to the context of use and system limitations." })]) }),
  ]),
  evidenceCards: Object.freeze([
    Object.freeze({ id: "kv-cache-serving-capacity", metric: "KV Cache", title: "Serving capacity is not determined by weights alone", finding: "The vLLM paper uses PagedAttention to manage dynamic KV-cache memory and addresses fragmentation, sharing, and continuous batching.", boundary: "Results are limited to the tested models, hardware, engine, and workloads and require validation on the target stack.", sourceId: "vllm-2023", accent: true }),
    Object.freeze({ id: "io-aware-attention", metric: "IO-aware", title: "Attention optimization is also a memory-systems problem", finding: "FlashAttention computes exact attention while reducing transfers between high-bandwidth and on-chip memory.", boundary: "It optimizes attention computation; it does not independently solve scheduling, KV-cache capacity, or application quality.", sourceId: "flashattention-2022" }),
    Object.freeze({ id: "inference-latency-components", metric: "Four latency views", title: "Average response time cannot accept inference", finding: "A service should separate first-token, inter-token, queue, and end-to-end latency and relate them to model, token usage, and error state.", boundary: "Specific SLOs must be defined independently for interactive, batch, and agentic work.", sourceId: "opentelemetry-genai-semconv" }),
  ]),
  terms: Object.freeze({
    token: Object.freeze({ name: "Token", definition: "A discrete unit consumed or generated by a model; its counts shape context use, memory, latency, throughput, and cost." }),
    "context-window": Object.freeze({ name: "Context Window", definition: "The token range accepted by one model operation; a maximum is not a guaranteed effective or affordable workload size." }),
    "kv-cache": Object.freeze({ name: "Key-Value Cache", abbr: "KV Cache", definition: "Attention Keys and Values retained across autoregressive decoding to avoid recomputing the complete prefix; memory grows with architecture, sequence length, precision, and active requests." }),
    "llm-inference": Object.freeze({ name: "LLM Inference", definition: "Execution of trained model weights to produce online or batch output under scheduling, memory, latency, and quality constraints." }),
    batching: Object.freeze({ name: "Dynamic Batching", definition: "Combining waiting or active inference requests into shared execution batches to improve throughput while controlling latency and fairness." }),
    quantization: Object.freeze({ name: "Quantization", definition: "Representing weights or activations with lower precision to reduce memory or computation, subject to kernel and quality validation." }),
    ttft: Object.freeze({ name: "Time to First Token", abbr: "TTFT", definition: "Elapsed time from request start until the first generated token reaches the measured boundary." }),
    tpot: Object.freeze({ name: "Time per Output Token", abbr: "TPOT", definition: "Average time for each generated token after the first; closely related to inter-token latency." }),
    vram: Object.freeze({ name: "Video Random Access Memory", abbr: "VRAM", definition: "Accelerator memory that holds weights, activations, caches, workspaces, and intermediate state." }),
    hbm: Object.freeze({ name: "High Bandwidth Memory", abbr: "HBM", definition: "High-capacity, high-throughput memory colocated with accelerators and central to model fit and data movement." }),
  }),
  sources: Object.freeze({
    "vllm-2023": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "vLLM / PagedAttention", note: "Introduces PagedAttention and KV-cache memory management for LLM serving. Reported throughput remains specific to the tested models, hardware, engine version, and workloads." }),
    "flashattention-2022": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "FlashAttention", note: "Presents an IO-aware exact-attention algorithm that reduces memory traffic. Realized benefit depends on sequence shape, hardware, kernels, and the surrounding training or inference stack." }),
    "opentelemetry-genai-semconv": Object.freeze({ kind: "Open observability specification", shortTitle: "OpenTelemetry GenAI Conventions", note: "Defines evolving telemetry conventions for generative-AI clients, retrieval, agents, MCP, and tool calls. Pin the adopted version and add business outcomes, authorization, approvals, evidence coverage, and risk fields." }),
    "nist-genai-profile": Object.freeze({ kind: "Official risk-management framework", shortTitle: "NIST AI 600-1", note: "Organizes generative-AI risk management around Govern, Map, Measure, and Manage. It is a voluntary resource, not a product certification, universal compliance checklist, model ranking, retrieval benchmark, or hardware-performance standard." }),
    "kubernetes-dra": Object.freeze({ kind: "Official technical documentation", shortTitle: "Kubernetes DRA", note: "Documents device requests, selection, preparation, and allocation through structured claims and drivers. Feature state, API fields, driver maturity, and vendor support depend on Kubernetes and implementation versions." }),
    "opentelemetry-semconv": Object.freeze({ kind: "Open observability specification", shortTitle: "OpenTelemetry SemConv", note: "Defines common semantics for traces, metrics, logs, events, and resources. AI-specific progress, data lineage, business outcomes, approvals, and risk evidence still require project-defined fields." }),
    "nist-zero-trust": Object.freeze({ kind: "Official architecture guidance", shortTitle: "NIST SP 800-207", note: "Defines resource access around explicit, current identity and policy evaluation rather than implicit trust from network location." }),
    "vllm-prefix-caching": Object.freeze({ kind: "Official engineering documentation", shortTitle: "vLLM Prefix Caching", note: "Documents automatic prefix caching that reuses KV cache for matching prefixes. It primarily reduces matched prefill work and does not reduce decode of newly generated tokens." }),
    "distserve-2024": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "DistServe", note: "Studies prefill-decode disaggregation and Goodput under TTFT and TPOT constraints. Reported gains depend on model, hardware, network, scheduler, and request distribution." }),
    "gptq-2023": Object.freeze({ kind: "Primary research paper", shortTitle: "GPTQ", note: "Presents one-shot weight quantization and reports compression, accuracy, and speed on specific GPT/OPT models and A100/A6000 configurations. Results do not generalize across models, bit widths, kernels, hardware, tasks, or service economics." }),
    "speculative-decoding-2023": Object.freeze({ kind: "Peer-reviewed paper", shortTitle: "Speculative Decoding", note: "Defines a draft-and-verify method that preserves the target distribution and reports acceleration on specific T5-XXL experiments. Gains depend on the approximation model, hardware, and implementation." }),
  }),
});
