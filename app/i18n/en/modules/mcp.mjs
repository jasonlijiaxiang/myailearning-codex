const freezeItems = (items) => Object.freeze(items.map((item) => Object.freeze(item)));
const block = (type, title, items, extra = {}) => Object.freeze({ type, title, ...extra, items: freezeItems(items) });
const section = (id, eyebrow, title, lead, blocks) => Object.freeze({ id, eyebrow, title, lead, blocks: Object.freeze(blocks) });
const question = (id, copy) => Object.freeze({ ...copy, id, evidence: freezeItems(copy.evidence) });

export const englishModule = Object.freeze({
  slug: "mcp",
  title: "Model Context Protocol",
  subtitle: "Standardize capability exchange while keeping trust and execution in authoritative systems",
  definition: "The Model Context Protocol (MCP) defines a common way for AI applications to discover and interact with externally provided Tools, Resources, and Prompts, reducing pairwise integration between clients and capability providers.",
  position: "This module sits in the protocol and interoperability layer. MCP lets a Host application connect to capabilities exposed by Servers; an Agent is not an MCP protocol role. It reuses rather than replaces business APIs, identity providers, authorization, gateways, transaction semantics, and security controls.",
  relatedSlugs: Object.freeze(["ai-agent", "a2a", "ai-gateway", "security", "ai-ops"]),
  sections: Object.freeze([
    section("mcp-adoption-decision", "REUSE BEFORE PROTOCOL", "Adopt a protocol only when repeated integration justifies it", "Use one enterprise ticketing service consumed by a customer-support agent, an employee assistant, and an operations assistant. MCP is justified only when a common capability contract removes enough repeated discovery, schema, invocation, and lifecycle work to cover protocol, testing, security, and operating cost.", [
      block("table", "Choose the smallest connection contract", [
        { id: "mcp-adoption-direct-api", title: "Direct API", cells: ["One application owns a few stable integrations", "Keep the application and business API contract direct", "Do not add a protocol layer without measured reuse"] },
        { id: "mcp-adoption-function-calling", title: "Function calling", cells: ["A model needs typed selection and arguments inside one application", "Expose a small application-owned tool set", "Provider tool syntax does not standardize independent capability providers"] },
        { id: "mcp-adoption-thin-adapter", title: "Thin MCP adapter", cells: ["Multiple hosts or provider teams repeat the same connection work", "Wrap stable business APIs and preserve their identity, authorization, idempotency, and audit", "The adapter must not become a second source of business policy or transaction truth"] },
      ], { columns: ["Customer signal", "Recommended contract", "Boundary"] }),
      block("cards", "Ticketing reuse must survive its full lifecycle", [
        { id: "mcp-adoption-value", title: "Value ledger", body: "Measure eliminated client-specific adapters, onboarding time, contract-test reuse, and governed cross-client adoption.", decision: "Compare those gains against Server engineering, admission, security, telemetry, versioning, support, and retirement.", boundary: "Tool count and registry presence do not establish a return on the integration." },
        { id: "mcp-adoption-authority", title: "Authority remains downstream", body: "The existing ticketing API continues to validate user or workload identity, tenant, operation, idempotency, and final ticket state.", decision: "The MCP Server translates the capability contract; it does not duplicate business rules.", boundary: "A successful protocol exchange is not an authorized or completed ticket operation." },
        { id: "mcp-adoption-gate", title: "Go, hold, or no-go", body: "Pilot a few narrow read and controlled-write capabilities across at least two real clients.", decision: "Proceed only when reuse and operability evidence exceed the added control cost.", boundary: "For one client and fixed APIs, direct integration may remain the better production design." },
      ]),
    ]),
    section("mcp-protocol-model", "PROTOCOL MODEL", "The protocol standardizes interaction; the host still owns trust and execution", "MCP 2026-07-28 is the current final specification. It replaces the handshake-and-session lifecycle used by 2025-11-25; publication establishes the normative baseline, but each SDK, Client, Server, gateway, and extension still needs compatibility evidence.", [
      block("cards", "Six operating principles", [
        { id: "principle-host-client-server", title: "Host, Client, and Server", body: "The Host owns experience, policy, and connection aggregation. Under the current 2026-07-28 specification, a Client exchanges self-contained requests with a Server; protocol sessions apply only when supporting legacy revisions such as 2025-11-25.", decision: "Assign identity, consent, policy, execution, and audit to explicit components rather than the protocol name." },
        { id: "principle-tools-resources-prompts", title: "Tools, Resources, and Prompts", body: "Tools are model-controlled callable operations, Resources are application-driven addressable context, and Prompts are user-controlled templates authored by Servers. A Tool may be read-only or perform writes, and any primitive may carry sensitive or untrusted content.", decision: "Choose by controlling actor, then classify data scope, sensitivity, side effects, authorization, idempotency, and audit separately." },
        { id: "principle-versioned-lifecycle", title: "Versioned lifecycle and capability negotiation", body: "MCP 2026-07-28 uses stateless, self-contained requests. Every request MUST carry protocolVersion and clientCapabilities; Clients SHOULD also use clientInfo to self-report software name and version metadata. Servers MUST implement server/discover, which Clients may call for optional up-front discovery; a dual-stack stdio Client SHOULD use it first to probe support for the current protocol. Revisions through 2025-11-25 use initialize/initialized and protocol sessions.", decision: "Pin the production baseline and maintain separate contract suites for modern and legacy protocol eras; never treat clientInfo as authenticated identity." },
        { id: "principle-transport-trust-boundary", title: "Transport changes the trust boundary", body: "stdio commonly connects a controlled local child process; Streamable HTTP supports remote services and introduces network, identity, tenant, gateway, and callback risks.", decision: "Repeat threat modeling, authentication, authorization, rate limiting, and audit when moving from local to remote." },
        { id: "principle-mcp-function-calling", title: "MCP complements function calling", body: "Function calling describes how a model proposes a tool and arguments. MCP standardizes how a host discovers and communicates with capability providers.", decision: "Use direct function calling for a few stable tools in one application; add MCP when reuse across clients or providers offsets protocol and governance cost." },
        { id: "principle-stateless-interaction", title: "Stateless does not mean interaction-free", body: "A 2026-07-28 request is self-contained, while Multi Round-Trip Requests can collect missing input, the Tasks extension can carry durable execution for its currently supported tools/call method, and scoped cache metadata can guide response reuse.", decision: "Model MRTR state, Task handles, private caches, and authoritative business state as separate contracts rather than one implicit session." },
      ]),
      block("table", "Primitive type and risk class are separate axes", [
        { id: "primitive-model-controlled-tool", title: "Tool · model-controlled", cells: ["The model may request a database query, API call, or computation", "Declare read/write class, data, authority, side effects, idempotency, and postconditions", "Selection and schema validity do not grant business permission"], sourceIds: ["mcp-tools-2026-07-28"] },
        { id: "primitive-application-driven-resource", title: "Resource · application-driven", cells: ["The Host decides how URI-addressed context is discovered and included", "Control visibility, ACLs, versions, subscriptions, caches, and context assembly", "Context may still contain sensitive, stale, or instruction-like content"], sourceIds: ["mcp-resources-2026-07-28"] },
        { id: "primitive-user-controlled-prompt", title: "Prompt · user-controlled", cells: ["A user chooses a reusable Server-authored template and supplies arguments", "Govern publisher, purpose, parameters, version, presentation, and consent", "User selection does not make the template trusted or authorize later Tools"], sourceIds: ["mcp-prompts-2026-07-28"] },
      ], { columns: ["Protocol control", "Production control", "Cannot be inferred"] }),
      block("table", "Stateless requests separate four state contracts", [
        { id: "state-self-contained-request", title: "Self-contained request", cells: ["Each request MUST carry protocolVersion and clientCapabilities; Clients SHOULD also include self-reported clientInfo name and version metadata", "Map request, Run, MCP Task, and downstream operation identifiers explicitly", "clientInfo is not authenticated identity, and removing the protocol session does not remove application or business state"], sourceIds: ["mcp-specification-2026-07-28"] },
        { id: "state-mrtr-input", title: "MRTR input", cells: ["prompts/get, resources/read, and tools/call may return input_required. inputRequests and an opaque requestState are each optional, but every InputRequiredResult MUST contain at least one of them; the Client retries the original operation with a new JSON-RPC id, submits corresponding inputResponses only for received inputRequests, echoes requestState unchanged only when supplied, and MUST NOT invent it", "Limit rounds and lifetime, bind any supplied state to the original user and operation, validate inputs, and re-evaluate current authorization", "requestState is neither a session nor authority, and MRTR is not a durable Task"], sourceIds: ["mcp-mrtr-2026-07-28"] },
        { id: "state-scoped-cache", title: "Scoped result cache", cells: ["resultType: complete results for server/discover, tools/list, prompts/list, resources/list, resources/templates/list, and resources/read require ttlMs and public or private cacheScope; input_required results and MRTR retries carrying inputResponses or requestState MUST NOT be cached", "Key private caches by authorization context such as access token, subject, tenant, scope, Server, protocol, and capability version", "TTL is a freshness hint, not consistency, revocation propagation, or tenant isolation"], sourceIds: ["mcp-list-cache-2026-07-28"] },
        { id: "state-http-routing", title: "HTTP routing metadata", cells: ["Every Client POST carrying a JSON-RPC request over Streamable HTTP requires Mcp-Method. This revision does not define that header for notification POSTs, and the core protocol defines no Client-to-Server notifications over Streamable HTTP. Core Mcp-Name mirrors params.name for tools/call and prompts/get or params.uri for resources/read; the Tasks extension additionally requires params.taskId for tasks/get, tasks/update, and tasks/cancel", "Reject header/body mismatch, then apply gateway policies scoped by identity, method, capability, and tenant", "Routing metadata is not authentication, authorization, or business policy"], sourceIds: ["mcp-http-routing-2026-07-28", "mcp-tasks-extension", "nist-zero-trust"] },
      ], { columns: ["Protocol mechanism", "Production contract", "Cannot be inferred"] }),
      block("boundary", "Critical boundary", [{ id: "critical-mcp-boundary", title: "Interoperability does not create trust", body: "MCP standardizes discovery and invocation. It does not grant authority, establish a supplier's identity, validate business semantics, or make an action safe.", boundary: "Preserve the API gateway, identity, policy, transaction, validation, and audit controls that existed before MCP." }]),
    ]),
    section("mcp-curriculum", "COURSE MAP", "Learn the current contract and its legacy compatibility boundary", "Treat 2026-07-28 as the normative protocol and 2025-11-25 as a legacy migration contract. Every implementation claim must still identify both the protocol revision and the actual product support it describes.", [
      block("cards", "Ten course chapters", [
        { id: "curriculum-protocol-roles", title: "Host, Client, and Server roles", body: "The Host owns experience and security decisions, the Client speaks MCP to a Server, and the Server exposes capabilities. The current revision removes protocol-level sessions; legacy 2025-11-25 connections retain them without changing the three logical responsibilities.", decision: "Assign runtime, upgrade, authentication, and audit ownership to every role and protocol version.", boundary: "Protocol roles are not a deployment topology, and a stateless core does not remove application or business state.", sourceIds: ["mcp-architecture", "mcp-lifecycle-2025-11-25", "mcp-specification-2026-07-28"] },
        { id: "curriculum-server-primitives", title: "Tools, Resources, and Prompts", body: "Tools are model-controlled calls, Resources application-driven URI context, and Prompts user-controlled templates. A Tool may query data without writing, while Resources and Prompts may still carry sensitive or hostile content.", decision: "Select by controlling actor, then classify data access, side effects, authorization, idempotency, and audit independently.", boundary: "Primitive type is neither a read/write label nor a security rating.", sourceIds: ["mcp-tools-2026-07-28", "mcp-resources-2026-07-28", "mcp-prompts-2026-07-28"] },
        { id: "curriculum-version-discovery", title: "Versioned lifecycle and capability discovery", body: "The current 2026-07-28 revision requires protocolVersion and clientCapabilities on every request, says Clients SHOULD use clientInfo for self-reported software name and version metadata, and requires Servers to implement server/discover. Clients may use discovery to prefetch capabilities; a dual-stack stdio Client SHOULD use it first to probe current-protocol support. Legacy revisions through 2025-11-25 negotiate through initialize/initialized and a session.", decision: "Manage protocol version separately from business-capability and Tool-schema versions; build independent contract suites.", boundary: "clientInfo is not authenticated identity. A published specification does not prove implementation support, and successful discovery does not prove business semantics or authorization.", sourceIds: ["mcp-lifecycle-2025-11-25", "mcp-specification-2026-07-28", "mcp-changelog-2026-07-28"] },
        { id: "curriculum-transport-deployment", title: "stdio and Streamable HTTP", body: "stdio usually binds a local child process to the operating-system user, while Streamable HTTP supports remote shared services. The current revision uses stateless requests; a legacy 2025-11-25 remote implementation may rely on a protocol session.", decision: "Pin the protocol version, draw the trust boundary, then select transport and state management.", boundary: "The absence of a protocol session does not mean the absence of application state, and local-development assumptions cannot be copied into a remote service.", sourceIds: ["mcp-lifecycle-2025-11-25", "mcp-specification-2026-07-28", "mcp-authorization"] },
        { id: "curriculum-identity-authorization", title: "Identity, authorization, and consent", body: "An authenticated Client principal, end-user identity, Server identity, resource audience, tenant, and scope can all differ. clientInfo is only self-reported software metadata, not an authenticated principal. The 2026-07-28 specification adds issuer validation, client application-type declaration, and credential-binding requirements.", decision: "Make every high-impact call traceable to a subject, authorization basis, resource, parameters, and necessary confirmation.", boundary: "Normative requirements do not prove that every implementation supports them; neither OAuth success nor a clientInfo claim establishes business authorization inside a Tool.", sourceIds: ["mcp-authorization", "mcp-specification-2026-07-28", "nist-zero-trust"] },
        { id: "curriculum-elicitation", title: "Elicitation and sensitive interaction", body: "Legacy 2025-11-25 lets a Server initiate Form or URL Elicitation directly. Under the current MRTR design, prompts/get, resources/read, and tools/call may return input_required. inputRequests and an opaque requestState are each optional, but every InputRequiredResult MUST contain at least one of them. The Client retries the original operation with a new JSON-RPC id, submits corresponding inputResponses only for received inputRequests, echoes requestState unchanged only when supplied, and MUST NOT invent it.", decision: "Select the interaction flow by protocol revision and sensitivity; test rejection, round limits, state binding, cancellation, and resumption.", boundary: "requestState is not a session or authorization, and no input flow replaces third-party authorization or business postconditions.", sourceIds: ["mcp-elicitation-2025-11-25", "mcp-specification-2026-07-28", "mcp-mrtr-2026-07-28", "mcp-authorization"] },
        { id: "curriculum-cache-http-routing", title: "Cache scope and HTTP gateway routing", body: "The current revision requires ttlMs and public/private cacheScope on resultType: complete results for server/discover, tools/list, prompts/list, resources/list, resources/templates/list, and resources/read; resultType: input_required intermediate results and MRTR retries carrying inputResponses or requestState MUST NOT be cached. Every Client POST carrying a JSON-RPC request over Streamable HTTP requires Mcp-Method. This revision does not define that header for notification POSTs, and the core protocol defines no Client-to-Server notifications over Streamable HTTP. Core Mcp-Name mirrors params.name for tools/call and prompts/get or params.uri for resources/read; the Tasks extension additionally requires params.taskId for tasks/get, tasks/update, and tasks/cancel.", decision: "Bind private cache keys to the authorization context such as the access token, subject, tenant, scope, Server, protocol revision, and capability version; reject header/body mismatch before applying gateway authorization, rate limits, and audit.", boundary: "TTL is not a consistency or revocation guarantee, `public` does not mean trusted, and routing headers do not grant authority.", sourceIds: ["mcp-list-cache-2026-07-28", "mcp-http-routing-2026-07-28", "mcp-tasks-extension", "mcp-specification-2026-07-28", "nist-zero-trust"] },
        { id: "curriculum-durable-requests", title: "Long calls and Tasks", body: "MCP 2026-07-28 defines Tasks as an opt-in extension. Currently only tools/call may return CreateTaskResult; the Client then uses the durable handle, tasks/get, tasks/update, and tasks/cancel, while the specification leaves room for future request types. The legacy 2025-11-25 contract placed experimental Tasks in the core protocol.", decision: "Use Tasks only after the Client and Server advertise the correct version-specific support, the operation is a currently supported tools/call, and the business truly requires durable work.", boundary: "Do not assume that any MCP request can become a Task. Cancellation is cooperative and does not prove downstream side effects stopped; an MCP tools/call Task is also not an A2A inter-agent Task.", sourceIds: ["mcp-tasks-2025-11-25", "mcp-tasks-extension", "a2a-spec-1-0-0"] },
        { id: "curriculum-production-supply-chain", title: "Production engineering and supply chain", body: "Schema versioning, timeout, idempotency, rate limits, sandboxing, logging, revocation, key rotation, and Server provenance determine operability. A public Server is an executable supply-chain component.", decision: "Maintain an approved catalog, ownership, isolation, update, and emergency-disable process.", boundary: "Discovery and a successful call do not prove reversibility, correctness, or supplier trust.", sourceIds: ["mcp-security", "nist-zero-trust"] },
        { id: "curriculum-failure-observability", title: "Failure semantics and observability", body: "A Host must distinguish invalid arguments, denied authorization, temporary failure, timeout, partial result, and unknown business state and correlate the request, Tool, identity, Server version, and downstream operation.", decision: "Validate retry safety and auditability before shared reuse.", boundary: "A protocol error is not the business transaction state, and a timeout does not prove that an action did not occur.", sourceIds: ["mcp-architecture", "opentelemetry-genai-semconv"] },
      ]),
    ]),
    section("mcp-decisions", "PRESALES DECISIONS", "Adopt MCP only where the reuse and governance boundary are explicit", "The protocol should be a thin interoperability layer over authoritative business services, not a parallel source of policy and transaction truth.", [
      block("table", "Six recurring decisions", [
        { id: "decision-need-mcp", title: "Does the system actually need MCP?", cells: ["The same capability serves multiple AI clients or one client integrates many independent providers and repeats discovery and invocation adapters.", "Add a thin MCP adapter over existing APIs, starting with high-value, narrow, well-governed capabilities.", "For one application with a few fixed APIs, direct function calling may remain simpler."] },
        { id: "decision-wrap-or-rewrite", title: "Wrap the existing API or rewrite the service?", cells: ["The existing API already has stable semantics, authorization, idempotency, and audit, but lacks MCP discovery.", "Add a thin adapter and keep the business API authoritative; refactor only when the underlying contract itself is defective.", "An MCP Server must not duplicate business rules or bypass the original authorization system."] },
        { id: "decision-primitive-choice", title: "Tool, Resource, or Prompt?", cells: ["The capability is invoked by the model, selected by the application as URI context, or chosen by the user as a reusable template; read/write is classified separately.", "Use a Tool for model-controlled operations, a Resource for application-driven context, and a Prompt for user-controlled templates. A Tool may be read-only or perform writes.", "Primitive type cannot replace sensitivity, side-effect, authorization, idempotency, or business-risk classification."] },
        { id: "decision-remote-production", title: "How does a remote Server enter production?", cells: ["Service identity, user authorization, tenant isolation, Tool risk classes, quotas, versions, and audit are defined.", "Use a gateway for authentication, authorization, allowlists, rate limits, version control, logs, and incident isolation.", "Registry presence and successful connection are not enterprise security approval."] },
        { id: "decision-stdio-http", title: "stdio or Streamable HTTP?", cells: ["The Server is either a controlled local tool or a shared multi-user, multi-host service.", "Prefer stdio for a controlled local process; use HTTP with enterprise identity, gateway, rate limits, and audit for remote sharing.", "stdio is not inherently safe and HTTP does not inherently authenticate; the full deployment boundary determines security."] },
        { id: "decision-evaluate-rc", title: "How should a 2025-11-25 deployment migrate to 2026-07-28?", cells: ["A team wants to switch immediately because the final specification now defines stateless requests, extensions, and authorization changes.", "Use 2026-07-28 as the protocol baseline, but validate each SDK, Client, Server, gateway, and private extension in an isolated compatibility track before rollout.", "Final-spec publication is not ecosystem compatibility; coexistence, canary, and rollback gates still apply."] },
      ], { columns: ["Customer signal", "Recommended action", "Boundary"] }),
    ]),
    section("mcp-practice", "LEARN, PRACTICE, PROVE", "Build a production method that survives protocol and Tool changes", "Learning treats the 2026-07-28 final contract, legacy compatibility, trust, failure, and Server lifecycle as separate concerns.", [
      block("cards", "Learning outcomes", [
        { id: "outcome-mcp-roles-primitives", title: "Explain roles and primitives", body: "Separate Host, Client, and Server responsibilities and select among Tools, Resources, and Prompts." },
        { id: "outcome-current-versus-rc", title: "Separate the current revision from legacy behavior", body: "Describe 2026-07-28 as the normative protocol while naming any 2025-11-25 session or Tasks behavior retained for compatibility." },
        { id: "outcome-state-contracts", title: "Separate interaction state contracts", body: "Distinguish self-contained requests, MRTR input, durable Tasks, scoped caches, and authoritative business state." },
        { id: "outcome-mcp-trust-boundary", title: "Select transport and trust boundary", body: "Distinguish local process risk from remote service identity, network, and tenant controls." },
        { id: "outcome-discovery-authorization", title: "Separate discovery from authorization", body: "Treat Server metadata as a candidate and authorize each resource and action independently." },
        { id: "outcome-mcp-release-gate", title: "Define a remote MCP release gate", body: "Prove identity, contract stability, failure semantics, observability, supply-chain ownership, and recovery." },
      ]),
      block("steps", "Recommended learning route", [
        { id: "route-protocol-objects-versions", title: "Understand protocol objects and versions", body: "Learn Tools, Resources, and Prompts and distinguish the current 2026-07-28 lifecycle from legacy 2025-11-25 behavior.", decision: "Checkpoint: every claim names the protocol version it describes." },
        { id: "route-map-trust-chain", title: "Map the invocation and trust chain", body: "Trace user, Host, Client, Server, authorization service, and downstream system identities and data.", decision: "Checkpoint: identify who authenticates, authorizes, validates, and audits each step." },
        { id: "route-separate-state-contracts", title: "Separate state and routing contracts", body: "Use MRTR for explicit input, Tasks for durable execution, cacheScope for cache isolation, and Mcp-Method/Mcp-Name for verifiable gateway routing.", decision: "Checkpoint: name who issues, stores, isolates, validates, and expires every state handle and cache entry." },
        { id: "route-production-controls", title: "Complete production controls", body: "Add schema versioning, timeout, idempotency, rate limits, revocation, isolation, and supply-chain governance.", decision: "Checkpoint: treat a remote Server as a privileged integration, not a harmless plugin." },
        { id: "route-failure-recovery", title: "Validate failure and recovery", body: "Define errors, timeouts, retries, cancellation, and business-state queries for read, write, and long-running Tools.", decision: "Checkpoint: the Host distinguishes retryable failure from unknown business outcome." },
        { id: "route-catalog-retirement", title: "Establish catalog and retirement ownership", body: "Register owner, version, provenance, compatibility, approval, update, vulnerability, and disable process.", decision: "Checkpoint: no Server becomes an ownerless executable dependency." },
      ]),
      block("cards", "Practice labs", [
        { id: "lab-wrap-order-api", title: "Wrap an existing read-only API", subtitle: "Multiple agent clients need one order-query capability", body: "Define the query as a model-controlled, read-only Tool with explicit schema and errors; propagate caller identity and least privilege; add timeout, audit, and sensitive-field filtering.", decision: "Deliverable: protocol contract, invocation sequence, and security checklist.", boundary: "Completion criterion: a read-only Tool can be discovered but cannot bypass original API permissions, and failure and revocation paths are testable.", sourceIds: ["mcp-tools-2026-07-28", "mcp-authorization", "mcp-security"] },
        { id: "lab-local-remote-deployment", title: "Compare local and remote deployment", subtitle: "The same Server can run through stdio or Streamable HTTP", body: "Draw process, network, and credential boundaries; compare update, isolation, observability, and blast radius; choose for development, controlled desktop, and shared service.", decision: "Deliverable: deployment decision record and migration triggers.", boundary: "Completion criterion: local assumptions are not copied into the remote service.", sourceIds: ["mcp-architecture", "mcp-security", "nist-zero-trust"] },
        { id: "lab-mcp-version-migration", title: "Review an MCP version migration", subtitle: "A 2025-11-25 implementation is moving to the current 2026-07-28 revision", body: "Freeze Client, Server, SDK, gateway, and extension inventory; compare initialize, protocol sessions, per-request metadata, server/discover, Tasks, and authorization; define isolated validation, ecosystem-support checks, coexistence, and rollback.", decision: "Deliverable: a version-separated impact matrix and validation plan.", boundary: "Completion criterion: normative status and product compatibility are recorded separately, and every breaking change has an owner and gate.", sourceIds: ["mcp-lifecycle-2025-11-25", "mcp-changelog-2026-07-28", "mcp-tasks-extension"] },
        { id: "lab-stateless-input-cache-routing", title: "Test stateless input, cache, and gateway routing", subtitle: "A multi-tenant remote Server needs input during execution, caches capability lists, and routes through a policy gateway", body: "Exercise MRTR with a new JSON-RPC id and bind requestState to the user and operation when the Server provides it; test public/private cacheScope, TTL, revocation, and tenant isolation; verify Mcp-Method and any applicable Mcp-Name against the body before applying method- and capability-aware policies.", decision: "Deliverable: MRTR sequence, cache-key model, gateway policy, and failure cases.", boundary: "Completion criterion: no implicit session is required, and input, cache, routing metadata, and business state never substitute for authority.", sourceIds: ["mcp-mrtr-2026-07-28", "mcp-list-cache-2026-07-28", "mcp-http-routing-2026-07-28", "nist-zero-trust"] },
        { id: "lab-enterprise-server-review", title: "Review an enterprise MCP Server", subtitle: "A ticketing capability will be shared by multiple agent clients", body: "Review primitive boundaries, schema, read/write separation, identity chain, timeout, duplicate submission, incompatibility, and retirement.", decision: "Deliverable: an MCP Server release review package.", boundary: "Completion criterion: the package proves who may invoke what, how failures recover, and who owns long-term maintenance.", sourceIds: ["mcp-architecture", "mcp-authorization", "mcp-security"] },
      ]),
    ]),
    section("mcp-trust-chain", "REMOTE MCP TRUST CHAIN", "Turn capability invocation into a verifiable authorization and execution chain", "The model may propose intent and arguments. Deterministic systems must still verify Server, version, caller, resource, action, and business outcome.", [
      block("steps", "Five trust boundaries in a remote Tool call", [
        { id: "trust-discover-verify-server", title: "Discover and verify the Server", body: "The Host obtains an endpoint and metadata and verifies publisher, domain, version, transport, artifact, and approved environment.", decision: "Use a managed private catalog, DNS, certificates, signed software, and admission workflow; treat third-party catalogs as leads.", boundary: "Resolving an endpoint or reading a Tool description does not make the service trusted.", sourceIds: ["mcp-architecture", "mcp-security"] },
        { id: "trust-pin-version-capability", title: "Pin version before capability", body: "The current revision declares version and capabilities on each self-contained request and supports server/discover; legacy revisions negotiate through initialize/initialized and a protocol session.", decision: "Maintain separate contract suites and log the exact protocol, Server, extension, and Tool-schema versions.", boundary: "A final specification is not proof of product support, and one earlier request cannot establish permanent capability.", sourceIds: ["mcp-lifecycle-2025-11-25", "mcp-specification-2026-07-28"] },
        { id: "trust-authorization-context", title: "Establish user or workload authorization", body: "Bind caller, resource audience, scope, tenant, and lifetime to the protected resource.", decision: "Use enterprise OAuth/OIDC or workload identity and short-lived credentials; the resource service validates access.", boundary: "The model's claim to represent a user and the Host's ability to connect are not authorization.", sourceIds: ["mcp-authorization", "nist-zero-trust"] },
        { id: "trust-policy-execution", title: "Apply policy before execution", body: "The model proposes Tool and arguments; the application enforces allowlist, schema, policy, confirmation, rate limit, and business validation.", decision: "Use gateway, policy engine, WAF, and authoritative API; preview high-impact changes before confirmation.", boundary: "A valid Tool schema proves shape, not authority or semantic correctness.", sourceIds: ["mcp-security", "nist-zero-trust"] },
        { id: "trust-result-audit", title: "Validate result and correlate audit", body: "A Tool result can contain errors, sensitive data, or prompt injection. Record execution outcome and filter before returning content to the model.", decision: "Correlate trace, Tool version, caller, policy decision, downstream state, and security events.", boundary: "HTTP success and valid JSON do not establish business success or trustworthy content.", sourceIds: ["mcp-security", "opentelemetry-genai-semconv"] },
      ]),
      block("table", "The same Server has different deployment risk", [
        { id: "deployment-local-tool", title: "Local developer tool", cells: ["A Host launches a controlled process over stdio; risks include files, command execution, and dependencies.", "Use sandboxing, directory allowlists, process isolation, minimal file rights, and pinned dependencies.", "Local execution does not automatically restrict what the process can read or execute."] },
        { id: "deployment-internal-service", title: "Internal shared service", cells: ["Multiple users and applications share identity, tenant, quota, and version control planes.", "Use private networking, gateway, enterprise identity, narrow scopes, rate limits, and centralized audit.", "Network location cannot replace per-request authorization."] },
        { id: "deployment-external-saas", title: "External SaaS Server", cells: ["An external provider controls descriptions, data handling, updates, and availability.", "Complete supplier review, data boundary, version pinning, egress control, and a minimum Tool allowlist.", "Protocol compatibility does not satisfy region, data, or incident requirements."] },
        { id: "deployment-high-impact-tool", title: "High-impact business Tool", cells: ["The Tool can pay, delete, publish, modify production, or access regulated data.", "Use strong identity, short-lived authorization, parameter constraints, approval, idempotency, compensation, and immutable audit.", "MCP must not lower the authorization standard of the underlying business system."] },
      ], { columns: ["Primary trust boundary", "Required controls", "Boundary"] }),
    ]),
    section("mcp-cloud", "DELIVERY RESPONSIBILITIES", "Map runtime, identity, adapters, and operations to explicit owners", "Managed services can host and govern the transport; business services remain the source of truth for permissions and outcomes.", [
      block("table", "Four delivery stages", [
        { id: "cloud-server-runtime", title: "Server runtime", cells: ["Serverless, containers, managed MCP runtimes, and service mesh", "Run Servers with isolation, scaling, version release, and health controls.", "Is the Server local, single-tenant, or remotely shared, and which protocol version and state assumptions apply?"] },
        { id: "cloud-identity-gateway", title: "Identity and gateway", cells: ["OAuth/OIDC, IAM, API gateway, WAF, keys, and certificates", "Centralize authentication, narrow authorization, rate limits, version control, network policy, and audit.", "Does execution use end-user or workload identity, and how are scope and tenant propagated?"] },
        { id: "cloud-api-data-adapters", title: "API and data adapters", cells: ["API management, databases, object storage, search, and enterprise SaaS connectors", "Reuse existing business APIs and data control planes instead of copying logic into the Server.", "Which existing interfaces already have stable schema, authorization, idempotency, and audit?"] },
        { id: "cloud-operations-security", title: "Operations and security", cells: ["Tracing, logs, SIEM, policy engine, vulnerability management, and private catalog", "Observe discovery and invocation, govern versions, detect abuse, and remove vulnerable dependencies.", "Who approves listing, update, revocation, vulnerability response, and disputed calls?"] },
      ], { columns: ["Service capabilities", "Value", "Discovery question"] }),
    ]),
  ]),
  qa: Object.freeze([
    question("mcp-versus-function-calling", { q: "If function calling already exists, why use MCP?", a: "Function calling defines how a model proposes a Tool and arguments. MCP defines how different hosts discover, describe, and invoke capabilities exposed by different Servers.", depth: "Direct function calling is sufficient for one application with a few fixed Tools. As clients, providers, and teams increase, MCP can reuse protocol adapters while underlying APIs, identity, and business policy remain. Adopt it according to duplicated integration cost, not market attention.", ask: "How many AI clients, Tool providers, and duplicate adapters exist, and must the capabilities be reused across teams?", tag: "Protocol boundary", basis: "MCP architecture plus function-calling mechanism", evidence: [{ sourceId: "mcp-architecture", supports: "Defines Host, Client, and Server roles and standardized capability discovery and context exchange." }, { sourceId: "openai-function-calling", supports: "Documents provider-specific function calling through Tool schemas and model-selected arguments." }] }),
    question("existing-user-permissions", { q: "Can an MCP Server directly reuse an existing user's permissions?", a: "Only through an explicit authorization chain. The Server must know the subject, resource audience, scope, tenant, and lifetime; a model's assertion that it represents a user is insufficient.", depth: "A remote MCP deployment should obtain short-lived, resource-bound credentials through mechanisms such as OAuth/OIDC, and the protected resource must validate them on each call. The Host presents consent and high-impact confirmation; the Server enforces resource authorization. Long-lived credentials should not be exposed to the model.", ask: "How is user identity propagated to the Server, and how does the protected resource validate scope and tenant?", tag: "Identity and authorization", basis: "MCP authorization plus zero trust", evidence: [{ sourceId: "mcp-authorization", supports: "Defines authorization roles, protected-resource metadata, and OAuth-related flows for remote MCP." }, { sourceId: "nist-zero-trust", supports: "Supports subject- and resource-focused authorization without implicit trust." }] }),
    question("public-server-versus-saas", { q: "Is connecting to a public MCP Server equivalent to integrating an ordinary SaaS API?", a: "No. In addition to API risks, Tool descriptions and annotations can influence model selection, capabilities can change, returned content can inject instructions, and automation can expand side effects.", depth: "Verify publisher, source artifact, and deployment, pin versions and allowed Tools, monitor description changes, and constrain outputs before they enter privileged context. Preview or confirm high-impact actions. A public catalog is a discovery source, not approval.", ask: "Who approves external Servers, how are versions pinned, and how are Tool-description changes reviewed?", tag: "Supply-chain security", basis: "MCP Tool trust boundary plus deployment security", evidence: [{ sourceId: "mcp-tools-2026-07-28", supports: "States that Tool annotations must be treated as untrusted unless the Server is trusted and that a human should be able to deny invocations." }, { sourceId: "mcp-security", supports: "Documents token, confused-deputy, local-Server, and related MCP deployment risks." }, { sourceId: "owasp-prompt-injection", supports: "Supports treating external Tool results as a possible indirect prompt-injection source." }] }),
    question("mcp-platform-lock-in", { q: "Can MCP create a new form of platform lock-in?", a: "It can reduce interface-layer lock-in while runtime, identity, gateway, extensions, and business data models remain platform-specific.", depth: "Separate the authoritative business API from the MCP adapter, prefer standard primitives and transports, limit unvalidated private extensions, and test a Server across more than one compatible client. Portability must include identity, operations, data, and exit—not only protocol messages.", ask: "Which capabilities are standard MCP and which depend on a particular managed platform or extension?", tag: "Portability", basis: "Interoperability boundary", evidence: [{ sourceId: "mcp-architecture", supports: "Defines interoperable Host, Client, Server, and message responsibilities." }] }),
    question("mcp-rc-production-switch", { q: "MCP 2026-07-28 is now final. Should production switch immediately?", a: "Not solely because the specification is final. The revision is now the normative baseline, but deployment depends on whether the chosen SDKs, Clients, Servers, gateways, and extensions pass compatibility testing.", depth: "Exercise the removal of initialize/initialized and protocol sessions, per-request version and capability metadata, server/discover, the Tasks extension, and authorization changes. Confirm every ecosystem component and private extension before setting coexistence, canary, and rollback gates. Protocol status and product compatibility are separate claims.", ask: "Which protocol revision is pinned today, and which SDKs, Servers, gateways, and private extensions are affected by the breaking changes?", tag: "Contract version", basis: "Current final revision plus legacy migration boundary", evidence: [{ sourceId: "mcp-lifecycle-2025-11-25", supports: "Documents the legacy initialization, protocol-version negotiation, and session behavior needed for migration comparison." }, { sourceId: "mcp-specification-2026-07-28", supports: "Establishes 2026-07-28 as the current final specification and its stateless, self-contained request model; it does not prove product compatibility." }, { sourceId: "mcp-changelog-2026-07-28", supports: "Documents removal of initialize and protocol sessions, per-request metadata, server/discover, and Tasks moving to an extension." }, { sourceId: "mcp-tasks-extension", supports: "Defines the current durable Task handle, update, polling, cancellation, and recovery contract." }], addedAt: "2026-07-21" }),
    question("server-not-gateway-iam", { q: "Can an MCP Server replace the API gateway and IAM?", a: "No. MCP standardizes capability exchange. The underlying API still needs identity, authorization, rate limits, network policy, transaction controls, and audit.", depth: "The Host binds a real user or workload identity. The Server exposes the minimum capabilities and authoritative systems revalidate access on each call. Remote MCP also introduces credential delegation and tenant isolation. A uniform Tool schema never converts a model-proposed call into permission to execute.", ask: "Whose identity performs the final action, and is permission checked only at connection time or for every resource and operation?", tag: "Security boundary", basis: "MCP architecture plus zero trust", evidence: [{ sourceId: "mcp-architecture", supports: "Defines separation among MCP Host, Client, and Server responsibilities." }, { sourceId: "nist-zero-trust", supports: "Rejects implicit trust from network location or an existing connection and requires resource-focused decisions." }] }),
    question("enterprise-server-reuse", { q: "How do we determine whether an MCP Server is worth enterprise reuse?", a: "Evaluate contract stability, security boundary, failure semantics, observability, and accountable maintenance—not the number of Tools.", depth: "Review whether capabilities are narrow, inputs and outputs are validatable, reads and writes are separated, retries are safe, versions are compatible, and sensitive logs are controlled. Prove cross-client reuse with a few high-value Tools, then establish catalog, owner, approval, update, and retirement.", ask: "Who maintains the Server, and how do clients detect, degrade, and assign responsibility after incompatibility or Tool failure?", tag: "Platform governance", basis: "Protocol contract plus operating ownership", evidence: [{ sourceId: "mcp-architecture", supports: "Supports capability and lifecycle interaction between Host and Server." }, { sourceId: "opentelemetry-genai-semconv", supports: "Provides evolving telemetry semantics for generative-AI and agent operations." }] }),
    question("tool-success-business-failure", { q: "Why can the business action fail after the MCP Tool reports success?", a: "Protocol success proves message exchange, not the authoritative business outcome. A downstream system can deny authorization, reject validation, partially commit, or enter an unknown state after timeout.", depth: "Separate protocol errors from business results and return stable correlation identifiers and actionable error categories. For writes, use idempotency keys, authoritative status queries, and compensation. The Host must not interpret a success string as proof that an order, payment, or resource change completed.", ask: "Which system confirms final completion, and how is an unknown timeout queried, retried, or compensated?", tag: "Failure semantics", basis: "Protocol boundary plus business state", evidence: [{ sourceId: "mcp-architecture", supports: "Defines MCP protocol responsibilities while business semantics remain in the implemented capability." }, { sourceId: "opentelemetry-genai-semconv", supports: "Supports correlating agent, Tool, and model operations for cross-layer diagnosis." }] }),
    question("automatic-server-installation", { q: "Should enterprise clients install any discovered MCP Server automatically?", a: "No. A Server is an executable capability and a data ingress/egress path; review provenance, software, permissions, versions, and operating responsibility before approval.", depth: "Use a managed catalog with publisher, artifact provenance, capabilities, required scopes, network egress, sensitive logging, update, and disable metadata. Test high-risk Servers in isolation, approve reads and writes separately, and prevent automatic discovery from bypassing enterprise installation and authorization policy.", ask: "Who approves a Server, how is an update re-reviewed, and how is a malicious or vulnerable version withdrawn?", tag: "Server supply chain", basis: "MCP security plus zero trust", evidence: [{ sourceId: "mcp-security", supports: "Supports assessment of authorization, token, and ecosystem threats in MCP deployments." }, { sourceId: "nist-zero-trust", supports: "Supports continuous validation and least-privilege access for subjects and resources." }] }),
    question("oauth-not-all-tools", { q: "After OAuth login, may an agent invoke every Tool on that Server?", a: "No. Authentication establishes a subject. Every resource, action, tenant, scope, and risk decision still requires authorization.", depth: "Separate user consent, client identity, Server scopes, and business-system permissions. Use narrow, current scopes and action-specific confirmation for high-impact writes. A Server must not turn one broad login into a permanent, all-powerful token.", ask: "Who issues identity, and at which layer is resource-level permission checked for every Tool?", tag: "Authorization chain", basis: "Authentication plus resource authorization", evidence: [{ sourceId: "mcp-authorization", supports: "Defines MCP authorization flows and protected-resource boundaries." }, { sourceId: "nist-zero-trust", supports: "Supports evaluating each access by subject, resource, and context." }] }),
    question("tool-schema-update", { q: "Can clients adapt automatically when an MCP Tool schema changes?", a: "Do not assume so. Changes to fields, enums, defaults, constraints, outputs, or error semantics can change business behavior even when the protocol connection still succeeds.", depth: "Assign each Tool a semantic version, a compatibility range, a deprecation window, and contract tests. Before upgrade, replay frozen calls through argument generation, output parsing, and failure handling. Publish a new version in parallel for breaking changes rather than silently replacing the old contract.", ask: "How are consumers notified, and which schema changes must block automatic update?", tag: "Contract version", basis: "Schema compatibility plus release governance", evidence: [{ sourceId: "mcp-architecture", supports: "Supports interaction through explicit capability contracts among Host, Client, and Server." }, { sourceId: "opentelemetry-genai-semconv", supports: "Supports recording Tool and invocation attributes for before/after comparison." }] }),
    question("trust-server-capability-claim", { q: "May a Host trust a Server's capability description directly?", a: "No. Discovery metadata creates a candidate; it is neither a security proof nor business acceptance.", depth: "A managed catalog should verify the publisher, software artifact, permissions, network egress, data processing, and downstream systems, then run contract and risk tests on representative Tools. Dynamic discovery is safe only inside an already approved trust domain.", ask: "Who signs and reviews capability metadata, and how does discovery map to the enterprise admission catalog?", tag: "Capability discovery", basis: "Discovery plus trust establishment", evidence: [{ sourceId: "mcp-architecture", supports: "Defines MCP capability discovery and negotiation." }, { sourceId: "mcp-security", supports: "Supports independent security assessment of ecosystem trust and attack surface." }] }),
    question("primitive-permission-separation", { q: "Should MCP Resources, Prompts, and Tools share one permission model?", a: "Not by default. The primitives differ first by the actor that controls their use: Tools are model-controlled, Resources application-driven, and Prompts user-controlled. Data access, side effects, and authority are a separate classification.", depth: "A Tool may be a read-only database query or perform a write; Resources and Prompts may still carry sensitive data or hostile instructions. Define Tool invocation and side-effect authority, Resource visibility and ACLs, and Prompt publication and user-selection responsibility separately, then attach current authorization, provenance, confirmation, idempotency, and audit to each capability.", ask: "Who controls use of this capability, what can it read or change, who authorizes it, and how is the result verified?", tag: "Capability separation", basis: "Control model plus least privilege", evidence: [{ sourceId: "mcp-tools-2026-07-28", supports: "Defines Tools as model-controlled callable operations and includes read-only database queries among examples." }, { sourceId: "mcp-resources-2026-07-28", supports: "Defines Resources as application-driven URI-addressed context." }, { sourceId: "mcp-prompts-2026-07-28", supports: "Defines Prompts as user-controlled templates authored by Servers." }, { sourceId: "nist-zero-trust", supports: "Supports fine-grained authorization by subject, resource, operation, and context." }] }),
    question("tool-result-untrusted-instruction", { q: "If Tool output comes from an enterprise system, may the model treat it as trusted instruction?", a: "No. It may be authoritative business data and still be untrusted as an instruction to the agent.", depth: "Separate instruction and data channels, mark provenance, validate type and length, and prevent returned text from changing system policy or future Tool authorization. High-impact decisions return to structured fields and the authoritative system.", ask: "Can Tool results contain user-controlled text, and can that text influence the next Tool selection or arguments?", tag: "Result injection", basis: "Data/instruction separation plus output validation", evidence: [{ sourceId: "owasp-prompt-injection", supports: "Supports treating external content as possible indirect prompt injection even when it arrives through a Tool." }, { sourceId: "mcp-security", supports: "Supports threat analysis for MCP Tools and content channels." }] }),
    question("long-running-mcp-call", { q: "Should an MCP call remain connected for minutes or hours?", a: "Usually not. In MCP 2026-07-28, the Client opts in per request by advertising io.modelcontextprotocol/tasks in its capabilities. A Server that advertises the extension through server/discover may currently return CreateTaskResult only for tools/call; the Client can then use the durable Task handle to poll, provide input, and resume after disconnection.", depth: "Have the Client advertise extension support in per-request capabilities and the Server advertise it through server/discover; verify that the operation is the currently supported tools/call, then handle working, input_required, completed, failed, cancelled, ttlMs, pollIntervalMs, tasks/get, tasks/update, tasks/cancel, and recovery. The specification leaves room for future request types, but current implementations must not assume them. Cancellation is cooperative intent; it does not prove downstream work or side effects stopped. Keep the legacy 2025-11-25 core experiment as a separate compatibility contract. An MCP Task currently wraps one tools/call; independent agent delegation and artifacts belong to an explicitly mapped A2A Task. The authoritative business system still owns the final outcome.", ask: "Which protocol version is pinned, which Task contract do both parties support, is this operation the supported tools/call, where is state recovered after disconnection, and how are protocol and business IDs mapped?", tag: "Long-running work", basis: "Versioned Tasks capability plus durable business state", evidence: [{ sourceId: "mcp-tasks-2025-11-25", supports: "Documents experimental Task status, polling, result, TTL, and cancellation in the legacy 2025-11-25 specification." }, { sourceId: "mcp-tasks-extension", supports: "Defines tools/call as the currently supported method that may return CreateTaskResult, followed by durable-handle polling, input_required, updates, recovery, and cooperative cancellation." }, { sourceId: "a2a-spec-1-0-0", supports: "Defines A2A Task semantics for independent agent collaboration, a different protocol boundary." }] }),
    question("stateless-mrtr-input", { q: "If MCP 2026-07-28 is stateless, how can a Tool request more information during execution?", a: "Use Multi Round-Trip Requests. prompts/get, resources/read, and tools/call may return input_required. inputRequests and an opaque requestState are each optional, but every InputRequiredResult MUST contain at least one of them. The Client retries the original operation with a new JSON-RPC id, submits corresponding inputResponses only for received inputRequests, echoes requestState unchanged only when supplied, and MUST NOT invent it. Use the Tasks extension only when durable execution is required.", depth: "The Host limits rounds, time, and fields and shows the user which Server is requesting information. It rejects passwords, tokens, and other sensitive credentials in Form or other in-band input; when sensitive interaction is required, URL mode completes it outside the MCP Client. When requestState is present, the implementation binds it to the original user, operation, and context; neither side treats it as authorization. MRTR, Tasks, scoped caches, and business state are separate contracts: the stateless design removes an implicit protocol session, not multi-round interaction.", ask: "Who presents and validates any requested input, how long does requestState remain valid when supplied, when does work become a Task, and where is authorization re-evaluated?", tag: "Long-running work", basis: "2026-07-28 MRTR and Tasks boundary", evidence: [{ sourceId: "mcp-mrtr-2026-07-28", supports: "Defines input_required for prompts/get, resources/read, and tools/call; inputRequests and opaque requestState are individually optional but at least one is required, followed by a new request identifier, corresponding inputResponses for received inputRequests, and unchanged requestState echo only when supplied." }, { sourceId: "mcp-tasks-extension", supports: "Defines the separate durable Task contract for polling, recovery, update, and cancellation." }], addedAt: "2026-09-04" }),
  ]),
  evidenceCards: Object.freeze([
    Object.freeze({ id: "mcp-role-separation", metric: "Host–Client–Server", title: "Three roles separate connection responsibility", finding: "MCP assigns experience and policy aggregation to the Host, protocol communication to the Client, and capability exposure to the Server.", boundary: "A product may combine these roles in one process, but their logical responsibilities remain distinct.", sourceId: "mcp-architecture", accent: true }),
    Object.freeze({ id: "mcp-primitive-control", metric: "Tool / Resource / Prompt", title: "Primitives identify different controlling actors", finding: "Tools are model-controlled, Resources application-driven, and Prompts user-controlled; a Tool can still be a read-only query.", boundary: "Control model is not a risk rating. Sensitivity, side effects, authorization, idempotency, and untrusted content remain separate concerns.", sourceId: "mcp-server-overview-2026-07-28" }),
    Object.freeze({ id: "mcp-final-versus-rc", metric: "2025-11-25 → 2026-07-28", title: "The final revision and the legacy compatibility path are different contracts", finding: "MCP 2026-07-28 is the current final specification. initialize, protocol sessions, and legacy core Tasks now describe pinned older implementations and migration comparison.", boundary: "Final publication does not establish ecosystem compatibility; validate each SDK, Client, Server, gateway, and private extension.", sourceId: "mcp-changelog-2026-07-28" }),
    Object.freeze({ id: "mcp-interoperability-not-trust", metric: "Interoperable ≠ trusted", title: "A successful connection is not safe use", finding: "Tool descriptions, external content, credentials, and automatic execution can still cause poisoning, confused-deputy behavior, and unauthorized side effects.", boundary: "Gateway, identity, allowlist, approval, result validation, and audit remain outside protocol interoperability.", sourceId: "mcp-security" }),
    Object.freeze({ id: "mcp-transport-threat-model", metric: "stdio ↔ HTTP", title: "A transport change changes the threat model", finding: "A local process and a remote shared service face different identity, network, tenant, callback, and operating risks.", boundary: "Every transport needs controls matched to the complete deployment boundary.", sourceId: "mcp-authorization" }),
    Object.freeze({ id: "mcp-mrtr-stateless-input", metric: "input_required → inputResponses", title: "A stateless request can still request additional input", finding: "Every MRTR InputRequiredResult carries at least inputRequests or requestState, then a new request retries the original operation and echoes an unchanged opaque requestState when supplied; durable execution remains a separate Tasks concern.", boundary: "requestState is not authority or business truth. When present, bind it to the original user and operation, limit rounds, validate fields, and protect sensitive information.", sourceId: "mcp-mrtr-2026-07-28" }),
  ]),
  terms: Object.freeze({
    mcp: Object.freeze({ name: "Model Context Protocol", abbr: "MCP", definition: "An open protocol for AI applications to discover and interact with external tools, resources, and prompt templates through a common capability contract." }),
    "tool-discovery": Object.freeze({ name: "Tool Discovery", definition: "Reading a Tool's name, description, arguments, outputs, and capability changes so a Host can decide whether to approve and use it." }),
    "identity-authorization": Object.freeze({ name: "Identity & Authorization", definition: "Verifying the real caller and target and deciding whether a subject may perform a specific action on a protected resource under the current context." }),
    "mcp-protocol-roles": Object.freeze({ name: "MCP Protocol Roles", definition: "The Host, Client, and Server responsibilities for user experience, isolated protocol communication, capability aggregation, and capability exposure." }),
    "mcp-primitives": Object.freeze({ name: "MCP Server Primitives", definition: "Tools, Resources, and Prompts express model-controlled calls, application-driven context, and user-controlled templates; read/write and risk are classified separately." }),
  }),
  sources: Object.freeze({
    "mcp-architecture": Object.freeze({ kind: "Official protocol specification", shortTitle: "MCP 2026-07-28 Architecture", note: "Defines MCP as a stateless protocol, the Host responsibilities, and the one-Client-to-one-Server logical relationship. It does not establish business authorization, supplier trust, or final outcomes." }),
    "mcp-lifecycle-2025-11-25": Object.freeze({ kind: "Official protocol specification", shortTitle: "MCP 2025-11-25 Lifecycle", note: "Documents the legacy initialization, version and capability negotiation, operation, and shutdown lifecycle. Since 2026-07-28 is now final, use this source only for pinned legacy implementations and migration comparison." }),
    "mcp-2026-07-28-rc": Object.freeze({ kind: "Official release-candidate announcement", shortTitle: "MCP 2026-07-28 RC", note: "Preserves the historical release-candidate announcement and proposed breaking changes. The milestone has been completed by the 2026-07-28 final specification, which is now the normative source." }),
    "mcp-authorization": Object.freeze({ kind: "Official protocol documentation", shortTitle: "MCP Authorization", note: "Defines MCP authorization roles and OAuth-related resource flows for remote Servers. It does not replace Tool-level business authorization or customer identity architecture." }),
    "mcp-security": Object.freeze({ kind: "Official security guidance", shortTitle: "MCP Security Best Practices", note: "Documents current state-handle, token, confused-deputy, local-Server, and permission risks. Legacy protocol sessions are a separate historical concern; guidance cannot prove a deployment is secure." }),
    "nist-zero-trust": Object.freeze({ kind: "Official architecture guidance", shortTitle: "NIST SP 800-207", note: "Defines zero-trust principles for explicit, resource-focused access decisions without implicit trust. It does not provide an MCP/A2A implementation, AI quality evaluation, or business compensation design." }),
    "owasp-prompt-injection": Object.freeze({ kind: "Community security guidance", shortTitle: "OWASP Prompt Injection", note: "Describes direct and indirect prompt-injection threats and layered mitigations. Guidance can reduce risk but cannot prove that a particular deployment prevents every attack." }),
    "openai-function-calling": Object.freeze({ kind: "Official API documentation", shortTitle: "OpenAI Function Calling", note: "Documents provider-specific Tool schemas and model-proposed arguments. Supported interfaces are time-sensitive, and function calling does not itself authorize or execute a business action." }),
    "opentelemetry-genai-semconv": Object.freeze({ kind: "Open observability specification", shortTitle: "OpenTelemetry GenAI Conventions", note: "Defines evolving telemetry conventions for generative-AI clients, retrieval, agents, MCP, and Tool calls. Projects must pin the adopted version and add their own business outcomes, access controls, approvals, evidence coverage, and risk fields." }),
    "mcp-tasks-2025-11-25": Object.freeze({ kind: "Official protocol specification", shortTitle: "MCP 2025-11-25 Tasks", note: "Documents the experimental core Tasks contract in legacy 2025-11-25. The current 2026-07-28 revision uses a distinct opt-in extension, so the two contracts must not be conflated." }),
    "a2a-spec-1-0-0": Object.freeze({ kind: "Official protocol specification", shortTitle: "A2A 1.0.0", note: "Pins the A2A 1.0.0 contract for A2A-Version, AgentInterface, protocol bindings, optional Agent Card signatures, task behavior, authentication, and authorization responsibilities. Compatibility still requires per-binding tests and customer acceptance." }),
    "mcp-elicitation-2025-11-25": Object.freeze({ kind: "Official protocol specification", shortTitle: "MCP 2025-11-25 Elicitation", note: "Defines Form and URL Elicitation in legacy MCP 2025-11-25. The current revision uses the Multi Round-Trip Requests pattern, so implementations must pin the version." }),
    "mcp-specification-2026-07-28": Object.freeze({ kind: "Official protocol specification", shortTitle: "MCP 2026-07-28", note: "Defines the current final revision with stateless, self-contained requests, per-request version and capability metadata, server/discover, and optional extensions including Tasks. It does not prove that any particular SDK, Client, Server, gateway, or product supports the revision." }),
    "mcp-changelog-2026-07-28": Object.freeze({ kind: "Official protocol specification", shortTitle: "MCP 2026-07-28 Key Changes", note: "Documents removal of initialize and protocol sessions, per-request metadata, mandatory Server discovery support, Tasks moving to an extension, and other breaking changes. It does not prove implementation compatibility." }),
    "mcp-mrtr-2026-07-28": Object.freeze({ kind: "Official protocol specification", shortTitle: "MCP Multi Round-Trip Requests", note: "Defines input_required for prompts/get, resources/read, and tools/call; individually optional inputRequests and opaque requestState, of which every InputRequiredResult MUST contain at least one; and a new request that retries the original operation with corresponding inputResponses for received inputRequests. requestState is echoed unchanged only when supplied and MUST NOT be invented. It is not a session, authorization, or durable Task." }),
    "mcp-list-cache-2026-07-28": Object.freeze({ kind: "Official protocol specification", shortTitle: "MCP Result Caching", note: "Requires ttlMs and public/private cacheScope on resultType: complete results for server/discover, tools/list, prompts/list, resources/list, resources/templates/list, and resources/read. resultType: input_required intermediate results carry no cache hints and MUST NOT be cached; MRTR retries containing inputResponses or requestState also MUST NOT be cached. TTL is a freshness hint rather than consistency or revocation proof, and private responses MUST NOT be reused across authorization contexts such as different access tokens. Identity-, tenant-, scope-, and version-aware cache keys are a production-control inference, not a normative protocol requirement." }),
    "mcp-http-routing-2026-07-28": Object.freeze({ kind: "Official protocol specification", shortTitle: "MCP HTTP Routing Metadata", note: "Defines Streamable HTTP routing metadata: every Client POST carrying a JSON-RPC request requires Mcp-Method. This revision does not define that header for notification POSTs, and the core protocol defines no Client-to-Server notifications over Streamable HTTP. Core Mcp-Name applies to tools/call and prompts/get params.name and resources/read params.uri. Headers must agree with the message body and do not supply identity, authorization, or business policy." }),
    "mcp-server-overview-2026-07-28": Object.freeze({ kind: "Official protocol specification", shortTitle: "MCP 2026-07-28 Server Features", note: "Defines the Server primitive control hierarchy: Prompts are user-controlled, Resources application-controlled, and Tools model-controlled. The hierarchy does not classify read/write risk or grant business authority." }),
    "mcp-tools-2026-07-28": Object.freeze({ kind: "Official protocol specification", shortTitle: "MCP 2026-07-28 Tools", note: "Defines model-controlled callable operations, including read-only queries, and requires careful human oversight and treatment of untrusted annotations. It does not grant business authority." }),
    "mcp-resources-2026-07-28": Object.freeze({ kind: "Official protocol specification", shortTitle: "MCP 2026-07-28 Resources", note: "Defines application-driven, URI-addressed context and related discovery and subscription behavior. Resource exposure still requires data governance and authorization." }),
    "mcp-prompts-2026-07-28": Object.freeze({ kind: "Official protocol specification", shortTitle: "MCP 2026-07-28 Prompts", note: "Defines user-controlled selection of Server-authored reusable templates. User selection does not establish template trust or authorize subsequent actions." }),
    "mcp-tasks-extension": Object.freeze({ kind: "Official protocol extension", shortTitle: "MCP Tasks Extension", note: "Currently allows only tools/call to return CreateTaskResult, then defines durable handles, ttlMs, pollIntervalMs, working/input_required/completed/failed/cancelled states, polling, updates, recovery, and cooperative cancellation while leaving room for future request types. For Streamable HTTP, tasks/get, tasks/update, and tasks/cancel require Mcp-Name to mirror params.taskId. An MCP tools/call Task is not an A2A inter-agent task or proof of business completion." }),
  }),
});
