export const englishModule = Object.freeze({
  slug: "ai-infra-compute",
  title: "AI Compute Infrastructure",
  subtitle: "Size accelerators, memory, fabrics, storage, power, and supply for the workload",
  definition: "AI Compute Infrastructure is the physical system that carries model training and inference: accelerators, device and host memory, scale-up fabric, scale-out network, storage, power delivery, and cooling.",
  position: "This is the lowest layer of the AI stack and supplies deliverable capacity to inference engines and AI platforms. It starts with the workload envelope and asks whether the complete physical path can sustain the workload under its defined quality and SLO criteria; it does not own scheduling policy, model routing, full-solution ROI, or application quality.",
  relatedSlugs: Object.freeze(["ai-infra-platform", "llm-training", "llm-inference", "model-landscape", "ai-ops", "security", "solution-patterns"]),
  sections: Object.freeze([
    Object.freeze({ id: "study-guide", eyebrow: "LEARN, PRACTICE, PROVE", title: "Start with the workload, then identify the bottleneck", lead: "Peak specifications are screening data. Procurement evidence comes from the target model, software stack, request or training shape, long-run profile, failure recovery, supply boundary, and full cost per result that meets the stated criteria.", blocks: Object.freeze([
      Object.freeze({ type: "cards", title: "Learning outcomes", items: Object.freeze([
        Object.freeze({ id: "outcome-workload-first", title: "Size from the workload", body: "Start from model, precision, context, batch, parallelism, data, concurrency, SLO, and growth rather than a popular accelerator." }),
        Object.freeze({ id: "outcome-resource-chain", title: "Read the complete resource chain", body: "Connect compute, HBM, host memory, scale-up fabric, scale-out network, storage, power, and cooling." }),
        Object.freeze({ id: "outcome-scale-boundaries", title: "Separate scale-up and scale-out", body: "Explain when expansion inside one tightly coupled accelerator domain differs from expansion across domains over a network." }),
        Object.freeze({ id: "outcome-sustainable-tco", title: "Procure for sustained results", body: "Use long-run performance, reliability, recoverability, availability, and TCO rather than one peak run." }),
      ]) }),
      Object.freeze({ type: "steps", title: "Recommended learning route", items: Object.freeze([
        Object.freeze({ id: "route-workload-profile", title: "Profile the workload", body: "Record model, precision, context, batch, parallel plan, data, concurrency, and SLO.", decision: "Checkpoint: explain why training and online serving need different resource curves." }),
        Object.freeze({ id: "route-constraint-layer", title: "Locate the constraint", body: "Test compute, HBM, scale-up fabric, network, storage, and facility power layer by layer.", decision: "Checkpoint: peak FLOPS never substitutes for end-to-end evidence." }),
        Object.freeze({ id: "route-supply-economics", title: "Validate supply and economics", body: "Compare purchased capacity, reserved capacity, on-demand cloud, model APIs, heterogeneous capacity, and delivery lead time.", decision: "Checkpoint: TCO includes software adaptation, idle and reserved capacity, failure, operations, and supply risk." }),
        Object.freeze({ id: "route-end-to-end-soak", title: "Run end-to-end soak tests", body: "Cover model loading, steady-state and peak load, data supply, degradation, failure, and recovery.", decision: "Checkpoint: comparison is not a short cache-warmed peak." }),
        Object.freeze({ id: "route-cost-per-result", title: "Measure cost per result that meets the stated criteria", body: "Include quality, sustained performance, idle capacity, energy, software, and people.", decision: "Checkpoint: owned, rented, and API capacity share one business denominator." }),
      ]) }),
      Object.freeze({ type: "cards", title: "Practice labs", items: Object.freeze([
        Object.freeze({ id: "lab-compute-requirement", title: "Build a compute-workload envelope", subtitle: "The customer provides only parameter count and a proposed GPU count", body: "Add model version, training-versus-inference purpose, precision, data or context, concurrency, quality, SLO, growth, and recovery; separate weights, activations, gradients, optimizer state, KV cache, workspaces, and reserve; list scale-up, scale-out, storage, facility, and failure-domain tests.", decision: "Deliverable: a workload envelope and a list of capacity hypotheses to falsify.", boundary: "Acceptance: every capacity number traces to the same load and quality gate, and training and serving estimates remain separate.", sourceIds: Object.freeze(["roofline-2009", "mlperf-training", "mlperf-inference-datacenter", "vllm-2023"]) }),
        Object.freeze({ id: "lab-cluster-investment", title: "Compare two cluster investments", subtitle: "Option A has higher peak compute; option B has a mature software stack and faster cloud delivery", body: "Under the same quality target and workload, measure cold start, steady state, peak, soak, scaling, and recovery; include migration, supply, energy, idle, facility, and failure cost; run sensitivity analysis for growth, utilization, and delivery delay.", decision: "Deliverable: an effective-capacity, risk, and resource-TCO decision record.", boundary: "Acceptance: the recommendation does not rest on one specification or public submission, and project ROI remains an application-level proof.", sourceIds: Object.freeze(["mlperf-training", "mlperf-inference-datacenter", "mlperf-storage-v2", "finops-unit-economics"]) }),
        Object.freeze({ id: "lab-bottleneck-hypothesis", title: "Test a compute-bottleneck hypothesis", subtitle: "Adding accelerators barely changes training throughput", body: "Capture arithmetic intensity, compute, HBM, scale-up fabric, scale-out network, data-load, and checkpoint metrics; compare one device, one domain, and multiple domains; change one major variable and rerun soak and recovery tests.", decision: "Deliverable: a bottleneck-evidence report and the next controlled experiment.", boundary: "Acceptance: distinguish compute, memory, communication, I/O, and recovery overhead and state where the conclusion cannot be extrapolated.", sourceIds: Object.freeze(["roofline-2009", "megatron-3d-parallelism-2021", "mlperf-storage-v2", "finops-unit-economics"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "curriculum", eyebrow: "CURRICULUM ATLAS", title: "The complete physical data path", lead: "AI capacity is a path from numeric representation through accelerator and memory to local fabric, network, storage, power, cooling, and the software ecosystem that makes the path usable.", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "curriculum-workload-first", title: "Workload envelope and acceptance contract", subtitle: "Workload First", body: "Training, online inference, long context, batch, and agentic systems stress compute, memory, network, and latency differently. Model version, precision, sequence or data, batch, parallelism, concurrency, quality gate, SLO, growth, and recovery are the sizing inputs.", decision: "Freeze one comparable workload and acceptance method before comparing hardware, cloud capacity, or an API.", boundary: "Parameter count and GPU count omit KV cache, activations, parallel efficiency, data path, facility, and operating reserve.", sourceIds: Object.freeze(["mlperf-training", "mlperf-inference-datacenter", "vllm-2023"]) }),
        Object.freeze({ id: "curriculum-compute-precision", title: "Accelerator compute, precision, and Roofline reasoning", subtitle: "Compute & Precision", body: "Roofline relates arithmetic intensity, peak compute, and memory-bandwidth ceilings to reason about whether a kernel is compute- or data-movement-bound. Precision changes throughput, memory, and quality, while kernels determine whether software can use the hardware path.", decision: "Profile target operators and frameworks instead of ranking peak FLOPS.", boundary: "Roofline is a kernel-level reasoning model; vendor peaks and operator results do not represent sustained end-to-end performance.", sourceIds: Object.freeze(["roofline-2009", "flashattention-2022"]) }),
        Object.freeze({ id: "curriculum-memory-system", title: "HBM and the memory hierarchy", subtitle: "Memory System", body: "Weights, activations, optimizer state, and KV cache have different memory accounts. HBM capacity determines fit, bandwidth affects data movement, and host memory plus local storage affect load and spill. Training and inference must be sized separately.", decision: "Evaluate capacity, bandwidth, fragmentation, runtime workspace, and safety reserve together.", boundary: "More VRAM solves capacity, not necessarily bandwidth, fabric, or end-to-end speed.", sourceIds: Object.freeze(["flashattention-2022", "vllm-2023"]) }),
        Object.freeze({ id: "curriculum-accelerator-choice", title: "Accelerators, software ecosystem, and heterogeneity", subtitle: "Accelerator Choice", body: "GPUs, cloud ASICs, and other accelerators compete through hardware, compilers, operators, frameworks, debugging, skills, and supply. Migration cost includes model and kernel coverage, operations, and future upgrade—not recompilation alone.", decision: "Run representative training, inference, failure, and upgrade paths under the same quality gate before volume purchase.", boundary: "One public submission or successful demo does not prove support for every model, operator, long-running condition, or future version.", sourceIds: Object.freeze(["mlperf-training", "mlperf-inference-datacenter"]) }),
        Object.freeze({ id: "curriculum-scale-up", title: "Scale-up fabric domain", subtitle: "Scale-up", body: "When model or parallel state spans devices, one high-bandwidth, low-latency accelerator domain carries fine-grained synchronization and collectives. That tightly coupled domain can span trays or system nodes and should not be equated mechanically with one host.", decision: "Choose the tightly coupled scale from the parallel plan, communication profile, and failure domain.", boundary: "NCCL defines communication primitives and diagnostics; it does not guarantee that the target framework sustains a link specification, and results inside one domain do not predict cross-domain scale-out.", sourceIds: Object.freeze(["nccl-collectives", "megatron-3d-parallelism-2021"]) }),
        Object.freeze({ id: "curriculum-scale-out", title: "Scale-out network and collectives", subtitle: "Scale-out", body: "When a workload crosses tightly coupled accelerator domains, data-, tensor-, or pipeline-parallel communication enters the cross-domain network. RDMA, congestion, topology, and measured collective share determine whether that network is the bottleneck.", decision: "Validate AllReduce, All-to-All, and representative long-running jobs on the target cross-domain network.", boundary: "NCCL and Megatron document mechanisms and specific experiments; they do not imply that every multi-node topology crosses domains or prove a universal network-upgrade benefit. A short link test also misses congestion and recovery.", sourceIds: Object.freeze(["nccl-collectives", "megatron-3d-parallelism-2021"]) }),
        Object.freeze({ id: "curriculum-feed-cluster", title: "Storage, data path, and power", subtitle: "Feed the Cluster", body: "Training input, checkpoint save and load, model distribution, and inference loading combine object stores, parallel file systems, local caches, and network. Power, cooling, rack density, cabling, and failure capacity bound physical deployment.", decision: "Include sustained data supply, checkpointing, recovery, and facility engineering acceptance in capacity planning.", boundary: "MLPerf Storage is a standardized workload and DGX H100 is a product reference design; neither replaces customer-site or application-state correctness tests.", sourceIds: Object.freeze(["mlperf-storage-v2", "nvidia-dgx-h100-data-center"]) }),
        Object.freeze({ id: "curriculum-tco-supply", title: "Compute economics and procurement evidence", subtitle: "TCO & Supply", body: "Purchase, long-term rental, on-demand cloud, and model APIs allocate capital, utilization, elasticity, and operations risk differently. TCO includes software adaptation, network, storage, energy, idle reserve, failure, skills, and delivery lead time.", decision: "Compare delivery risk and cost per training or inference result that meets the same quality and SLO contract.", boundary: "Resource-level TCO is not project ROI; current hourly price, one inventory snapshot, and one benchmark cannot establish durable capacity.", sourceIds: Object.freeze(["finops-ai-category", "finops-unit-economics"]) }),
        Object.freeze({ id: "curriculum-benchmark-acceptance", title: "End-to-end benchmark and acceptance", subtitle: "Benchmark & Acceptance", body: "Use the target model, precision, sequences or data, concurrency, framework, and data path and include the same quality target, cold start, soak, scaling, failure recovery, and whole-system cost.", decision: "Base acceptance on sustained performance, recoverability, supply evidence, and full cost per result that meets the stated criteria.", boundary: "A public result can structure comparison, but its benchmark version and submitted configuration do not represent the customer production load.", sourceIds: Object.freeze(["mlperf-training", "mlperf-inference-datacenter", "mlperf-storage-v2", "finops-unit-economics"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "principles", eyebrow: "PHYSICAL FOUNDATION", title: "Size compute, memory, fabric, data path, and facility as one system", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "principle-workload-sizing", title: "Workload-first sizing", body: "Training, tuning, online serving, batch generation, and vector processing stress resources differently.", decision: "Freeze model, precision, context, batch, concurrency, SLO, and growth before procurement." }),
        Object.freeze({ id: "principle-compute-precision", title: "Compute and numerical precision", body: "Accelerators deliver different sustained throughput depending on data type, sparsity, operator mix, and software support.", decision: "Compare a runnable target framework and model rather than peak FLOPS." }),
        Object.freeze({ id: "principle-memory-hierarchy", title: "Memory hierarchy", body: "Weights, optimizer state, activations, and KV cache determine capacity; bandwidth and data movement can be as important as arithmetic.", decision: "Size training and inference separately and reserve for buffers, fragmentation, concurrency, and runtime." }),
        Object.freeze({ id: "principle-scale-up", title: "Scale-up fabric domain", body: "High-bandwidth links inside one tightly coupled accelerator domain govern fine-grained exchange of parameters, activations, and cache; the domain need not equal one host.", decision: "Validate topology, bandwidth, collectives, and failure boundary before spreading a model across devices." }),
        Object.freeze({ id: "principle-scale-out", title: "Scale-out network", body: "Training synchronization, expert routing, distributed inference, and storage traffic need low-latency, high-throughput, stable networking.", decision: "Design network from communication pattern and failure domain with congestion, telemetry, and recovery." }),
        Object.freeze({ id: "principle-storage-power-tco", title: "Storage, power, and total cost", body: "Loading, checkpoints, power, cooling, idle capacity, and operations jointly determine usable supply and TCO.", decision: "Include sustained job time, utilization, and scaling lead time in the cost model rather than relying on device purchase price alone." }),
      ]) }),
      Object.freeze({ type: "boundary", title: "Critical boundary", items: Object.freeze([Object.freeze({ id: "compute-foundation-boundary", title: "Physical capacity is not the complete AI outcome", body: "Compute infrastructure supplies hardware and data-path capacity.", boundary: "It does not decide queue priority, sharing, model release, routing, or business quality. Peak FLOPS, device count, and VRAM require target-stack validation, and resource TCO still does not prove application ROI." })]) }),
    ]) }),
    Object.freeze({ id: "decisions", eyebrow: "PRESALES DECISIONS", title: "Choose hardware and supply from evidence", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "decision-workload-before-accelerator", title: "Choose the workload before the accelerator", body: "A popular GPU is being discussed before model, framework, precision, and SLO are frozen.", decision: "Build representative training and inference loads, then screen hardware that supports the target stack and benchmark end to end.", boundary: "Peak specifications and synthetic operator benchmarks can only shortlist candidates; they do not prove model or distributed efficiency." }),
        Object.freeze({ id: "decision-buy-rent-api", title: "Purchase, reserve, on-demand, or API?", body: "Loads vary between exploration, bursts, and stable production and the organization values capital and operating control differently.", decision: "Use elastic cloud or API for exploration and peaks; compare reserved, dedicated, and owned capacity for sustained high utilization; retain a hybrid path.", boundary: "TCO includes idle, software, network, storage, energy, people, resilience, and upgrades." }),
        Object.freeze({ id: "decision-single-multi-node", title: "One scale-up domain or cross-domain scale-out?", body: "The model or required throughput exceeds one device's capacity, but the communication and failure costs are unclear.", decision: "Use the smallest tightly coupled domain that satisfies the parallel plan; design cross-domain network and recovery only when measured capacity requires scale-out.", boundary: "More domains increase communication, debugging, congestion, and failure exposure; measure scaling efficiency and cost per result that meets the stated criteria." }),
        Object.freeze({ id: "decision-heterogeneous-accelerator", title: "Use alternative or heterogeneous accelerators?", body: "Cost, supply, region, or energy motivates multiple GPU, NPU, or custom-accelerator types.", decision: "Validate model coverage, operators, compiler, quantization, collectives, images, telemetry, operations, and skills before comparing portable layers.", boundary: "Running a demo does not establish production ecosystem maturity or an upgrade path." }),
        Object.freeze({ id: "decision-investment-layer", title: "Invest in compute, network, or storage first?", body: "Devices wait for data, checkpoints are slow, communication congests, or load time dominates.", decision: "Profile compute, memory, communication, and I/O wait end to end and invest at the measured bottleneck.", boundary: "Local peak upgrades move bottlenecks; balance matters more than one overprovisioned layer." }),
      ]) }),
    ]) }),
    Object.freeze({ id: "deep-dive", eyebrow: "BOTTLENECK & PROCUREMENT PROOF", title: "Use profiles and controlled comparisons to direct investment", lead: "The same slow job or underutilized GPU can be compute-, memory-, communication-, or I/O-limited. Only staged evidence can determine the next purchase.", blocks: Object.freeze([
      Object.freeze({ type: "table", title: "Four resource bottlenecks", columns: Object.freeze(["Bottleneck", "Evidence", "Action", "Boundary"]), items: Object.freeze([
        Object.freeze({ id: "bottleneck-compute", title: "Compute-bound", body: "The target kernels approach the relevant Roofline compute ceiling, operator time scales with problem size, and data-movement changes have limited effect.", decision: "Prove the target precision and kernels use the purchased compute path before comparing accelerators, compilation, or model parallelism.", boundary: "Peak FLOPS does not prove the target framework or end-to-end model can sustain it.", sourceIds: Object.freeze(["roofline-2009", "flashattention-2022"]) }),
        Object.freeze({ id: "bottleneck-memory", title: "Memory- or bandwidth-bound", body: "HBM transfers, KV-cache access, or activation movement dominate while arithmetic utilization appears modest.", decision: "Compare capacity, bandwidth, fusion, precision, and cache management and reduce movement before buying more peak compute.", boundary: "Larger memory solves capacity, not necessarily bandwidth or speed.", sourceIds: Object.freeze(["flashattention-2022", "vllm-2023"]) }),
        Object.freeze({ id: "bottleneck-communication", title: "Communication-bound", body: "Collective and synchronization wait rises with device count and is sensitive to parallel strategy, topology, congestion, and stragglers.", decision: "Collect one-device, one-domain, and cross-domain scaling curves and collective profiles before choosing fabric or parallel changes.", boundary: "Results inside one scale-up domain cannot predict cross-domain training or inference.", sourceIds: Object.freeze(["nccl-collectives", "megatron-3d-parallelism-2021"]) }),
        Object.freeze({ id: "bottleneck-io", title: "Storage- or I/O-bound", body: "Devices wait for data, weights, or checkpoints and show long gaps at startup, save, load, or recovery.", decision: "Measure input, prefetch, cache, checkpoint, and restore throughput before choosing local NVMe, parallel storage, object storage, or network.", boundary: "A standardized storage workload and cache-warmed speed do not prove customer capacity or application-state correctness.", sourceIds: Object.freeze(["mlperf-storage-v2"]) }),
      ]) }),
      Object.freeze({ type: "steps", title: "Five procurement proofs", items: Object.freeze([
        Object.freeze({ id: "proof-replayable-workload", title: "Replayable workload", body: "Freeze model, framework, precision, data or sequence distribution, parallel plan, quality target, SLO, recovery, and growth as one benchmark package.", decision: "Run the same contract on every candidate without changing batch, quantization, or model to showcase each device.", boundary: "MLPerf offers comparable rules, not a substitute for the customer package; refresh that package as demand changes.", sourceIds: Object.freeze(["mlperf-training", "mlperf-inference-datacenter"]) }),
        Object.freeze({ id: "proof-end-to-end-profile", title: "End-to-end profile", body: "Record arithmetic intensity, compute, memory, bandwidth, communication, I/O, queue, and failure so every wait maps to a resource layer.", decision: "Fix the main current bottleneck and anticipate where it moves next.", boundary: "Short samples and averages miss congestion, checkpoints, tails, and recovery.", sourceIds: Object.freeze(["roofline-2009", "nccl-collectives", "mlperf-storage-v2"]) }),
        Object.freeze({ id: "proof-scaling-efficiency", title: "Scaling-efficiency curve", body: "Increase device count step by step, first within a tightly coupled domain and then across domains. For training, record time to the stated quality target; for serving, record SLO-satisfying Goodput. In both cases, record communication share, failures, and cost per result that meets the stated criteria.", decision: "Select an economic scale before marginal benefit collapses.", boundary: "Twice the devices does not imply twice the performance, and a peak short run does not prove stability.", sourceIds: Object.freeze(["megatron-3d-parallelism-2021", "nccl-collectives", "finops-unit-economics"]) }),
        Object.freeze({ id: "proof-failure-recovery", title: "Failure-recovery drill", body: "Inject degraded devices, node failures, network disruptions, and capacity loss; measure replay behavior, checkpoint recovery, replacement time, and data consistency.", decision: "Include restore time, lost work, and replay cost in capacity and resilience.", boundary: "Infrastructure self-healing does not prove training state or business request recovery.", sourceIds: Object.freeze(["nvidia-gpu-operator", "nist-genai-profile"]) }),
        Object.freeze({ id: "proof-supply-cost", title: "Supply and full cost", body: "Combine regional inventory, lead time, reserved capacity, spot interruptions, licenses, network, storage, facility, energy, idle capacity, failure, and people.", decision: "Use different supply modes for the stable base, elastic peaks, and recovery, and compare cost per result that meets the stated criteria.", boundary: "One hourly price or inventory snapshot does not establish long-term deliverability, and resource unit economics does not prove application ROI.", sourceIds: Object.freeze(["finops-ai-category", "finops-unit-economics"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "cloud", eyebrow: "CLOUD DELIVERY", title: "Use cloud capacity according to workload shape", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "cloud-elastic-compute", title: "Elastic accelerated compute", subtitle: "GPU or NPU instances, bare metal, dedicated hosts, on-demand capacity, and spot capacity", body: "Select accelerators by model and stage and obtain experiment, peak, or production capacity quickly.", decision: "Discover training versus inference, sustained utilization, start deadline, interruptibility, and region." }),
        Object.freeze({ id: "cloud-cluster-fabric", title: "AI cluster and fabric", subtitle: "High-performance network, RDMA, placement, dedicated links, and load balancing", body: "Provide predictable communication for distributed training and inference while controlling failure domains.", decision: "Discover parallel strategy, communication share, scale, and recovery objective." }),
        Object.freeze({ id: "cloud-data-checkpoint", title: "Data and checkpoint path", subtitle: "Object storage, parallel file systems, local NVMe, cache, backup, and archive", body: "Move data, weights, and checkpoints while balancing cost, durability, and restore time.", decision: "Discover dataset and model-load size, checkpoint cadence, recovery point, and retention." }),
        Object.freeze({ id: "cloud-capacity-economics", title: "Capacity and economics", subtitle: "Reserved capacity, commitment discounts, cost management, energy, and infrastructure telemetry", body: "Combine stable base capacity with elastic peaks and track actual cost per model version that reaches the quality target and per task that meets the stated criteria.", decision: "Discover utilization, queue, replay, idle cost, and ownership of future capacity forecasts." }),
      ]) }),
    ]) }),
  ]),
  qa: Object.freeze([
    Object.freeze({ id: "peak-compute-not-speed", q: "Why does higher theoretical accelerator compute not guarantee faster training or inference?", a: "Execution also depends on arithmetic intensity, numeric type, memory bandwidth, operators, communication, batch shape, kernels, scheduling, and I/O. Peak compute matters only when the workload can keep the units fed.", depth: "Roofline separates compute and memory-bandwidth ceilings; a complete model can also be limited by collectives, storage, KV state, or scheduling. Freeze model, precision, framework, and workload. For training, compare time to the stated quality target; for serving, compare SLO-satisfying Goodput, tail latency, and whole-system energy. Use profile evidence to identify the bottleneck.", ask: "Ask the customer: what evidence identifies the current bottleneck, and which precision, parallel strategy, and software version are used?", tag: "Performance reasoning", basis: "Roofline plus end-to-end profiling", evidence: Object.freeze([Object.freeze({ sourceId: "roofline-2009", supports: "Relates attainable kernel performance to arithmetic intensity, peak compute, and memory-bandwidth ceilings." }), Object.freeze({ sourceId: "flashattention-2022", supports: "Supports the performance impact of transfers between high-bandwidth and on-chip memory." })]) }),
    Object.freeze({ id: "weights-fit-not-serviceable", q: "If the weights fit in VRAM, can the accelerator serve the model reliably?", a: "Not necessarily. Inference also needs KV cache, workspaces, runtime, and concurrency; training additionally needs activations, gradients, and optimizer state.", depth: "Add precision, context, batch, concurrency, fragmentation, framework reserve, and communication buffers. Long-context serving may be capacity-limited by the KV cache. Validate the service with rejection rate, P95 latency, SLO-satisfying Goodput, and a sustained soak test—not merely a successful model load.", ask: "Ask the customer: is the target loading, one completed request, or peak-concurrency SLO?", tag: "Memory planning", basis: "KV-cache memory management", evidence: Object.freeze([Object.freeze({ sourceId: "vllm-2023", supports: "Identifies KV cache as a major dynamic memory cost affecting serving concurrency and throughput." })]) }),
    Object.freeze({ id: "on-prem-needs-cloud", q: "Why might a customer with an existing data center still use cloud AI compute?", a: "Cloud can supplement experiment speed, scarce device types, peak capacity, region, and managed data paths rather than simply replace owned devices. The decision depends on the workload envelope and boundary.", depth: "Keep stable, sensitive, highly utilized base load on premises and use cloud for evaluation, migration, peaks, or disaster recovery. Compare data movement, links, identity, images, licenses, reserve, idle capacity, and operations. Product availability remains provider- and region-specific.", ask: "Ask the customer: is the local gap capacity, device type, delivery lead time, elasticity, or operations, and which data or models cannot move?", tag: "Hybrid cloud", basis: "Workload tiering plus TCO", evidence: Object.freeze([Object.freeze({ sourceId: "finops-ai-category", supports: "Places AI spend across model services, cloud, data centers, SaaS, and multiple vendors rather than one compute line item." }), Object.freeze({ sourceId: "nist-zero-trust", supports: "Supports continuing identity- and policy-based access across hybrid environments rather than trusting location." })]) }),
    Object.freeze({ id: "gpu-hours-cost", q: "Can project cost be estimated directly from GPU count or GPU-hours?", a: "Only as a rough bound. Real cost depends on utilization, queueing, failed replays, scaling efficiency, storage, network, operations, and outcomes measured against stated criteria.", depth: "Training cost includes data preparation, experiments, and failed runs per model version that reaches the stated quality target. Serving cost is measured per task that meets stated quality and SLO criteria. Compare on-demand, reserved, spot, and dedicated capacity, including interruption, recovery, and idle cost. Build the scaling model from a profile.", ask: "Ask the customer: is the denominator one experiment, one model version that reaches the quality target, one million tokens, or one task that meets the stated criteria?", tag: "TCO", basis: "End-to-end unit economics", evidence: Object.freeze([Object.freeze({ sourceId: "finops-unit-economics", supports: "Distinguishes resource-efficiency units from cost per product, service, or accepted outcome; customer baselines still determine the decision." })]) }),
    Object.freeze({ id: "procurement-beyond-gpu", q: "Why can AI procurement not compare GPU model and memory alone?", a: "Deliverable capacity also depends on topology, network, storage, CPU, power, cooling, software, failure domains, and the target workload.", depth: "Freeze model, precision, data or request distribution, quality gate, concurrency, SLO, software, and recovery. Training stresses collectives, data supply, and long-run stability; serving stresses scenarios, KV state, tail latency, SLO-satisfying Goodput, and whole-system power. Compare candidates against the same stated quality and SLO criteria.", ask: "Ask the customer: is the main load training, batch inference, or online service, and which quality target, model, precision, concurrency, and topology support the capacity claim?", tag: "Procurement method", basis: "Comparable system-level acceptance", evidence: Object.freeze([Object.freeze({ sourceId: "mlperf-training", supports: "Compares complete systems by time to a specified quality target and binds results to benchmark and submission configuration." }), Object.freeze({ sourceId: "mlperf-inference-datacenter", supports: "Uses standard scenarios, load generation, quality, and compliance rules for datacenter inference; it does not represent the customer's load." })]) }),
    Object.freeze({ id: "high-speed-interconnect", q: "When is a high-speed interconnect necessary, and when is one multi-GPU node enough?", a: "A stronger cross-domain network has value when the required parallel plan crosses tightly coupled accelerator domains and collective communication is a measured bottleneck.", depth: "Independent batch partitions and workloads that stay inside one tightly coupled domain may instead be limited by memory, scheduling, or request shape. For cross-domain training, identify the data-, tensor-, or pipeline-parallel communication, measure collective time and effective bandwidth, and compare time to the stated quality target before and after scale-out. Multiple nodes do not necessarily mean multiple domains.", ask: "Ask the customer: how is the model partitioned, which collectives dominate, and how do time to the stated quality target and SLO-satisfying Goodput change from one tightly coupled domain to several?", tag: "Network selection", basis: "Parallel topology plus profiling", evidence: Object.freeze([Object.freeze({ sourceId: "nccl-collectives", supports: "Documents GPU collective operations and communication diagnostics; it does not guarantee that any network class is necessary or faster for a workload." }), Object.freeze({ sourceId: "megatron-3d-parallelism-2021", supports: "Shows that data, tensor, and pipeline parallelism can be limited by cross-node communication and waiting in the paper's tested configurations." })]) }),
    Object.freeze({ id: "peak-flops-comparison", q: "Why should AI accelerators not be compared by peak FLOPS alone?", a: "Peak FLOPS applies to specific numeric formats and ideal operators. Real performance also depends on arithmetic intensity, memory, fabric, kernels, data supply, and workload shape.", depth: "Use Roofline to identify whether target kernels are more likely compute- or data-movement-bound, then run the target model, precision, sequence, batch, and framework over a sustained interval. Training and inference have different bottlenecks, and one peak cannot explain time to quality, TTFT, or scaling efficiency.", ask: "Ask the customer: is the target compute-, memory-, communication-, or I/O-bound, and where does each candidate become constrained?", tag: "Specification boundary", basis: "Roofline plus end-to-end validation", evidence: Object.freeze([Object.freeze({ sourceId: "roofline-2009", supports: "Relates attainable kernel performance to arithmetic intensity, peak compute, and memory-bandwidth ceilings." }), Object.freeze({ sourceId: "flashattention-2022", supports: "Shows that reducing HBM traffic can improve attention efficiency; the result remains operator- and configuration-specific." })]) }),
    Object.freeze({ id: "double-gpu-not-double-speed", q: "Why does doubling GPUs often fail to double training speed?", a: "More devices can increase collective communication and waiting, while pipeline schedules can add bubbles; scaling therefore depends on the chosen parallel plan and target topology.", depth: "Compare profiles for one device, one tightly coupled domain, and cross-domain execution, including collective time, pipeline idle time, topology, input, checkpoints, and stragglers. Add devices only when the measured reduction in compute time exceeds the added coordination cost.", ask: "Ask the customer: which wait grew fastest after expansion, and how do training throughput and time to the same quality target compare inside one tightly coupled domain and across domains?", tag: "Scaling efficiency", basis: "Communication plus long-run systems", evidence: Object.freeze([Object.freeze({ sourceId: "megatron-3d-parallelism-2021", supports: "Directly studies data, tensor, and pipeline parallelism, cross-node communication, waiting, and pipeline bubbles; reported scaling is configuration-specific." }), Object.freeze({ sourceId: "nccl-collectives", supports: "Defines AllReduce, All-to-All, and related collectives and diagnostics but does not promise linear job scaling." })]) }),
    Object.freeze({ id: "highest-network-not-required", q: "Does every training cluster need the highest-specification inter-node network?", a: "No. A network upgrade helps only when the target parallel plan crosses tightly coupled accelerator domains and measured communication materially limits training throughput or time to the stated quality target.", depth: "Profile the relevant collectives, compute overlap, and scaling from one domain to multiple domains, then compare cross-domain networks under the same model and parallel configuration. A workload that stays inside one domain or waits on data may not benefit from a more expensive network.", ask: "Ask the customer: when must the target model cross tightly coupled domains, and what is the network cost per added unit of training throughput or reduction in time to the stated quality target?", tag: "Network investment", basis: "Communication share plus scaling efficiency", evidence: Object.freeze([Object.freeze({ sourceId: "nccl-collectives", supports: "Documents collective operations and network/topology diagnostics; it does not identify a universal best network specification." }), Object.freeze({ sourceId: "megatron-3d-parallelism-2021", supports: "Shows communication and waiting can constrain composed parallelism in the paper's tested cluster; it does not prescribe every customer's network." })]) }),
    Object.freeze({ id: "low-gpu-utilization-accelerator", q: "Does low GPU compute utilization mean that a stronger accelerator is needed?", a: "No. The bottleneck may be storage, CPU preprocessing, host memory, network, input, or checkpoint I/O; a stronger GPU can simply wait faster.", depth: "Measure object storage, file system, CPU decode, host memory, PCIe, HBM, operators, and checkpoint save and load along the data path. Repair the narrowest layer and rerun a sustained target task. MLPerf Storage treats accelerator data supply and checkpointing as separate measurable workloads, reinforcing that one GPU metric cannot direct procurement.", ask: "Ask the customer: what is the device waiting for, and how much wall time goes to input preparation and checkpoint save and load?", tag: "Data supply", basis: "Roofline plus storage workload", evidence: Object.freeze([Object.freeze({ sourceId: "roofline-2009", supports: "Shows that attainable performance depends on arithmetic intensity and memory bandwidth as well as peak compute." }), Object.freeze({ sourceId: "mlperf-storage-v2", supports: "Defines training-data and checkpoint save-load storage workloads; it does not replace customer capacity planning." })]) }),
    Object.freeze({ id: "rack-space-not-facility-ready", q: "Does available rack space mean a facility is ready for a high-density AI cluster?", a: "No. The target system also has power, heat-rejection, airflow, cabling, delivery, safety, and operating requirements that need facility engineering acceptance.", depth: "Build the facility plan from the exact system and rack configuration, including continuous and peak electrical load, cooling and airflow, power redundancy, network cable paths, maintenance access, and expansion constraints. Do not reuse one reference design's numeric limits for another platform.", ask: "Ask the customer: what continuous power and heat rejection can each rack support, and how much capacity may be lost under failure or high temperature?", tag: "Power and cooling", basis: "Facility constraints plus sustained performance", evidence: Object.freeze([Object.freeze({ sourceId: "nvidia-dgx-h100-data-center", supports: "Directly documents power, cooling, rack density, cabling, airflow, and cross-team planning for DGX H100 SuperPOD; its numeric values are product- and design-specific." })]) }),
    Object.freeze({ id: "heterogeneous-supply-risk", q: "Does using several GPU or AI-accelerator types automatically reduce supply risk?", a: "Only when the software, model formats, operators, precision, and operations are actually portable. Otherwise heterogeneity can increase fragmentation and maintenance cost.", depth: "Run representative training, inference, quantization, monitoring, failure, and upgrade paths and record missing operators, performance, and skills. A portable layer need not be the lowest common denominator; critical work can retain specialized kernels with an explicit dependency.", ask: "Ask the customer: must all workloads move, or only critical ones, and which layer is currently bound to one hardware ecosystem?", tag: "Heterogeneous portability", basis: "Supply continuity plus software adaptation", evidence: Object.freeze([Object.freeze({ sourceId: "flashattention-2022", supports: "Shows that high-performance operators depend on concrete memory hierarchy and implementation." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports identifying third-party and supply-chain dependencies and preparing alternatives." })]) }),
  ].map((item) => Object.freeze({
    ...item,
    ask: item.ask
      .replace(/^Ask the customer:\s*/, "")
      .replace(/^[a-z]/, (character) => character.toUpperCase()),
  }))),
  evidenceCards: Object.freeze([
    Object.freeze({ id: "io-aware-effective-performance", metric: "Roofline", title: "Higher peak compute does not guarantee higher effective performance", finding: "Roofline relates attainable kernel performance to arithmetic intensity, peak compute, and the memory-bandwidth ceiling, so data movement may constrain performance first.", boundary: "This is a bottleneck-reasoning model, not a full-model, network, storage, long-run, or business-SLO guarantee.", sourceId: "roofline-2009", accent: true }),
    Object.freeze({ id: "dynamic-serving-memory", metric: "Dynamic memory", title: "Serving capacity includes runtime state", finding: "The vLLM paper shows that KV-cache allocation, fragmentation, and sharing materially affect concurrent requests and serving throughput.", boundary: "Capacity still depends on model, precision, context, engine configuration, hardware, and workload.", sourceId: "vllm-2023" }),
    Object.freeze({ id: "complete-gpu-runtime", metric: "Same quality target", title: "Comparable benchmarks freeze quality and workload rules", finding: "MLPerf Training compares complete systems by wall-clock time to a specified quality target.", boundary: "A public result still represents one benchmark version and submission configuration, not the customer workload, soak, recovery, or business outcome.", sourceId: "mlperf-training" }),
  ]),
  terms: Object.freeze({
    "access-spectrum": Object.freeze({ name: "Model Access Spectrum", definition: "The range from managed APIs to open-weight and open-source systems, which changes hardware control and operating responsibility." }),
    "kv-cache": Object.freeze({ name: "Key-Value Cache", abbr: "KV Cache", definition: "Attention Keys and Values retained across autoregressive decoding to avoid recomputing the complete prefix; memory grows with architecture, sequence length, precision, and active requests." }),
    "ai-infra-compute": Object.freeze({ name: "AI Compute Infrastructure", definition: "The physical accelerator, memory, fabric, network, storage, power, and cooling foundation for AI workloads." }),
    "heterogeneous-compute": Object.freeze({ name: "Heterogeneous Compute", definition: "Operating workloads across different CPU, GPU, or accelerator types with explicit software, placement, observability, and portability boundaries." }),
    vram: Object.freeze({ name: "Video Random Access Memory", abbr: "VRAM", definition: "Accelerator memory that holds weights, activations, caches, workspaces, and intermediate state." }),
    hbm: Object.freeze({ name: "High Bandwidth Memory", abbr: "HBM", definition: "High-capacity, high-throughput memory colocated with accelerators and central to model fit and data movement." }),
    "scale-up": Object.freeze({ name: "Scale-up", definition: "Expanding capacity within one tightly coupled, high-bandwidth, low-latency accelerator domain; the domain is not necessarily identical to one host." }),
    "scale-out": Object.freeze({ name: "Scale-out", definition: "Expanding a workload across tightly coupled accelerator domains through network, collective communication, congestion control, and distributed recovery." }),
    tco: Object.freeze({ name: "Total Cost of Ownership", abbr: "TCO", definition: "Acquisition, software, network, storage, energy, idle reserve, people, failure, recovery, supply, and operations cost over the lifecycle." }),
  }),
  sources: Object.freeze({
    "flashattention-2022": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "FlashAttention", note: "Presents an IO-aware exact-attention algorithm that reduces memory traffic. Realized benefit depends on sequence shape, hardware, kernels, and the surrounding training or inference stack." }),
    "roofline-2009": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "Roofline Model", note: "Relates attainable kernel performance to arithmetic intensity, peak compute, and memory-bandwidth ceilings. It is a bottleneck-reasoning model, not an end-to-end AI workload, network, storage, or SLO guarantee." }),
    "mlperf-training": Object.freeze({ kind: "Official industry benchmark description", shortTitle: "MLPerf Training", note: "Measures wall-clock time for a complete system to reach a specified quality target and distinguishes benchmark divisions. Results remain tied to the benchmark version, model, data, quality target, software, and submitted hardware." }),
    "mlperf-inference-datacenter": Object.freeze({ kind: "Official industry benchmark description", shortTitle: "MLPerf Inference: Datacenter", note: "Uses standard load generation, scenarios, quality, compliance, and optional whole-system wall-power measurement. Results remain tied to the submitted version and configuration and do not represent a customer's request distribution or outcome." }),
    "mlperf-storage-v2": Object.freeze({ kind: "Official industry benchmark description", shortTitle: "MLPerf Storage v2.0", note: "Defines training-data supply and checkpoint save-load workloads and scores completed operations, including the slowest participating process. It does not prove application-state completeness, customer capacity, failure rate, or recovery correctness." }),
    "finops-ai-category": Object.freeze({ kind: "Industry foundation guidance", shortTitle: "FinOps for AI", note: "Places AI spend across model services, cloud, data centers, SaaS, and multiple vendors and discusses allocation, forecasting, unit economics, and collaboration. It does not supply a customer's bill or ROI." }),
    "finops-unit-economics": Object.freeze({ kind: "Industry foundation framework", shortTitle: "FinOps Unit Economics", note: "Relates technology spend to product, service, or activity value and distinguishes technical efficiency units from business-outcome units. It does not prove ROI, causality, or a customer's value baseline." }),
    "vllm-2023": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "vLLM / PagedAttention", note: "Introduces PagedAttention and KV-cache memory management for LLM serving. Reported throughput remains specific to the tested models, hardware, engine version, and workloads." }),
    "nist-genai-profile": Object.freeze({ kind: "Official risk-management framework", shortTitle: "NIST AI 600-1", note: "Organizes generative-AI risk management around Govern, Map, Measure, and Manage. It is a voluntary resource, not a product certification, universal compliance checklist, model ranking, retrieval benchmark, or hardware-performance standard." }),
    "nvidia-gpu-operator": Object.freeze({ kind: "Official product documentation", shortTitle: "NVIDIA GPU Operator", note: "Documents lifecycle management of NVIDIA drivers, container toolkit, device plugins, discovery, monitoring, and related Kubernetes components. Platform support, component versions, and upgrade compatibility remain dynamic." }),
    "nist-zero-trust": Object.freeze({ kind: "Official architecture guidance", shortTitle: "NIST SP 800-207", note: "Defines resource access around explicit, current identity and policy evaluation rather than implicit trust from network location." }),
    "chinchilla-2022": Object.freeze({ kind: "Primary research paper", shortTitle: "Chinchilla Scaling", note: "Studies compute-optimal relationships among model size, training tokens, and compute within the paper's model family, data, and settings. It is not a universal training-budget or capacity formula." }),
    "opentelemetry-semconv": Object.freeze({ kind: "Open observability specification", shortTitle: "OpenTelemetry SemConv", note: "Defines common semantics for traces, metrics, logs, events, and resources. AI-specific progress, data lineage, business outcomes, approvals, and risk evidence still require project-defined fields." }),
    "nccl-collectives": Object.freeze({ kind: "Official technical documentation", shortTitle: "NCCL Collectives", note: "Documents GPU collective operations, communicators, topology, network, and performance diagnostics. It defines communication mechanisms but does not promise linear scaling or one best network." }),
    "megatron-3d-parallelism-2021": Object.freeze({ kind: "Primary systems paper", shortTitle: "Megatron 3D Parallelism", note: "Studies composed data, tensor, and pipeline parallelism and the effects of cross-node communication, waiting, and bubbles. Results are limited to the paper's models, hardware, scale, and configuration." }),
    "nvidia-dgx-h100-data-center": Object.freeze({ kind: "Official facility-design guide", shortTitle: "DGX H100 Data Center Design", note: "Documents power, cooling, rack density, cabling, airflow, and deployment coordination for DGX H100 SuperPOD. Its numeric requirements do not generalize to other accelerators or sites." }),
    "torchsnapshot-checkpoint": Object.freeze({ kind: "Official framework documentation", shortTitle: "TorchSnapshot Checkpoint", note: "Documents capture and restore of model, optimizer, learning-rate scheduler, custom application state, and RNG, plus consistent asynchronous snapshots. Data-cursor and topology coverage remain application-specific." }),
  }),
});
