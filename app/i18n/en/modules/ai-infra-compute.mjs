export const englishModule = Object.freeze({
  slug: "ai-infra-compute",
  title: "AI Compute Infrastructure",
  subtitle: "Size accelerators, memory, fabrics, storage, power, and supply from the workload",
  definition: "AI Compute Infrastructure is the physical system that carries model training and inference: accelerators, device and host memory, scale-up fabric, scale-out network, storage, power delivery, and cooling.",
  position: "This is the lowest layer of the AI stack and supplies deliverable capacity to inference engines and AI platforms. It answers whether hardware and the data path can sustain the target workload; it does not own scheduling policy, model routing, or application quality.",
  relatedSlugs: Object.freeze(["ai-infra-platform", "llm-training", "llm-inference", "model-landscape", "ai-ops"]),
  sections: Object.freeze([
    Object.freeze({ id: "study-guide", eyebrow: "LEARN, PRACTICE, PROVE", title: "Start with the workload, then locate the narrowest resource", lead: "Peak specifications are screening data. Procurement evidence comes from the target model, software stack, request or training shape, long-run profile, failure recovery, supply boundary, and full cost per conforming result.", blocks: Object.freeze([
      Object.freeze({ type: "cards", title: "Learning outcomes", items: Object.freeze([
        Object.freeze({ id: "outcome-workload-first", title: "Size from the workload", body: "Start from model, precision, context, batch, parallelism, data, concurrency, SLO, and growth rather than a popular accelerator." }),
        Object.freeze({ id: "outcome-resource-chain", title: "Read the complete resource chain", body: "Connect compute, HBM, host memory, scale-up fabric, scale-out network, storage, power, and cooling." }),
        Object.freeze({ id: "outcome-scale-boundaries", title: "Separate scale-up and scale-out", body: "Explain when tightly coupled in-node expansion differs from multi-node network scaling." }),
        Object.freeze({ id: "outcome-sustainable-tco", title: "Purchase sustainable results", body: "Use long-run performance, reliability, recoverability, availability, and TCO rather than one peak run." }),
      ]) }),
      Object.freeze({ type: "steps", title: "Recommended learning route", items: Object.freeze([
        Object.freeze({ id: "route-workload-profile", title: "Profile the workload", body: "Record model, precision, context, batch, parallel plan, data, concurrency, and SLO.", decision: "Checkpoint: explain why training and online serving need different resource curves." }),
        Object.freeze({ id: "route-constraint-layer", title: "Locate the constraint", body: "Test compute, HBM, scale-up fabric, network, storage, and facility power layer by layer.", decision: "Checkpoint: peak FLOPS never substitutes for end-to-end evidence." }),
        Object.freeze({ id: "route-supply-economics", title: "Validate supply and economics", body: "Compare purchase, reserve, on-demand cloud, model API, heterogeneous capacity, and delivery lead time.", decision: "Checkpoint: TCO includes software adaptation, idle reserve, failure, operations, and supply risk." }),
        Object.freeze({ id: "route-end-to-end-soak", title: "Run end to end and long", body: "Cover loading, steady state, peak, data feed, degradation, failure, and recovery.", decision: "Checkpoint: comparison is not a short cache-warmed peak." }),
        Object.freeze({ id: "route-cost-per-result", title: "Cost the conforming result", body: "Include quality, sustained performance, idle capacity, energy, software, and people.", decision: "Checkpoint: owned, rented, and API capacity share one business denominator." }),
      ]) }),
      Object.freeze({ type: "cards", title: "Practice labs", items: Object.freeze([
        Object.freeze({ id: "lab-compute-requirement", title: "Build a compute-requirement profile", subtitle: "The customer provides only parameter count and a proposed GPU count", body: "Add training-versus-inference purpose, precision, context, concurrency, and SLO; separate weights, activations, optimizer state, and KV cache; list network, storage, power, and recovery tests.", decision: "Deliverable: workload assumptions and capacity hypotheses.", boundary: "Acceptance: every capacity number traces to a load assumption and training and serving estimates remain separate.", sourceIds: Object.freeze(["flashattention-2022", "vllm-2023"]) }),
        Object.freeze({ id: "lab-cluster-investment", title: "Compare two cluster investments", subtitle: "Option A has higher peak compute; option B has a mature software stack and faster cloud delivery", body: "Measure sustained target-framework performance and stability; include migration, supply, energy, and recovery cost; run sensitivity analysis for growth and delivery delay.", decision: "Deliverable: a performance-risk-TCO decision.", boundary: "Acceptance: the recommendation does not rest on one specification sheet and identifies dynamic facts to recheck at purchase.", sourceIds: Object.freeze(["nvidia-gpu-operator", "nist-genai-profile"]) }),
        Object.freeze({ id: "lab-bottleneck-hypothesis", title: "Test a compute-bottleneck hypothesis", subtitle: "Adding accelerators barely changes training throughput", body: "Capture compute, HBM, in-node fabric, network, data-load, and checkpoint metrics; compare single- and multi-node; change one major variable and rerun long and failure tests.", decision: "Deliverable: a bottleneck-evidence report.", boundary: "Acceptance: distinguish compute, memory, communication, I/O, and recovery overhead and state where the conclusion cannot be extrapolated.", sourceIds: Object.freeze(["flashattention-2022", "opentelemetry-semconv", "nist-genai-profile"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "curriculum", eyebrow: "CURRICULUM ATLAS", title: "The complete physical data path", lead: "AI capacity is a path from numeric representation through accelerator and memory to local fabric, network, storage, power, cooling, and the software ecosystem that makes the path usable.", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "curriculum-workload-first", title: "Workload profile and five-layer stack", subtitle: "Workload First", body: "Training, online inference, long context, batch, and agentic systems stress compute, memory, network, and latency differently. Model, precision, sequence, batch, parallelism, data, SLO, and growth are the sizing inputs.", decision: "Freeze workload assumptions and acceptance before comparing hardware.", boundary: "Parameter count and GPU count omit KV cache, activations, parallel efficiency, and operating reserve.", sourceIds: Object.freeze(["vllm-2023", "nist-genai-profile"]) }),
        Object.freeze({ id: "curriculum-compute-precision", title: "Accelerator compute, precision, and Roofline reasoning", subtitle: "Compute & Precision", body: "Matrix units provide high throughput under specific numeric formats. Roofline reasoning compares compute capability with memory bandwidth to determine whether the workload is compute- or bandwidth-limited. Kernels determine whether software can use the hardware path.", decision: "Profile target models and operators instead of ranking peak FLOPS.", boundary: "Vendor peaks often assume particular precision and sparsity and do not represent sustained end-to-end performance.", sourceIds: Object.freeze(["flashattention-2022", "nist-genai-profile"]) }),
        Object.freeze({ id: "curriculum-memory-system", title: "HBM and the memory hierarchy", subtitle: "Memory System", body: "Weights, activations, optimizer state, and KV cache have different memory accounts. HBM capacity determines fit, bandwidth affects data movement, and host memory plus local storage affect load and spill. Training and inference must be sized separately.", decision: "Evaluate capacity, bandwidth, fragmentation, runtime workspace, and safety reserve together.", boundary: "More VRAM solves capacity, not necessarily bandwidth, fabric, or end-to-end speed.", sourceIds: Object.freeze(["flashattention-2022", "vllm-2023"]) }),
        Object.freeze({ id: "curriculum-accelerator-choice", title: "Accelerators, software ecosystem, and heterogeneity", subtitle: "Accelerator Choice", body: "GPUs, cloud ASICs, and other accelerators compete through hardware, compilers, operators, frameworks, debugging, skills, and supply. Migration cost includes model and kernel coverage, operations, and future upgrade—not recompilation alone.", decision: "Run representative training, inference, failure, and upgrade paths before volume purchase.", boundary: "One successful benchmark does not prove support for every model, operator, or future version.", sourceIds: Object.freeze(["nvidia-gpu-operator", "nist-genai-profile"]) }),
        Object.freeze({ id: "curriculum-scale-up", title: "Scale-up fabric", subtitle: "Scale-up", body: "When model or parallel state spans devices, local fabric carries collectives such as AllReduce, AllGather, and ReduceScatter. Topology, link behavior, and communication volume jointly determine scale.", decision: "Choose the in-node device count from the parallel plan and communication profile.", boundary: "NCCL defines communication primitives and diagnostics; it does not guarantee that the target framework sustains a link specification, and single-node results do not predict multi-node scale.", sourceIds: Object.freeze(["nccl-collectives", "megatron-3d-parallelism-2021"]) }),
        Object.freeze({ id: "curriculum-scale-out", title: "Scale-out network and collectives", subtitle: "Scale-out", body: "Multi-node training carries data-, tensor-, or pipeline-parallel communication across the network. RDMA, congestion, topology, and the measured collective share determine whether fabric is the bottleneck.", decision: "Validate AllReduce, All-to-All, and representative long-running jobs on the target network.", boundary: "NCCL and Megatron document mechanisms and specific experiments, not a universal network-upgrade benefit. A short link test also misses congestion and recovery.", sourceIds: Object.freeze(["nccl-collectives", "megatron-3d-parallelism-2021"]) }),
        Object.freeze({ id: "curriculum-feed-cluster", title: "Storage, data path, and power", subtitle: "Feed the Cluster", body: "Training input, checkpoints, model distribution, and inference load combine object stores, parallel file systems, local caches, and network. Power, cooling, rack density, cabling, and delivery conditions bound physical deployment.", decision: "Include sustained data-to-device throughput, recovery, and facility engineering acceptance in capacity planning.", boundary: "DGX H100 power and airflow values apply only to that reference architecture; cache-warmed steady state cannot hide first load, recovery, or peak power.", sourceIds: Object.freeze(["nvidia-dgx-h100-data-center", "torchsnapshot-checkpoint"]) }),
        Object.freeze({ id: "curriculum-tco-supply", title: "Compute economics and procurement evidence", subtitle: "TCO & Supply", body: "Purchase, long-term rental, on-demand cloud, and model APIs allocate capital, utilization, elasticity, and operations risk differently. TCO includes software adaptation, network, storage, energy, idle reserve, failure, skills, and delivery lead time.", decision: "Compare full cost and delivery risk per conforming training or inference result.", boundary: "Current hourly price, one inventory snapshot, and one benchmark cannot establish durable capacity.", sourceIds: Object.freeze(["nist-genai-profile", "vllm-2023"]) }),
        Object.freeze({ id: "curriculum-benchmark-acceptance", title: "End-to-end benchmark and acceptance", subtitle: "Benchmark & Acceptance", body: "Use the target model, precision, sequences, concurrency, framework, and data path and include cold start, long-run stability, failure recovery, and quality.", decision: "Accept sustained performance, recoverability, supply, and full cost per conforming result.", boundary: "A public benchmark, peak FLOPS, or cache-warmed steady state does not represent the customer production load.", sourceIds: Object.freeze(["flashattention-2022", "vllm-2023", "nist-genai-profile"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "principles", eyebrow: "PHYSICAL FOUNDATION", title: "Size compute, memory, fabric, data path, and facility as one system", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "principle-workload-sizing", title: "Workload-first sizing", body: "Training, tuning, online serving, batch generation, and vector processing stress resources differently.", decision: "Freeze model, precision, context, batch, concurrency, SLO, and growth before procurement." }),
        Object.freeze({ id: "principle-compute-precision", title: "Compute and numerical precision", body: "Accelerators provide different useful throughput by data type, sparsity, operator, and software support.", decision: "Compare a runnable target framework and model rather than peak FLOPS." }),
        Object.freeze({ id: "principle-memory-hierarchy", title: "Memory hierarchy", body: "Weights, optimizer state, activations, and KV cache determine capacity; bandwidth and data movement can be as important as arithmetic.", decision: "Size training and inference separately and reserve for buffers, fragmentation, concurrency, and runtime." }),
        Object.freeze({ id: "principle-scale-up", title: "Scale-up fabric", body: "High-bandwidth links inside a node or tightly coupled domain govern exchange of parameters, activations, and cache.", decision: "Validate topology, bandwidth, and collectives before spreading a model across devices." }),
        Object.freeze({ id: "principle-scale-out", title: "Scale-out network", body: "Training synchronization, expert routing, distributed inference, and storage traffic need low-latency, high-throughput, stable networking.", decision: "Design network from communication pattern and failure domain with congestion, telemetry, and recovery." }),
        Object.freeze({ id: "principle-storage-power-tco", title: "Storage, power, and total cost", body: "Loading, checkpoints, power, cooling, idle capacity, and operations jointly determine usable supply and TCO.", decision: "Cost useful job time and scaling lead time rather than device purchase price alone." }),
      ]) }),
      Object.freeze({ type: "boundary", title: "Critical boundary", items: Object.freeze([Object.freeze({ id: "compute-foundation-boundary", title: "Physical capacity is not the complete AI outcome", body: "Compute infrastructure supplies hardware and data-path capacity.", boundary: "It does not decide queue priority, GPU sharing, model release, routing, or business quality. Peak FLOPS, device count, and VRAM must be validated with the target model and software stack." })]) }),
    ]) }),
    Object.freeze({ id: "decisions", eyebrow: "PRESALES DECISIONS", title: "Choose hardware and supply from evidence", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "decision-workload-before-accelerator", title: "Choose the workload before the accelerator", body: "A popular GPU is being discussed before model, framework, precision, and SLO are frozen.", decision: "Build representative training and inference loads, then screen hardware that supports the target stack and benchmark end to end.", boundary: "Peak specifications and synthetic operators only shortlist; they do not prove model or distributed efficiency." }),
        Object.freeze({ id: "decision-buy-rent-api", title: "Purchase, reserve, on-demand, or API?", body: "Loads vary between exploration, bursts, and stable production and the organization values capital and operating control differently.", decision: "Use elastic cloud or API for exploration and peaks; compare reserve, dedicated, and owned capacity for sustained high utilization; retain a hybrid path.", boundary: "TCO includes idle, software, network, storage, energy, people, resilience, and upgrades." }),
        Object.freeze({ id: "decision-single-multi-node", title: "Multi-GPU node or multi-node cluster?", body: "Model or throughput exceeds one device, while the communication cost is unclear.", decision: "Prefer high-bandwidth in-node scale-up first; design network and recovery from the parallel strategy only when scale-out is required.", boundary: "More nodes increase communication, debugging, and failure exposure; measure scaling efficiency." }),
        Object.freeze({ id: "decision-heterogeneous-accelerator", title: "Use alternative or heterogeneous accelerators?", body: "Cost, supply, region, or energy motivates multiple GPU, NPU, or custom-accelerator types.", decision: "Validate model coverage, operators, compiler, quantization, collectives, images, telemetry, operations, and skills before comparing portable layers.", boundary: "Running a demo does not establish production ecosystem maturity or an upgrade path." }),
        Object.freeze({ id: "decision-investment-layer", title: "Invest in compute, network, or storage first?", body: "Devices wait for data, checkpoints are slow, communication congests, or load time dominates.", decision: "Profile compute, memory, communication, and I/O wait end to end and invest at the measured bottleneck.", boundary: "Local peak upgrades move bottlenecks; balance matters more than one overprovisioned layer." }),
      ]) }),
    ]) }),
    Object.freeze({ id: "deep-dive", eyebrow: "BOTTLENECK & PROCUREMENT PROOF", title: "Use profiles and controlled comparisons to direct investment", lead: "The same slow job or underutilized GPU can be compute-, memory-, communication-, or I/O-limited. Only staged evidence can determine the next purchase.", blocks: Object.freeze([
      Object.freeze({ type: "table", title: "Four resource bottlenecks", columns: Object.freeze(["Bottleneck", "Evidence", "Action", "Boundary"]), items: Object.freeze([
        Object.freeze({ id: "bottleneck-compute", title: "Compute-bound", body: "Compute units remain busy, operator time scales with problem size, and memory-layout or I/O changes have limited effect.", decision: "Prove the target precision and kernels use the purchased compute path before comparing accelerators, compilation, or model parallelism.", boundary: "Peak FLOPS does not prove the target framework can sustain them.", sourceIds: Object.freeze(["flashattention-2022"]) }),
        Object.freeze({ id: "bottleneck-memory", title: "Memory- or bandwidth-bound", body: "HBM transfers, KV-cache access, or activation movement dominate while arithmetic utilization appears modest.", decision: "Compare capacity, bandwidth, fusion, precision, and cache management and reduce movement before buying more peak compute.", boundary: "Larger memory solves capacity, not necessarily bandwidth or speed.", sourceIds: Object.freeze(["flashattention-2022", "vllm-2023"]) }),
        Object.freeze({ id: "bottleneck-communication", title: "Communication-bound", body: "Collective and synchronization wait rises with device count and is sensitive to topology, congestion, and stragglers.", decision: "Collect 1-, 2-, 4-, and 8-node scaling curves and communication profiles before choosing fabric or parallel changes.", boundary: "Single-node benchmarks cannot predict multi-node training or inference.", sourceIds: Object.freeze(["opentelemetry-semconv"]) }),
        Object.freeze({ id: "bottleneck-io", title: "Storage- or I/O-bound", body: "Devices wait for data, weights, or checkpoints and show long gaps at startup, save, or recovery.", decision: "Measure load, prefetch, cache, checkpoint, and restore throughput before choosing local NVMe, parallel storage, object storage, or network.", boundary: "Cache-warmed speed cannot hide first load, data updates, and recovery.", sourceIds: Object.freeze(["opentelemetry-semconv"]) }),
      ]) }),
      Object.freeze({ type: "steps", title: "Five procurement proofs", items: Object.freeze([
        Object.freeze({ id: "proof-replayable-workload", title: "Replayable workload", body: "Freeze model, framework, precision, sequence distribution, parallel plan, SLO, and growth as one benchmark package.", decision: "Run the same version on every candidate without changing batch, quantization, or model to showcase each device.", boundary: "Refresh the package as business demand changes.", sourceIds: Object.freeze(["nist-genai-profile"]) }),
        Object.freeze({ id: "proof-end-to-end-profile", title: "End-to-end profile", body: "Record compute, memory, bandwidth, communication, I/O, queue, and failure so every wait maps to a resource layer.", decision: "Fix the main current bottleneck and anticipate where it moves next.", boundary: "Short samples and averages miss congestion, checkpoints, and tails.", sourceIds: Object.freeze(["flashattention-2022", "vllm-2023"]) }),
        Object.freeze({ id: "proof-scaling-efficiency", title: "Scaling-efficiency curve", body: "Increase devices and nodes stepwise and record useful throughput, duration, communication share, failures, and cost per result.", decision: "Select an economic scale before marginal benefit collapses.", boundary: "Twice the devices does not imply twice the performance, and a peak short run does not prove stability.", sourceIds: Object.freeze(["opentelemetry-semconv"]) }),
        Object.freeze({ id: "proof-failure-recovery", title: "Failure-recovery drill", body: "Inject degraded devices, node exit, network disturbance, and capacity loss and measure replay, checkpoints, replacement, and data consistency.", decision: "Include restore time, lost work, and replay cost in capacity and resilience.", boundary: "Infrastructure self-healing does not prove training state or business request recovery.", sourceIds: Object.freeze(["nvidia-gpu-operator", "nist-genai-profile"]) }),
        Object.freeze({ id: "proof-supply-cost", title: "Supply and full cost", body: "Combine regional inventory, lead time, reserve, Spot interruption, licenses, network, storage, energy, idle, and people.", decision: "Use different supply modes for stable base, elastic peak, and recovery and compare cost per successful result.", boundary: "One hourly price or inventory snapshot does not establish long-term deliverability.", sourceIds: Object.freeze(["nist-genai-profile"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "cloud", eyebrow: "CLOUD DELIVERY", title: "Use cloud capacity according to workload shape", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "cloud-elastic-compute", title: "Elastic accelerated compute", subtitle: "GPU or NPU instances, bare metal, dedicated hosts, on-demand, and Spot", body: "Select accelerators by model and stage and obtain experiment, peak, or production capacity quickly.", decision: "Discover training versus inference, sustained utilization, start deadline, interruptibility, and region." }),
        Object.freeze({ id: "cloud-cluster-fabric", title: "AI cluster and fabric", subtitle: "High-performance network, RDMA, placement, dedicated links, and load balancing", body: "Provide predictable communication for distributed training and inference while controlling failure domains.", decision: "Discover parallel strategy, communication share, scale, and recovery objective." }),
        Object.freeze({ id: "cloud-data-checkpoint", title: "Data and checkpoint path", subtitle: "Object storage, parallel file systems, local NVMe, cache, backup, and archive", body: "Move data, weights, and checkpoints while balancing cost, durability, and restore time.", decision: "Discover dataset and model-load size, checkpoint cadence, recovery point, and retention." }),
        Object.freeze({ id: "cloud-capacity-economics", title: "Capacity and economics", subtitle: "Reserve, commitment discounts, cost management, energy, and infrastructure telemetry", body: "Combine stable base and elastic peak and observe real cost by useful job and successful task.", decision: "Discover utilization, queue, replay, idle cost, and ownership of future capacity forecasts." }),
      ]) }),
    ]) }),
  ]),
  qa: Object.freeze([
    Object.freeze({ id: "peak-compute-not-speed", q: "Why does higher theoretical accelerator compute not guarantee faster training or inference?", a: "Execution also depends on numeric type, memory bandwidth, operators, communication, batch shape, kernels, scheduling, and I/O. Peak compute matters only when the workload can keep the units fed.", depth: "Attention can be memory-traffic limited, distributed work can be collective-communication limited, and serving can be KV-cache and scheduler limited. Compare the same model, precision, framework, and workload on useful tokens per second, duration, SLO, energy, and profile evidence.", ask: "Ask the customer: what evidence identifies the current bottleneck, and which precision, parallel strategy, and software version are used?", tag: "Performance reasoning", basis: "IO-aware algorithms plus serving systems", evidence: Object.freeze([Object.freeze({ sourceId: "flashattention-2022", supports: "Supports the performance impact of transfers between high-bandwidth and on-chip memory." }), Object.freeze({ sourceId: "vllm-2023", supports: "Supports the serving impact of KV-cache memory management and batching." })]) }),
    Object.freeze({ id: "weights-fit-not-serviceable", q: "If the weights fit in VRAM, can the accelerator serve the model reliably?", a: "Not necessarily. Inference also needs KV cache, workspaces, runtime, and concurrency; training additionally needs activations, gradients, and optimizer state.", depth: "Add precision, context, batch, concurrency, fragmentation, framework reserve, and communication buffers. Long-context serving may be capacity-limited by the KV cache. Accept with rejection, P95, throughput, and a sustained soak test—not a successful load.", ask: "Ask the customer: is the target loading, one completed request, or peak-concurrency SLO?", tag: "Memory planning", basis: "KV-cache memory management", evidence: Object.freeze([Object.freeze({ sourceId: "vllm-2023", supports: "Identifies KV cache as a major dynamic memory cost affecting serving concurrency and throughput." })]) }),
    Object.freeze({ id: "on-prem-needs-cloud", q: "Why might a customer with an existing data center still use cloud AI compute?", a: "Cloud can supplement experiment speed, scarce device types, peak capacity, region, and managed data paths rather than simply replace owned devices. The decision depends on workload and boundary.", depth: "Keep stable, sensitive, highly utilized base load on premises and use cloud for evaluation, migration, peaks, or disaster recovery. Compare data movement, links, identity, images, licenses, reserve, and operations. Product availability remains provider- and region-specific.", ask: "Ask the customer: is the local gap capacity, device type, delivery lead time, elasticity, or operations, and which data or models cannot move?", tag: "Hybrid cloud", basis: "Workload tiering plus TCO", evidence: Object.freeze([Object.freeze({ sourceId: "nist-zero-trust", supports: "Supports continuing identity- and policy-based access across hybrid environments rather than trusting location." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports deployment decisions based on context, supply chain, and organizational risk." })]) }),
    Object.freeze({ id: "gpu-hours-cost", q: "Can project cost be estimated directly from GPU count or GPU-hours?", a: "Only as a rough bound. Real cost depends on useful utilization, queueing, failed replays, scaling efficiency, storage, network, operations, and accepted business outcomes.", depth: "Training cost includes data preparation, experiments, and failed runs per acceptable model version. Serving cost is per successful task at quality and SLO. Compare on-demand, reserve, Spot, and dedicated capacity with interruption, restore, and idle cost. Build the scaling model from a profile.", ask: "Ask the customer: is the denominator one experiment, one accepted model, one million tokens, or one successful business task?", tag: "TCO", basis: "End-to-end cost model", evidence: Object.freeze([Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports evaluating system performance, resources, risks, and impacts in context rather than one metric." })]) }),
    Object.freeze({ id: "procurement-beyond-gpu", q: "Why can AI procurement not compare GPU model and memory alone?", a: "Deliverable capacity also depends on node topology, network, storage, CPU, power, cooling, software stack, and the target workload.", depth: "Training stresses collectives, data supply, and long-run stability; serving stresses KV cache, TTFT, concurrency, and failure domains. Accept representative models on useful throughput, job duration, SLO, scale, energy, and recovery.", ask: "Ask the customer: is the main load training, batch inference, or online service, and which model, precision, concurrency, and topology support the capacity claim?", tag: "Procurement method", basis: "End-to-end workload modeling", evidence: Object.freeze([Object.freeze({ sourceId: "flashattention-2022", supports: "Supports the role of memory hierarchy and data movement in operator performance." }), Object.freeze({ sourceId: "vllm-2023", supports: "Supports the role of cache management and scheduling in serving capacity." })]) }),
    Object.freeze({ id: "high-speed-interconnect", q: "When is a high-speed interconnect necessary, and when is one multi-GPU node enough?", a: "A faster inter-node network has value when the required parallel plan crosses nodes and collective communication is a measured bottleneck.", depth: "Independent batch partitions and workloads that fit one node may instead be limited by memory, scheduling, or request shape. For multi-node training, identify the data-, tensor-, or pipeline-parallel communication, measure collective time and effective bandwidth, and compare useful throughput before and after scale-out.", ask: "Ask the customer: how is the model partitioned, which collectives dominate, and how do useful throughput and duration change from one to multiple nodes?", tag: "Network selection", basis: "Parallel topology plus profiling", evidence: Object.freeze([Object.freeze({ sourceId: "nccl-collectives", supports: "Documents GPU collective operations and communication diagnostics; it does not guarantee that any network class is necessary or faster for a workload." }), Object.freeze({ sourceId: "megatron-3d-parallelism-2021", supports: "Shows that data, tensor, and pipeline parallelism can be limited by cross-node communication and waiting in the paper's tested configurations." })]) }),
    Object.freeze({ id: "peak-flops-comparison", q: "Why should AI accelerators not be compared by peak FLOPS alone?", a: "Peak FLOPS applies to specific numeric formats and ideal operators. Real performance also depends on memory, fabric, kernels, data supply, and workload shape.", depth: "Run the target model, precision, sequence, batch, and framework over a sustained interval and observe compute, HBM bandwidth, communication, data wait, and recovery. Training and inference have different bottlenecks, and one peak does not explain TTFT, throughput, or scale.", ask: "Ask the customer: is the target compute-, memory-, or communication-bound, and where does each candidate become constrained?", tag: "Specification boundary", basis: "Roofline reasoning plus end-to-end validation", evidence: Object.freeze([Object.freeze({ sourceId: "flashattention-2022", supports: "Shows that reducing HBM traffic improves attention efficiency, making memory access a material constraint." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports performance and risk validation in the specific system and use context." })]) }),
    Object.freeze({ id: "double-gpu-not-double-speed", q: "Why does doubling GPUs often fail to double training speed?", a: "More devices can increase collective communication and waiting, while pipeline schedules can add bubbles; scaling therefore depends on the chosen parallel plan and target topology.", depth: "Compare single-device, single-node, and multi-node profiles for collective time, pipeline idle time, topology, input, checkpoints, and stragglers. Add devices only when the measured reduction in compute time exceeds the added coordination cost.", ask: "Ask the customer: which wait grew fastest after expansion, and what is useful training progress on one versus multiple nodes?", tag: "Scaling efficiency", basis: "Communication plus long-run systems", evidence: Object.freeze([Object.freeze({ sourceId: "megatron-3d-parallelism-2021", supports: "Directly studies data, tensor, and pipeline parallelism, cross-node communication, waiting, and pipeline bubbles; reported scaling is configuration-specific." }), Object.freeze({ sourceId: "nccl-collectives", supports: "Defines AllReduce, All-to-All, and related collectives and diagnostics but does not promise linear job scaling." })]) }),
    Object.freeze({ id: "highest-network-not-required", q: "Does every training cluster need the highest-specification inter-node network?", a: "No. A network upgrade helps only when the target parallel plan crosses nodes and measured communication materially limits useful progress.", depth: "Profile the relevant collectives, compute overlap, and one-to-multiple-node scaling, then compare networks under the same model and parallel configuration. A workload that fits one node or waits on data may not benefit from a more expensive fabric.", ask: "Ask the customer: at how many devices does communication dominate, and what is the network cost per added unit of Goodput?", tag: "Network investment", basis: "Communication share plus scaling efficiency", evidence: Object.freeze([Object.freeze({ sourceId: "nccl-collectives", supports: "Documents collective operations and network/topology diagnostics; it does not identify a universal best network specification." }), Object.freeze({ sourceId: "megatron-3d-parallelism-2021", supports: "Shows communication and waiting can constrain composed parallelism in the paper's tested cluster; it does not prescribe every customer's network." })]) }),
    Object.freeze({ id: "low-gpu-utilization-accelerator", q: "Does low GPU compute utilization mean that a stronger accelerator is needed?", a: "No. The bottleneck may be storage, CPU preprocessing, network, input, or checkpoint I/O; a stronger GPU can simply wait faster.", depth: "Measure object storage, file system, CPU decode, host memory, PCIe, HBM, and operator time along the data path. Repair the narrowest layer and rerun a sustained target task; one utilization number cannot direct procurement.", ask: "Ask the customer: what is the device waiting for, and how much wall time goes to input preparation and checkpoints?", tag: "Data supply", basis: "End-to-end path plus bottleneck location", evidence: Object.freeze([Object.freeze({ sourceId: "flashattention-2022", supports: "Supports memory access rather than theoretical arithmetic as a potential bottleneck." }), Object.freeze({ sourceId: "opentelemetry-semconv", supports: "Provides common telemetry semantics across infrastructure stages." })]) }),
    Object.freeze({ id: "rack-space-not-facility-ready", q: "Does available rack space mean a facility is ready for a high-density AI cluster?", a: "No. The target system also has power, heat-rejection, airflow, cabling, delivery, safety, and operating requirements that need facility engineering acceptance.", depth: "Build the facility plan from the exact system and rack configuration, including continuous and peak electrical load, cooling and airflow, power redundancy, network cable paths, maintenance access, and expansion constraints. Do not reuse one reference design's numeric limits for another platform.", ask: "Ask the customer: what continuous power and heat rejection can each rack support, and how much capacity may be lost under failure or high temperature?", tag: "Power and cooling", basis: "Facility constraints plus sustained performance", evidence: Object.freeze([Object.freeze({ sourceId: "nvidia-dgx-h100-data-center", supports: "Directly documents power, cooling, rack density, cabling, airflow, and cross-team planning for DGX H100 SuperPOD; its numeric values are product- and design-specific." })]) }),
    Object.freeze({ id: "heterogeneous-supply-risk", q: "Does using several GPU or AI-accelerator types automatically reduce supply risk?", a: "Only when the software, model formats, operators, precision, and operations are actually portable. Otherwise heterogeneity can increase fragmentation and maintenance cost.", depth: "Run representative training, inference, quantization, monitoring, failure, and upgrade paths and record missing operators, performance, and skills. A portable layer need not be the lowest common denominator; critical work can retain specialized kernels with an explicit dependency.", ask: "Ask the customer: must all workloads move, or only critical ones, and which layer is currently bound to one hardware ecosystem?", tag: "Heterogeneous portability", basis: "Supply continuity plus software adaptation", evidence: Object.freeze([Object.freeze({ sourceId: "flashattention-2022", supports: "Shows that high-performance operators depend on concrete memory hierarchy and implementation." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports identifying third-party and supply-chain dependencies and preparing alternatives." })]) }),
  ]),
  evidenceCards: Object.freeze([
    Object.freeze({ id: "io-aware-effective-performance", metric: "IO-aware", title: "Higher peak compute is not higher effective performance", finding: "FlashAttention explains attention performance through memory traffic as well as arithmetic, demonstrating the importance of data movement to accelerator utilization.", boundary: "This is operator-level evidence and does not represent full-model, network, or cluster performance by itself.", sourceId: "flashattention-2022", accent: true }),
    Object.freeze({ id: "dynamic-serving-memory", metric: "Dynamic memory", title: "Serving capacity includes runtime state", finding: "The vLLM paper shows that KV-cache allocation, fragmentation, and sharing materially affect concurrent requests and serving throughput.", boundary: "Capacity still depends on model, precision, context, engine configuration, hardware, and workload.", sourceId: "vllm-2023" }),
    Object.freeze({ id: "complete-gpu-runtime", metric: "Software + hardware", title: "Delivering a device requires the complete runtime stack", finding: "NVIDIA GPU Operator manages Kubernetes components such as drivers, device plugins, container toolkit, node labeling, and monitoring.", boundary: "This is one vendor's Kubernetes operating solution and does not represent every accelerator or platform implementation.", sourceId: "nvidia-gpu-operator" }),
  ]),
  terms: Object.freeze({
    "access-spectrum": Object.freeze({ name: "Model Access Spectrum", definition: "The range from managed APIs to open-weight and open-source systems, which changes hardware control and operating responsibility." }),
    "kv-cache": Object.freeze({ name: "Key-Value Cache", abbr: "KV Cache", definition: "Attention Keys and Values retained across autoregressive decoding to avoid recomputing the complete prefix; memory grows with architecture, sequence length, precision, and active requests." }),
    "ai-infra-compute": Object.freeze({ name: "AI Compute Infrastructure", definition: "The physical accelerator, memory, fabric, network, storage, power, and cooling foundation for AI workloads." }),
    "heterogeneous-compute": Object.freeze({ name: "Heterogeneous Compute", definition: "Operating workloads across different CPU, GPU, or accelerator types with explicit software, placement, observability, and portability boundaries." }),
    vram: Object.freeze({ name: "Video Random Access Memory", abbr: "VRAM", definition: "Accelerator memory that holds weights, activations, caches, workspaces, and intermediate state." }),
    hbm: Object.freeze({ name: "High Bandwidth Memory", abbr: "HBM", definition: "High-capacity, high-throughput memory colocated with accelerators and central to model fit and data movement." }),
    "scale-up": Object.freeze({ name: "Scale-up", definition: "Expanding tightly coupled accelerator capacity within a node or local high-bandwidth fabric." }),
    "scale-out": Object.freeze({ name: "Scale-out", definition: "Expanding a workload across nodes through network, collective communication, congestion control, and distributed recovery." }),
    tco: Object.freeze({ name: "Total Cost of Ownership", abbr: "TCO", definition: "Acquisition, software, network, storage, energy, idle reserve, people, failure, recovery, supply, and operations cost over the lifecycle." }),
  }),
  sources: Object.freeze({
    "flashattention-2022": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "FlashAttention", note: "Presents an IO-aware exact-attention algorithm that reduces memory traffic. Realized benefit depends on sequence shape, hardware, kernels, and the surrounding training or inference stack." }),
    "vllm-2023": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "vLLM / PagedAttention", note: "Introduces PagedAttention and KV-cache memory management for LLM serving. Reported throughput remains specific to the tested models, hardware, engine version, and workloads." }),
    "nist-genai-profile": Object.freeze({ kind: "Official risk-management framework", shortTitle: "NIST AI 600-1", note: "Organizes generative-AI risk management around Govern, Map, Measure, and Manage. It is a voluntary resource, not a product certification, universal compliance checklist, model ranking, retrieval benchmark, or hardware-performance standard." }),
    "nvidia-gpu-operator": Object.freeze({ kind: "Official product documentation", shortTitle: "NVIDIA GPU Operator", note: "Documents lifecycle management of NVIDIA drivers, container toolkit, device plugins, discovery, monitoring, and related Kubernetes components. Platform support, component versions, and upgrade compatibility remain dynamic." }),
    "nist-zero-trust": Object.freeze({ kind: "Official architecture guidance", shortTitle: "NIST SP 800-207", note: "Defines resource access around explicit, current identity and policy evaluation rather than implicit trust from network location." }),
    "chinchilla-2022": Object.freeze({ kind: "Primary research paper", shortTitle: "Chinchilla Scaling", note: "Studies compute-optimal relationships among model size, training tokens, and compute within the paper's model family, data, and settings. It is not a universal training-budget or capacity formula." }),
    "opentelemetry-semconv": Object.freeze({ kind: "Open observability specification", shortTitle: "OpenTelemetry SemConv", note: "Defines common semantics for traces, metrics, logs, events, and resources. AI-specific progress, data lineage, business outcomes, approvals, and risk evidence still require project-defined fields." }),
    "nccl-collectives": Object.freeze({ kind: "Official technical documentation", shortTitle: "NCCL Collectives", note: "Documents GPU collective operations, communicators, topology, network, and performance diagnostics. It defines communication mechanisms but does not promise linear scaling or one best network." }),
    "megatron-3d-parallelism-2021": Object.freeze({ kind: "Primary systems paper", shortTitle: "Megatron 3D Parallelism", note: "Studies composed data, tensor, and pipeline parallelism and the effects of cross-node communication, waiting, and bubbles. Results are limited to the paper's models, hardware, scale, and configuration." }),
    "nvidia-dgx-h100-data-center": Object.freeze({ kind: "Official facility-design guide", shortTitle: "DGX H100 Data Center Design", note: "Documents power, cooling, rack density, cabling, airflow, and deployment coordination for DGX H100 SuperPOD. Its numeric requirements do not generalize to other accelerators or sites." }),
    "torchsnapshot-checkpoint": Object.freeze({ kind: "Official framework documentation", shortTitle: "TorchSnapshot Checkpoint", note: "Documents capture and restore of model, optimizer, learning-rate scheduler, custom application state, and RNG, plus consistent asynchronous snapshots. Data-cursor and topology coverage remain application-specific." }),
  }),
});
