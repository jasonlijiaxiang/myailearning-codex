export const englishModule = Object.freeze({
  slug: "llm",
  title: "Large Language Model Foundations",
  subtitle: "Use model mechanisms to diagnose application behavior",
  definition: "Large Language Model foundations explain how text becomes tokens, how a Transformer uses the current context to form a next-token distribution, and why that mechanism creates recognizable capability, reliability, latency, and control boundaries.",
  position: "This is the shared model-mechanism layer. It gives Model Landscape, prompt engineering, RAG, agents, training, and inference teams a common language, then routes application symptoms to the layer that can actually test and fix them.",
  relatedSlugs: Object.freeze(["model-landscape", "llm-training", "llm-inference", "prompt-engineering", "rag", "fine-tuning", "multimodal"]),
  sections: Object.freeze([
    Object.freeze({
      id: "study-guide",
      eyebrow: "LEARN, PRACTICE, PROVE",
      title: "Diagnose an application request through model mechanisms",
      lead: "Start with a visible failure in an enterprise knowledge assistant. Use tokens, attention, context, and autoregressive generation to explain plausible causes, then hand each hypothesis to the module that owns the deciding experiment.",
      blocks: Object.freeze([
        Object.freeze({
          type: "cards",
          title: "Learning outcomes",
          items: Object.freeze([
            Object.freeze({ id: "outcome-representation", title: "Read the input representation", body: "Explain tokens, embeddings, positional information, and the repeated structure of a Transformer block." }),
            Object.freeze({ id: "outcome-attention", title: "Explain attention without mystifying it", body: "Use the Query, Key, and Value intuition while keeping attention weights separate from consciousness, causality, and audit evidence." }),
            Object.freeze({ id: "outcome-runtime", title: "Separate knowledge, context, and generation", body: "Distinguish patterns encoded in parameters, evidence supplied in the current request, and the autoregressive process that produces the answer." }),
            Object.freeze({ id: "outcome-capacity", title: "Route application symptoms", body: "Assign a testable failure hypothesis to model selection, prompt design, RAG, inference, orchestration, or an external control." }),
          ]),
        }),
        Object.freeze({
          type: "steps",
          title: "Recommended learning route",
          items: Object.freeze([
            Object.freeze({ id: "route-sequence-representation", title: "Begin with sequence representation", body: "Follow text through tokenization and vector representation, and establish why order must be encoded.", decision: "Checkpoint: explain why the same text can consume different token counts across models and therefore change cost." }),
            Object.freeze({ id: "route-information-flow", title: "Trace how information moves", body: "Follow attention, feed-forward layers, residual paths, and normalization as they transform a sequence representation.", decision: "Checkpoint: distinguish an attention weight, an explanatory hypothesis, and evidence for a fact." }),
            Object.freeze({ id: "route-generation-engineering", title: "Return to the application failure", body: "Study the autoregressive loop, sampling, the context window, and the KV cache, then compare those mechanisms with the observed request trace.", decision: "Checkpoint: name the next responsible module and the one-variable experiment that can falsify each hypothesis." }),
          ]),
        }),
        Object.freeze({
          type: "cards",
          title: "Practice labs",
          items: Object.freeze([
            Object.freeze({ id: "lab-minimal-attention", title: "Work a minimal attention example", subtitle: "Use simplified vectors for three tokens", body: "Calculate dot products, scaling, and Softmax weights; form the weighted sum of the Values; then change one token and observe the new result.", decision: "Deliverable: an attention calculation sheet with intermediate values and a plain-language explanation.", boundary: "Acceptance: the calculation demonstrates relevance-weighted aggregation without claiming that the model exposes readable internal thought.", sourceIds: Object.freeze(["transformer-2017"]) }),
            Object.freeze({ id: "lab-generation-request", title: "Triage one enterprise knowledge-assistant failure", subtitle: "The answer drifts, evidence is missed, and long documents delay the first token", body: "Capture the user request, assembled context, model and decoding configuration, token counts, evidence, tools, and end-to-end trace. Form separate hypotheses for base capability, evidence supply, generation control, serving, and orchestration.", decision: "Deliverable: a request timeline, a failure tree, and an evidence-backed handoff to Model Landscape, RAG, Prompt, LLM Inference, Agent, or Security.", boundary: "Acceptance: every conclusion names a falsifiable next test. Fluency, one successful run, and one latency metric are not sufficient attribution.", sourceIds: Object.freeze(["transformer-2017", "lost-middle", "vllm-2023", "nist-genai-profile"]) }),
          ]),
        }),
      ]),
    }),
    Object.freeze({
      id: "curriculum",
      eyebrow: "CURRICULUM ATLAS",
      title: "From application request to model behavior",
      lead: "Follow one request from tokenization through contextual computation and autoregressive output. The goal is to recognize what the model mechanism can explain and where another module must take over.",
      blocks: Object.freeze([
        Object.freeze({ type: "cards", items: Object.freeze([
          Object.freeze({ id: "curriculum-input-representation", title: "Tokens, embeddings, and position", subtitle: "Input Representation", body: "A tokenizer maps text to token IDs, embeddings map those IDs to vectors, and positional mechanisms preserve order. Languages, numbers, and code can have very different token densities, directly changing context occupancy, cost, and truncation risk.", decision: "Measure the token distribution with the candidate model's actual tokenizer and representative data.", boundary: "An embedding is a learned representation, not a directly readable fact database.", sourceIds: Object.freeze(["transformer-2017"]) }),
          Object.freeze({ id: "curriculum-attention-heads", title: "Queries, keys, values, and head sharing", subtitle: "MHA, MQA & GQA", body: "A Query represents what the current position is seeking, a Key supplies matching features, and a Value supplies information to aggregate. Multi-head attention uses independent KV heads; multi-query attention shares one set; grouped-query attention shares them in groups, trading representation choices against KV-cache size, memory bandwidth, and serving cost.", decision: "Record the Query-to-KV-head organization and validate its runtime consequences when comparing architectures.", boundary: "Fewer KV heads do not guarantee unchanged task quality, and attention weights are not a complete causal explanation or proof of provenance.", sourceIds: Object.freeze(["transformer-2017", "gqa-2023", "attention-not-explanation-2019"]) }),
          Object.freeze({ id: "curriculum-transformer-block", title: "Transformer block anatomy", subtitle: "Block Anatomy", body: "Attention exchanges information across positions; feed-forward layers transform each position; residual paths and normalization support deep optimization. How parameters and computation are distributed across these parts affects both capability and cost.", decision: "Connect architectural terms to training stability, inference compute, and memory instead of learning components in isolation.", boundary: "Models labeled 'Transformer' can differ materially in depth, width, attention design, and training data.", sourceIds: Object.freeze(["transformer-2017"]) }),
          Object.freeze({ id: "curriculum-architecture-variants", title: "Decoder-only models, causal masks, and sparse experts", subtitle: "Architecture Variants", body: "A decoder-only model uses a causal mask so each position conditions only on earlier tokens. A Mixture of Experts routes each token to a subset of experts, increasing parameter capacity while adding routing, load-balancing, and distributed-communication constraints.", decision: "Evaluate architecture through task and serving behavior, never through parameter count alone.", boundary: "Total parameters, active parameters, and inference cost are different quantities; a sparse model is not automatically faster.", sourceIds: Object.freeze(["transformer-2017", "switch-transformer-2022", "nist-genai-profile"]) }),
          Object.freeze({ id: "curriculum-autoregressive-generation", title: "Autoregressive generation and test-time compute", subtitle: "Autoregressive & Test-time Compute", body: "The model generates one token at a time. Temperature, top-p, stop conditions, structured constraints, and any supported reasoning configuration shape the resulting sequence. Model identity, decoding configuration, tools, and test-time compute collectively define an operating candidate.", decision: "Version generation and reasoning settings, and regress quality, latency, and token use together.", boundary: "More internal computation does not guarantee correctness, and a visible reasoning summary is not a verbatim chain of thought or an audit explanation.", sourceIds: Object.freeze(["deepseek-r1-2025", "openai-model-spec-hidden-cot", "nist-genai-profile"]) }),
          Object.freeze({ id: "curriculum-runtime-context", title: "Context, prefill, and the KV cache", subtitle: "Runtime Context", body: "Prefill processes the input sequence and retains Keys and Values for already processed context tokens so decode does not recompute the complete prefix at every step. Decode remains sequential. Longer contexts add prefill work and cache demand, while relevant evidence can still be used unevenly by position.", decision: "Distinguish missing knowledge, missing context, poor context assembly, and insufficient serving capacity.", boundary: "The advertised context limit is an allowed input size, not a guarantee that every position will be used reliably.", sourceIds: Object.freeze(["vllm-2023", "flashattention-2022", "lost-middle"]) }),
        ]) }),
      ]),
    }),
    Object.freeze({
      id: "principles",
      eyebrow: "CORE MECHANISM",
      title: "The model stack from tokens to generated output",
      blocks: Object.freeze([
        Object.freeze({ type: "cards", items: Object.freeze([
          Object.freeze({ id: "principle-token-unit", title: "Tokens are the processing units", body: "A tokenizer converts text into tokens and the model maps those tokens to vectors. Token counts vary with language, vocabulary, and model, affecting context capacity and API cost.", decision: "Use the candidate model's actual tokenizer; do not substitute characters or words." }),
          Object.freeze({ id: "principle-attention-context", title: "Attention aggregates context", body: "Queries, Keys, and Values separate what a position seeks, what each location can match, and what information is aggregated into a contextual representation.", decision: "Use attention to explain contextual aggregation, not as a direct causal audit." }),
          Object.freeze({ id: "principle-block-refinement", title: "Transformer blocks refine representations", body: "Attention exchanges information across positions, feed-forward networks transform each position, and residual paths plus normalization support stable deep learning.", decision: "Compare architecture, active parameters, and task evidence alongside total parameter count." }),
          Object.freeze({ id: "principle-autoregressive-loop", title: "Generation is an autoregressive loop", body: "The model normally emits one new token and conditions the next step on the extended sequence. Prefill and decode therefore have different compute and memory profiles.", decision: "Measure prefill, decode, and the complete request separately." }),
          Object.freeze({ id: "principle-context-budget", title: "Context is a finite budget", body: "The KV cache avoids repeated computation but consumes accelerator memory. Longer context can increase input cost, dilute useful evidence, and reduce concurrency.", decision: "Combine long context, retrieval, summarization, and external memory according to the task; do not treat the maximum window as an effective-use guarantee." }),
        ]) }),
        Object.freeze({ type: "boundary", title: "Critical boundary", items: Object.freeze([
          Object.freeze({ id: "principle-model-boundary", title: "Fluency is not authority", body: "An LLM predicts subsequent tokens from learned parameters and the current context.", boundary: "Fluent output does not establish factual correctness, authorization, or the validity of a business action. External systems must validate factual claims and enforce security, permissions, and deterministic business rules." }),
        ]) }),
      ]),
    }),
    Object.freeze({
      id: "decisions",
      eyebrow: "PRESALES DECISIONS",
      title: "Translate model mechanisms into testable customer decisions",
      blocks: Object.freeze([
        Object.freeze({ type: "cards", items: Object.freeze([
          Object.freeze({ id: "decision-capability-or-context", title: "Is the gap model capability or context supply?", body: "Signal: the task still fails after prompt and evidence changes, or the model never receives the required facts.", decision: "Use controlled experiments to separate model capability, knowledge supply, and context assembly before changing the model, adding retrieval, or rewriting prompts.", boundary: "A larger model cannot repair stale facts, incorrect permissions, or missing business rules." }),
          Object.freeze({ id: "decision-application-failure-owner", title: "Which layer owns this knowledge-assistant failure?", body: "Signal: the same visible symptom could come from base capability, evidence supply, instructions, decoding, serving, or orchestration.", decision: "Capture the effective request and change one variable at a time before routing work to Model Landscape, RAG, Prompt, LLM Inference, Agent, or Security.", boundary: "The LLM module explains mechanisms and forms hypotheses; it does not absorb every application defect into 'the model'." }),
          Object.freeze({ id: "decision-inference-handoff", title: "When should diagnosis move to the inference platform?", body: "Signal: the task result is acceptable in a controlled run, but latency, throughput, memory pressure, queueing, or availability changes under the target workload.", decision: "Hand the exact model, token-length distribution, concurrency shape, service objective, and trace evidence to LLM Inference for engine and capacity experiments.", boundary: "This module explains prefill, decode, and KV-cache concepts; it does not prescribe GPU sizing, batching, quantization, or engine configuration." }),
          Object.freeze({ id: "decision-long-context-or-rag", title: "Can long context replace retrieval?", body: "Signal: the corpus fits in the window but still has update, access, citation, repeated-transfer, or middle-position use problems.", decision: "Benchmark direct context for small, stable corpora; retain retrieval for dynamic, multi-source, access-controlled, and evidence-bearing knowledge.", boundary: "Retrieval is not inherently more accurate either; both designs require customer-data evaluation." }),
          Object.freeze({ id: "decision-architecture-depth", title: "How deeply should the customer learn architecture terms?", body: "Signal: discussion centers on parameters, heads, or new variants without connecting them to business results.", decision: "Translate each architectural choice into a falsifiable effect on quality, latency, concurrency, cost, or ecosystem support.", boundary: "Architecture is an explanatory and diagnostic tool, not a procurement outcome." }),
        ]) }),
      ]),
    }),
    Object.freeze({
      id: "deep-dive",
      eyebrow: "APPLICATION TRIAGE",
      title: "Turn one visible failure into testable ownership",
      lead: "An enterprise knowledge assistant can fail in the model weights, the current context, the decoding configuration, the serving path, or an external system. The model-foundation task is to separate those possibilities without duplicating each specialist module.",
      blocks: Object.freeze([
        Object.freeze({ type: "table", title: "Triage the request before choosing a remedy", intro: "Start with the effective input and trace, then ask which layer can run the deciding experiment.", columns: Object.freeze(["Failure class", "Mechanism", "Verification and handoff", "Boundary"]), items: Object.freeze([
          Object.freeze({ id: "triage-capability-gap", title: "Base capability or post-training gap", body: "With correct evidence, controlled instructions, and a stable execution path, the model still fails the target reasoning, language, modality, or structured-output task. The gap may be base capability or post-training behavior.", decision: "Compare candidates on a frozen evaluation set under identical context and output contracts; route model choice to Model Landscape, acceptance design and failed-case evidence to Evaluation, and any adaptation experiment to LLM Training or Fine-tuning.", boundary: "One failure does not prove a capability gap; first rule out input truncation, conflicting instructions, and an incorrect reference answer.", sourceIds: Object.freeze(["transformer-2017", "nist-genai-profile"]) }),
          Object.freeze({ id: "triage-context-evidence-gap", title: "Context or evidence gap", body: "The model never receives the authoritative fact, receives a stale or conflicting version, or uses evidence unevenly because of assembly and position.", decision: "Inspect retrieved items, authority, order, truncation, and citation coordinates; hand ingestion and lifecycle defects to Data Engineering and retrieval behavior to RAG.", boundary: "A larger context window increases admissible input; it does not create authority, freshness, or reliable evidence use.", sourceIds: Object.freeze(["lost-middle", "nist-genai-profile"]) }),
          Object.freeze({ id: "triage-instruction-decode-gap", title: "Instruction or decoding gap", body: "The evidence is present, but output format, tool arguments, or repeatability changes with instruction hierarchy or decoding settings; refusal can also originate in model- or provider-level policy.", decision: "Hold evidence and model constant while testing prompt structure, output constraints, and sampling; route prompt design to Prompt Engineering and unresolved policy behavior to Model Landscape or Security.", boundary: "Lower temperature may reduce variation but cannot turn an unsupported statement into a fact.", sourceIds: Object.freeze(["transformer-2017", "nist-genai-profile"]) }),
          Object.freeze({ id: "triage-serving-gap", title: "Serving gap", body: "A controlled request is acceptable, but queueing, prefill, decode, memory pressure, or availability degrades under the target token and concurrency distribution.", decision: "Preserve the request trace and workload distribution; hand engine, batching, quantization, caching, accelerator, and capacity experiments to LLM Inference.", boundary: "One latency number cannot identify the responsible phase, and mechanism knowledge is not a capacity plan.", sourceIds: Object.freeze(["vllm-2023", "flashattention-2022"]) }),
          Object.freeze({ id: "triage-orchestration-control-gap", title: "Orchestration or control gap", body: "Retrieval, tools, retries, policy checks, model routing, or external side effects change the effective request and outcome outside the model mechanism.", decision: "Trace authoritative state, evidence, tool calls, identities, policies, retries, and outcomes end to end; route tool and workflow failures to Agent, AI Gateway, or AI Ops, and enforcement failures to Security/IAM.", boundary: "Model explanations, attention weights, or successful prose cannot replace current authorization, idempotency, transaction controls, or business acceptance.", sourceIds: Object.freeze(["nist-genai-profile"]) }),
        ]) }),
        Object.freeze({ type: "cards", title: "Do not label every variation as model randomness", items: Object.freeze([
          Object.freeze({ id: "variance-knowledge-boundary", title: "Weights and knowledge boundary", body: "Parameters encode statistical patterns learned during training; they do not guarantee current, private, or authorized business facts.", decision: "Use frozen factual tests to separate capability and knowledge gaps; connect dynamic facts to retrieval, databases, or controlled APIs with provenance.", boundary: "More context can supply evidence but does not guarantee correct interpretation or citation.", sourceIds: Object.freeze(["transformer-2017", "nist-genai-profile"]) }),
          Object.freeze({ id: "variance-context-competition", title: "Context competition", body: "Instructions, history, retrieved passages, and tool results compete in a finite window; order, conflict, and irrelevant material can change the cues used.", decision: "Version prompt and context assembly; run ablation and position tests; measure evidence use rather than only the advertised window.", boundary: "Attention can aggregate information without using all positions equally reliably.", sourceIds: Object.freeze(["lost-middle", "nist-genai-profile"]) }),
          Object.freeze({ id: "variance-decoding", title: "Decoding variance", body: "The model produces a next-token distribution; temperature, top-p, stop conditions, and structured constraints determine how a sequence is selected.", decision: "Preserve controlled diversity for creative work; tighten sampling and validate schemas for classification, extraction, and tool arguments.", boundary: "Low temperature can improve repeatability but cannot turn an error probability into a factual guarantee.", sourceIds: Object.freeze(["transformer-2017"]) }),
          Object.freeze({ id: "variance-orchestration", title: "External orchestration", body: "Retrieval results, tool responses, gateway routing, model versions, and retry paths can change the effective input while the user's visible question remains identical.", decision: "Trace the model, prompt, evidence, tools, and policy versions end to end.", boundary: "Model repeatability is not system repeatability; external side effects still require idempotency and transaction controls.", sourceIds: Object.freeze(["nist-genai-profile"]) }),
        ]) }),
      ]),
    }),
    Object.freeze({
      id: "cloud",
      eyebrow: "CLOUD DELIVERY",
      title: "Connect model behavior to the delivery stack",
      blocks: Object.freeze([
        Object.freeze({ type: "cards", items: Object.freeze([
          Object.freeze({ id: "cloud-model-service", title: "Managed model service", subtitle: "Hosted APIs, model catalogs, and dedicated endpoints", body: "Select capability and service level by task while obtaining explicit model versions, quotas, and enterprise controls.", decision: "Discover whether quality, latency, region, cost, or dedicated capacity is the binding constraint." }),
          Object.freeze({ id: "cloud-private-inference", title: "Context and evidence interface", subtitle: "Prompt assembly, retrieval payloads, tool results, and model input contracts", body: "Preserve exactly what the model received so missing, stale, conflicting, or position-sensitive evidence can be separated from base capability.", decision: "Discover whether a failed answer can be replayed with the same context, versions, and authorization state." }),
          Object.freeze({ id: "cloud-cache-elasticity", title: "Inference-service handoff", subtitle: "Model version, token distribution, concurrency shape, service objectives, and request traces", body: "Pass a reproducible workload to the inference platform when the task succeeds in isolation but service behavior fails at scale.", decision: "Discover whether queue, prefill, decode, network, or external tools dominate the observed objective." }),
          Object.freeze({ id: "cloud-performance-finops", title: "Evaluation and end-to-end tracing", subtitle: "Task results, evidence use, model configuration, tools, policy decisions, latency, and unit cost", body: "Connect model-mechanism hypotheses to accepted task outcomes rather than treating tokens or accelerator utilization as the result.", decision: "Discover whether the trace can attribute a failure to evidence, generation, service, orchestration, or control." }),
        ]) }),
      ]),
    }),
  ]),
  qa: Object.freeze([
    Object.freeze({ id: "transformer-mainstream-foundation", q: "Why did the Transformer become the dominant foundation for large language models?", a: "Its attention mechanism directly models relationships among sequence positions, while training can process positions in parallel more effectively than recurrent architectures, making scale-out across data, model size, and compute practical.", depth: "The original Transformer was an encoder-decoder machine-translation architecture. Modern language models include decoder-only designs, new positional methods, attention variants, and sparse experts. 'Dominant' therefore describes a scalable engineering foundation, not one identical implementation.", ask: "Ask the customer: is the decision about quality, long context, inference cost, or private-serving capacity? Each requires a different depth of architectural evidence.", tag: "Foundations", basis: "Original Transformer architecture", evidence: Object.freeze([Object.freeze({ sourceId: "transformer-2017", supports: "Introduces an attention-based sequence architecture and demonstrates the parallelizable training design on which later Transformer families build." })]) }),
    Object.freeze({ id: "attention-not-consciousness", q: "Do attention weights show what the model was thinking or why an answer is true?", a: "No. Attention is an information-routing computation inside a layer. Its weights are neither consciousness, a complete causal explanation, nor provenance for a factual claim.", depth: "Queries, Keys, and Values explain how one layer scores and aggregates representations, but the final output also depends on other heads, feed-forward layers, residual paths, later layers, decoding, and external context. Research has shown that attention weights can diverge from other importance measures and can sometimes be changed without preserving a simple explanatory interpretation. Production audit therefore needs replayable inputs, sources, tools, policies, versions, and outcomes.", ask: "Ask the customer: do you need a teaching aid, a diagnostic hypothesis, or defensible audit evidence? Those require different evidence.", tag: "Conceptual boundary", basis: "Attention mechanism plus explanation limits", evidence: Object.freeze([Object.freeze({ sourceId: "transformer-2017", supports: "Defines scaled dot-product and multi-head attention as representation aggregation." }), Object.freeze({ sourceId: "attention-not-explanation-2019", supports: "Provides direct empirical evidence that attention weights should not automatically be treated as explanations." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports multiple forms of measurement, transparency, and control evidence for generative-AI risk." })]) }),
    Object.freeze({ id: "context-window-effective-use", q: "Does a larger context window mean that the model will use everything more accurately?", a: "No. A context limit describes how many tokens a request may contain; it does not guarantee equally reliable use of every position or solve authority, freshness, access, and citation.", depth: "Controlled studies have found that answer quality can depend on where relevant evidence appears within long input. Direct long-context analysis can still suit a small, stable corpus, while enterprise knowledge often needs retrieval, update, withdrawal, filtering, and provenance. Compare evidence-use accuracy, position sensitivity, TTFT, end-to-end latency, transfer cost, and authorization on the customer workload.", ask: "Ask the customer: how large and dynamic is the corpus, where does decisive evidence appear, is access user-specific, and must answers cite sources?", tag: "Context", basis: "Position-sensitive evidence use plus governance", evidence: Object.freeze([Object.freeze({ sourceId: "lost-middle", supports: "Directly evaluates how the position of relevant information affects performance in long-context tasks." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports managing source, access, measurement, and lifecycle risks." })]) }),
    Object.freeze({ id: "output-slower-costlier", q: "Why can long input and long output create different latency and cost shapes?", a: "Input is processed during prefill, while output normally extends the sequence through repeated autoregressive decode steps. The two phases therefore expose different compute, memory, and user-perceived latency behavior.", depth: "This mechanism does not determine a universal price ratio. Provider pricing, cache policy, model tier, batching, and the serving engine remain dynamic. Measure input tokens, output tokens, time to first token, output rate, end-to-end latency, and concurrency separately, and isolate queueing, network, retrieval, and tool time.", ask: "Ask the customer: is the objective first-token responsiveness, complete-response latency, throughput, or cost per accepted task result?", tag: "Performance and cost", basis: "Autoregressive generation plus service boundary", evidence: Object.freeze([Object.freeze({ sourceId: "transformer-2017", supports: "Provides the autoregressive decoder mechanism; current service implementations and prices must still be verified separately." }), Object.freeze({ sourceId: "vllm-2023", supports: "Supports separating prompt processing from iterative decoding in production LLM serving; measured behavior remains engine- and workload-specific." })]) }),
    Object.freeze({ id: "token-count-varies", q: "Why does the same Chinese, English, or code sample consume different token counts across models?", a: "A token is not necessarily a character or word. Each model's tokenizer, vocabulary, and normalization rules segment the string into its own subword or byte-level units.", depth: "The tokenizer converts raw text into token IDs, and the model processes their vectors. A frequent string may form one token in one vocabulary and several rare fragments in another; language, spacing, punctuation, and code formatting all matter. Capacity and cost must therefore be measured with the candidate tokenizer, not a fixed character-to-token ratio.", ask: "Ask the customer: what languages and formats dominate, do inputs contain long code, tables, or logs, and which tokenizer underlies the current estimate?", tag: "Tokenization", basis: "Subword tokenization plus model vocabulary", evidence: Object.freeze([Object.freeze({ sourceId: "sentencepiece-2018", supports: "Demonstrates a vocabulary-based subword method that converts raw text to a reversible token sequence." }), Object.freeze({ sourceId: "transformer-2017", supports: "Uses embeddings of discrete sequence tokens as Transformer inputs." })]) }),
    Object.freeze({ id: "embedding-types", q: "Is an input embedding inside an LLM the same thing as an embedding stored in a vector database?", a: "They are both vectors, but their objective, granularity, and use differ, so they are not interchangeable.", depth: "An input embedding maps a token ID into the model's hidden space and is transformed into contextual states through the network. A retrieval embedding usually compresses a query, passage, or image into a stable representation for similarity search. The dimension and meaning of distance are specific to the trained model and objective.", ask: "Ask the customer: are we discussing internal model representations, semantic retrieval, or vector-storage capacity?", tag: "Representation boundary", basis: "Token representation plus retrieval representation", evidence: Object.freeze([Object.freeze({ sourceId: "transformer-2017", supports: "Connects token embeddings, positional information, and layered attention to contextual sequence representations." }), Object.freeze({ sourceId: "dpr-2020", supports: "Uses separate dense encoders to represent queries and passages for candidate retrieval." })]) }),
    Object.freeze({ id: "pretraining-to-instruction-following", q: "If pretraining mainly predicts the next token, why does the model later follow instructions?", a: "Pretraining first learns statistical patterns from data. Post-training with demonstrations, preference signals, and safety policies then shapes those capabilities into more consistent assistant behavior.", depth: "Next-token prediction does not inherently establish user intent, refusal behavior, output contracts, or tool discipline. Supervised examples demonstrate preferred responses; preference or reinforcement stages alter relative choices; system and application controls constrain the runtime task. None of these turns the model into an authoritative fact store or permission system.", ask: "Ask the customer: is the gap missing base capability, unreliable behavior and format, or missing external facts, permissions, and controls?", tag: "Training stages", basis: "Pretraining plus post-training", evidence: Object.freeze([Object.freeze({ sourceId: "transformer-2017", supports: "Provides the autoregressive mechanism for predicting subsequent tokens from the preceding sequence." }), Object.freeze({ sourceId: "instructgpt-2022", supports: "Demonstrates a pipeline using supervised demonstrations, preference data, and reinforcement learning to improve instruction following." })]) }),
    Object.freeze({ id: "parameters-and-moe", q: "Does a larger parameter count always mean a more capable model, and how should MoE parameters be interpreted?", a: "No. Capability also depends on data, training compute, architecture, post-training, and task. In a Mixture of Experts, each token normally activates only part of the parameter set, so total parameters and per-token computation are different quantities.", depth: "A sparse MoE routes tokens through selected experts to increase capacity without using every expert for each token. The design introduces routing, load balance, communication, memory, and serving constraints. Compare total and active parameters, data and compute, customer-task results, throughput, and deployment cost together.", ask: "Ask the customer: is the parameter number being used to infer quality, memory, throughput, or procurement cost, and does the supplier disclose total or active parameters?", tag: "Scale boundary", basis: "Scaling plus sparse experts", evidence: Object.freeze([Object.freeze({ sourceId: "chinchilla-2022", supports: "Studies the joint relationship among model size, training tokens, and compute rather than parameter growth in isolation." }), Object.freeze({ sourceId: "switch-transformer-2022", supports: "Demonstrates sparse expert routing and distinguishes model capacity from the parameters activated for a token." })]) }),
    Object.freeze({ id: "kv-cache-concurrency", q: "What does the KV cache do, and when should this question move to LLM Inference?", a: "The KV cache retains Keys and Values for already processed context tokens so autoregressive decode does not recompute the complete prefix at every step.", depth: "Its size and operational impact depend on architecture, sequence length, precision, live requests, and the serving engine. The foundation-level conclusion is that context has persistent runtime state. Once the decision concerns batching, fragmentation, quantization, accelerator sizing, or capacity, preserve the real token and concurrency distribution and hand the experiment to LLM Inference.", ask: "Ask the customer: are we explaining the mechanism, or deciding a target engine and capacity plan? The latter needs workload-specific inference testing.", tag: "Cache mechanism", basis: "Autoregressive decode plus inference handoff", evidence: Object.freeze([Object.freeze({ sourceId: "vllm-2023", supports: "Identifies KV-cache management as a major dynamic memory problem in LLM serving and introduces PagedAttention." }), Object.freeze({ sourceId: "transformer-2017", supports: "Provides the autoregressive conditioning mechanism on which incremental decode is based." })]) }),
    Object.freeze({ id: "temperature-not-hallucination-control", q: "Can lowering temperature eliminate hallucinations?", a: "No. Lower temperature reduces sampling variation, but missing evidence, wrong learned associations, and misunderstood instructions can still produce the same wrong answer consistently.", depth: "Temperature changes the sampling distribution and can trade creative diversity against repeatability. Factual tasks still need authoritative evidence, citations, schema checks, appropriate refusal, and evaluation. Model versions and serving implementations can also change behavior even under deterministic-looking settings.", ask: "Ask the customer: is the failure that repeated runs differ, or that every run consistently uses or interprets the wrong fact?", tag: "Generation mechanism", basis: "Autoregressive generation plus grounded controls", evidence: Object.freeze([Object.freeze({ sourceId: "transformer-2017", supports: "Provides the sequence-generation mechanism from contextual representations." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports system-level measurement and controls for generative-AI reliability and confabulation risks." })]) }),
  ]),
  evidenceCards: Object.freeze([
    Object.freeze({ id: "transformer-parallel-sequence", metric: "Parallel sequence training", title: "The Transformer supplied a scalable foundation", finding: "The original paper replaced recurrent sequence processing with attention, enabling more parallel computation during training.", boundary: "The experiments were in machine translation, and modern LLMs add many architectural and systems changes.", sourceId: "transformer-2017", accent: true }),
    Object.freeze({ id: "attention-qkv-aggregation", metric: "Q · K · V", title: "Attention is an information-aggregation mechanism", finding: "Scaled dot-product attention derives weights from Queries and Keys and uses them to combine Values.", boundary: "This computation is not consciousness, a complete causal explanation, or factual evidence by itself.", sourceId: "transformer-2017" }),
    Object.freeze({ id: "attention-not-complete-explanation", metric: "Explanation boundary", title: "Attention weights are not a complete explanation", finding: "Direct empirical work shows that attention weights need not align with other importance measures and should not automatically be treated as explanations.", boundary: "The result does not make attention useless for analysis; it limits the strength of causal or audit claims made from weights alone.", sourceId: "attention-not-explanation-2019" }),
    Object.freeze({ id: "long-context-position-sensitivity", metric: "Evidence position", title: "In-window does not mean equally usable", finding: "Long-context experiments show that performance can change when decisive information moves between the beginning, middle, and end of the input.", boundary: "The reported tasks and models are specific; customer workloads still require position and assembly tests.", sourceId: "lost-middle" }),
  ]),
  terms: Object.freeze({
    llm: Object.freeze({ name: "Large Language Model", abbr: "LLM", definition: "A model trained on large corpora to represent language patterns and generate tokens conditioned on the current context." }),
    token: Object.freeze({ name: "Token", definition: "A discrete unit consumed or generated by a model; its counts shape context use, memory, latency, throughput, and cost." }),
    embedding: Object.freeze({ name: "Embedding", definition: "A learned numeric representation of a token, passage, image, or other object; its geometry and meaning depend on the model and training objective." }),
    transformer: Object.freeze({ name: "Transformer Architecture", definition: "A sequence architecture built around attention, feed-forward transformations, residual paths, and normalization." }),
    attention: Object.freeze({ name: "Attention Mechanism", definition: "A computation that scores relationships between Queries and Keys and aggregates the corresponding Values." }),
    qkv: Object.freeze({ name: "Query, Key and Value", abbr: "Q / K / V", definition: "The three representations used to express what a position seeks, what another position can match, and what information will be aggregated." }),
    "context-window": Object.freeze({ name: "Context Window", definition: "The token range accepted by one model operation; a maximum is not a guaranteed effective or affordable workload size." }),
    "kv-cache": Object.freeze({ name: "Key-Value Cache", abbr: "KV Cache", definition: "Attention Keys and Values retained across autoregressive decoding to avoid recomputing the complete prefix; memory grows with architecture, sequence length, precision, and active requests." }),
    moe: Object.freeze({ name: "Mixture of Experts", abbr: "MoE", definition: "A sparse architecture that routes each token through a subset of expert networks, separating total parameter capacity from active computation." }),
    generation: Object.freeze({ name: "Generation", definition: "Producing text, code, or structured output token by token from the current model context and decoding policy." }),
    pretraining: Object.freeze({ name: "Pretraining", definition: "Large-scale learning of general language or multimodal patterns before task- and behavior-oriented post-training." }),
  }),
  sources: Object.freeze({
    "transformer-2017": Object.freeze({ kind: "Peer-reviewed paper", shortTitle: "Transformer", note: "Introduces the attention-centered Transformer and its parallel training structure. The original experiments concern machine translation and do not describe every later LLM architecture, training pipeline, or production serving implementation." }),
    "vllm-2023": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "vLLM / PagedAttention", note: "Introduces PagedAttention and KV-cache memory management for LLM serving. Reported throughput remains specific to the tested models, hardware, engine version, and workloads." }),
    "flashattention-2022": Object.freeze({ kind: "Peer-reviewed systems paper", shortTitle: "FlashAttention", note: "Presents an IO-aware exact-attention algorithm that reduces memory traffic. Realized benefit depends on sequence shape, hardware, kernels, and the surrounding training or inference stack." }),
    "nist-genai-profile": Object.freeze({ kind: "Official risk-management framework", shortTitle: "NIST AI 600-1", note: "Organizes generative-AI risk management around Govern, Map, Measure, and Manage. It is a voluntary resource, not a product certification, universal compliance checklist, model ranking, retrieval benchmark, or hardware-performance standard." }),
    "openai-models": Object.freeze({ kind: "Official model catalog", shortTitle: "OpenAI Models", note: "Documents current OpenAI model products and supported capabilities. Identifiers, interfaces, context limits, retirement, and pricing are dynamic and must be rechecked at use time." }),
    "anthropic-models": Object.freeze({ kind: "Official model catalog", shortTitle: "Claude Models", note: "Documents Claude model products. Capability, lifecycle, regional availability, and pricing remain time-sensitive and require procurement-time verification." }),
    "google-models": Object.freeze({ kind: "Official model catalog", shortTitle: "Gemini Models", note: "Documents Gemini API model products and limits. The catalog changes and does not support freezing a permanent cross-provider ranking into this module." }),
    "sentencepiece-2018": Object.freeze({ kind: "Peer-reviewed paper", shortTitle: "SentencePiece", note: "Presents a language-independent subword tokenization method trained from raw text. It explains vocabulary-dependent token boundaries but does not imply that every model uses the same algorithm or vocabulary." }),
    "dpr-2020": Object.freeze({ kind: "Peer-reviewed paper", shortTitle: "DPR", note: "Presents dual-encoder dense passage retrieval. Its reported top-20 retrieval improvement is scoped to the paper's open-domain QA datasets and Lucene-BM25 baseline." }),
    "instructgpt-2022": Object.freeze({ kind: "Peer-reviewed paper", shortTitle: "InstructGPT", note: "Reports a post-training pipeline using supervised demonstrations, preference data, a reward model, and reinforcement learning. Results remain specific to the studied models and evaluations." }),
    "chinchilla-2022": Object.freeze({ kind: "Primary research paper", shortTitle: "Chinchilla Scaling", note: "Studies compute-optimal relationships among model size, training tokens, and compute within the paper's model family, data, and settings. It is not a universal training-budget or capacity formula." }),
    "switch-transformer-2022": Object.freeze({ kind: "Peer-reviewed journal paper", shortTitle: "Switch Transformer", note: "Studies sparse expert routing in which each token activates selected experts. It separates total capacity from active computation but does not show that sparse models always outperform dense models." }),
    "nist-zero-trust": Object.freeze({ kind: "Official architecture guidance", shortTitle: "NIST SP 800-207", note: "Defines resource access around explicit, current identity and policy evaluation rather than implicit trust from network location." }),
    "gqa-2023": Object.freeze({ kind: "Peer-reviewed paper", shortTitle: "Grouped-Query Attention", note: "Compares multi-head, multi-query, and grouped-query attention and reports quality-speed trade-offs from reducing KV heads. Results remain model-, hardware-, and setup-specific." }),
    "deepseek-r1-2025": Object.freeze({ kind: "Peer-reviewed journal paper", shortTitle: "DeepSeek-R1", note: "Reports one model family's combination of verifiable rewards, reinforcement learning, cold-start data, staged training, and distillation. It does not establish universal benefit for open-ended tasks or make internal reasoning an audit explanation." }),
    "openai-model-spec-hidden-cot": Object.freeze({ kind: "Official model-behavior specification", shortTitle: "OpenAI Model Spec · Hidden CoT", note: "Documents a provider-specific interface boundary for hidden chain of thought. It is not a universal audit standard or evidence that internal reasoning is correct." }),
    "lost-middle": Object.freeze({ kind: "Peer-reviewed paper", shortTitle: "Lost in the Middle", note: "Evaluates how the position of relevant evidence affects performance in long-context tasks. Results are scoped to the studied models and tasks and do not define a universal context limit." }),
    "attention-not-explanation-2019": Object.freeze({ kind: "Peer-reviewed paper", shortTitle: "Attention is not Explanation", note: "Tests attention weights against alternative importance measures and counterfactual distributions. It limits automatic explanatory claims from weights; it does not claim that attention is never analytically useful." }),
  }),
});
