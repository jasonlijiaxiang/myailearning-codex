export const englishModule = Object.freeze({
  slug: "data-engineering",
  title: "Data Engineering for AI",
  subtitle: "Build authorized, versioned, traceable data products for retrieval, evaluation, and training",
  definition: "Data Engineering for AI turns distributed, heterogeneous, continuously changing information into data products that can be parsed, synchronized, authorized, retrieved, trained on, evaluated, and operated over time.",
  position: "This discipline sits between source systems and retrieval, fine-tuning, evaluation, and agent applications. It owns ingestion, parsing, structure, quality, indexes, permissions, versions, and lifecycle before data reaches a model; it does not own how the model generates the final answer.",
  relatedSlugs: Object.freeze(["rag", "fine-tuning", "multimodal", "security", "ai-ops", "solution-patterns"]),
  primer: Object.freeze({
    id: "ai-data-lineage",
    layout: "pipeline",
    eyebrow: "DATA LINEAGE",
    title: "An AI data pipeline must prove where its sources, changes, and deletions went",
    intro: "Source material becomes usable input only after connection, parsing, segmentation, labeling, indexing, and publication. Authorization changes and deletion must propagate through every derived layer.",
    termIds: Object.freeze(["rag", "bm25", "ann", "hnsw"]),
    steps: Object.freeze([
      Object.freeze({ code: "01", label: "CONNECT", title: "Connect authoritative sources", detail: "Record the owner, version, authorization, update, deletion, and permitted purpose.", signal: "Establish the authoritative version before collecting content." }),
      Object.freeze({ code: "02", label: "PARSE", title: "Parse and recover structure", detail: "Recover text, layout, tables, page coordinates, regions, and reading order.", signal: "Correct characters do not prove correct structure." }),
      Object.freeze({ code: "03", label: "TRANSFORM", title: "Transform and index", detail: "Create retrievable units through segmentation, metadata, embeddings, lexical indexes, and authorization filters.", signal: "Vector similarity does not establish authority." }),
      Object.freeze({ code: "04", label: "PUBLISH", title: "Publish and observe", detail: "Release a governed data version to RAG, evaluation, or training while retaining quality, lineage, and failure queues.", signal: "Each purpose has distinct rights and leakage controls." }),
      Object.freeze({ code: "05", label: "REVOKE", title: "Revoke access and delete", detail: "Propagate and verify withdrawal across sources, caches, chunks, vectors, indexes, and other derived assets.", signal: "Deleting a source file does not make model weights forget it." }),
    ]),
    checks: Object.freeze([
      Object.freeze({ title: "Integrity", detail: "Creates, updates, withdrawals, and failed records can all be reconciled." }),
      Object.freeze({ title: "Traceability", detail: "Every output can return to a stable document, version, and source coordinate." }),
      Object.freeze({ title: "Governability", detail: "Current subject authorization and deletion state take effect at query time." }),
    ]),
    application: "Start with one authoritative data domain and a representative question set. Prove parsing, authorization, retrieval, update, and deletion before selecting a vector product.",
    links: Object.freeze([
      Object.freeze({ href: "#curriculum", label: "Review the data lifecycle" }),
      Object.freeze({ href: "#study-guide", label: "Run the lineage lab" }),
      Object.freeze({ href: "#qa", label: "Prepare for data questions" }),
    ]),
  }),
  sections: Object.freeze([
    Object.freeze({ id: "study-guide", eyebrow: "LEARN, PRACTICE, PROVE", title: "Treat AI data as governed products, not files to embed", lead: "A useful data product has an owner, purpose, authority, structure, permissions, version, quality evidence, and a lifecycle that reaches every derived copy.", blocks: Object.freeze([
      Object.freeze({ type: "cards", title: "Learning outcomes", items: Object.freeze([
        Object.freeze({ id: "outcome-data-product", title: "Establish the data product", body: "Define ownership, authority, use, contract, permissions, versions, quality, and lifecycle." }),
        Object.freeze({ id: "outcome-data-pipeline", title: "Understand the derived pipeline", body: "Connect parsing, synchronization, deduplication, chunking, embedding, indexing, caching, and authorization." }),
        Object.freeze({ id: "outcome-data-quality", title: "Measure quality by layer", body: "Detect structural, semantic, freshness, access, duplication, and downstream-task defects before generation." }),
        Object.freeze({ id: "outcome-change-propagation", title: "Close the change loop", body: "Propagate updates, access withdrawal, deletion, and adjudicated feedback through every derived asset." }),
      ]) }),
      Object.freeze({ type: "steps", title: "Recommended learning route", items: Object.freeze([
        Object.freeze({ id: "route-source-authority", title: "Establish authority", body: "Record owner, version, permission, time semantics, and business meaning.", decision: "Checkpoint: identify the authoritative copy and every derived copy." }),
        Object.freeze({ id: "route-verifiable-pipeline", title: "Build a verifiable pipeline", body: "Separate parsing, chunking, synchronization, indexing, and quality checks into observable stages.", decision: "Checkpoint: detect silent corruption before it reaches generation." }),
        Object.freeze({ id: "route-derived-feedback", title: "Manage derivatives and feedback", body: "Propagate permissions, deletion, lineage, and failed samples to the layer that can fix them.", decision: "Checkpoint: trace one source change to every affected asset." }),
        Object.freeze({ id: "route-purpose-products", title: "Publish purpose-specific products", body: "Define separate rights, update, labeling, and leakage boundaries for RAG, evaluation, and training.", decision: "Checkpoint: share source lineage without mixing incompatible uses." }),
        Object.freeze({ id: "route-prove-propagation", title: "Prove change propagation", body: "Reconcile creates, updates, revocations, and deletions end to end and replay failures.", decision: "Checkpoint: source changes reach every derivative within the stated SLO." }),
      ]) }),
      Object.freeze({ type: "cards", title: "Practice labs", items: Object.freeze([
        Object.freeze({ id: "lab-document-acceptance", title: "Build a document-pipeline acceptance set", subtitle: "Documents vary by year, template, language, scan quality, and table layout", body: "Sample by layout and quality; define text, structure, table, page, and metadata checks; add human review and blocking for silent errors.", decision: "Deliverable: a golden set and stage-specific quality gates.", boundary: "Acceptance: critical tables and evidence coordinates are reviewable; OCR character accuracy alone is insufficient.", sourceIds: Object.freeze(["docling-report", "pp-ocr-2020"]) }),
        Object.freeze({ id: "lab-access-deletion", title: "Rehearse access withdrawal and deletion", subtitle: "A source revokes one employee's access and removes an incorrect version", body: "Trace caches, objects, parsed blocks, embeddings, indexes, and evaluation samples; define the propagation window and retry path; produce auditable completion evidence.", decision: "Deliverable: a propagation map, SLO, and verification record.", boundary: "Acceptance: search and generation no longer return the content within the promised window; exceptions require owner approval.", sourceIds: Object.freeze(["nist-zero-trust", "hnsw-2016"]) }),
        Object.freeze({ id: "lab-stale-rag-version", title: "Diagnose an update that did not reach RAG", subtitle: "The source is revised but answers still cite the old version", body: "Reconcile connector, parser, chunk, index, cache, and answer version; distinguish missed events, quarantined failures, alias changes, and stale caches; design replay and full reconciliation.", decision: "Deliverable: a change-propagation diagnosis.", boundary: "Acceptance: identify the layer retaining the old version and prove both replacement and withdrawal after repair.", sourceIds: Object.freeze(["nist-zero-trust", "opentelemetry-semconv", "docling-report"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "curriculum", eyebrow: "CURRICULUM ATLAS", title: "From source contract to purpose-specific data products", lead: "AI data engineering transforms source material into durable products with structure, permissions, versions, quality, and lineage.", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "curriculum-data-readiness", title: "Data readiness and contracts", subtitle: "Data Readiness", body: "Owner, semantics, format, update, permission, region, retention, and quality objectives form the data contract. Establish the authoritative version and permitted use before parsing, indexing, evaluation, or training.", decision: "Turn 'the data is messy' into testable contract clauses.", boundary: "RAG, evaluation, and training have different rights, freshness, and leakage requirements even when they share a source.", sourceIds: Object.freeze(["nist-genai-profile", "nist-zero-trust"]) }),
        Object.freeze({ id: "curriculum-document-parsing", title: "Document parsing and structure recovery", subtitle: "Document Parsing", body: "Extraction includes layout, reading order, tables, headings, pages, images, and metadata—not text alone. Route digital documents, scans, and high-risk structures through the appropriate parser, document AI, vision model, and review path.", decision: "Validate difficult customer layouts and evidence coordinates, not character accuracy alone.", boundary: "Recognizing every number does not prove that table relationships and business meaning survived.", sourceIds: Object.freeze(["docling-report", "pp-ocr-2020"]) }),
        Object.freeze({ id: "curriculum-connect-sync", title: "Connectors and incremental synchronization", subtitle: "Connect & Sync", body: "Connectors must handle authentication, pagination, cursors, retries, idempotency, rate limits, deletion, and access events. Batch, CDC, and event flows all need reconciliation and rebuild paths.", decision: "Choose the mode from source capability and business freshness, while retaining a full validation baseline.", boundary: "A successful API call does not prove complete propagation of objects, permissions, and deletion.", sourceIds: Object.freeze(["nist-zero-trust", "opentelemetry-semconv"]) }),
        Object.freeze({ id: "curriculum-retrieval-preparation", title: "Chunking, embeddings, and indexes", subtitle: "Retrieval Preparation", body: "Chunking defines semantic and evidence boundaries; embeddings provide vector representations; indexes trade recall, latency, memory, and update cost. Metadata, permissions, and version filters must be co-designed with recall.", decision: "Tune structure and indexes to the task before selecting a vector product.", boundary: "Approximate-neighbor indexes such as HNSW do not decide the authoritative version, permissions, or answer correctness.", sourceIds: Object.freeze(["hnsw-2016", "nist-zero-trust"]) }),
        Object.freeze({ id: "curriculum-data-quality", title: "Quality, lineage, and feedback", subtitle: "Data Quality", body: "Coverage, correctness, freshness, uniqueness, and traceability must be measured at every stage. A failed answer should be attributed to source absence, parsing, chunking, access, indexing, retrieval, or model behavior.", decision: "Repair the data mechanism that creates the defect rather than hiding it permanently with a prompt patch.", boundary: "Parser or model confidence is not factual accuracy; thresholds require customer-material calibration.", sourceIds: Object.freeze(["docling-report", "nist-genai-profile"]) }),
        Object.freeze({ id: "curriculum-label-synthesize", title: "Labeling, synthesis, and two repair lines", subtitle: "Label & Synthesize", body: "Expert labels create authoritative examples; weak supervision and synthetic data expand coverage. Failed cases can enter a data-repair line or a model-and-application evaluation line, with shared identity and provenance.", decision: "Spend expert judgment on high-risk, disputed, and calibration samples.", boundary: "Synthetic data inherits teacher and prompt bias and must not become truth without adjudication.", sourceIds: Object.freeze(["nist-genai-profile"]) }),
        Object.freeze({ id: "curriculum-data-governance", title: "Authorization, deletion, and lifecycle", subtitle: "Governance", body: "Query-time access uses the current subject and policy. Deletion and withdrawal propagate to caches, objects, chunks, embeddings, indexes, and evaluation assets, with lineage and completion evidence.", decision: "Define propagation SLOs, retries, and negative verification for every derived layer.", boundary: "Deleting a source does not make trained weights forget it automatically; legal retention conflicts require owner adjudication.", sourceIds: Object.freeze(["nist-zero-trust", "hnsw-2016"]) }),
        Object.freeze({ id: "curriculum-purpose-products", title: "Purpose-specific products", subtitle: "Purpose-specific Products", body: "RAG, evaluation, and training derivatives differ in update cadence, rights, deduplication, labels, and leakage. Share source identity and lineage, but publish separately governed versions.", decision: "Declare purpose and acceptance before transforming data.", boundary: "Retrieval rights do not imply training rights, and internal evaluation rights do not imply production-log retention rights.", sourceIds: Object.freeze(["nist-genai-profile", "nist-zero-trust"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "principles", eyebrow: "CORE PIPELINE", title: "Build authority, structure, synchronization, retrieval readiness, quality, and lifecycle", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "principle-source-contract", title: "Source and data contract", body: "Register the owner, authoritative system, schema, update cadence, sensitivity, rights, and purpose before selecting ingestion.", decision: "Data without ownership and use boundaries does not enter production merely because it can be scraped." }),
        Object.freeze({ id: "principle-structure-recovery", title: "Parsing and structure recovery", body: "Preserve heading hierarchy, reading order, page coordinates, table relationships, images, and source locations across digital documents and scans.", decision: "Accept parsing with downstream tasks; character accuracy does not prove table or citation fidelity." }),
        Object.freeze({ id: "principle-incremental-sync", title: "Connectors and incremental synchronization", body: "Batch, CDC, event, and API flows must handle create, update, delete, replay, and source access change.", decision: "Set freshness from business risk rather than making every source real time." }),
        Object.freeze({ id: "principle-retrieval-readiness", title: "Indexing and retrieval readiness", body: "Build keyword, vector, metadata, graph, or hybrid indexes from query patterns while preserving stable IDs, versions, and access fields.", decision: "A vector database is an index component, not the entire knowledge-engineering system." }),
        Object.freeze({ id: "principle-quality-lineage", title: "Quality and lineage", body: "Operate coverage, accuracy, freshness, consistency, duplication, parse failure, and access leakage with rules, samples, and accountable owners.", decision: "Connect quality metrics to downstream failure rather than document or embedding counts." }),
        Object.freeze({ id: "principle-governance-lifecycle", title: "Governance and lifecycle", body: "Classification, permissions, region, retention, and audit requirements follow data through ingestion, processing, indexes, use, and deletion.", decision: "Propagate deletion to source copies, parsed text, indexes, caches, evaluation sets, and training candidates." }),
      ]) }),
      Object.freeze({ type: "boundary", title: "Critical boundary", items: Object.freeze([Object.freeze({ id: "data-engineering-boundary", title: "Data engineering is not bulk embedding", body: "The output is an authorized, traceable, updateable data product.", boundary: "It does not replace retrieval and generation design, training methods, the business data owner, or security policy, although all of them depend on its quality." })]) }),
    ]) }),
    Object.freeze({ id: "decisions", eyebrow: "PRESALES DECISIONS", title: "Choose parsing, synchronization, indexes, ownership, and reuse", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "decision-parser-route", title: "General parser, document AI, or custom pipeline?", body: "The estate includes digital PDFs, scans, complex tables, charts, equations, code, and proprietary layouts.", decision: "Route by type: native parse for digital documents, OCR for scans, and document AI plus review for high-risk structures.", boundary: "Do not rasterize every page before OCR, and do not assume one parser covers every layout." }),
        Object.freeze({ id: "decision-sync-mode", title: "Batch, CDC, or event synchronization?", body: "Sources differ in change rate, deletion behavior, API capability, and freshness risk.", decision: "Use batch plus reconciliation for low-frequency documents, CDC for structured databases, and event streams for urgent change, with a replayable baseline.", boundary: "Real time cannot sacrifice idempotency, ordering, deletion, or source-permission consistency." }),
        Object.freeze({ id: "decision-vector-store", title: "Reuse database vector support or add a dedicated vector store?", body: "The choice depends on scale, filters, updates, hybrid search, latency, isolation, operations, and team capability.", decision: "Test the existing platform against target queries and filters; add a specialized store only for a demonstrated gap.", boundary: "Vector dimension and one-search latency do not cover updates, deletion, backup, tenancy, and hybrid retrieval." }),
        Object.freeze({ id: "decision-quality-ownership", title: "Does the platform team or business team own data quality?", body: "The platform detects parse and synchronization anomalies but cannot adjudicate business truth such as contract terms or product state.", decision: "The platform owns observability, rules, and workflow; the data owner owns definitions and adjudication; the contract joins their SLOs.", boundary: "The AI team cannot become the default owner for all upstream data quality." }),
        Object.freeze({ id: "decision-data-reuse", title: "Can one dataset serve RAG, evaluation, and fine-tuning?", body: "The team wants reuse, but authority, split, labels, rights, and leakage controls differ by purpose.", decision: "Share a governed raw layer and publish purpose-specific derivatives with rights, version, lineage, and acceptance.", boundary: "Retrievable does not mean trainable; trained examples cannot prove generalization in the evaluation set." }),
      ]) }),
    ]) }),
    Object.freeze({ id: "deep-dive", eyebrow: "SILENT CORRUPTION & CHANGE", title: "Detect invisible document damage and prove lifecycle propagation", lead: "A pipeline may report success while meaning, evidence location, access, or current version has already been corrupted.", blocks: Object.freeze([
      Object.freeze({ type: "table", title: "Five silent document failures", columns: Object.freeze(["Failure", "Mechanism", "Verification", "Boundary"]), items: Object.freeze([
        Object.freeze({ id: "corruption-reading-order", title: "Reading-order corruption", body: "Columns, sidebars, footnotes, and floating text can be concatenated incorrectly while the prose still appears fluent.", decision: "Use gold pages with complex layouts and compare block order, hierarchy, and citation return.", boundary: "One passing template does not cover vendors, years, languages, and later formats.", sourceIds: Object.freeze(["docling-report"]) }),
        Object.freeze({ id: "corruption-table-semantics", title: "Table-semantics loss", body: "Cell text survives while headers, merged cells, units, and notes are lost, making numbers ambiguous.", decision: "Validate header-cell-unit relationships with business questions and retain image or human review for critical tables.", boundary: "Recognizing every number does not prove table meaning.", sourceIds: Object.freeze(["docling-report", "pp-ocr-2020"]) }),
        Object.freeze({ id: "corruption-boilerplate", title: "Repeated boilerplate", body: "Copyright, navigation, page numbers, and repeated headings enter every chunk and create near-duplicate retrieval noise.", decision: "Measure cross-page frequency and compare retrieval before and after removal, with false-removal review.", boundary: "Legal notices and version statements can be valid evidence and cannot be removed by coordinates alone.", sourceIds: Object.freeze(["docling-report"]) }),
        Object.freeze({ id: "corruption-ocr-skew", title: "OCR quality skew", body: "Aggregate accuracy is dominated by clean pages while stamps, handwriting, rotation, and low contrast continue to fail.", decision: "Sample by page quality and field risk; record confidence, failure image, and adjudication before routing.", boundary: "Model confidence is not factual accuracy; thresholds require customer calibration.", sourceIds: Object.freeze(["pp-ocr-2020", "nist-genai-profile"]) }),
        Object.freeze({ id: "corruption-version-collision", title: "Version collision", body: "Unstable document IDs, timestamps, or tombstones allow old and new clauses to coexist in the index.", decision: "Use stable identity and effective dates to test conflicts and prove one authoritative active version.", boundary: "Approximate-neighbor search does not decide business authority.", sourceIds: Object.freeze(["hnsw-2016", "nist-genai-profile"]) }),
      ]) }),
      Object.freeze({ type: "steps", title: "Withdrawal and deletion across five derived layers", items: Object.freeze([
        Object.freeze({ id: "change-authoritative-event", title: "Create the authoritative change event", body: "Emit subject, object, version, time, and change type from the authority; use replayable tombstones for deletion and ACL withdrawal.", decision: "Confirm event order and the authority; reconcile sources that cannot emit events.", boundary: "The pipeline cannot invent legal retention or business exceptions.", sourceIds: Object.freeze(["nist-zero-trust"]) }),
        Object.freeze({ id: "change-quarantine", title: "Quarantine source and derivatives", body: "Block new access to affected originals, parsed blocks, images, and features while retaining controlled lineage for cleanup.", decision: "Prioritize access withdrawal over completion of background deletion.", boundary: "Quarantined copies retain the same sensitivity and audit requirements.", sourceIds: Object.freeze(["nist-zero-trust"]) }),
        Object.freeze({ id: "change-index-cache", title: "Update indexes and caches", body: "Delete or rebuild keyword, vector, metadata, semantic-cache, and replica entries by stable document ID while handling in-flight jobs.", decision: "Run negative queries for the old ID, similar text, and cross-tenant paths.", boundary: "API success does not prove user-visible deletion; visibility windows are implementation-specific.", sourceIds: Object.freeze(["hnsw-2016", "nist-zero-trust"]) }),
        Object.freeze({ id: "change-downstream-datasets", title: "Reconcile purpose-specific datasets", body: "Use lineage to find affected evaluation, labeling, fine-tuning, export, and analytics records and apply delete, replace, or retained-exception decisions.", decision: "Prove each separate right: retrievable, evaluable, and trainable.", boundary: "Removing data cannot be described as automatic unlearning from completed weights.", sourceIds: Object.freeze(["nist-genai-profile"]) }),
        Object.freeze({ id: "change-propagation-evidence", title: "Issue propagation evidence", body: "Aggregate system state, retries, negative probes, retention exceptions, and completion time into an auditable record.", decision: "Put maximum propagation time and escalation responsibility into the data SLO.", boundary: "Execution evidence does not itself establish regulatory or contractual compliance.", sourceIds: Object.freeze(["nist-zero-trust", "nist-genai-profile"]) }),
      ]) }),
    ]) }),
    Object.freeze({ id: "cloud", eyebrow: "CLOUD DELIVERY", title: "Map the data lifecycle to cloud capabilities", blocks: Object.freeze([
      Object.freeze({ type: "cards", items: Object.freeze([
        Object.freeze({ id: "cloud-ingestion-sync", title: "Ingestion and synchronization", subtitle: "Connectors, ETL/ELT, CDC, queues, and workflow orchestration", body: "Connect SaaS, databases, files, and events while operating retries, incremental updates, deletion, and monitoring.", decision: "Discover sources, API or CDC capability, and update and deletion SLOs." }),
        Object.freeze({ id: "cloud-parsing-multimodal", title: "Parsing and multimodal processing", subtitle: "OCR, document AI, storage, batch, serverless, and human review", body: "Process documents, scans, tables, and images elastically while retaining structure, pages, and failed samples.", decision: "Discover which document structures directly change customer answers when lost." }),
        Object.freeze({ id: "cloud-storage-indexing", title: "Storage and indexing", subtitle: "Lakehouse, databases, search, vector retrieval, catalogs, and caches", body: "Combine lexical, vector, and metadata filters and operate versions, backups, and lifecycle.", decision: "Discover exact-term, semantic, structured-filter, and relationship needs plus scale and update shape." }),
        Object.freeze({ id: "cloud-quality-governance", title: "Quality and governance", subtitle: "Quality, catalog, lineage, IAM, DLP, KMS, audit, and cost", body: "Register ownership, sensitivity, permissions, quality rules, and downstream use and continue to verify them.", decision: "Discover who signs business correctness and how access and deletion propagate." }),
      ]) }),
    ]) }),
  ]),
  qa: Object.freeze([
    Object.freeze({ id: "vector-db-before-data-contract", q: "Can we deploy a vector database first and fix messy data as we go?", a: "A vector store indexes what it receives; it cannot repair unknown provenance, parsing loss, duplication, stale versions, overbroad access, or failed deletion. Establish a minimum data contract and quality loop first.", depth: "A pilot can start in one domain, but it still needs an authority, stable document ID, version, permission, update time, and delete event. Quarantine parse failure and use real questions to distinguish missing data, parse loss, index failure, and ranking failure.", ask: "Ask the customer: is the dominant defect missing, stale, misparsed, unauthorized, or unretrievable content, and who adjudicates it?", tag: "Build order", basis: "Data-product boundary plus retrieval engineering", evidence: Object.freeze([Object.freeze({ sourceId: "docling-report", supports: "Supports recovering layout, reading order, and tables rather than exporting unstructured text alone." }), Object.freeze({ sourceId: "hnsw-2016", supports: "Defines an approximate-neighbor index; it does not provide upstream data quality or governance." })]) }),
    Object.freeze({ id: "ocr-rag-errors", q: "Why can RAG still answer incorrectly after a scanned PDF has been OCRed?", a: "OCR addresses character recognition only. Lost reading order, hierarchy, table relationships, boilerplate handling, image meaning, and evidence coordinates can corrupt chunking and retrieval.", depth: "Inspect chapter ownership, cell relationships, cross-page continuity, and return-to-page coordinates. Use structured extraction or review for critical tables and retain original images and parser versions. Accept with real questions whose evidence is complete and locatable.", ask: "Ask the customer: do answers depend on prose, tables, or charts, and can a failed answer be traced back to the original page?", tag: "Document parsing", basis: "Document-AI technical report", evidence: Object.freeze([Object.freeze({ sourceId: "docling-report", supports: "Supports layout, table, reading-order, and structured representation as document-conversion responsibilities." })]) }),
    Object.freeze({ id: "vector-store-selection", q: "Does a more specialized vector database necessarily produce better retrieval?", a: "No. Retrieval quality depends on embeddings, corpus, chunking, filters, index configuration, reranking, and query distribution; the database is one layer.", depth: "Test lexical and semantic recall, metadata filters, updates, deletion, tenancy, P95, scale, backup, and cost on real data and queries. HNSW trades recall against latency, memory, and build cost. Reusing an adequate existing search platform can reduce governance complexity.", ask: "Ask the customer: which filters and hybrid modes are required, how quickly must updates and deletions become visible, and what isolation is required?", tag: "Vector-store selection", basis: "Approximate-retrieval research plus engineering acceptance", evidence: Object.freeze([Object.freeze({ sourceId: "hnsw-2016", supports: "Supports hierarchical graph search for approximate nearest neighbors and its engineering tradeoffs." })]) }),
    Object.freeze({ id: "source-access-deletion-sync", q: "How should an AI system follow a source-system permission withdrawal or deletion?", a: "Treat access and deletion as first-class events and propagate them through source copies, parsed output, indexes, caches, evaluation sets, and every derivative.", depth: "Use stable IDs, source versions, and lineage; process tombstones, retries, and reconciliation. Enforce subject-level authorization at query time instead of relying only on indexed ACLs. Isolate retained backups and prove the index against sampled source permissions.", ask: "Ask the customer: which system owns authorization, what is the deletion SLO, and which derivatives cannot yet be traced?", tag: "Access and deletion", basis: "Zero trust plus data lifecycle", evidence: Object.freeze([Object.freeze({ sourceId: "nist-zero-trust", supports: "Supports evaluating current subject and resource policy on each access." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports managing provenance, privacy, and generative-AI data lifecycle risk." })]) }),
    Object.freeze({ id: "deletion-propagation-slo", q: "How should access-withdrawal and deletion propagation be expressed as an acceptable SLO?", a: "The endpoint is that the affected subject can no longer retrieve, reuse, or hit the old content. State windows, failures, and completion proof for every derived layer.", depth: "Bind stable identity, version, permission, and lineage to the event. Define detection and propagation time, negative probes, retry, quarantine, exceptions, and owner, distinguishing legal deletion, business withdrawal, and version replacement. A successful job status is not user-visible proof.", ask: "Ask the customer: what derivatives exist, when must each become unavailable, and who runs which negative query to sign completion?", tag: "Lifecycle", basis: "Zero trust plus lineage", evidence: Object.freeze([Object.freeze({ sourceId: "nist-zero-trust", supports: "Supports access according to the current subject and authorization." }), Object.freeze({ sourceId: "hnsw-2016", supports: "Shows that a vector index is an independent retrieval structure whose updates and deletion require explicit management." })]) }),
    Object.freeze({ id: "complex-pdf-acceptance", q: "How do we prove that complex-PDF parsing is good enough for RAG?", a: "Build structure and question-answer acceptance sets across real layouts and validate headings, tables, reading order, cross-page relationships, and return-to-page evidence—not characters alone.", depth: "Sample scans, columns, tables, captions, footnotes, and cross-page material. Check structural fidelity, chunk boundaries, evidence recall, and citation coordinates. Retain source coordinates, parser version, confidence, and a failure queue, with domain review for critical tables.", ask: "Ask the customer: which layouts are hardest, which real questions accept each type, and who adjudicates table and page evidence?", tag: "Parsing quality", basis: "Document understanding plus traceability", evidence: Object.freeze([Object.freeze({ sourceId: "docling-report", supports: "Supports recovering layout, reading order, tables, and structured representations." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports recording provenance and limitations and retaining human oversight for consequential outcomes." })]) }),
    Object.freeze({ id: "shared-data-different-purpose", q: "Can the same source data be used for RAG, evaluation, and training?", a: "The source can be shared, but use cannot be assumed. Rights, updates, labels, leakage, retention, and deletion differ across the three purposes.", depth: "RAG needs the current authorized version, evaluation needs stable samples not contaminated by tuning, and training involves replication, longer retention, and memorization risk. Publish purpose-specific derivatives from shared source identity and lineage.", ask: "Ask the customer: which uses does the owner permit, how are evaluation samples kept out of training, and when must withdrawal reach each downstream?", tag: "Purpose governance", basis: "Lineage plus purpose limitation", evidence: Object.freeze([Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports governing generative-AI data provenance, purpose, privacy, and lifecycle." }), Object.freeze({ sourceId: "nist-zero-trust", supports: "Supports authorizing access to the current subject and resource." })]) }),
    Object.freeze({ id: "deleted-source-still-searchable", q: "Why can an AI application still find a document after the source deleted it?", a: "Copies can remain in caches, chunks, embeddings, indexes, evaluation assets, exports, or failed asynchronous queues.", depth: "Give each layer stable document identity, version, delete event, propagation state, and completion evidence. Make deletion replayable and reconcilable and enforce current authorization at query time. Treatment of completed model weights is a separate technical and governance question.", ask: "Ask the customer: when must deletion or withdrawal take effect, and can the team enumerate every derivative of one document?", tag: "Deletion propagation", basis: "Derived lineage plus current authorization", evidence: Object.freeze([Object.freeze({ sourceId: "nist-zero-trust", supports: "Supports evaluating current policy at resource access." }), Object.freeze({ sourceId: "hnsw-2016", supports: "Defines vector indexing but not business deletion, authorization, or lineage." })]) }),
    Object.freeze({ id: "scheduled-index-rebuild", q: "After one full source sync, can we rely only on scheduled index rebuilds?", a: "That can work at small scale, but production must define incremental creates, updates, deletion, out-of-order events, replay, and a consistency window.", depth: "Retain stable IDs, versions, and change cursors. Make the incremental flow retryable and reconcilable, with periodic full checks to find missed events. Index completion does not mean every derivative is consistent.", ask: "Ask the customer: does the source provide snapshots or a change log, and how quickly would one missed deletion be detected?", tag: "Incremental synchronization", basis: "Change capture plus reconciliation", evidence: Object.freeze([Object.freeze({ sourceId: "opentelemetry-semconv", supports: "Provides common telemetry semantics for correlating processing stages." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports managing provenance, updates, and lifecycle." })]) }),
    Object.freeze({ id: "index-strategy-release", q: "Can a new chunking or embedding strategy overwrite the existing index directly?", a: "It should not. Build a version in parallel, run regression, switch atomically, and retain rollback and document lineage.", depth: "Bind the index version to parser, chunker, embedding model, metadata, and access policy. During replay or dual write, compare recall, answers, latency, and deletion. After cutover, monitor old caches and in-flight requests before retirement.", ask: "Ask the customer: can every chunk be traced to its source and transformation version, and how would the switch roll back?", tag: "Index release", basis: "Data versioning plus zero-downtime migration", evidence: Object.freeze([Object.freeze({ sourceId: "hnsw-2016", supports: "Shows that a vector index is an independent approximate-search structure whose configuration must be validated." }), Object.freeze({ sourceId: "opentelemetry-semconv", supports: "Provides common semantics used to connect versions to downstream calls." })]) }),
    Object.freeze({ id: "pipeline-success-not-quality", q: "Why do row counts and pipeline success rates fail to prove AI data quality?", a: "Technical completion does not establish content coverage, structural fidelity, current authorization, or suitability for downstream tasks.", depth: "Measure coverage, parsing, structure, freshness, access, duplication, labels, and task effect. Bind each to samples and an owner and validate hard documents, retrieval, and evaluation. Do not let one processing-success rate stand in for business usability.", ask: "Ask the customer: which defects create serious wrong answers or access breaches, and do current metrics catch them before the model?", tag: "Quality contract", basis: "Data metrics plus downstream validation", evidence: Object.freeze([Object.freeze({ sourceId: "docling-report", supports: "Supports independent validation of document structure and layout recovery." }), Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports connecting data quality to concrete generative-AI risks." })]) }),
    Object.freeze({ id: "duplicate-policy-sources", q: "Should every system copy of the same policy be indexed to improve recall?", a: "No. Duplicate and conflicting versions amplify stale evidence. Establish an authority and effective-version rule.", depth: "Create canonical document identity, source priority, version, effective period, and conflict status. Preserve necessary copy lineage but expose only the current authoritative version by default; query history explicitly for legal or audit needs.", ask: "Ask the customer: which system is authoritative, and are conflicts adjudicated by source, version, or effective date?", tag: "Master-data governance", basis: "Authoritative source plus version conflict", evidence: Object.freeze([Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports recording data provenance, limitations, and lifecycle." }), Object.freeze({ sourceId: "nist-zero-trust", supports: "Supports binding resource access to current authoritative policy." })]) }),
    Object.freeze({ id: "redact-after-embedding", q: "Is embedding raw content and redacting only the final answer sufficient to protect sensitive information?", a: "No. Sensitive content has already entered vectors, indexes, caches, and logs; output redaction cannot remove those derived exposures.", depth: "Classify, minimize, and where required de-identify before ingestion, while retaining authorized identity for records that must remain intact. Enforce current permissions at query time and add an output check as a second control. Isolate and audit retained sensitive originals.", ask: "Ask the customer: which fields must never enter an index, and which must remain intact but retrievable only by defined subjects?", tag: "Sensitive data", basis: "Pre-ingestion control plus query-time authorization", evidence: Object.freeze([Object.freeze({ sourceId: "nist-genai-profile", supports: "Supports managing privacy and sensitive information across the generative-AI data lifecycle." }), Object.freeze({ sourceId: "nist-zero-trust", supports: "Supports least-privilege decisions by subject, resource, and purpose." })]) }),
  ]),
  evidenceCards: Object.freeze([
    Object.freeze({ id: "document-structure-preservation", metric: "Structure preservation", title: "Parsing is not plain-text export", finding: "The Docling technical report describes document conversion around layout, reading order, tables, and a unified structured representation.", boundary: "Quality still depends on document type, language, scan quality, and configuration and must be retested on customer material.", sourceId: "docling-report", accent: true }),
    Object.freeze({ id: "ann-index-component", metric: "ANN", title: "A vector index is one retrieval component", finding: "HNSW uses hierarchical navigable small-world graphs for approximate-neighbor search, exposing tradeoffs among recall, latency, memory, and build cost.", boundary: "The algorithm does not own embedding quality, permissions, updates, or final-answer correctness.", sourceId: "hnsw-2016" }),
    Object.freeze({ id: "authorization-every-access", metric: "Every access", title: "Authorization cannot be frozen only at ingestion", finding: "Zero-trust guidance requires evaluating subject, resource, and policy at access time, supporting continuing least-privilege enforcement at query time.", boundary: "The data platform still has to implement and test ACL synchronization, index filtering, and deletion propagation.", sourceId: "nist-zero-trust" }),
  ]),
  terms: Object.freeze({
    embedding: Object.freeze({ name: "Embedding", definition: "A learned numeric representation of a token, passage, image, or other object; its geometry and meaning depend on the model and training objective." }),
    ocr: Object.freeze({ name: "Optical Character Recognition", abbr: "OCR", definition: "Recognition of text in images or scans; it does not by itself recover reading order, table semantics, or factual correctness." }),
    rag: Object.freeze({ name: "Retrieval-Augmented Generation", abbr: "RAG", definition: "Generation supplied with retrieved external evidence under an explicit retrieval, authorization, and citation process." }),
    retrieval: Object.freeze({ name: "Retrieval", definition: "Selecting candidate evidence from an index or source system in response to a query." }),
    chunking: Object.freeze({ name: "Chunking", definition: "Dividing material into retrievable units while preserving structure, stable identity, location, version, and permission." }),
    "sparse-retrieval": Object.freeze({ name: "Sparse Retrieval", definition: "Term-weighted retrieval suited to exact language, identifiers, and rare names." }),
    "dense-retrieval": Object.freeze({ name: "Dense Retrieval", definition: "Retrieval based on similarity in a learned vector space." }),
    "hybrid-search": Object.freeze({ name: "Hybrid Search", definition: "Combining lexical, vector, metadata, or other retrieval signals before ranking." }),
    "vector-database": Object.freeze({ name: "Vector Database", definition: "A system that stores, indexes, and queries vectors and metadata; it is not the complete data-governance pipeline." }),
    bm25: Object.freeze({ name: "Best Matching 25", abbr: "BM25", definition: "A classic term-weighting retrieval function using frequency, inverse document frequency, and length normalization." }),
    ann: Object.freeze({ name: "Approximate Nearest Neighbor", abbr: "ANN", definition: "A family of methods that trades a bounded amount of exactness for scalable vector-neighbor search." }),
    hnsw: Object.freeze({ name: "Hierarchical Navigable Small World", abbr: "HNSW", definition: "A layered proximity-graph index used for approximate nearest-neighbor search." }),
    "data-engineering": Object.freeze({ name: "Data Engineering for AI", definition: "The discipline that creates authorized, structured, versioned, quality-controlled, traceable data products for AI systems." }),
    "document-intelligence": Object.freeze({ name: "Document Intelligence", definition: "Recovering text, layout, tables, images, and evidence coordinates from complex documents for downstream use and review." }),
    "data-contract": Object.freeze({ name: "Data Contract", definition: "A testable agreement for owner, authority, meaning, format, update, permission, retention, rights, and quality." }),
    "data-lineage": Object.freeze({ name: "Data Lineage", definition: "Traceability from a source record through parsing, chunks, embeddings, indexes, evaluations, exports, and downstream use." }),
    "deletion-propagation": Object.freeze({ name: "Deletion Propagation", definition: "Applying a source deletion or access withdrawal to every derived copy and retaining evidence of completion." }),
    dlp: Object.freeze({ name: "Data Loss Prevention", abbr: "DLP", definition: "Controls that identify and restrict sensitive data exposure through input, output, storage, logs, or files." }),
    "feature-store": Object.freeze({ name: "Feature Store", definition: "A governed system for feature definitions, history, and online values used consistently across training and serving." }),
    "point-in-time-correctness": Object.freeze({ name: "Point-in-time Correctness", definition: "Ensuring training examples use only information available at the prediction time." }),
    "training-serving-skew": Object.freeze({ name: "Training-serving Skew", definition: "Behavioral differences caused by inconsistent features, time semantics, defaults, or sources between training and production." }),
  }),
  sources: Object.freeze({
    "docling-report": Object.freeze({ kind: "Primary technical report", shortTitle: "Docling", note: "Describes PDF conversion, layout analysis, reading order, and table-structure processing. It demonstrates parser capabilities but does not guarantee RAG accuracy on every document estate." }),
    "hnsw-2016": Object.freeze({ kind: "Primary research paper", shortTitle: "HNSW", note: "Introduces hierarchical proximity graphs for approximate-neighbor search. It explains the vector-index mechanism, not permissions, source authority, or answer correctness." }),
    "nist-genai-profile": Object.freeze({ kind: "Official risk-management framework", shortTitle: "NIST AI 600-1", note: "Organizes generative-AI risk management around Govern, Map, Measure, and Manage. It is a voluntary resource, not a product certification, universal compliance checklist, model ranking, retrieval benchmark, or hardware-performance standard." }),
    "nist-zero-trust": Object.freeze({ kind: "Official architecture guidance", shortTitle: "NIST SP 800-207", note: "Defines resource access around explicit, current identity and policy evaluation rather than implicit trust from network location." }),
    "opentelemetry-semconv": Object.freeze({ kind: "Open observability specification", shortTitle: "OpenTelemetry SemConv", note: "Defines common semantics for traces, metrics, logs, events, and resources. AI-specific progress, data lineage, business outcomes, approvals, and risk evidence still require project-defined fields." }),
    "pp-ocr-2020": Object.freeze({ kind: "Primary research paper", shortTitle: "PP-OCR", note: "Describes a practical OCR system combining text detection, direction classification, and recognition. It does not guarantee reliable recovery of every scan quality, layout, or table relationship." }),
  }),
});
