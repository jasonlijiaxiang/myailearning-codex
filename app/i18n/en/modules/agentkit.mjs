import { agentPlatformSourceCopy } from "../agent-platform-source-copy.mjs";

export const englishModule = Object.freeze({
  slug: "agentkit",
  title: "AgentKit",
  subtitle: "Carry an agent application through a traceable build, deployment, Runtime, state, and operations lifecycle",
  definition: "AgentKit is a platform and developer-tooling layer for packaging, configuring, deploying, and operating agent applications. Its SDK, CLI, application contract, and Runtime connect source code or an image to a managed execution environment and its bound services.",
  position: "AgentKit sits after agent behavior has a runnable application entry point and before the organization can claim a production outcome. It manages delivery and Runtime concerns, but it does not author the agent's domain logic, make caller-supplied identity trustworthy, turn memory into a source of truth, prove downstream services can sustain load, or define the customer's quality and service objectives.",
  relatedSlugs: Object.freeze(["veadk", "ai-agent", "ai-infra-platform", "ai-ops", "evaluation", "security", "ai-gateway", "solution-patterns"]),
  sections: Object.freeze([
    Object.freeze({
      id: "study-guide",
      eyebrow: "BUILD, DEPLOY, VERIFY",
      title: "Prove each handoff from application contract to an operated Runtime",
      lead: "Treat the application, image, deployment, Runtime, state resources, and release evidence as separate objects with separate owners and acceptance checks.",
      blocks: Object.freeze([
        Object.freeze({
          type: "cards",
          title: "Learning outcomes",
          items: Object.freeze([
            Object.freeze({ id: "outcome-app-contract", title: "Define the application contract", body: "Identify the entry point, startup behavior, health surface, dependency set, configuration, and invocation contract that the platform will run." }),
            Object.freeze({ id: "outcome-command-lifecycle", title: "Use lifecycle verbs precisely", body: "Explain what build produces, what deploy changes, and why launch is a convenience path rather than a new lifecycle state." }),
            Object.freeze({ id: "outcome-runtime-evidence", title: "Separate readiness from acceptance", body: "Trace resource readiness, application health, dependency connectivity, functional quality, state continuity, and SLO evidence separately." }),
            Object.freeze({ id: "outcome-memory-observability", title: "Operate state and evidence boundaries", body: "Validate shared sessions, trusted identity, memory control and data planes, traces, evaluations, load tests, and customer-owned objectives." }),
          ]),
        }),
        Object.freeze({
          type: "steps",
          title: "Recommended learning route",
          items: Object.freeze([
            Object.freeze({ id: "route-freeze-app-contract", title: "Freeze a deployable application contract", body: "Record the entry point, dependencies, configuration schema, health behavior, invocation protocol, secrets, and release identity.", decision: "Checkpoint: no undeclared local environment assumption is required to start the application." }),
            Object.freeze({ id: "route-run-lifecycle", title: "Run build, deploy, and launch deliberately", body: "Use build to create an image, deploy to place a built image in a target environment, and launch when the intended workflow is the supported build-then-deploy sequence.", decision: "Checkpoint: logs identify the source revision, image identity, target Runtime, and command path." }),
            Object.freeze({ id: "route-bind-state-identity", title: "Bind state and identity", body: "Connect a shared session backend and memory resource, then resolve a trusted end-user identity outside model-controlled input and propagate its scope consistently.", decision: "Checkpoint: replica handoff and cross-user isolation tests pass in the target Runtime." }),
            Object.freeze({ id: "route-verify-data-planes", title: "Verify every data plane", body: "Test model, tool, memory, database, network, secret, and telemetry connections independently of control-plane resource status.", decision: "Checkpoint: each binding has a successful trace, an expected denial, and a failure-recovery case." }),
            Object.freeze({ id: "route-prove-release", title: "Prove the release under representative conditions", body: "Run functional evaluation, external load generation, observability checks, failure drills, and rollback against the same deployed identity.", decision: "Checkpoint: customer SLO and release decisions refer to measured end-to-end outcomes, not platform readiness alone." }),
          ]),
        }),
        Object.freeze({
          type: "cards",
          title: "Practice labs",
          items: Object.freeze([
            Object.freeze({ id: "lab-command-artifacts", title: "Trace lifecycle artifacts", subtitle: "The team uses build, deploy, and launch interchangeably", body: "Run the commands in a test project and retain the source revision, build logs, image identity, deployment target, Runtime revision, and endpoint trace for each path.", decision: "Deliverable: a command-to-artifact state diagram and rollback entry points.", boundary: "Acceptance: the team can state which object changed and which object did not after every command.", sourceIds: Object.freeze(["agentkit-cli-overview-2026-08-15", "agentkit-cli-commands-2026-08-15"]) }),
            Object.freeze({ id: "lab-session-identity", title: "Test replica handoff and identity isolation", subtitle: "The same session moves between Runtime instances", body: "Send a session through different processes, restart one process, attempt a cross-user lookup, and compare the authenticated subject with the user scope passed to session and memory services.", decision: "Deliverable: continuity, denial, restart, and audit traces.", boundary: "Acceptance: caller-supplied user_id is never accepted as proof of identity.", sourceIds: Object.freeze(["agentkit-runtime-quickstart-2026-08-15", "agentkit-memory-quickstart-2026-08-15"]) }),
            Object.freeze({ id: "lab-runtime-not-ready", title: "Turn Runtime readiness into release evidence", subtitle: "The Runtime reports ready but the service is not yet accepted", body: "Verify application health, model and tool calls, shared state, memory retrieval, identity and network policy, telemetry, functional evaluation, external load, failure recovery, and rollback.", decision: "Deliverable: a release matrix with pass, fail, and pending evidence.", boundary: "Acceptance: cloud deployment, observability, and capacity stay pending until their target tests complete.", sourceIds: Object.freeze(["agentkit-platform-overview-2026-08-15", "agentkit-runtime-quickstart-2026-08-15"]) }),
          ]),
        }),
      ]),
    }),
    Object.freeze({
      id: "curriculum",
      eyebrow: "CURRICULUM ATLAS",
      title: "From application identity to cloud release and operating evidence",
      lead: "AgentKit becomes easier to reason about when its application contract, lifecycle commands, Runtime, bindings, state services, and operational evidence remain distinct.",
      blocks: Object.freeze([
        Object.freeze({
          type: "cards",
          items: Object.freeze([
            Object.freeze({ id: "curriculum-platform-boundary", title: "AgentKit platform and VeADK framework", subtitle: "Platform Boundary", body: "VeADK can define and adapt a root agent; AgentKit provides the application, CLI, SDK, deployment, and Runtime path that delivers it. Either side can be discussed independently, but the integration requires an explicit application contract.", decision: "Assign framework behavior and delivery behavior to different owners and test suites.", boundary: "A working root agent is not a deployed Runtime, and a ready Runtime does not prove correct agent behavior.", sourceIds: Object.freeze(["agentkit-platform-overview-2026-08-15", "veadk-agentkit-integration-2026-08-15"]) }),
            Object.freeze({ id: "curriculum-application-contract", title: "Deployable AgentKit application contract", subtitle: "Application", body: "The contract makes the entry point, startup and health behavior, dependencies, routes or protocol surface, configuration, secrets, and resource expectations reproducible for the platform.", decision: "Freeze the contract with the source revision and make configuration changes reviewable.", boundary: "A local process that starts through implicit credentials or filesystem state is not yet a portable application artifact.", sourceIds: Object.freeze(["agentkit-platform-overview-2026-08-15", "agentkit-config-reference-2026-08-15"]) }),
            Object.freeze({ id: "curriculum-build-deploy-launch", title: "Build, deploy, and launch", subtitle: "Lifecycle", body: "In the normal CLI flow, build creates the image, deploy places a built image in a local or cloud target, and launch runs the supported build-then-deploy sequence. Some command options can trigger a cloud build during deployment, so logs and flags remain part of the evidence.", decision: "Use separate commands when artifact inspection, promotion, or rollback requires an explicit boundary; use launch for a deliberate combined iteration path.", boundary: "A command exit status does not prove image provenance, Runtime health, or application acceptance.", sourceIds: Object.freeze(["agentkit-cli-overview-2026-08-15", "agentkit-cli-commands-2026-08-15"]) }),
            Object.freeze({ id: "curriculum-runtime-binding", title: "Runtime readiness and resource bindings", subtitle: "Runtime", body: "The Runtime executes a deployed application version with configuration and bound services. Control-plane status reports resource reconciliation; the application must still prove data-plane connectivity to models, tools, state, memory, network, and telemetry.", decision: "Test each binding from inside the target Runtime and correlate the result with the deployed release identity.", boundary: "Ready is a platform state, not proof that business-quality requirements or a customer-defined SLO have been met.", sourceIds: Object.freeze(["agentkit-runtime-quickstart-2026-08-15", "agentkit-config-reference-2026-08-15"]) }),
            Object.freeze({ id: "curriculum-shared-session", title: "Shared sessions and trusted user scope", subtitle: "State", body: "A replicated application needs a shared session backend. Session and memory keys must be derived from a trusted identity context rather than accepted directly from model output or an unauthenticated request field.", decision: "Propagate one verified subject and tenant scope through Runtime, session, memory, tool, and audit records.", boundary: "A user_id parameter is an identifier only; the identity layer must establish who is allowed to use it.", sourceIds: Object.freeze(["agentkit-runtime-quickstart-2026-08-15", "veadk-short-term-memory-2026-08-15", "agentkit-memory-quickstart-2026-08-15"]) }),
            Object.freeze({ id: "curriculum-memory-planes", title: "Memory control plane and data plane", subtitle: "Managed Memory", body: "Validate the AgentKit Memory resource binding and its data-plane connectivity as separate checkpoints. If Mem0 is evaluated independently, compare Mem0 Platform with Mem0 OSS as a second decision; the cited sources do not establish that AgentKit Memory uses or is operated by Mem0.", decision: "Assign operational ownership, data controls, retrieval tests, and migration duties to the option actually selected.", boundary: "A binding does not prove reachability or retrieval behavior, and the Mem0 comparison does not establish regional availability, data residency, or SLA terms for AgentKit Memory.", sourceIds: Object.freeze(["agentkit-memory-quickstart-2026-08-15", "mem0-oss-overview-2026-08-15", "mem0-platform-vs-oss-2026-08-15"]) }),
            Object.freeze({ id: "curriculum-operating-evidence", title: "Observability, evaluation, load, and customer SLO", subtitle: "Operating Evidence", body: "Platform telemetry explains Runtime and application behavior. Evaluation measures task quality and risk. An external load generator measures end-to-end capacity and dependency effects. The customer and service owner define the SLO and release rule.", decision: "Correlate all evidence to the same application, image, Runtime, configuration, and dependency versions.", boundary: "No single trace, score, load result, or ready status substitutes for the complete acceptance decision.", sourceIds: Object.freeze(["agentkit-platform-overview-2026-08-15", "agentkit-runtime-quickstart-2026-08-15"]) }),
          ]),
        }),
      ]),
    }),
    Object.freeze({
      id: "principles",
      eyebrow: "DELIVERY PRINCIPLES",
      title: "Make every lifecycle object, binding, and acceptance decision independently inspectable",
      blocks: Object.freeze([
        Object.freeze({
          type: "cards",
          items: Object.freeze([
            Object.freeze({ id: "principle-artifact-before-runtime", title: "Separate artifact from Runtime", body: "A build creates a versioned artifact. A deployment reconciles a target Runtime to an artifact and configuration. Invocation evidence belongs to that deployed combination.", boundary: "Rebuilding and redeploying during one convenience command must not erase artifact identity.", sourceIds: Object.freeze(["agentkit-cli-overview-2026-08-15"]) }),
            Object.freeze({ id: "principle-binding-not-connection", title: "A binding is not a connection test", body: "Control-plane configuration can name a model, memory, secret, or other service. The application still needs data-plane authentication, authorization, network, protocol, and failure-path verification.", boundary: "A resource shown in configuration is not proof that the deployed process can use it correctly.", sourceIds: Object.freeze(["agentkit-config-reference-2026-08-15", "agentkit-memory-quickstart-2026-08-15"]) }),
            Object.freeze({ id: "principle-externalize-state", title: "Externalize continuity before scaling", body: "Runtime replicas are replaceable execution units. Session continuity and durable business effects must live in shared services with explicit consistency and recovery contracts.", boundary: "A warm process is not a durable state service.", sourceIds: Object.freeze(["agentkit-runtime-quickstart-2026-08-15", "veadk-short-term-memory-2026-08-15"]) }),
            Object.freeze({ id: "principle-platform-evidence", title: "Managed execution does not remove application ownership", body: "AgentKit can reduce delivery and Runtime operations, while the application team still owns domain correctness, identity use, tool effects, memory governance, evaluation, capacity assumptions, and release acceptance.", boundary: "Platform capability is not a customer commitment.", sourceIds: Object.freeze(["agentkit-platform-overview-2026-08-15"]) }),
          ]),
        }),
      ]),
    }),
    Object.freeze({
      id: "decisions",
      eyebrow: "DELIVERY DECISIONS",
      title: "Choose command, deployment, state, and memory paths by the evidence the release needs",
      blocks: Object.freeze([
        Object.freeze({
          type: "cards",
          items: Object.freeze([
            Object.freeze({ id: "decision-launch-or-separate", title: "Launch or separate build and deploy", body: "Use launch for a controlled iteration that intentionally builds and deploys in sequence. Separate build and deploy when the image must be inspected, promoted, approved, reused, or rolled back independently.", decision: "Preserve the source-to-image-to-Runtime chain in either path.", boundary: "The convenience path does not weaken provenance or acceptance requirements.", sourceIds: Object.freeze(["agentkit-cli-overview-2026-08-15", "agentkit-cli-commands-2026-08-15"]) }),
            Object.freeze({ id: "decision-local-cloud-hybrid", title: "Local, cloud, or hybrid validation", body: "Use local execution for fast application-contract debugging, cloud execution for target Runtime and binding evidence, and a hybrid path when the platform supports the intended split and the network boundary is part of the test.", decision: "Choose the mode that reproduces the risk being evaluated, then label the scope precisely.", boundary: "Local success cannot be generalized to cloud identity, network, scaling, state, or capacity.", sourceIds: Object.freeze(["agentkit-cli-overview-2026-08-15"]) }),
            Object.freeze({ id: "decision-managed-or-oss-memory", title: "Managed or self-managed memory", body: "When evaluating a Mem0-based option, choose the managed platform when reduced service operations outweigh control or portability needs. Choose the self-managed OSS project when the team accepts deployment, upgrade, security, observability, and recovery ownership.", decision: "Compare data location, identity model, network, customization, operations, migration, and tested retrieval behavior.", boundary: "The Mem0 comparison does not establish that AgentKit Memory uses or is operated by Mem0, nor does it establish regional availability, data residency, or SLA terms for an AgentKit deployment. The delivery choice also does not make retrieved memory authoritative truth.", sourceIds: Object.freeze(["mem0-oss-overview-2026-08-15", "mem0-platform-vs-oss-2026-08-15"]) }),
            Object.freeze({ id: "decision-ready-or-release", title: "Runtime ready or production release", body: "Treat ready as a prerequisite for deployed testing. Release only after identity, dependency, state, quality, load, observability, failure, and rollback evidence meets the declared rule.", decision: "Use a release matrix with explicit pass, fail, pending, owner, and evidence links.", boundary: "Do not convert missing cloud or observability work into an implied pass.", sourceIds: Object.freeze(["agentkit-runtime-quickstart-2026-08-15"]) }),
          ]),
        }),
      ]),
    }),
    Object.freeze({
      id: "deep-dive",
      eyebrow: "LIFECYCLE DEEP DIVE",
      title: "Follow desired state and evidence through source, image, Runtime, bindings, and outcomes",
      blocks: Object.freeze([
        Object.freeze({
          type: "steps",
          items: Object.freeze([
            Object.freeze({ id: "deep-source-to-image", title: "Source to image", body: "Record the source revision, dependency lock, build configuration, build logs, image identity, and security checks.", decision: "Build evidence ends at the artifact boundary." }),
            Object.freeze({ id: "deep-image-to-runtime", title: "Image to Runtime", body: "Record the target environment, Runtime revision, configuration, resource bindings, rollout state, endpoint, and rollback target.", decision: "Deployment evidence ends at reconciled infrastructure and application startup." }),
            Object.freeze({ id: "deep-runtime-to-dependencies", title: "Runtime to dependencies", body: "Exercise identity, network, model, tool, session, memory, secret, and telemetry data planes from the deployed process.", decision: "A configuration reference becomes usable only after successful, access-denied, and failure-path tests." }),
            Object.freeze({ id: "deep-dependencies-to-outcome", title: "Dependencies to accepted outcome", body: "Correlate end-to-end traces with task evaluation, authoritative business state, external load, service indicators, and release rules.", decision: "The application and customer own the final acceptance boundary." }),
          ]),
        }),
        Object.freeze({
          type: "boundary",
          title: "Lifecycle traps",
          items: Object.freeze([
            Object.freeze({ id: "trap-command-name", title: "Inferring state from a command name", body: "Options can alter the normal lifecycle path, including a deployment path that performs cloud build work.", boundary: "Read the selected flags and retain stage logs instead of reasoning from the top-level verb alone." }),
            Object.freeze({ id: "trap-ready-equals-healthy", title: "Equating ready with healthy business behavior", body: "Infrastructure may be reconciled while an application route, credential, dependency, or domain result is wrong.", boundary: "Require an end-to-end trace and authoritative result check." }),
            Object.freeze({ id: "trap-session-equals-memory", title: "Combining session and memory", body: "An active conversation store and a long-term retrieval service have different consistency, scope, deletion, and truth boundaries.", boundary: "Test and govern them as separate data products." }),
            Object.freeze({ id: "trap-platform-trace-equals-slo", title: "Treating platform telemetry as an SLO", body: "Traces and metrics expose behavior but do not choose the acceptable quality, latency, availability, or cost objective.", boundary: "The service owner defines the indicator and objective; external tests validate the full path." }),
          ]),
        }),
      ]),
    }),
    Object.freeze({
      id: "cloud-connections",
      eyebrow: "CLOUD DELIVERY",
      title: "Turn the cloud path into five independently accepted objects",
      lead: "The current knowledge work can document the target and pending gates without pretending that deployment, observability, or capacity tests have already run.",
      blocks: Object.freeze([
        Object.freeze({
          type: "cards",
          items: Object.freeze([
            Object.freeze({ id: "cloud-object-application", title: "Application", body: "Versioned entry point, dependencies, startup, health, routes, configuration schema, and evaluation baseline.", boundary: "Owner: application engineering." }),
            Object.freeze({ id: "cloud-object-artifact", title: "Build artifact", body: "Traceable image connected to source, build inputs, logs, checks, and promotion history.", boundary: "Owner: build and supply-chain workflow." }),
            Object.freeze({ id: "cloud-object-runtime", title: "Runtime deployment", body: "Target environment, Runtime revision, rollout status, endpoint, resource bindings, and rollback target.", boundary: "Owner: platform delivery." }),
            Object.freeze({ id: "cloud-object-state", title: "State and identity services", body: "Shared session, scoped memory, verified user and tenant identity, secrets, network policy, and authoritative business systems.", boundary: "Owner: application, platform, security, and data teams by control." }),
            Object.freeze({ id: "cloud-object-evidence", title: "Operating evidence", body: "Correlated traces, service indicators, task evaluation, external load, failure recovery, cost, and release decision.", boundary: "Owner: service team with customer-approved objectives." }),
          ]),
        }),
        Object.freeze({
          type: "steps",
          title: "Pending production sequence",
          items: Object.freeze([
            Object.freeze({ id: "cloud-pending-deploy", title: "Deploy and identify the target release", body: "Retain source, image, Runtime, configuration, and endpoint identity from the actual cloud path.", decision: "Status remains pending until the target Runtime is exercised." }),
            Object.freeze({ id: "cloud-pending-observe", title: "Complete observability wiring", body: "Correlate ingress, Runtime, model, tool, session, memory, and authoritative-system events without leaking sensitive content.", decision: "Status remains pending until missing spans, metrics, logs, and redaction behavior are tested." }),
            Object.freeze({ id: "cloud-pending-load", title: "Run external load and recovery tests", body: "Drive representative traffic from outside the Runtime, include downstream limits and failures, and verify scaling, saturation, recovery, and rollback.", decision: "Status remains pending until measured behavior is compared with a declared customer SLO." }),
          ]),
        }),
      ]),
    }),
  ]),
  qa: Object.freeze([
    Object.freeze({
      id: "agentkit-veadk-responsibility",
      q: "How do AgentKit and VeADK divide responsibility?",
      a: "VeADK is an agent-development framework and integration layer. AgentKit is the application-delivery and Runtime platform that builds, configures, deploys, and operates a compatible application.",
      depth: "VeADK can define a root agent and wrap it in an AgentKit-compatible application. AgentKit then owns lifecycle and Runtime surfaces, while the application team still owns agent behavior, tool authorization, data correctness, evaluation, and business acceptance. The boundary is a contract, not an automatic handoff of responsibility.",
      ask: "Does the current problem belong to agent code, the application adapter, platform delivery, or production acceptance?",
      tag: "Platform boundary",
      basis: "Platform overview plus framework adapter",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "agentkit-platform-overview-2026-08-15", supports: "Describes AgentKit's application and delivery scope." }),
        Object.freeze({ sourceId: "veadk-agentkit-integration-2026-08-15", supports: "Shows the VeADK adapter that creates an AgentKit-compatible application around a root agent." }),
      ]),
      addedAt: "2026-08-15",
    }),
    Object.freeze({
      id: "agentkit-application-contract",
      q: "What is the AgentKit application contract?",
      a: "It is the reproducible boundary that tells the platform how to start, check, configure, and invoke an agent application and what dependencies and resources the application expects.",
      depth: "Freeze the entry point, startup command or behavior, health route, invocation surface, dependency set, configuration schema, secret references, resource bindings, and release identity. Remove implicit local credentials and filesystem assumptions. Validate the contract locally, then validate the same identity in the target Runtime.",
      ask: "Can a clean target environment start and inspect the application using only declared artifacts, configuration, secrets, and bindings?",
      tag: "Application contract",
      basis: "Platform application model plus configuration reference",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "agentkit-platform-overview-2026-08-15", supports: "Frames the agent application as the unit delivered through AgentKit." }),
        Object.freeze({ sourceId: "agentkit-config-reference-2026-08-15", supports: "Documents configurable application and Runtime inputs; target connectivity still needs data-plane tests." }),
      ]),
      addedAt: "2026-08-15",
    }),
    Object.freeze({
      id: "agentkit-build-deploy-launch",
      q: "How should build, deploy, and launch be distinguished?",
      a: "Build creates an image. Deploy places a built image into a target environment. Launch runs the supported build-then-deploy sequence for a combined workflow.",
      depth: "Keep source revision, build inputs, image identity, deployment target, Runtime revision, and logs even when launch combines stages. Inspect command options because some deployment paths can invoke cloud build work. Use separate stages when artifact approval, promotion, reuse, or rollback needs an explicit boundary.",
      ask: "Which artifact was produced, which Runtime was changed, and can either stage be repeated or rolled back independently?",
      tag: "Build and deployment",
      basis: "CLI lifecycle semantics and command behavior",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "agentkit-cli-overview-2026-08-15", supports: "Defines the normal build, deploy, and combined launch semantics." }),
        Object.freeze({ sourceId: "agentkit-cli-commands-2026-08-15", supports: "Documents command options and lifecycle paths, including cases that require stage-level inspection." }),
      ]),
      addedAt: "2026-08-15",
    }),
    Object.freeze({
      id: "agentkit-runtime-ready-boundary",
      q: "Why does a ready AgentKit Runtime not mean the business service is live?",
      a: "Ready reports a platform or application-runtime state. Production acceptance also requires correct identity, dependency connectivity, shared state, domain behavior, evaluation, load, observability, recovery, and rollback.",
      depth: "Test the deployed endpoint from the intended network path. Correlate the request through Runtime, model, tools, session, memory, and authoritative systems. Exercise denied access and dependency failure. Compare functional and load results with the release rule. Keep every missing gate marked pending rather than interpreting readiness as a pass.",
      ask: "Which end-to-end behavior, failure, load, and rollback evidence exists for this exact Runtime revision?",
      tag: "Runtime acceptance",
      basis: "Runtime delivery plus application-owned acceptance",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "agentkit-runtime-quickstart-2026-08-15", supports: "Documents Runtime deployment and invocation steps; it does not establish a customer's complete production acceptance." }),
        Object.freeze({ sourceId: "agentkit-platform-overview-2026-08-15", supports: "Describes platform capabilities whose use still requires application-specific verification." }),
      ]),
      addedAt: "2026-08-15",
    }),
    Object.freeze({
      id: "agentkit-session-user-identity",
      q: "How should multi-instance sessions and user_id be handled?",
      a: "Put sessions in a shared backend and derive user scope from authenticated identity. A caller-supplied user_id is not proof that the caller owns that identity or session.",
      depth: "Propagate a verified subject and tenant scope through Runtime, session, memory, tools, and audit records. Test replica handoff, restart, concurrent updates, cross-user and cross-tenant denial, retention, and recovery. Keep display identity, external identity, and internal storage keys distinct when their lifecycles differ.",
      ask: "Which identity provider establishes the subject, and can another replica enforce the same session and memory scope after restart?",
      tag: "Shared state and identity",
      basis: "Replicated Runtime plus scoped state services",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "agentkit-runtime-quickstart-2026-08-15", supports: "Documents the Runtime environment in which a replicated application executes; identity trust remains an application and platform integration concern." }),
        Object.freeze({ sourceId: "veadk-short-term-memory-2026-08-15", supports: "Documents short-term session backends and distinguishes distributed persistence from in-memory and local-file options." }),
        Object.freeze({ sourceId: "agentkit-memory-quickstart-2026-08-15", supports: "Documents user-scoped memory integration inputs; it does not authenticate a caller-supplied identifier." }),
      ]),
      addedAt: "2026-08-15",
    }),
    Object.freeze({
      id: "agentkit-memory-planes-choice",
      q: "How should memory bindings, data-plane checks, OSS versus managed delivery, and retrieval timing be handled?",
      a: "Treat the AgentKit Memory binding as control-plane configuration and prove its data plane separately. If Mem0 is evaluated independently, compare Mem0 Platform with Mem0 OSS as a separate delivery decision; the cited sources do not establish a product mapping between AgentKit Memory and Mem0.",
      depth: "For the selected option, validate credentials, network, user and tenant scope, write acknowledgment, processing, retrieval visibility, correction, deletion, observability, backup, and migration. If processing is asynchronous, a successful write can precede retrieval visibility. The Mem0 comparison does not establish who operates AgentKit Memory, its regional availability, data residency, or SLA terms, and no delivery choice makes retrieved content authoritative.",
      ask: "Who operates the memory service, how is identity scope enforced, when does a write become retrievable, and which system confirms current facts?",
      tag: "Memory governance",
      basis: "Control-plane binding plus data-plane and ownership tests",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "agentkit-memory-quickstart-2026-08-15", supports: "Documents the AgentKit memory resource and application integration path." }),
        Object.freeze({ sourceId: "mem0-oss-overview-2026-08-15", supports: "Documents the self-managed Mem0 project surface and leaves service operations to the adopting team." }),
        Object.freeze({ sourceId: "mem0-platform-vs-oss-2026-08-15", supports: "Compares managed and self-managed delivery boundaries; application-specific governance and retrieval validation remain necessary." }),
      ]),
      addedAt: "2026-08-15",
    }),
    Object.freeze({
      id: "agentkit-operating-evidence",
      q: "How do platform observability, evaluation, external load testing, and the customer SLO divide responsibility?",
      a: "Platform observability explains Runtime and application behavior. Evaluation measures task quality and risk. External load testing exercises the complete service and its dependencies. The customer and service owner define the indicators, objectives, and release decision.",
      depth: "Correlate traces, metrics, logs, evaluation results, load-generator measurements, downstream saturation, errors, cost, and authoritative outcomes to one release identity. Use platform signals for diagnosis, not as an automatic SLO. Keep deployment, observability, capacity, and recovery evidence pending until it is measured in the target environment.",
      ask: "What customer outcome and service indicator define success, and which target-environment test supplies each piece of evidence?",
      tag: "Observability and SLO",
      basis: "Platform telemetry plus application acceptance",
      evidence: Object.freeze([
        Object.freeze({ sourceId: "agentkit-platform-overview-2026-08-15", supports: "Describes AgentKit platform capabilities; it does not define a customer-specific SLO or task-quality threshold." }),
        Object.freeze({ sourceId: "agentkit-runtime-quickstart-2026-08-15", supports: "Documents target Runtime delivery and invocation, which is the environment where end-to-end operating evidence must be collected." }),
      ]),
      addedAt: "2026-08-15",
    }),
  ]),
  evidenceCards: Object.freeze([
    Object.freeze({ id: "agentkit-cli-lifecycle", metric: "Build -> deploy", title: "Launch combines the normal build and deployment sequence", finding: "The AgentKit CLI documentation distinguishes image build, target deployment, and launch as the supported combined path.", boundary: "Command options can alter stage behavior, and command success does not prove image provenance, Runtime health, or business acceptance.", sourceId: "agentkit-cli-overview-2026-08-15", accent: true }),
    Object.freeze({ id: "agentkit-state-memory-boundary", metric: "External state", title: "Memory integration is a resource and data-plane contract", finding: "AgentKit documents a memory resource and application integration path that must be scoped to the intended user and application context.", boundary: "A binding does not prove connectivity, retrieval freshness, tenant isolation, deletion, or source-of-truth status; replicated session state remains a separate concern.", sourceId: "agentkit-memory-quickstart-2026-08-15" }),
    Object.freeze({ id: "agentkit-cloud-evidence-pending", metric: "Target Runtime", title: "Cloud deployment and operating evidence require target tests", finding: "The Runtime quickstart establishes the delivery and invocation path on which application, dependency, and operational tests can run.", boundary: "It does not prove that this application has completed identity, shared-state, observability, evaluation, external-load, capacity, recovery, or acceptance against a customer-defined SLO.", sourceId: "agentkit-runtime-quickstart-2026-08-15" }),
  ]),
  terms: Object.freeze({
    agentkit: Object.freeze({ name: "Volcengine AgentKit", definition: "A platform and developer-tooling layer for packaging, configuring, deploying, and operating agent applications." }),
    "agentkit-runtime": Object.freeze({ name: "AgentKit Runtime", definition: "The managed execution environment that runs a deployed agent-application version with its configuration and bound services." }),
    "agent-application": Object.freeze({ name: "Agent Application", definition: "A deployable program with an explicit entry point, startup and health behavior, dependencies, configuration, and invocation surface." }),
    "runtime-binding": Object.freeze({ name: "Runtime Binding", definition: "A control-plane association between a Runtime and a model, memory, secret, network, or other required resource; data-plane usability requires separate verification." }),
    "deployment-mode": Object.freeze({ name: "Deployment Mode", definition: "The selected local, cloud, or hybrid execution path used to validate a defined delivery and operating scope." }),
    "build-deploy-lifecycle": Object.freeze({ name: "Build–Deploy Lifecycle", definition: "The traceable progression from source and build inputs to an image, target Runtime revision, verified invocation, and recoverable release." }),
    "managed-agent-memory": Object.freeze({ name: "Managed Agent Memory", definition: "A platform-operated memory service connected to an agent application, with application-owned identity scope, governance, retrieval validation, and truth boundaries." }),
    "agentkit-app-adapter": Object.freeze({ name: "AgentKit App Adapter", definition: "The application-layer wrapper that exposes a root agent through the interface expected by AgentKit without provisioning the target Runtime." }),
  }),
  sources: Object.freeze({
    "veadk-agentkit-integration-2026-08-15": agentPlatformSourceCopy["veadk-agentkit-integration-2026-08-15"],
    "veadk-short-term-memory-2026-08-15": agentPlatformSourceCopy["veadk-short-term-memory-2026-08-15"],
    "agentkit-platform-overview-2026-08-15": agentPlatformSourceCopy["agentkit-platform-overview-2026-08-15"],
    "agentkit-cli-overview-2026-08-15": agentPlatformSourceCopy["agentkit-cli-overview-2026-08-15"],
    "agentkit-cli-commands-2026-08-15": agentPlatformSourceCopy["agentkit-cli-commands-2026-08-15"],
    "agentkit-config-reference-2026-08-15": agentPlatformSourceCopy["agentkit-config-reference-2026-08-15"],
    "agentkit-runtime-quickstart-2026-08-15": agentPlatformSourceCopy["agentkit-runtime-quickstart-2026-08-15"],
    "agentkit-memory-quickstart-2026-08-15": agentPlatformSourceCopy["agentkit-memory-quickstart-2026-08-15"],
    "mem0-oss-overview-2026-08-15": agentPlatformSourceCopy["mem0-oss-overview-2026-08-15"],
    "mem0-platform-vs-oss-2026-08-15": agentPlatformSourceCopy["mem0-platform-vs-oss-2026-08-15"],
  }),
});
