const freezeItems = (items) => Object.freeze(items.map((item) => Object.freeze(item)));
const freezeQa = (items) => freezeItems(items.map((item) => ({ ...item, evidence: freezeItems(item.evidence) })));

/**
 * 按模块真实决策面补充的问题集。
 *
 * 这里刻意不使用统一题数：每个模块先列出尚未被既有问题覆盖的架构、
 * 风险、运营与采购判断，再决定需要多少问题。
 */
export const moduleQuestionDepthExpansion = Object.freeze({
  "model-landscape": freezeQa([
    { q: "所有任务都使用能力最强、推理配置最高的前沿模型，是否最稳妥？", a: "通常不是。低风险、结构化或高并发任务可能更适合小模型或较低 reasoning effort；而且不是所有模型都提供显式推理配置。", depth: "先区分推理难度、工具要求、数据边界、延迟和错误代价，再把模型、可用的推理配置与工具集组成可回归候选。更长推理可能提高部分复杂任务表现，却也增加 Token、尾延迟和版本影响面；应按每次有效结果成本选择。", ask: "追问客户：目标服务是否提供推理配置？哪些请求真正需要额外测试时计算，增益能否覆盖时延与成本？", tag: "任务分层", basis: "任务门槛 + 可用推理配置 + 模型组合", evidence: [{ sourceId: "openai-reasoning-guide", supports: "支持部分 OpenAI 模型按 reasoning effort 在质量、速度和 Token 使用之间取舍，且支持值依模型而异。" }, { sourceId: "deepseek-r1-2025", supports: "支持强化学习与测试时推理共同形成复杂任务能力，同时显示方法和任务边界。" }, { sourceId: "nist-genai-profile", supports: "支持按使用情境与影响程度选择和测量生成式 AI。" }] },
    { q: "同名模型在不同区域、云平台或托管方式上，可以当作完全相同吗？", a: "不能默认相同。模型快照、服务参数、内容策略、工具接口、配额和数据处理边界都可能不同。", depth: "模型身份应至少绑定提供方、区域、端点、精确版本、默认参数和能力契约。迁移时在相同评估集和真实调用链上复测输出、结构化结果、工具调用、尾延迟和错误语义，不能只比较产品名称。", ask: "追问客户：当前能否精确还原一次请求使用的端点、模型快照和默认参数？", tag: "模型身份", basis: "版本身份 + 环境差异", evidence: [{ sourceId: "openai-models", supports: "支持模型版本与能力需按具体模型标识理解。" }, { sourceId: "google-models", supports: "支持模型服务包含不同型号与能力边界，不能只按通用名称比较。" }] },
    { q: "开放权重模型能在自有环境部署，是否就代表更可控？", a: "不一定。它提高了部署和检查自由度，也把安全加固、推理栈、升级、容量和事件响应责任转给使用方。", depth: "可控性要拆成权重获取、许可证、数据路径、运行时、版本冻结、补丁、评估和退出能力。自托管能满足部分地域或定制要求，但如果缺少模型制品治理、服务可靠性和专业团队，实际风险可能高于托管 API。", ask: "追问客户：选择开放权重是为了数据边界、成本、定制还是供应连续性？对应责任由谁承担？", tag: "部署责任", basis: "控制权 + 运营责任", evidence: [{ sourceId: "nist-genai-profile", supports: "支持识别模型供应链、部署情境与组织责任。" }, { sourceId: "anthropic-models", supports: "支持托管模型按服务能力与使用边界交付，和自托管责任范围不同。" }] },
    { q: "模型支持超长上下文，选型时是否可以直接把最大窗口当作可用容量？", a: "不可以。最大窗口是协议上限，不等于所有位置都同样有效，也不代表在目标延迟、并发和成本下可持续使用。", depth: "应按真实输入长度和证据位置测试任务成功、首字延迟、输出速度与单位成本，并检查工具定义、历史和检索内容共同占用。选型表要记录常用工作区间和退化点，而不是只抄最大 Token 数。", ask: "追问客户：真实请求的长度分布、证据位置和并发是什么？达到多长后质量或 SLO 开始失守？", tag: "上下文边界", basis: "有效上下文 + 容量约束", evidence: [{ sourceId: "openai-models", supports: "支持模型具有具体上下文与能力规格，但规格需要结合任务使用。" }, { sourceId: "opentelemetry-genai-semconv", supports: "支持记录生成式 AI 请求的模型与用量属性，以验证真实负载。" }] },
  ]),
  multimodal: freezeQa([
    { q: "图像分辨率和视频帧数越高，多模态效果就一定越好吗？", a: "不一定。更多像素和帧会增加成本与延迟，也可能引入重复信息；关键是目标证据是否被稳定保留。", depth: "为小字、图表、动作变化和长视频分别设计采样策略，比较原图、裁剪、关键帧和分段后的召回与理解。验收同时记录遗漏证据、重复 Token、首包时间和人工回看成本。", ask: "追问客户：任务依赖细小文字、空间关系还是时间变化？最低需要保留哪类证据？", tag: "输入预算", basis: "证据保真 + 计算预算", evidence: [{ sourceId: "vit-2021", supports: "支持视觉输入被切分为表示后参与模型计算，输入规模会影响计算。" }, { sourceId: "nist-genai-profile", supports: "支持在具体使用条件下共同测量性能与风险。" }] },
    { q: "长视频可以整段交给模型后直接问问题吗？", a: "可以做探索，但生产方案通常需要镜头、时间段、音轨和字幕的分段索引，否则成本、遗漏和证据定位都难控制。", depth: "视频理解要区分采样遗漏、跨段关系、说话人、画面文字和最终推断。先建立时间轴与证据坐标，再验证模型能否在关键事件前后保留必要上下文；高影响结论应返回时间段而不是只给摘要。", ask: "追问客户：问题需要定位具体时间点还是概括全片？允许漏掉哪些短暂事件？", tag: "时序证据", basis: "时间采样 + 可回看证据", evidence: [{ sourceId: "clip-2021", supports: "支持图文表示对齐，但不自动解决长视频的时间连续性。" }, { sourceId: "nist-genai-profile", supports: "支持记录系统限制并为高影响输出保留验证机制。" }] },
    { q: "语音转写很准确，为什么实时语音 Agent 仍可能体验很差？", a: "因为实时体验还取决于端点检测、打断、说话人、噪声、首包、工具等待和恢复，不只是字错率。", depth: "把一轮语音交互拆成收音、检测结束、转写、理解、工具、合成和播放，分别观察 P50/P95。测试重叠说话、用户打断、口音、静音和网络抖动，并验证错误时是否能澄清或切回人工。", ask: "追问客户：用户允许等待多久？打断和多人说话时系统应怎样表现？", tag: "语音交互", basis: "端到端时延 + 对话控制", evidence: [{ sourceId: "opentelemetry-semconv", supports: "支持用核心 Trace 关联语音处理、网络与播放等通用阶段。" }, { sourceId: "opentelemetry-genai-semconv", supports: "支持关联生成式 AI 模型与工具调用的专用属性。" }, { sourceId: "nist-genai-profile", supports: "支持在真实交互情境中评估可靠性与人机监督。" }] },
    { q: "图片或语音转成 Embedding 后，敏感性是否就降低了？", a: "不能这样假设。Embedding 仍可能泄露身份、属性或相似关系，也可能继承原数据的用途和访问限制。", depth: "对人脸、证件、声音和医疗影像等输入，在采集前明确合法用途、最小范围、保留期和删除传播。向量库需要租户隔离、当前授权和审计；不能用‘不可直接阅读’代替数据分类。", ask: "追问客户：这些模态是否包含生物特征或身份信息？谁可检索，撤回后哪些派生数据必须删除？", tag: "模态隐私", basis: "数据分类 + 派生治理", evidence: [{ sourceId: "nist-genai-profile", supports: "支持管理多模态生成式 AI 的隐私与数据治理风险。" }, { sourceId: "nist-zero-trust", supports: "支持按主体和资源持续执行最小权限。" }] },
    { q: "多模态模型看不清输入时，应该继续猜一个答案还是自动降级？", a: "高影响任务不应静默猜测。系统应识别不可读、缺页、低置信或不支持的输入，并转专用解析、要求重传或人工复核。", depth: "为每种模态定义可处理性门槛和失败代码，把‘没有证据’与‘模型没有找到’区分开。降级路径也要保留原始坐标和权限，避免重新处理时丢失证据或扩大访问范围。", ask: "追问客户：哪些输入质量问题必须阻断？可接受的降级顺序和人工 SLA 是什么？", tag: "降级策略", basis: "可处理性检测 + 安全失败", evidence: [{ sourceId: "docling-report", supports: "支持文档处理存在独立解析与结构恢复阶段，需要显式质量控制。" }, { sourceId: "nist-genai-profile", supports: "支持为系统限制配置响应、恢复与人工监督。" }] },
  ]),
  mcp: freezeQa([
    { q: "用户完成 OAuth 登录，是否就代表 Agent 可以调用该 MCP Server 的所有工具？", a: "不代表。登录确认主体，具体资源、动作、租户和风险仍需逐次授权。", depth: "把用户同意、客户端身份、Server scope 和业务系统权限分开。高风险写入使用窄 scope、当前资源授权和必要的动作确认；Server 不能把一次宽泛登录转换成永久全权令牌。", ask: "追问客户：身份由谁签发？每个工具最终在哪一层检查资源级权限？", tag: "授权链", basis: "身份认证 + 资源授权", evidence: [{ sourceId: "mcp-authorization", supports: "支持 MCP 授权流程和资源服务器边界。" }, { sourceId: "nist-zero-trust", supports: "支持每次访问按主体和资源重新决策。" }] },
    { q: "MCP 工具 Schema 更新后，客户端能否自动适配？", a: "不能默认。字段、枚举、默认值和错误语义变化都可能改变业务行为，即使协议连接仍然成功。", depth: "为工具建立语义版本、兼容范围、弃用期和契约测试。Host 在升级前用冻结调用样本验证参数生成、返回解析和失败处理；破坏性变更应并行提供新版本，而不是静默覆盖。", ask: "追问客户：Server 升级如何通知使用方？哪些 Schema 变化必须阻断自动更新？", tag: "契约版本", basis: "Schema 兼容 + 发布治理", evidence: [{ sourceId: "mcp-architecture", supports: "支持 Host、Client 和 Server 通过明确能力契约交互。" }, { sourceId: "opentelemetry-genai-semconv", supports: "支持记录工具与调用属性，为版本前后比较提供依据。" }] },
    { q: "Server 宣称支持某项能力，Host 是否可以直接信任能力描述？", a: "不可以。能力发现是候选信息，不是安全证明或业务验收。", depth: "受管目录应验证发布者、制品、权限、网络出口、数据处理和目标系统，并对代表性工具做契约与风险测试。动态发现只能在已批准信任域内工作，不能自动把未知 Server 带入生产。", ask: "追问客户：能力元数据由谁签名和审核？发现结果如何映射到企业准入目录？", tag: "能力发现", basis: "发现机制 + 信任建立", evidence: [{ sourceId: "mcp-architecture", supports: "支持 MCP 的能力发现与协商机制。" }, { sourceId: "mcp-security", supports: "支持对 MCP 生态中的信任与攻击面进行独立安全评估。" }] },
    { q: "MCP 的 Resources、Prompts 和 Tools 可以使用同一套权限吗？", a: "不应默认相同。读取上下文、获取模板和执行动作的风险、数据范围与副作用不同。", depth: "分别定义资源可见性、Prompt 发布责任和工具动作权限。只读内容也可能含敏感数据或恶意指令；写入工具则需要更强的当前授权、确认、幂等和审计。Host 应按能力类型展示来源和风险。", ask: "追问客户：哪些能力只读，哪些会改变业务状态？三类能力分别由谁批准？", tag: "能力分权", basis: "能力类型 + 最小权限", evidence: [{ sourceId: "mcp-architecture", supports: "支持 MCP 将 Resources、Prompts 与 Tools 作为不同能力类型。" }, { sourceId: "nist-zero-trust", supports: "支持按资源和操作执行细粒度授权。" }] },
    { q: "工具返回的文本来自企业系统，模型能否把它当成可信指令？", a: "不能。工具结果可以是业务数据，但仍属于不可信内容，不能自动升级为控制 Agent 的指令。", depth: "Host 要把指令与数据通道分开，对工具输出做来源标记、长度和类型校验，并防止其中的文本改变后续工具权限或系统规则。高风险决策回到结构化字段和权威系统验证。", ask: "追问客户：工具结果里可能包含用户可控文本吗？它能影响下一次工具选择或参数吗？", tag: "结果注入", basis: "数据指令分离 + 输出校验", evidence: [{ sourceId: "owasp-prompt-injection", supports: "支持外部内容可携带间接提示注入，不能因来源于工具就视为可信指令。" }, { sourceId: "mcp-security", supports: "支持评估 MCP 工具与内容通道带来的安全威胁。" }] },
    { q: "一个 MCP 调用需要几分钟甚至几小时，是否应该一直保持连接等待？", a: "通常不应。当前正式版 2025-11-25 可用实验性 Tasks 包装支持的请求；已公布的 2026-07-28 RC 计划把 Tasks 改为独立扩展，但尚不能当作生效规范。", depth: "按 2025-11-25 实现时，先协商双方与具体请求类型的 Task 能力，再处理 working、input_required、completed、failed、cancelled、TTL、轮询、取消和断线恢复。评估 RC 时要另行验证扩展协商以及 tasks/get、tasks/update、tasks/cancel 等新生命周期，不能把两版 API 混用。MCP Task 只包装 client / server 请求；若是独立 Agent 间委托、消息与产物交付，应显式映射 A2A Task。最终业务状态仍由权威系统确认。", ask: "追问客户：当前固定哪个协议版本？双方支持哪套 Task 契约，最长多久、断线后从哪里恢复，MCP 与业务任务 ID 如何映射？", tag: "长任务", basis: "版本化 Tasks 能力 + 业务耐久状态", evidence: [{ sourceId: "mcp-tasks-2025-11-25", supports: "支持当前正式版中实验性 Task 的轮询、结果、状态和取消语义。" }, { sourceId: "mcp-2026-07-28-rc", supports: "支持 RC 计划把 Tasks 移为扩展并调整生命周期；不支持宣称该候选机制已经正式生效。" }, { sourceId: "a2a-spec-1-0-0", supports: "支持 A2A Task 作为独立 Agent 协作对象，语义边界不同。" }] },
  ]),
  a2a: freezeQa([
    { q: "发现一张 Agent Card，是否就能相信远端 Agent 的身份和能力？", a: "不能。Agent Card 是发现入口，还需要验证来源、传输、组织身份、版本和实际能力。", depth: "在受控目录中绑定 Agent Card、服务端身份、所有者、支持任务、认证方式和停用状态。首次协作前做契约测试；动态发现不能绕过供应商准入、网络策略和业务授权。", ask: "追问客户：Agent Card 由谁发布和签名？远端能力变化后如何重新验证？", tag: "发现信任", basis: "能力发现 + 组织信任", evidence: [{ sourceId: "a2a-specification", supports: "支持通过 Agent Card 描述 Agent 能力与交互入口。" }, { sourceId: "nist-zero-trust", supports: "支持不因网络位置或声明而给予隐式信任。" }] },
    { q: "委托方能否把用户的全部权限直接传给远端 Agent？", a: "不应该。应传递与当前任务、资源和时限绑定的最小委托，而不是复制长期身份或全量权限。", depth: "区分最终用户、委托 Agent、执行 Agent 和业务资源服务器。远端只获得完成明确任务所需的受限能力，并保留授权链、同意、撤销和审计；跨组织时还要映射双方主体与租户。", ask: "追问客户：远端究竟代表用户还是委托服务？权限何时到期，谁能撤销？", tag: "跨域委托", basis: "身份链 + 最小授权", evidence: [{ sourceId: "a2a-specification", supports: "支持 A2A 通信需要明确认证与交互契约。" }, { sourceId: "nist-zero-trust", supports: "支持短时、资源级和持续验证的访问控制。" }] },
    { q: "A2A 返回了 Artifact，是否就可以直接进入下一个业务环节？", a: "不可以默认。Artifact 是交付物，需要验证类型、完整性、来源、版本和业务接受条件。", depth: "把消息、任务状态和 Artifact 分开处理。大文件或敏感制品使用受控存储引用与校验值；接收方在消费前做格式、安全和业务验证，并记录谁接受了哪个版本。技术完成不等于交付验收。", ask: "追问客户：产物由谁验证和接受？错误版本已经被下游使用时如何撤回？", tag: "产物验收", basis: "Artifact 完整性 + 业务接受", evidence: [{ sourceId: "a2a-concepts", supports: "支持 A2A 用 Task、Message 与 Artifact 表达协作结果。" }, { sourceId: "nist-genai-profile", supports: "支持对生成式 AI 输出进行验证并配置人类监督。" }] },
    { q: "服务宣称兼容 A2A 1.0，网络连通是否就足以验收？", a: "不够。要验证 A2A-Version、AgentInterface 中的 URL / Binding / 协议版本，以及各绑定的功能与错误映射是否等价。", depth: "客户端每次请求明确 Major.Minor，服务端对不支持版本返回显式错误；为 HTTP+JSON、JSON-RPC 或 gRPC 分别跑 Task、Message、Artifact、流式、认证与失败用例。Agent Card 签名只有在受信任密钥、传输链路与撤销状态被正确验证后才能支持来源和完整性判断；它仍不证明能力质量，也不授予调用权限。", ask: "追问客户：当前接口声明哪些协议版本与 Binding？升级时如何阻断静默降级并回归完整能力？", tag: "可靠性", basis: "版本协商 + 绑定契约", evidence: [{ sourceId: "a2a-spec-1-0-0", supports: "支持 A2A-Version、AgentInterface、多绑定和 Agent Card 签名的 1.0 契约。" }], addedAt: "2026-07-20" },
  ]),
  evaluation: freezeQa([
    { q: "能够用代码精确检查的规则，还需要交给 LLM-as-a-Judge 吗？", a: "通常不需要。Schema、引用存在、数值范围、权限和工具终态应优先使用确定性检查。", depth: "把评估拆成硬规则、语义质量和业务裁决。确定性检查便宜、可复现并适合零容忍约束；模型评审用于开放语义维度；人工负责校准、争议和高影响样本。三者的失败不能被一个平均分互相抵消。", ask: "追问客户：哪些条件可以直接计算？哪些必须判断语义，哪些需要业务负责人裁决？", tag: "评估分工", basis: "确定性检查 + 语义评审", evidence: [{ sourceId: "ragas", supports: "支持将生成质量拆成可分别测量的语义维度。" }, { sourceId: "nist-genai-profile", supports: "支持按风险选择合适的测量与人工监督方法。" }] },
    { q: "总体准确率很高，为什么仍可能不能上线？", a: "总体平均会掩盖高风险、少数语言、长输入和边界条件上的严重失败。", depth: "按任务价值、用户群、语言、长度、工具、风险和数据来源切片，分别设置最低门槛和零容忍错误。总体分数只用于概览；发布结论要展示最差关键切片、样本量和不确定性。", ask: "追问客户：哪类用户或任务失败一次就不可接受？当前评估是否能单独看到它？", tag: "切片评估", basis: "风险分层 + 分布覆盖", evidence: [{ sourceId: "nist-genai-profile", supports: "支持识别受影响群体、使用情境和差异化风险。" }, { sourceId: "opentelemetry-genai-semconv", supports: "支持按调用属性和版本分析线上表现。" }] },
    { q: "两位人工评审经常意见不同，应该听资深专家的吗？", a: "不能只靠职位决定。先校准评分量表、记录分歧类型，再由明确规则处理争议。", depth: "用共同样本进行盲评，计算一致性并审查分歧；把模糊维度拆成可观察标准，保留无法裁决的样本。专家仲裁需要写理由，之后回补量表，而不是把个人偏好直接当成永久真值。", ask: "追问客户：当前评分维度是否可观察？分歧是知识不足、规则含糊还是风险偏好不同？", tag: "人工校准", basis: "评审一致性 + 争议裁决", evidence: [{ sourceId: "nist-genai-profile", supports: "支持记录测量方法、限制和人类监督责任。" }] },
    { q: "做过安全红队，是否就等于完成了模型评估？", a: "不等于。红队主动寻找攻击路径，常规评估衡量已知任务表现；两者目标和抽样方式不同。", depth: "质量回归覆盖正常与困难任务，红队覆盖对抗输入、越权、泄露和滥用。红队发现要转成可重复的安全回归，但不能用通过固定攻击集证明没有未知风险；上线后仍需监测新路径。", ask: "追问客户：哪些风险通过常规样本看不到？红队发现由谁转成长期门禁？", tag: "红队边界", basis: "能力评估 + 对抗验证", evidence: [{ sourceId: "nist-genai-profile", supports: "支持将常规测量、对抗测试和持续风险管理结合。" }] },
    { q: "质量更高但延迟和成本翻倍，评估应该判它赢吗？", a: "不能只看质量。要用客户任务的最低质量、SLO、风险和单位成功成本共同判断。", depth: "先设不可补偿的硬门槛，再比较通过门槛后的效率。对高价值任务可接受更高成本，对批量低风险任务可能相反；报告 Pareto 前沿和业务权重，而不是把所有维度随意加成一个分数。", ask: "追问客户：哪些维度是硬门槛？通过后每多一点质量愿意付出多少时延和成本？", tag: "多目标评估", basis: "质量门槛 + 效率取舍", evidence: [{ sourceId: "nist-genai-profile", supports: "支持综合性能、风险和组织容忍度作出部署判断。" }, { sourceId: "opentelemetry-genai-semconv", supports: "支持关联质量版本与时延、用量等运行证据。" }] },
    { q: "重复运行几次分数不同，应该取最好一次还是平均值？", a: "都不能单独使用。应固定可控变量、重复采样，并报告分布、置信区间和严重失败。", depth: "生成具有随机性，模型评审也会波动。对同一版本在一致条件下运行足够次数，区分平均变化与尾部风险；小样本差异不应被包装成确定提升。零容忍错误即使只出现一次也需单独处理。", ask: "追问客户：当前差异是否超过自然波动？样本量和重复次数足以支持发布结论吗？", tag: "统计可信度", basis: "重复测量 + 不确定性", evidence: [{ sourceId: "nist-genai-profile", supports: "支持记录测量有效性、不确定性和限制。" }] },
  ]),
  "ai-gateway": freezeQa([
    { q: "AI 网关缓存命中，是否可以把结果直接返回给另一个用户？", a: "不能默认。缓存键必须包含会改变输出或权限的租户、主体、模型、Prompt、工具和数据版本。", depth: "区分公共静态内容、租户内容和用户权限内容。缓存前判断是否允许复用，命中后仍执行当前授权；策略变更、撤权和模型升级需要明确失效。无法证明隔离时，高风险响应不缓存。", ask: "追问客户：缓存键里有哪些身份和版本维度？撤权后多久能保证旧结果不可再命中？", tag: "缓存隔离", basis: "缓存语义 + 当前授权", evidence: [{ sourceId: "nist-zero-trust", supports: "支持每次访问按当前主体和资源授权，缓存不能绕过。" }, { sourceId: "cloudflare-ai-gateway", supports: "支持 AI 网关可提供缓存能力，具体隔离语义仍需应用设计。" }] },
    { q: "所有模型流量都经过网关，是否就能准确核算每个业务的成本？", a: "还不够。需要稳定关联租户、任务、模型、重试、缓存、工具和最终业务结果。", depth: "提供方账单通常只看到端点用量。网关应记录每个逻辑请求和实际 attempt，区分用户请求、自动重试、影子流量与评估调用，再按成功任务而非单纯 Token 分摊。无法映射业务结果时只能叫调用成本。", ask: "追问客户：一次业务任务会产生多少模型和工具调用？重试与影子流量算给谁？", tag: "成本归因", basis: "逻辑请求 + 实际调用", evidence: [{ sourceId: "opentelemetry-genai-semconv", supports: "支持记录模型、用量和调用关联属性。" }, { sourceId: "cloudflare-ai-gateway", supports: "支持在统一入口观察多模型请求与用量。" }] },
    { q: "应用可以保留一条直连模型的备用通道吗？", a: "可以设计紧急通道，但不能成为绕过治理的常态后门。", depth: "备用通道要有与主路径等价的身份、数据、审计和安全控制，并限定启用条件、负责人和自动失效时间。故障演练必须证明切换不会绕过地域、内容策略或敏感日志要求。", ask: "追问客户：谁能启用直连？启用后哪些控制仍然存在，怎样确认并关闭？", tag: "旁路治理", basis: "故障连续性 + 控制等价", evidence: [{ sourceId: "nist-zero-trust", supports: "支持所有访问路径持续执行策略，不能因网络路径变化获得隐式信任。" }, { sourceId: "nist-genai-profile", supports: "支持为依赖故障设计受控响应与恢复。" }] },
    { q: "流式响应已经向用户输出一部分后，网关还能无损切换模型吗？", a: "通常不能无损。已输出内容、上下文状态和计费都使中途切换具有语义风险。", depth: "在首字前可按策略重试或回退；开始流式后要明确是终止、续写还是重新开始，并向应用返回可识别状态。高风险输出可先缓冲验证再展示，但会牺牲首字延迟。", ask: "追问客户：流式中断时用户看到什么？允许续写、重新生成还是必须明确失败？", tag: "流式故障", basis: "输出承诺 + 恢复语义", evidence: [{ sourceId: "opentelemetry-genai-semconv", supports: "支持区分生成调用阶段与响应属性，为流式故障归因提供基础。" }, { sourceId: "nist-genai-profile", supports: "支持为系统失效建立透明的响应与恢复机制。" }] },
  ]),
  "ai-ops": freezeQa([
    { q: "只采样 1% 的 Trace，能否覆盖 AI 系统的关键故障？", a: "不能只靠固定随机采样。低频高风险错误、长尾延迟和特定租户问题很容易被漏掉。", depth: "保留基础聚合指标，再使用错误、风险、版本、租户和尾延迟驱动的优先采样。对高风险工具动作和发布灰度提高保留率；采样规则本身也要版本化并监测偏差。", ask: "追问客户：哪些事件即使万分之一也必须保留？当前采样能否回看它的完整调用链？", tag: "采样策略", basis: "风险采样 + 可归因性", evidence: [{ sourceId: "opentelemetry-genai-semconv", supports: "支持用标准属性关联生成式 AI 与 Agent 调用链。" }, { sourceId: "nist-genai-profile", supports: "支持按风险程度持续监测生成式 AI。" }] },
    { q: "用户点赞和点踩可以直接当作模型质量标签吗？", a: "不可以直接使用。反馈受界面、预期、任务难度和用户动机影响，需要结合任务结果与抽样复核。", depth: "记录反馈对象、版本、任务和上下文，区分回答风格、事实错误、未解决和安全问题。高频反馈先去重与裁决，再进入评估集；避免优化点赞率却损害真实解决率。", ask: "追问客户：反馈对应哪个业务结果？谁负责把主观意见转成可复用的失败标签？", tag: "反馈治理", basis: "用户信号 + 业务真值", evidence: [{ sourceId: "nist-genai-profile", supports: "支持收集受影响主体反馈，同时记录其限制并纳入治理。" }, { sourceId: "opentelemetry-genai-semconv", supports: "支持把反馈与具体调用和版本关联。" }] },
    { q: "AI 成本突然上升，为什么不能只看 Token 单价和调用次数？", a: "因为成本可能由重试、工具环路、上下文膨胀、路由变化、缓存失效或失败任务增加造成。", depth: "从业务任务追到每次模型与工具 attempt，拆分输入、输出、缓存、错误和最终状态。单位 Token 变便宜但一次任务调用更多，整体仍可能变贵；应监测单位成功任务成本和无效调用比例。", ask: "追问客户：成本增长来自更多业务、更多 attempt，还是每次请求更长？最终成功率是否同步变化？", tag: "成本异常", basis: "调用链 + 单位成功成本", evidence: [{ sourceId: "opentelemetry-genai-semconv", supports: "支持记录模型用量、调用和 Agent 工具属性。" }] },
    { q: "可以把生产请求原样重放来复现 Agent 事故吗？", a: "不能默认。重放可能再次发送通知、下单或修改权限，必须隔离副作用。", depth: "建立只读回放、模拟工具和影子环境，固定模型、Prompt、数据与策略版本。需要真实写入验证时使用专用租户、幂等键和审批；对无法安全重放的步骤保留足够证据做离线判定。", ask: "追问客户：哪些工具重放会产生真实副作用？是否有模拟端点和隔离数据？", tag: "安全回放", basis: "可复现性 + 副作用隔离", evidence: [{ sourceId: "nist-genai-profile", supports: "支持在测试与事件响应中控制生成式 AI 风险和影响。" }, { sourceId: "nist-zero-trust", supports: "支持测试访问仍执行最小权限与隔离。" }] },
    { q: "告警阈值应该固定，还是让 AI 自动学习动态基线？", a: "两者结合。硬风险和业务 SLO 使用明确阈值，流量与分布波动可用动态基线辅助发现异常。", depth: "动态基线可能把缓慢退化学成正常，也可能因活动流量产生噪声。为质量、安全、成本和延迟分别定义告警目标、抑制、聚合和人工责任，版本变更期间使用更严格的发布观察窗。", ask: "追问客户：哪些指标越线必须立即止损？哪些异常需要结合历史与业务事件判断？", tag: "告警设计", basis: "硬门槛 + 动态异常", evidence: [{ sourceId: "opentelemetry-genai-semconv", supports: "支持标准化采集生成式 AI 运行指标和属性。" }, { sourceId: "nist-genai-profile", supports: "支持持续监测并依据风险容忍度采取行动。" }] },
  ]),
  "llm-training": freezeQa([
    { q: "训练数据去重后规模变小，是否会让模型能力下降？", a: "不一定。去除近重复可以减少记忆和评估污染，但过度去重也可能删掉有价值的语言与领域变化。", depth: "按文档、段落和语义层分别测量重复，保留来源、许可和删除血缘。用未见任务、记忆风险和训练效率比较阈值，而不是只追求更大的 Token 数或更高去重率。", ask: "追问客户：重复来自模板、转载还是必要的领域频率？评估集与训练集如何检测污染？", tag: "数据去重", basis: "数据质量 + 记忆风险", evidence: [{ sourceId: "nist-genai-profile", supports: "支持管理训练数据来源、质量、隐私和评估污染风险。" }, { sourceId: "chinchilla-2022", supports: "支持模型规模与有效训练数据需要共同考虑。" }] },
    { q: "更换 Tokenizer 后继续使用原有权重和数据流程，可以只做兼容适配吗？", a: "通常不能视为简单适配。词表和切分变化会影响输入表示、Embedding、序列长度和训练分布。", depth: "明确哪些权重可以复用、特殊 Token 如何映射、聊天模板和数据编码如何迁移。先用小规模恢复训练与关键任务评估检查稳定性，再决定全量继续预训练；旧模型指标不能直接与新 Tokenizer 版本混比。", ask: "追问客户：为什么要换 Tokenizer？特殊 Token、词表和下游模板由谁共同版本化？", tag: "Tokenizer 变更", basis: "输入契约 + 权重兼容", evidence: [{ sourceId: "transformer-2017", supports: "支持 Token 表示进入 Transformer 计算，输入编码是模型契约的一部分。" }, { sourceId: "nist-genai-profile", supports: "支持模型组件变更后重新测量性能与风险。" }] },
    { q: "数据并行、张量并行和流水线并行，应该优先选哪一种？", a: "没有固定顺序。选择取决于模型是否单卡可放、计算通信比、网络拓扑、批量和容错目标。", depth: "先做单卡与单节点剖析，再逐级增加并行维度。数据并行复制模型，张量并行切分算子，流水线并行切分层；组合越复杂，通信、气泡、调试和恢复成本越高。用有效训练进度而非理论设备数验收。", ask: "追问客户：当前瓶颈是显存、计算还是通信？增加哪种并行后 Goodput 实际提升多少？", tag: "并行策略", basis: "模型放置 + 拓扑效率", evidence: [{ sourceId: "flashattention-2022", supports: "支持算子内存访问会显著影响训练效率，不能只看理论计算。" }, { sourceId: "opentelemetry-semconv", supports: "支持以分布式遥测关联运行阶段；训练框架指标需共同使用。" }] },
    { q: "Checkpoint 越频繁，训练就越可靠吗？", a: "不一定。频繁保存会占用 I/O 和训练时间，过少则扩大故障损失；关键是按故障率和恢复成本选择间隔。", depth: "计算保存耗时、平均故障间隔、可接受丢失进度和存储压力，并验证 Checkpoint 是否完整、可加载、可恢复。异步保存要处理一致性，旧版本要有保留和校验策略。", ask: "追问客户：一次故障最多允许丢多少有效训练时间？最近一次恢复演练是否证明制品可用？", tag: "检查点策略", basis: "恢复点目标 + I/O 成本", evidence: [{ sourceId: "opentelemetry-semconv", supports: "支持记录分布式运行事件，为故障与恢复分析提供基础。" }, { sourceId: "nist-genai-profile", supports: "支持为模型生命周期准备恢复与验证机制。" }] },
  ]),
  "llm-inference": freezeQa([
    { q: "连续批处理提高吞吐后，为什么有些用户反而更慢？", a: "调度器可能让长请求占用更多解码步，短请求在队列中等待；吞吐提升不保证每类请求的尾延迟。", depth: "按输入长度、输出长度、优先级和租户观察排队、TTFT 与 TPOT，设置准入、批量上限和公平策略。不能只用总 Token/s 验收；高优先级和交互请求要有独立 SLO。", ask: "追问客户：请求长度分布和优先级怎样？吞吐优化后哪类用户的 P95 变差？", tag: "调度公平", basis: "连续批处理 + 尾延迟", evidence: [{ sourceId: "vllm-2023", supports: "支持连续批处理和 KV Cache 管理提高吞吐，但具体调度仍影响请求延迟。" }, { sourceId: "opentelemetry-genai-semconv", supports: "支持按请求属性观测生成调用延迟。" }] },
    { q: "投机解码能提高速度，是否应该对所有模型和请求默认开启？", a: "不应该。收益取决于草稿模型命中率、序列长度、硬件和额外计算，某些负载可能更慢。", depth: "在相同质量和采样设置下比较 TTFT、TPOT、吞吐、显存和能耗，并按短答、长生成和高并发切片。草稿模型与主模型的版本组合也要一起发布和回滚。", ask: "追问客户：目标是降低单请求延迟还是提高集群吞吐？草稿接受率在真实任务上是多少？", tag: "投机解码", basis: "接受率 + 额外计算", evidence: [{ sourceId: "vllm-2023", supports: "支持推理优化收益依赖具体模型、调度和内存条件。" }, { sourceId: "nist-genai-profile", supports: "支持系统优化变更后复测性能与风险。" }] },
    { q: "推理服务自动扩容，为什么高峰时仍会大量超时？", a: "模型加载和缓存预热可能比流量增长慢，指标也可能只在队列已经拥塞后才触发。", depth: "把容量分成已就绪、正在加载和不可用，使用队列深度、到达率、服务时间和模型加载时间预测扩容。为关键模型保留热容量，定义排队、拒绝与降级，而不是无限积压。", ask: "追问客户：从零到可服务需要多久？高峰前能否预热，超过容量后优先保护哪些任务？", tag: "弹性冷启动", basis: "扩容时延 + 准入控制", evidence: [{ sourceId: "kubernetes-dra", supports: "支持设备资源需要被声明和调度，资源分配不等于模型已经可服务。" }, { sourceId: "opentelemetry-semconv", supports: "支持关联队列、调用和服务阶段的遥测。" }] },
    { q: "多租户共享推理实例时，KV Cache 会不会泄露其他客户内容？", a: "实现正确不应泄露，但不能只靠假设。需要验证缓存寻址、复用条件、清理和故障路径的隔离。", depth: "区分前缀缓存、会话缓存和普通 KV Cache，缓存键包含租户与完整上下文身份。跨租户复用只允许明确公共内容；日志、调试接口和内存转储也要受控。通过隔离测试和升级回归证明边界。", ask: "追问客户：哪些缓存会跨请求复用？租户身份在哪里进入缓存键与清理策略？", tag: "缓存安全", basis: "缓存隔离 + 多租户验证", evidence: [{ sourceId: "vllm-2023", supports: "支持 KV Cache 的块级管理会影响共享推理内存。" }, { sourceId: "nist-zero-trust", supports: "支持多租户资源访问必须持续执行隔离与授权。" }] },
    { q: "只看平均响应时间，为什么很难判断推理服务哪里慢？", a: "平均值混合了排队、首字和逐 Token 生成，也掩盖长请求与尾部拥塞。", depth: "至少分开记录队列时间、TTFT、TPOT、输出长度、拒绝率和完成状态，并按模型、版本、租户与长度切片。首字慢可能是排队或 Prefill，输出慢可能是解码或调度，优化方法不同。", ask: "追问客户：慢发生在首字前还是生成中？哪些长度和并发下 P95 首先失守？", tag: "性能归因", basis: "阶段指标 + 负载切片", evidence: [{ sourceId: "opentelemetry-genai-semconv", supports: "支持用生成式 AI 调用属性关联模型、用量和时延。" }, { sourceId: "vllm-2023", supports: "支持 Prefill、Decode、批处理与缓存共同决定推理表现。" }] },
    { q: "服务支持最大上下文后，是否应该允许所有请求都使用这个上限？", a: "不应该默认开放。单个超长请求会占用大量 KV Cache 和计算，可能挤压其他用户。", depth: "按租户和任务设输入、输出、并发与总 Token 预算，使用长度感知的准入和队列。需要超长上下文的任务单独验证质量、成本与时限；超过预算时采用检索、摘要、异步或拒绝。", ask: "追问客户：最大窗口是偶发需求还是常态？一个超长请求允许影响多少并发容量？", tag: "准入控制", basis: "上下文预算 + 共享容量", evidence: [{ sourceId: "vllm-2023", supports: "支持 KV Cache 占用与序列长度和并发相关。" }, { sourceId: "nist-genai-profile", supports: "支持依据使用情境与系统限制配置风险控制。" }] },
  ]),
  "data-engineering": freezeQa([
    { q: "全量同步一次数据源后，后续只做定时重建索引可以吗？", a: "小规模可行，但生产需要明确增量、删除、乱序、失败重放和一致性窗口。", depth: "为源记录保留稳定 ID、版本和变更游标，区分创建、更新、删除与撤权。增量管线要可重试、可对账，周期全量校验用于发现漏事件；索引完成时间不等于所有派生层已一致。", ask: "追问客户：源系统提供快照还是变更日志？漏一条删除事件多久能被发现？", tag: "增量同步", basis: "变更捕获 + 一致性对账", evidence: [{ sourceId: "opentelemetry-semconv", supports: "支持用跨阶段遥测关联数据处理与下游调用。" }, { sourceId: "nist-genai-profile", supports: "支持管理生成式 AI 数据来源、更新和生命周期。" }] },
    { q: "更换切块或 Embedding 策略时，可以直接覆盖现有索引吗？", a: "不建议。应并行构建新版本、回归验证并原子切换，保留回滚与文档血缘。", depth: "索引版本绑定解析器、切块、Embedding、元数据和权限策略。双写或回放期间比较召回、答案、时延与删除传播；切换后继续监测旧缓存和在途请求，确认后再下线旧版本。", ask: "追问客户：当前能否把一个 Chunk 追到源文档和加工版本？切换失败如何回退？", tag: "索引发布", basis: "数据版本 + 无停机迁移", evidence: [{ sourceId: "hnsw-2016", supports: "支持向量索引是独立近似搜索结构，策略变化需要重新验证召回。" }, { sourceId: "opentelemetry-semconv", supports: "支持把版本与下游调用结果关联。" }] },
    { q: "数据管线记录了行数和成功率，为什么仍不能证明 AI 数据质量？", a: "因为技术成功不代表内容完整、结构正确、权限有效或适合下游任务。", depth: "把质量拆成覆盖、解析、结构、时效、权限、重复、标签和下游效果。每项绑定样本与责任人，并通过困难文档、检索和评估任务验证；不要用单一‘处理成功率’替代业务可用性。", ask: "追问客户：哪些数据错误会导致严重错答或越权？当前指标能在进入模型前发现吗？", tag: "质量契约", basis: "数据指标 + 下游验证", evidence: [{ sourceId: "docling-report", supports: "支持文档处理需要独立验证结构与版面恢复。" }, { sourceId: "nist-genai-profile", supports: "支持将数据质量与具体生成式 AI 风险联系。" }] },
    { q: "多个系统保存同一份政策文档，RAG 应该全部纳入以提高召回吗？", a: "不应该无条件全部纳入。重复和冲突版本会放大旧证据，需要确定权威来源与生效规则。", depth: "建立规范文档身份、来源优先级、版本、生效期和冲突状态。保留必要副本血缘但只让当前权威版本参与默认检索；法规或审计需要历史时，显式按时间查询。", ask: "追问客户：谁是权威系统？同名内容冲突时按来源、版本还是生效日期裁决？", tag: "主数据治理", basis: "权威来源 + 版本冲突", evidence: [{ sourceId: "nist-genai-profile", supports: "支持记录数据来源、限制和生命周期。" }, { sourceId: "nist-zero-trust", supports: "支持资源访问与当前权威策略绑定。" }] },
    { q: "先把原文做 Embedding，再在回答阶段脱敏，是否足够保护敏感信息？", a: "不够。敏感内容已经进入向量、索引、缓存和日志，后置脱敏无法消除这些派生暴露。", depth: "在摄取前按用途做分类、最小化和必要脱敏，并保留可授权的文档身份。查询时继续执行当前权限，输出阶段再做二次检查；对必须保留原文的场景使用受控隔离和审计。", ask: "追问客户：哪些字段根本不应进入索引？哪些必须保留但只能被特定主体检索？", tag: "敏感数据", basis: "摄取前控制 + 查询时授权", evidence: [{ sourceId: "nist-genai-profile", supports: "支持在生成式 AI 数据生命周期中管理隐私与敏感信息。" }, { sourceId: "nist-zero-trust", supports: "支持按主体、资源与用途执行最小权限。" }] },
  ]),
  "ai-infra-platform": freezeQa([
    { q: "GPU 队列按先来先服务，是否就是最公平的调度？", a: "不一定。长作业可能阻塞短任务，部门突发提交也可能耗尽共享资源；公平需要结合配额、优先级和业务承诺。", depth: "为在线、训练、开发和紧急任务定义不同队列与抢占规则，观察排队原因、等待分布和完成价值。资源公平不等于每人相同卡时，而是透明地兑现已约定的服务级别。", ask: "追问客户：哪些任务有截止时间？谁可以抢占，成本和丢失进度由谁承担？", tag: "队列公平", basis: "配额治理 + 业务优先级", evidence: [{ sourceId: "kubernetes-dra", supports: "支持设备资源按声明和调度条件分配。" }, { sourceId: "opentelemetry-semconv", supports: "支持用核心 Trace 关联资源等待与工作负载执行；业务结果仍需项目字段补充。" }] },
    { q: "调度器分配到同型号 GPU，为什么分布式任务表现仍可能差很多？", a: "因为拓扑、链路、NUMA、设备健康和同机干扰不同，同型号不等于等价放置。", depth: "把设备型号、互联、节点拓扑、健康、驱动和占用状态纳入调度。对多卡任务验证节点内与跨节点带宽、降速设备和故障域；异常节点自动隔离并保留诊断证据。", ask: "追问客户：当前调度是否理解 NVLink、网络和故障域？性能差时能否追到具体放置？", tag: "拓扑调度", basis: "设备属性 + 拓扑感知", evidence: [{ sourceId: "kubernetes-dra", supports: "支持用设备属性和声明参与资源分配。" }, { sourceId: "nvidia-gpu-operator", supports: "支持 GPU 驱动、设备和监控组件的集群化管理。" }] },
    { q: "AI 平台已经做了租户配额，是否就完成了隔离？", a: "没有。还需要身份、网络、存储、镜像、模型制品、日志和设备接口的隔离。", depth: "配额只限制数量，不能阻止跨租户数据读取、恶意镜像、调试接口或共享缓存泄露。建立命名空间与工作负载身份、网络策略、制品签名、密钥边界和审计，并验证故障与运维权限。", ask: "追问客户：租户能看到哪些共享存储、日志和设备指标？平台管理员的高权限如何审计？", tag: "平台隔离", basis: "资源配额 + 安全边界", evidence: [{ sourceId: "nist-zero-trust", supports: "支持对身份、工作负载和资源访问持续执行最小权限。" }, { sourceId: "nvidia-gpu-operator", supports: "支持 GPU 运行组件的统一管理，但租户安全边界仍需平台设计。" }] },
  ]),
  "ai-infra-compute": freezeQa([
    { q: "训练集群一定需要最高规格的节点间网络吗？", a: "不一定。只有目标并行策略和规模受到通信限制时，高速网络投资才会转化为有效训练进度。", depth: "先用单机、多机剖析 AllReduce、All-to-All、数据加载和计算重叠，再比较不同网络下的扩展效率。小模型、单机推理或数据受限任务可能无法利用昂贵互联。", ask: "追问客户：目标模型在多少卡后通信开始主导？每提升一点 Goodput 的网络成本是多少？", tag: "网络投资", basis: "通信占比 + 扩展效率", evidence: [{ sourceId: "opentelemetry-semconv", supports: "支持分布式遥测关联计算与等待阶段。" }, { sourceId: "chinchilla-2022", supports: "支持计算投入需与模型和数据规模共同考虑。" }] },
    { q: "GPU 计算利用率低，是否说明应该更换更强的加速器？", a: "不一定。瓶颈可能在数据读取、CPU 预处理、存储、网络或 Checkpoint，更强 GPU 只会更快等待。", depth: "沿数据路径测量对象存储、文件系统、CPU 解码、主机内存、PCIe、HBM 和算子时间。先改善最窄层，再用端到端任务长跑验证；单一 GPU 指标不能确定采购方向。", ask: "追问客户：设备空闲时在等什么？数据准备和 Checkpoint 分别占用多少墙钟时间？", tag: "数据供给", basis: "端到端管线 + 瓶颈定位", evidence: [{ sourceId: "flashattention-2022", supports: "支持内存访问而非理论算力可成为关键性能约束。" }, { sourceId: "opentelemetry-semconv", supports: "支持跨基础设施阶段建立可关联遥测。" }] },
    { q: "机房有足够机架空间，是否就具备部署高密度 AI 集群的条件？", a: "不代表。还要验证供电、散热、网络、消防、维护窗口和故障容量。", depth: "按持续功耗而非铭牌峰值规划机柜与供电，检查液冷或风冷能力、冗余、热密度和扩容周期。把降频、停机和能源成本纳入每个达标训练或推理结果的 TCO。", ask: "追问客户：单柜可持续供电和散热是多少？故障或高温时允许损失多少容量？", tag: "能源与散热", basis: "设施约束 + 持续性能", evidence: [{ sourceId: "nvidia-gpu-operator", supports: "支持监控 GPU 设备与运行状态；设施容量仍需独立工程验证。" }, { sourceId: "nist-genai-profile", supports: "支持识别部署环境与依赖带来的系统风险。" }] },
    { q: "使用多种 GPU 或 AI 加速器，是否能自动降低供应风险？", a: "只有软件、模型格式、算子、精度和运维真正可迁移时才会降低；否则可能增加碎片与维护成本。", depth: "选择代表性模型验证训练、推理、量化、监控和故障处理，记录每种设备的缺失算子、性能与人员能力。建立可移植层不等于最低公分母，关键任务仍可能绑定特定内核。", ask: "追问客户：需要迁移的是全部负载还是少数关键任务？目前哪一层最依赖特定硬件？", tag: "异构可移植", basis: "供应连续性 + 软件适配", evidence: [{ sourceId: "flashattention-2022", supports: "支持高性能算子依赖具体内存层次和实现。" }, { sourceId: "nist-genai-profile", supports: "支持识别第三方与供应链依赖并准备替代方案。" }] },
  ]),
});
