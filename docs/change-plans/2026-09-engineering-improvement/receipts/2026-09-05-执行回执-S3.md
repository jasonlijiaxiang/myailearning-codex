# 阶段 3 回执（执行者：DeepSeek Harness，2026-09-05）

分支：`codex/improve-s3`（基于 codex/improve-s2 tip 线性堆叠）。阶段 2 完成后快照基线 H0′′′ = `50fd5f20303aca7dee9b3738351d2c0c4ec0bb676f092570b0bf73e7eda6d7e2`。

## 回执 S3-T1 + S3-T7（wave 1，提交 14f58cc）
- S3-T1（CSS 移出 layout）：方案 C（client 组件作用域）——app/home-styles.tsx 承载 home-refresh.css（仅两个首页渲染）、model-radar.css 进 model-radar-explorer.tsx；两个 layout 删 import。结果：全局 index.css 439,712→401,894 B（移除 330 条规则，模块页不再携带这两份 CSS）；首页/雷达页级联顺序与改前等价（证据：/tmp/s3t1-* 系列文件，选择器集合 diff 为空）。**偏差**：/modules/ai-gateway 的 stylesheet link 计数 3→3——本打包器把所有 RSC CSS 合并进全局 index 且每页链接，-2 在结构上不可达；实质目标（模块页不携带 CSS 字节）已达成。
- S3-T7（kb-tool 拆分）：kb-tool.mjs 2,402→380 行 + lib/ 11 个模块（全部 ≤600 行）；两个 JSON Schema 校验器合并为 lib/json-schema.mjs（kb/lite 双 dialect），scripts/lib/json-schema-lite.mjs 变 4 行 re-export。等价证据：CLI 11 个调用对拍逐字一致、校验器 13/13 与 48/48 差分一致、test:portable 80/80。
- 门禁：npm run check 通过（FINAL-EXIT=0）。

## 回执 S3-T3（提交 13d1641）
- 新建 app/site-chrome.tsx（SiteNav/SiteFooter），替换 15 个页面手写 topbar/footer；链接文本与目标逐字保留；标题跳级 h4→h3 修复 20 处 + 对应 CSS 选择器。
- 验收：`grep -rn 'className="topbar"' app | wc -l` = 1（仅 site-chrome.tsx）；渲染 HTML 无 h2→h4 跳级（新增断言）；快照不变；npm run check 通过。
- 偏差：任务项 3「统一跳转目标为 <main id="main-content">」——渲染测试要求 main 带 fieldbookTheme class、跳转目标在 </nav> 后、topbar CSS 要求 nav 在主题元素内，三者互斥；保留三条更强不变量（15 页沿用 nav 后的唯一 <div id="main-content" class="skipTarget">），unified 壳本就是 <main id="main-content">。

## 回执 S3-T4（部分，随 S2-T3 已达成）
- englishUnifiedReaderConfigs 已在 S2-T3 从 manifest 派生（english-pilot-module-page.tsx 仅剩派生与渲染）。
- 偏差：文件 715 行 > 500 行验收——剩余为 legacy 渲染分支（14 个无统一 reader 配置的模块仍在用，见 S1-T4 阻塞说明），不能删。

## 回执 S3-T5 + S3-T8（wave 2，提交 7b9426b）
- S3-T5（阅读模式按需渲染）：服务端只渲染默认视图，其余切换时挂载（mountedModes 同 commit 挂载保深链）；英文页面板客户端化（english-reading-panels.tsx 按 slug 动态 import，每模块独立懒 chunk）+ 叶子模块 english-section-grouping/step-visualization。结果：/en/modules/rag 529,327→127,801 B；模块正文 <12px 字号全部提级（装饰性保留并逐条注明）；触控目标全 ≥32px（清单在报告）。bundle:budget 总量基线同步更新。
- S3-T8（questions 分页）：条目改为服务端拼装静态 HTML（dangerouslySetInnerHTML）+ 每模块 <details> 默认收起 + question-directory.zh.json 渐进增强（展开时注入深答）。结果：HTML 2,313,264→387,322 B；条目 DOM 14,226→1,867；data-question-key=355 且无 JS 可到达全部短答（新可达性断言替代旧服务端呈现断言）。
- 门禁：npm run check 通过（FINAL-EXIT=0）。

## 回执 S3-T2 / T6（进行中，完成后补）
