# 阶段 1 回执（执行者：DeepSeek Harness，2026-09-05）

分支：`codex/improve-s1`（基于 codex/improve-s0 tip 线性堆叠）。阶段 0 的账本协议（执行手册 0.5）在 S1-T2 后失效。
快照基线：H0 = `170d2c5e…`（S0-T6 前）；H0' = `09e15b47812e1a0a786fd27cfb1328519910499238c5291f7f664bbd4445ab19`（S0-T6 verifiedAt 写回后）。

## 回执 S1-T1
- 分支 / 提交：codex/improve-s1 / `fdc7585`
- 改动文件：app/preflight.css（新，复制 node_modules/tailwindcss/preflight.css，保留许可证头）、app/globals.css（@import 换源）、删除 postcss.config.mjs + public/{file,globe,window}.svg + 空目录 app/knowledge-graph/explore/、package.json（name → cloud-ai-presales-fieldbook，去 tailwindcss/@tailwindcss/postcss）、eslint.config.mjs（去掉 build/** 忽略）、kb.config.json（去 postcss.config.mjs）、package-lock.json
- 内容快照哈希：H0' / H0' / 相同
- 删除前 grep：`file.svg|globe.svg|window.svg` 在 app/tests/public 零引用；`tailwind` 在 .tsx 零工具类（`grep -rln 'className="[^"]*\b(flex|grid|hidden|absolute|relative|sticky)\b' app --include=*.tsx` → 0）
- 验收项：
  - [x] `npm ls tailwindcss @tailwindcss/postcss` → empty
  - [x] CSS 选择器差集：构建后集合 4241 → 4151，新增 0；删除的 90 个选择器全部为 Tailwind 工具类/.theme/@layer/@property（`.absolute`、`.flex`、`.grid`、`--tw-*` 等），没有任何站点自有 `.` 选择器丢失
  - [x] `npm run check` 通过（FINAL-EXIT=0）
- 门禁：npm run check 通过
- 与手册不符之处：无
- 发现：.agents/kb-tool.mjs 的 ALLOWED_PORTABLE_INCLUDES 仍含 postcss.config.mjs（白名单，无害）；scripts/release-check.mjs 的 drizzle 拷贝分支仍存在（S0-T2 后为 no-op）——两处均不在本任务范围，留待后续

## 回执 S1-T2（最大任务，另含 S0-T5/T6 延后件）
- 分支 / 提交：codex/improve-s1 / `c735b0a`（账本退役）+ `824f8a8`+`03651f7`（归档）+ `62335cb`（fold-ins）
- 改动文件：新建 knowledge/localization/status.json（23 模块：4 aligned / 19 deferred）、tests/localization-status.test.mjs、scripts/localization-diff.mjs；重写 scripts/audit-english-modules.mjs（去掉子进程调用，deferred 读 status.json）、tests/bilingual-pilot.test.mjs（删旧账本依赖；两道 parity 测试改为对全部 23 模块跑断言，aligned 失败即失败、deferred 收集计数逐模块打印）；删除 scripts/audit-localization-deferments.mjs、scripts/lib/localization-contract.mjs、tests/localization-deferment.test.mjs、knowledge/localization-deferments.json（2.5 MB）、knowledge/schemas/{localization-deferment,bilingual-review}.schema.json、knowledge/claims/bilingual-reviews/（188 份）、scripts/generate-bilingual-review-records.mjs、scripts/{stage0-validate.mjs,lib/stage0-contract.mjs}、tests/stage0-validator.test.mjs；package.json（去 audit:english:reviews/audit:localization*/stage0:validate，test:bilingual 改新链，加 localization:diff）；kb.config.json（targetedValidationCommands 去 audit:english:reviews）；归档 docs/change-plans/2026-08-* → docs/archive/change-plans/ 与五份历史文档 → docs/archive/；docs/archive/README.md；.gitignore（extracted 路径）；文档句改（ENGLISH-EDITORIAL-STANDARD、MODULE-QUALITY-GATES 47/49 行、AGENTS.md）；顺带落地 S0-T5 的 6 个模块路由 canonical/hreflang（账本退役后无冲突）与 S0-T6 的 8 条 verifiedAt 写回
- 内容快照哈希：改前 H0' / 改后 H0' / 相同（fold-ins 后为 H0'，与 S0-T6 回执一致）
- 删除前 grep：localization-deferments|bilingual-reviews|candidate-matrix|stage0 在 .agents/、kb-tool.mjs、module-polish.mjs 零引用；剩余命中仅为执行手册自身的引用文字
- 验收项：
  - [x] `git ls-files knowledge | wc -l` → 14（≤20，原 203）；`git ls-files docs | grep -v archive | wc -l` → 8（≤12）
  - [x] `time npm run test:bilingual` → 1.8 秒（≤60 秒；原 567 秒），32/32 通过
  - [x] `npm run localization:diff -- ai-ops` 有输出（11 个命中文件，无差异）
  - [x] `npm run check` 通过（FINAL-EXIT=0）
- 门禁：npm run check 通过
- 与手册不符之处（需检查者注意）：
  1. 手册预期「19 个 deferred 模块的『N 处不对齐』行」——实测 19 个 deferred 模块的问答数、证据 sourceId 关系与 addedAt 结构对齐检查全部通过（用注册表数据交叉验证：19/19 结构一致），backlog 为空。即延期的是英文撰写/呈现，而不是证据关系结构；这比手册预期的结果更强，但输出里没有「N 处不对齐」行。
  2. 手册步骤 d 未列出的第 15 条测试「bilingual review contract blocks inconsistent release verdicts」依赖被删除的 bilingual-review.schema.json（四阶段审校契约随记录体系整体退役），整条删除。
  3. docs 归档时 git mv 对目录重命名在本机失败（No such file or directory，疑似 macOS 大小写/索引问题），改用 `mv` + `git add -A`，最终树等价（git 识别为重命名）。
- 发现：knowledge/claims/index.json 的 claims 结构未受影响；json-schema-lite.mjs 暂时无引用者（S3-T7 计划合并进 kb-tool），按手册未提前删除

## 回执 S1-T3
- 分支 / 提交：codex/improve-s1 / `f1fdd19`
- 改动文件：package.json（check:fast/test:unit/test:render/test:bilingual/test:portable/check/test 新结构）、AGENTS.md、README.md、docs/REPOSITORY-WORKFLOW.md（check:fast 引导句）
- 内容快照哈希：H0' / H0' / 相同
- 验收项：
  - [x] `time npm run check:fast` → 6.75 秒（≤60 秒）
  - [x] `time npm run check` → 约 5 分 40 秒（≤8 分钟；bilingual 已从 9.5 分钟降到 1.8 秒）
  - [x] `npm run test:portable` 首次完整运行 80/80 通过（首跑因「tracked changes 未 staged」失败，提交后重跑通过）
- 门禁：npm run check 通过
- 与手册不符之处：test:portable 首跑需要先提交（portable 打包器要求所有 tracked 改动已 staged），提交顺序做了调整并在本回执说明
- 发现：无

## 回执 S1-T4（部分完成 + 阻塞说明）
- 分支 / 提交：codex/improve-s1 / `64bcaf9`（+ 修复 `S1-T4-fix`）
- 改动文件：删除/降级 11 处候选死代码（详见 diff：module-publication 的 isPublishedModule、dedicatedModuleSlugs 去 export；knowledge-map resolveModuleSlug 去 export；module-qa-expansion requireModuleQaExpansion；module-brief-content moduleBriefSlugs；question-field-kit defaultIntentForTag/questionIntentOverrides 去 export；model-radar-data modelRadarMetrics；english-update-dates englishUpdatedDates 去 export；registry englishPilotQuestions；locale-config DEFAULT_LOCALE/ENGLISH_LOCALE/englishPilotSlugs/isEnglishPilotSlug/chineseModulePath；english-section-outline sharedSectionRoleOrder/focusedSectionRoleOrder）；合并 module-extension-views-zh.mjs 的 17 个标题与 MCP CLIENT 步覆盖进 module-extension-views.mjs 并删除该文件（新增 getModuleExtensionView 访问器）
- 内容快照哈希：H0' / H0' / 相同
- 删除前 grep：每个符号都在 app/ tests/ scripts/ 全量 grep，零外部引用才动
- 验收项：
  - [x] 每项附 grep 证据（见删除前 grep）
  - [x] `npm run check` 通过（修复重复 import 后 FINAL-EXIT=0）
  - [x] 快照哈希不变
  - [ ] **app/ 行数减少 ≥ 200 未达成**（净减约 59 行）——原因见下
- 与手册不符之处 / 阻塞（手册规则 9）：
  - 手册前提「englishUnifiedReaderConfigs 已覆盖全部 23 个 slug」不成立：实测只覆盖 9 个（rag/multimodal/llm/mcp/a2a/veadk/agentkit/evaluation/security），其余 14 个模块由 [slug] 路由按 `englishUnifiedReaderSlugs.includes(slug) ? "unified" : "legacy"` 走 legacy 分支。因此 item 1（删 reader 参数 + legacy 分支）与 item 2（删 usesFocusedEnglishPreview/completeFocusedProjection 与三个恒等选择器）会破坏 14 个模块的英文页面，本轮不执行，留待统一 reader 覆盖全部模块后另行处理。这也解释了行数验收未达标。
- 发现：恒等选择器 selectVisibleEnglish* 实际被 english-pilot-module-page.tsx 的 legacy 渲染路径使用（手册标记为无人引用），已保留

## 回执 S1-T5
- 分支 / 提交：codex/improve-s1 / `3340b1c`
- 改动文件：.github/workflows/ci.yml（fast/full/portable 三 job，portable 仅 schedule+dispatch）、.github/workflows/nightly.yml（新）、.github/dependabot.yml（新，weekly、dev-tooling/runtime 两组）、scripts/check-source-links.mjs（新）、README.md（CI 结构句）
- 内容快照哈希：H0' / H0' / 相同
- 验收项：
  - [x] YAML 可解析（pyyaml 三文件全过）
  - [x] `node scripts/check-source-links.mjs --limit 5` 本地可跑（5/5 200，汇总行正常；写入 $GITHUB_STEP_SUMMARY 逻辑就位）
  - [x] 三个 job 的 step 列表见 ci.yml；actions/cache 用 SHA 锁定 v4.2.3（5a3ec84…，经 GitHub API 核验）
- 门禁：check:fast 通过（阶段 1 任务门禁；全量 check 在阶段收尾统一跑）
- 与手册不符之处：无
- 发现：`fetch` 对部分站点的 HEAD 会被拒（403/405），脚本已按手册自动降级 GET

## 回执 S1-T6（阻塞，未实施）
- 卡在：把 `.codex/hooks.json` 四个入口压缩为「最短 import」形式时
- 现象：安全测试 tests/portable-capture-security.test.mjs「hook ignores shadow markers, rejects nested Git capture, and safely no-ops in a Git-less subdirectory」要求（一行不改、全绿）：(a) 从嵌套目录运行钩子时真实项目仍要完成 1 次采集；(b) 嵌套 git 仓库下不得再采集；(c) 影子 .agents 永不执行。用最短形式 `node -e "import('./.agents/…/hook-bootstrap.mjs')"` 实测（/tmp 复刻夹具）：cwd=nested/deeper 时相对导入解析不到任何脚本 → 0 次采集（测试要求 1 次）→ 必红。影子拒绝与「嵌套 git 不采集」依赖命令体内自带的 git 根遍历与遮蔽检测，该逻辑无法移入 bootstrap 后仍让入口保持最短。
- 已尝试：解码现命令全文、复刻夹具实证（结论如上）；另外 kb-tool.mjs:1058 以 `EXPECTED_HOOK_COMMAND_SHA256` 硬校验命令哈希，改命令会同时打红 kb:validate → test:portable。
- 需要检查者决定：是否接受「保留压缩单行 + 修改安全测试以适配 bootstrap 内校验」的替代方案（需要重写测试夹具的断言语义），或维持现状（D4=否）。
- 当前分支状态：无改动（未提交任何 S1-T6 内容）

## 阶段 1 收尾
- `npm run check` 最终干净运行 FINAL-EXIT=0（/tmp/check-s1-final.log）；分支推送 origin/codex/improve-s1。
