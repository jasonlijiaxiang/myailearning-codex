# 决策记录

本文件只记录已经确认的范围、顺序和例外。新的提议在用户确认前不得标记为已决定。

## D-001：先计划，后实施

- 状态：已确认
- 决定：本次提升先完成计划设计，之后再修改正式内容。
- 影响：计划 Review 期间不修改公开正文，不触发提交、推送和 Sites 发布。

## D-002：逐阶段 Review

- 状态：已确认
- 决定：一次只 Review 一个阶段。
- 影响：该决定完成了第 0 阶段的独立 Review；后续计划 Review 节奏已被 D-010 的最新指示调整，但实施仍按阶段和小批次推进。

## D-003：本轮只处理中文

- 状态：已确认
- 决定：英文版本延期到未来独立任务。
- 影响：
  - 不修改 `app/i18n/en/**`。
  - 不修改 `app/en/**` 的英文模块 authored 知识内容；`/en/questions` 可修复死锚并纠正目录导航说明，但不得新增模块知识断言。
  - 不进行英文翻译、写作、审校或内容同步。
  - 只记录未来英文同步涉及的稳定 ID。
  - 共享结构必须保持向后兼容，英文页面不得损坏或误报同步日期。

## D-004：当前库保持唯一正式知识源

- 状态：已确认
- 决定：不把对照库整体迁入，不建立第二份问答、来源或模块目录。
- 影响：所有正式增量最终必须落回当前模块、问答、Reference、Claim 和术语契约。

## D-005：对照库是发现线索，不是最终证据

- 状态：已确认
- 决定：可以吸收其选题、结构、客户措辞和研究线索，但正式事实必须重新使用一手或权威来源核验。
- 影响：对照库路径可记录在研究 provenance 中，不作为高风险结论的正式 `sourceId`。

## D-006：排除循环回流

- 状态：已确认
- 决定：目标库从当前库旧版本吸收的 Claim 生命周期、深度标尺、失败矩阵、边界、云落点和质量门禁等，不作为反向吸收创新。
- 影响：第 0 阶段差异矩阵必须分别记录内容 lineage、证据 lineage、证据、置信度、循环风险、继承基础和净增量。

## D-007：建立专用计划目录

- 状态：已确认
- 决定：本次提升的计划、设计、决策和阶段 Review 文档集中存放在本目录。
- 影响：
  - 本目录不替代 `knowledge/module-polish/plan.json`。
  - 本目录不成为正式内容或发布输入的第二真源。
  - 计划、设计、决策和 Review 留痕都集中在此目录。

## D-008：先完整写出计划，再逐阶段 Review

- 状态：已确认
- 决定：第 0～5 阶段及必要横向契约先完整落盘，但仍按阶段顺序逐一 Review。
- 影响：
  - 文档存在不代表计划已批准。
  - 第 0 阶段已经完成独立 Review。
  - 所有计划确认前不开始正式内容实施。

## D-009：第 0 阶段计划通过

- 状态：已确认
- 决定：采用输入覆盖账、occurrence 到 canonical candidate、核心矩阵加类型化投影、建议与用户裁决分离、独立覆盖/冲突/owner 判断，以及 JSON 规范矩阵加 Markdown Review 视图。
- 影响：
  - `01-STAGE-0-GAP-MATRIX-PLAN.md` 状态改为 `approved`。
  - 这里只批准计划设计，不代表差异矩阵已经执行。
  - Stage 0 实际产物仍需再次提交用户裁决，才能成为 Stage 1～4 的正式输入。

## D-010：后续阶段计划一次性写全并联合 Review

- 状态：已确认
- 决定：第 1～5 阶段和中文单线横向契约不再逐份等待后再写，而是本轮全部细化并统一检查。
- 影响：
  - 后续计划状态进入 `in-review`。
  - 联合 Review 不授权公开内容实施。
  - 实际实施仍按阶段、研究门、写入门和最多三个模块的小批次执行。
  - 各阶段确认只形成专项分支检查点；正式 Git 与 Sites 发布闭环按 D-011 延迟到最终合并。

## D-011：专项分支隔离，最终由用户决定合并

- 状态：已确认
- 决定：本专项使用 `codex/ai-knowledge-base-content-improvement`，全部完成并通过验证后，才由用户亲自合并或明确授权合并到 `main`。
- 影响：
  - `main` 在最终合并授权前保持不变。
  - Stage 1～4 的确认是工作分支检查点，不是正式发布确认。
  - 中间默认不推送功能分支、不部署 Sites。
  - 最终必须提供功能分支相对 `main` 的完整 diff、提交、风险、延期和验证摘要。
  - 合并后的 `main` 重新运行 `npm run check`，随后推送并从同一精确提交发布 Sites。
  - Codex 不自行合并，也不自动删除工作分支。

## 待 Review

- 第 1 阶段中文现场备战层与学习路径的详细设计。
- 第 2～5 阶段的详细实施范围与门禁。
- 中文单线交付与英文延期契约。
- 第 4 阶段最终采用保险理赔材料助手还是备选行业场景。

## D-012：用户指示按计划全量自主执行

- 状态：已确认（2026-08-05）
- 决定：用户指示「直接全部按照 change plan 里面的计划做完」，授权主 Agent 不再逐门等待 Review，按计划顺序自主完成第 0～5 阶段与中文单线契约，并按总体计划的分支合并与发布协议完成最终合并、推送与 Sites 发布。
- 影响：
  - 第 1～5 阶段计划与 `07-CHINESE-ONLY-DELIVERY-CONTRACT.md` 由 `in-review` 转为 `approved-for-execution`，其文档状态同步更新。
  - 第 0 阶段候选级裁决由用户委托给主 Agent：按研究建议执行；`absorb`/`expression-only` 进入对应阶段写入，`research` 只做研究，`no-op`/`reject`/`watch` 只留痕。
  - 计划硬门不因委托而放宽：法律、安全、行业类结论未通过官方来源核验与相应专业复核前一律 `hold`，不进入公开正文；缺行业专家时案例标记为教学蓝图。
  - 中间批次在专项分支形成检查点；最终 `main` 合并、推送与 Sites 发布按 D-011 与总体计划第 14 节执行，发布地址为 https://cloud-ai-presales-fieldbook.lijx.chatgpt.site 。
  - 本决定不代表「已完成的检查可以跳过」；`npm run check`、release-check 与 Sites 轮询仍为硬门。

## D-013：执行边界修订——只到分支检查点，不合并、不推送、不发布

- 状态：已确认（2026-08-05，用户澄清）
- 决定：按 D-012 全量执行第 0～5 阶段与中文单线契约，但停止在专项分支本地检查点；**不合并 `main`、不推送任何远端、不部署 Sites**；全部完成后交付完整 Review 包（差异、提交、receipt、风险与延期摘要）供用户终审，用户 Review 后再决定是否合并与发布。
- 影响：
  - 所有批次/阶段确认只形成 `codex/ai-knowledge-base-content-improvement` 上的本地检查点提交与 receipt。
  - Stage 5 的 Git/Sites 发布链路整体后移：`npm run check`、`sites:release-check` 只作为本地验证执行，不产生远端或公开部署动作。
  - 最终 Review 包交付前，任务视为未完成；用户终审后按 D-011 决定合并与发布。

## D-016：英文服务端文档壳独立维护

- 状态：已确认（2026-08-10）
- 决定：英文路由使用独立 `(en)` route-group root layout，在服务器直接输出 `<html lang="en">`、英文 metadata 和可索引的 `summary` 分享卡；中文根布局保持独立。该变更作为 document-shell runtime maintenance 记录，不改变任何模块的英文 authored 内容、审校结果或更新时间。
- 影响：
  - 不再依赖 hydration 后的 document-language mutation。
  - 共享文档壳变更须以 `document-shell` maintenance receipt 记录，并通过中英文 renderer 投影、root language、metadata、robots 与路由 HTML 回归。
  - 本决定不改变 D-013 的分支检查点边界，不授权远端推送或公开部署。
